# 变更日志 (CHANGELOG)

## [2026-09-07] 技术债与风险治理批次：OPT-1~OPT-8 全部落地

一次性执行任务书《优化任务_技术债与风险治理_20260907》全部 8 项，每项独立 commit、验收基线（fast pytest + ruff + mypy + check_redlines）全程保持绿：

- **OPT-1（P0）临时产物清理**：根目录与 scripts/ 的临时脚本归档/删除；lint 债务清零（ruff 19 处违规机械修复）。
- **OPT-2（P0）备份保留策略**：`cleanup_backups.py` 增加保留窗口与数量上限，`_prune_old_backups` 防无限增长；排除 2026-08-13 一次性父项目 .git 备份（处置权留用户）。
- **OPT-5（P1）测试分层**：pyproject 默认 `-m "not slow" --timeout=60`，默认层 **14.8s**（基线 431s，提速 29 倍，759 passed）；全量 `-m "slow or not slow"` 保留 slow 层（TickBackoff 真实时钟退避 4 例为主因）；pytest-timeout 走 R17 白名单 + RED_LINES §6 豁免。
- **OPT-7（P2）前端 CDN 本地化**：echarts/marked 落地 `static/vendor/`（SHA256 锁版本），`app.py` 按各自 mtime 独立版本号；断网可用。
- **OPT-3（P1）data_collector 拆分**：6,055 行巨石按数据源拆为 **modules/collector/ 18 子模块**（AST 纯搬移），`data_collector.py` 保留为 facade（192 符号表面逐符号一致，调用方零改动）；`_rotate_em_host`/`_call_ak_with_timeout`/`_num_float` 随调用方迁移断 3 处循环导入；共享可变状态与唯一 global 写入者同模块单宿；check_redlines 10 处源码锚点改扫 facade+全包（28/28 绿）；4 个测试文件约 70 处 monkeypatch 由 facade 重指向实现子模块；mypy 51→50（唯一减少来自死定义删除）。已登记偏差：capital_flow.py 1,154 行（`fetch_capital_flow` 单函数约 700 行不可拆）。
- **OPT-4（P1）前端拆分**：app.js（7,094 行/415KB）按业务域拆为 **8 文件** core/watchlist/analysis/portfolio/backtest/alerts/market/boot（区间平铺校验无缝全覆盖）；顶层执行语句依赖分析确认仅 3 处立即求值引用，加载顺序 core→…→boot 即满足；index.html 按序渲染 8 个 `<script>`，`_ver()` 扩展为 `js_versions` 有序 dict（每文件独立 mtime 版本号，零构建不变）；node --check 8/8 + 离线冒烟 + **真实浏览器回归**（无头 Chrome 实测 UI 完整渲染、window.onload 初始化链路正常）。前端文件结构变更后需服务重启生效（看门狗自愈）。
- **OPT-6（P2）级联完整性测试**：新增 `tests/test_cascade_integrity.py` 37 例——29 张 stock_id 子表清单驱动，`child_tables` 与清单双向一致性断言（**新表不同步级联即红**）；删自选股残留参数化、删分组迁移、删账户 force_confirm 流、删持仓/流水安全锁（T+1/已清算/大额）与重算一致性，含 AGENTS §3 同股多仓断言。**测出真实缺口 OPT-6-G1**：`api_delete_stock` 仅覆盖 10/29 子表，19 表残留孤儿行——已登记任务书"发现的问题"另行立项（本批次零实现改动）。
- **OPT-8（P3）数据源健康度**：新增 `GET /api/health/sources`（只读）——近 7 天按维度聚合 data_status（成功率/最后成功/领先连续失败段）、按模块聚合 error_logs；红/黄/绿分级（红=成功率<50% 或 连败≥3 或模块 24h 内有错）。看板新增 🩺 健康度卡片（异步填充不阻塞主渲染）。真实库实测即时暴露 express/forecast/orderbook/restricted_release 四维 6 连败；真实浏览器 DOM 验证卡片完整渲染。新增 `tests/test_health_sources.py` 5 例。

**终态**：fast 796 passed / 6 deselected（~23s）；全量 765+（6m18s，含 slow）；ruff 全仓绿；check_redlines 28/28；mypy 51→50（既有债务登记待专项）。服务经看门狗两度自愈加载新代码，线上 UI 实测正常。

## [2026-09-03] 看板建议卡补齐买入区间/补仓档位（021BM）

- 用户诉求：持仓股操作建议显示"买入"却看不到买入区间。
- **根因**：操作建议卡片是"评级×持仓状态"静态矩阵，不接价格建议数据；价格建议只在个股页与每日报告里有。
- **方案（零重算）**：每日报告本就落库完整 `price_advice` JSON（区间+网格+止损止盈）——`watchlist-scores` 端点直接从**最新报告行**读出（新增 `lr.price_advice` 列 + `_parse_pa_zone` 提取器），每股附 `pa_zone`（区间标签/上下沿 + 买入侧前两档网格 + 止损）。**语义自动区分**：无持仓档位为"第一/第二买入位"，有持仓即"补仓一档/补仓二档"（10%/15%，锚定止损+0.5ATR）；减仓/离场档被过滤不进看板。
- **前端**：买入/加仓动作的胶囊卡追加蓝色区间标签（`买入区间 10.00~12.50 · 第一买入位 10.00`），悬停显示全部档位+止损+报告日期，点击进个股报告。
- **验证**：`tests/test_pa_zone_021bm.py` 4 例（空仓档/持仓补仓档/纯离场档过滤/坏输入）passed；线上实测 32/39 只自选带区间。
- 改动：`blueprints/portfolio.py` · `static/js/app.js` · `tests/test_pa_zone_021bm.py`（新）。

## [2026-09-03] 盈亏口径对齐券商：持仓盈亏 + 摊薄成本（021BL）

- 用户诉求：已实现/浮动/总盈亏三列用得不顺，券商 APP 只看**持仓盈亏**；成本应是**每笔交易后重算的摊薄成本**，不是停留在买入价。
- **发现的真实偏差**（重算引擎 `_recalculate_holding`）：①买入/卖出按 `价格×数量` 计算，**金额直填（021AM）的实际成交金额被无视**→成本价与券商对不上；②清仓后成本价残留旧值不归零；③展示层"已实现/浮动/总收益"三列并排与券商习惯冲突。
- **重算引擎修正**（保持 021S（股票, 账户）隔离口径不变）：买入/卖出**优先实际成交金额**（缺失回退 价格×数量，旧数据兼容）；卖出仍不改摊薄成本价（东财/银河口径）；已实现盈亏 = 卖出金额 − 费用 − 卖出数量×摊薄成本（超卖钳制按比例折算）；**清仓后成本价归零**。
- **展示重构**（券商列表口径）：持仓表三列 → **持仓盈亏 + 盈亏比例**（现价 vs 摊薄成本，API 新增 `unrealized_pnl_pct`）；已实现盈亏移至持仓盈亏单元格悬停提示 + 汇总卡；汇总卡"总浮动盈亏/总收益"→"**总持仓盈亏**/（含分红与卖出差额）"；看板"浮动盈亏"卡同步改名。
- **验证**：新增 `tests/test_holding_recalc_021bl.py` 3 例（隔离库 API 全流程：买入→部分卖→补仓→清仓，断言摊薄成本 10.005→9.5075、已实现 992.5→980.0、清仓归零；金额直填优先；金额缺失回退）；与 021BK 费用估算 14 例合计 **17 passed**；JS 语法校验通过。
- 改动：`blueprints/portfolio.py`（重算引擎 + 盈亏比例字段）· `static/js/app.js`（持仓表/汇总/看板）· `templates/index.html`（汇总卡标签）· `tests/test_holding_recalc_021bl.py`（新）。

## [2026-09-03] 交易费用自动估算（021BK）

- 用户诉求：交割单隔天才出，佣金没法实时录入——希望系统先自动算，有误差隔天再调。
- **现状**：流水表已有 `commission` 字段（021X：买入计入成本、卖出扣减已实现盈亏），但 44 笔流水中 36 笔为 0；编辑/重算通道现成，T+1 锁恰好允许"次日改实际值"。
- **费率模型**（`config.py` 可随时改一处）：A股 = 佣金（按券商，最低 5 元）+ 印花税 0.05%（卖出单边）+ 过户费 0.001%（双边）；港股简化 = 佣金 + 印花税 0.1%（双边）+ 杂费 0.01%。
- **券商匹配**：用户双券商——银河证券万 1.8 最低 5 元、东方财富万 1.5 最低 5 元。`modules/trade_fees.py` 按**交易账户名关键词**匹配券商档（银河→1.8‱、东财→1.5‱、未命中→默认 1.5‱），纯函数离线可测。
- **行为**：录入流水佣金**留空 → 自动估算入库**并标记 `commission_estimated=1`（列表显示 `≈5.10`，悬停有说明）；明确填写则完全尊重；隔天编辑为实际值 → 标记清除、持仓自动重算（T+1 锁恰好允许次日修改，用户确认历史流水**不**补估）。
- **验证**：单元 11 例（券商匹配/最低佣金/印花税仅卖/过户费/港股/分红零费）+ API 集成 3 例（隔离临时库走 test client：留空自动估算入库带标记、明确值尊重、编辑清标记）= **14 passed**。T+1 锁为红线无旁路，故当日 API 增删验证改由隔离库承担。
- 改动：`config.py` · `modules/trade_fees.py`（新）· `database/db_manager.py`（迁移列）· `blueprints/portfolio.py` · `static/js/app.js` · `templates/index.html` · `tests/test_trade_fees_021bk.py`（新）。

## [2026-09-03] 行业资金流缺口回补（021BJ）

- 用户报告：昨天（09-02）行业资金流向没有数据。根因：09-02 东财断连日未采集到该日快照，而实时接口"错过即丢失"；库里日期序列为 `09-01 → [缺 09-02] → 09-03`（09-03 东财已恢复，刷新正常拿到 496 个行业）。
- **修复（缺口回补机制）**：`modules/market_overview.py` 新增东财 push2his **历史日K资金流接口**接入（`/api/qt/stock/fflow/daykline/get`，按行业 BK 代码逐个回看每日主力净流入）。`backfill_industry_fund_flow(trade_date)`：
  - 复用请求层的**主机轮换 × 直连/代理 × 重试**策略；
  - **探针先行**：第 1 个行业历史中无该日（休市/节假日）直接跳过全部，避免 496 次空跑；
  - **连续失败预算**：连续 5 个行业失败即中止（东财整体不可达时快速放弃）；
  - 字段映射：历史行 f52主力/f53小单/f54中单/f55大单/f56超大单/f57主力占比/f63涨跌幅；领涨股历史接口无此字段，回补行如实置空。
- **自动化**：刷新端点成功后调 `maybe_backfill_gap_async()`——从最新交易日向前扫描近 10 个日历日的**连续工作日缺口**（支持多日断连后逐日回补），有缺口则**后台 daemon 线程**回补（幂等锁防重复，不阻塞响应，休市由探针自愈）。
- **实施结果（当日实测）**：09-02 回补落库 **450/496 个行业**（91%，46 个行业重试穷尽后如实放弃）；三个工程强化：①**成功主机粘性**（探针实证 push2his 当日丢包约 2/3，成功组合优先重试，速度约 3 倍）；②**分块落库**（每 100 条提交，中止最多损失当前块）；③首次运行发现"冷却回放被误当成功、钩子未触发"，确认为正常设计后用真实刷新重触发。
- 解析器与工作日推算做成纯函数，离线测试 7 例（`tests/test_market_overview_021bj.py`）。
- 已知边界：回补行缺领涨股；回补依赖东财历史接口可用性（与实时接口同源，东财整体不可达时冷却后重试）。

## [2026-08-28] 全市场选股扫描器：快照粗筛 + 8 类技术信号精筛（021BI）

- 用户诉求演变：想加全部股票 → 讨论后定 B 方向（全市场扫描器）→ 明确"先把接口验证好再做" → 反馈"不会选换手/量比参数，想按 KDJ/MACD 信号选（水下金叉/水上金叉/超卖）"→ 定稿两段漏斗信号选股。
- **接口前置探测（本机实测，全部通过后才动工）**：
  - ❌ 东财 clist / akshare spot_em：仍断连（021AX 老问题）→ **扫描器架构与东财完全解耦**；
  - ✅ 新浪 hs_a 分页列表：5553 只全量、~56 页、0.4s 礼貌间隔实测稳定 → **主源**；
  - ✅ 腾讯 qt.gtimg.cn 批量行情（量比/振幅/PE/PB/市值）→ **增强源**；web.ifzq.gtimg.cn 120日前复权K线 → 信号数据源；
  - ✅ 新浪 newSinaHy.php 行业类别表 + 同端点行业节点 → 行业映射（缓存 7 天；首版解析失败系正则未匹配小写节点码 new_xxx，已修正）；
  - ❌ 腾讯 getRank 榜单：无登录态返回空 → 弃用。
- **架构（两段漏斗）**：
  1. **第①段 快照粗筛**（~1 分钟，手动触发）：新浪全量 5553 只 → 卫生线（剔ST/北交所/停牌、市值≥100亿、换手≥1%，默认值可改）→ 行业映射回填 → 腾讯增强前 300 名量比 → 快照整表落库（`market_snapshot`，仅存最新轮）；筛选条件改动在前端本地即时收窄（放宽需重扫）；
  2. **第②段 技术信号精筛**（对粗筛结果 ≤300 只，前端 25 只/批驱动 + 进度条/可停止）：腾讯 120 日前复权 K 线逐票计算 → **8 类信号**：MACD 水上金叉/水下金叉/死叉、KDJ 低位金叉（D<25 优先于普通金叉去重）/金叉/超卖/超买、RSI 超卖(<30)/超买(>70)；触发窗口默认近 3 个交易日（1/3/5 可选）；结果按信号分组，每组附白话解读。
- **指标口径与评分引擎同源**：MACD(12,26,9) 直接复用 `technical_detail._ema_list`；KDJ(9,3,3) 种子 k=d=50 与 `technical_detail._kdj` 逐值一致（测试断言 1e-6）；RSI14 Wilder 口径。
- **边界**：扫描器只生产候选，加自选 ≤20 只（复用红线批量上限）；快照标注"非评级"；信号结果不落库（刷新即重扫）；无新 pip 依赖；UI 随四套主题。
- **新表**：`market_snapshot`（symbol 主键，整表替换）+ `sina_industry_map`（7 天缓存）。
- **新端点**：`POST /api/market/scan`（粗筛）、`POST /api/market/scan-signals`（信号单批 ≤50）、`GET /api/market/scan/library`、`GET /api/market/scan/status`。
- **跟进（同日，用户反馈）**：换手率改范围值（min~max）；新增量比范围筛选（min~max）。为此把量比增强从"市值前 300"扩展到"量比筛选前全部存活者（≤2000 只，腾讯 60 码/批 ≈ 34 批 ≈ 25s）"，量比筛选改为增强后补跑，避免只筛到前 300；`apply_filters` 支持 `volume_ratio_max`；再新增**流通市值范围筛选**（`nmc_cap_min/max`，纯本地字段）；筛选用例增至 22 passed。
- **共振信号（同日，用户提出"单一指标不太靠谱"）**：新增跨指标系共振推导 `detect_resonances`（零新增指标、零额外请求，从同一份检测结果推导）。四级规则：⭐⭐⭐ 底部反转共振（KDJ低位金叉+MACD水下金叉同窗）→ ⭐⭐ 双金叉共振（MACD系+KDJ系金叉同窗，每股取最高档）→ ⚠️ 空头共振预警（死叉+超买，用于排除）→ 💤 超卖待确认（双超卖状态且无金叉）。设计约束：**跨指标系才成立**（KDJ金叉+KDJ超卖属自我确认不算）、**同窗口才有效**（与触发窗口同一 N）、RSI 状态类只做环境层（+RSI超卖环境/MA20上下方注记，不定级）。前端共振组置顶渲染（星级徽标+构成信号清单）+「只看共振」开关，加自选按 symbol 去重。已知边界：共振为经验规则，历史命中率验证（哪档共振 T+5/T+20 更准）在 P2。形态测试 7 例（实证调参），扫描器测试增至 29 passed。
- **验证（真实全链路，2026-09-02）**：`tests/test_market_screener_021bi.py` 19 passed（指标口径一致性 / 8 信号合成K线触发与窗口外排除 / 低位金叉优先级 / 新浪行解析换算与北交所停牌剔除 / 筛选引擎）；合成形态经实证调参（线性反弹不产生 MACD 交叉，改"回踩+缺口大阳"确定性构造）；线上 `POST /api/market/scan` 实测：5209 只 →（剔ST 5012 → 市值≥100亿 1824 → 换手≥1%）1255 只，耗时 30s（行业缓存命中）；信号端点实测 5 只真实股票命中合理（建行 KDJ金叉+超买、宁德 KDJ超卖）。首次跑通时发现并修复：行业映射回填误存 (行业,节点) 元组致 sqlite 绑定失败。
- **已知限制（如实记录）**：新浪一级行业分类（newSinaHy，49 节点）仅覆盖约 3003 只——为新浪历史遗留的静态分类（2020 年代口径），约 2200 只后来上市个股（如中国移动 2022 上市）不在任何节点，界面行业列显示"—"，行业筛选作用于已分类子集；探针实证节点全部分页拉取无失败，缺口在数据源本身。

## [2026-08-28] 操作建议动作词统一："持有观望"仅保留为评级名（021BH）

- 用户反馈：操作建议里既有"持有"又有"持有观望"，有什么不同？能不能统一。
- **回答**：没有实质区别——都是"不加不减继续拿着"。混乱的根源是**"持有观望"既是评级档位名又被拿来当动作词**：看板里"建议减仓+浮盈"持仓的动作显示"持有观望"，用户无法分清这是在复述评级还是在下达指令。
- **统一规则**：动作词与档位名分离——**"持有观望"仅保留为评级档位名**（R6 档位定义与判定零改动），动作词一律"持有"词根；浮亏语境保留"继续持有"（自带"系统知道你在浮亏"的信息量）。统一后的动作词表：买入 / 加仓 / 持有 / 继续持有 / 关注 / 观望 / 回避 / 考虑减仓 / 减仓 / 建议止损。
- **改动点**（全部为字符串替换，决策逻辑零改动）：
  1. `advisor._determine_action`（B24，已登记 §6）：持有观望·浮亏格、建议减仓·浮盈格 '持有观望'→'持有'；持仓 prose 2 处 '建议持有观望'→'建议持有'；
  2. `price_advisor`：ACTION_MATRIX 3 格（推荐买入·浮亏 / 持有观望·浮盈浮亏）+ RATING_ACTION_SUGGESTION 基线 + 状态机兜底词 →'持有'；
  3. 前端看板 MATRIX 2 格同步；ACTION_COLOR/ACTION_LEVEL 保留'持有观望'映射（兼容历史库中已存的旧文案）。
- 语义澄清：建议减仓·浮盈格动作显示"持有"看似弱化警示——真正的减仓指导由价格建议卡承担（减仓区间/两级止损网格，021AS/AQ），看板动作列只是速览。
- 验证：`test_advisor.py` 矩阵断言更新（2 格 + 未知评级兜底），advisor+price_advisor+routes **123 passed**；动作词残留扫描确认剩余"持有观望"全部为评级名/档位键（合法用途）；§6 豁免已登记（用户 021BH 批准）。

## [2026-08-28] 支撑观察预案：减仓评级的下方支撑梯队网格（021BG）

- 用户需求：跌破支撑参考区间不建议买入没问题，但下方还有支撑吗？能否根据下一个支撑区间给出网格计划。
- **设计**：减仓/卖出评级（仓位 0%）不给"买入网格"，但给"**支撑观察预案**"——观察位不是买入建议，是逐级观察企稳的参照系；若日后评级改善，重新分析即会给出真正的买入网格。
- **实现**（`price_advisor.py`）：
  1. 新增 `_recent_lows(stock_id)`：raw_kline 近 60 根 K 线的 20日/60日最低价（真实技术锚点，数据不足的周期返回 None）；
  2. 新增 `_build_support_watch_grid(close, buy_low, atr, low20, low60)`：**真实低点优先、ATR 外推补足**——区间下沿（未跌破时）/20日低点/60日低点依次入选（仅取现价下方），不足 3 档用 0.8ATR/1.2ATR 步长外推；严格自上而下递减去重，最多 3 档；`type='watch'`、`pct=None`（无仓位语义）；
  3. `_gen_no_position` 减仓分支：`grid` 从空数组改为观察梯队，签名新增 `low20/low60`；主入口读取近期低点并传入。
- 前端：网格卡标题三分支（操作网格计划/支撑观察预案（非买入建议）/网格买入计划）；`watch` 类型灰色（新增 `.pa-watch` 样式，跟随主题变量）、仓位列显示"观察"。
- 验证：`test_price_advisor_021bf.py` 更新+新增梯队构造测试（真实低点优先/跌破时补位/无 ATR 回退/严格递减），相关 **104 passed**；`node --check` 通过；宁德时代线上实测（见下）。
- 语义边界（诚实记录）：观察位是技术参照而非预测——跌破观察位不代表会反弹，评级不改善仍不构成买入理由。

## [2026-08-28] 价格建议评级感知修复：减仓评级不再输出"可逢低买入"（021BF）

- 用户反馈：宁德时代评级"建议减仓"、当前价 358.10 低于买入区间 361.76-376.01，语义矛盾。
- **根因**：`price_advisor._gen_no_position` 中建议仓位感知评级（减仓→0%），但区间话术与买入网格完全不感知——现价低于区间下沿时无条件输出"当前价低于买入区间，可逢低买入"，且给 3 档买入网格（第一买入位 40%），与 0% 仓位直接打架。价格建议是 generate_advice 之后的后处理模块（B24 红线不涉及，修改合规）。
- **修复**（区间语义随评级三档化）：
  1. **买入档**（强烈推荐买入/推荐买入）：维持"买入区间"+逢低买入话术+3 档买入网格不变；
  2. **观望档**（持有观望）：区间更名为"参考区间"（20% 仓位语义一致），话术与网格保留；
  3. **减仓/卖出档**（建议减仓/强烈建议卖出）：区间更名为"**支撑参考区间**"（技术支撑位仍有参考价值，但不再叫买入区间），操作建议改为"评级为建议减仓（建议仓位0%），下方价格仅为技术支撑参考，不建议买入"，**买入网格置空**（0% 仓位不给买入计划）。
- 接口：`_gen_no_position` 新增 `zone_label` 字段（买入区间/参考区间/支撑参考区间）；前端价格建议卡标签从硬编码"买入区间"改为读 `pa.zone_label`（缺省回退买入区间，向后兼容）。
- 验证：新增 `tests/test_price_advisor_021bf.py` 7 条（含宁德时代矛盾场景复现：现价跌破下沿+减仓评级→断言不得出现"可逢低买入"）；价格建议+advisor 相关 **103 passed** 无回归；`node --check` 通过。语义说明：评分低（基本面/消息面弱）但股价跌破技术区间，两者可以同时为真——修复后系统如实表达"技术便宜≠值得买"，不再自相矛盾。

## [2026-08-28] 主题白色面残留修复：报告页/详情弹窗/看板卡全面接入主题变量（021BE）

- 用户反馈：浅色皮肤卡片白色可接受，其他皮肤（尤其深色）白色卡片不对。
- **根因**（021BD 两层策略的盲区）：适配层只覆盖了 body/.card/topnav 等约 20 个结构选择器，但 `app.css` 另有 20+ 个组件选择器带白色/浅色背景未被覆盖——`.dim-card`/`.score-card`/`.radar-card`/`.kline-card`/`.pa-side-card`/`.rp-card`/`.bt-interp-card`/`.pa-kv-wrap`/`.pa-grid-table`/`.view-summary-card`/`.data-popup`/`.advice-detail-text`/`.export-bar`/`.dash-card` 等（报告页、四维详情、K线弹窗几乎全是）；HTML 另有 7 处 `background:white` 写法的弹窗面（上一轮只匹配了 `background:#fff` 无空格格式）；ECharts tooltip 默认白底黑字。
- **修复**（改为就地变量化，比补适配层彻底）：
  1. `app.css` 原规则内 **46 处**浅色背景直接替换为 `var(--xxx, 原值)`（surface×3 档 / nav-hover 等 18 种字面色值归入 4 个变量）；`@media print` 打印块（914 字符）经括号配对精确定位后**原样保留**——打印就该白纸黑字；
  2. `index.html` 7 处 `background:white` → `var(--surface,#fff)`；
  3. 4 个 ECharts 图表（饼/柱/雷达/K线）tooltip 全部接入 `_themeCol` 实色（背景/边框/文字），深色下悬浮提示不再是白块。
- 语义色 tints（评级徽章/操作建议色块/预警分类色/涨跌色）维持原样——它们是带语义的彩色芯片，浅底深字在四套主题下均可读，不属于"白色面"范畴。
- 验证：`node --check` 通过；print 块外白色背景残留 **0**（逐字符核验，唯一命中为打印块内的正确白底）；HTML 残留 0；重启服务（index.html 属 Jinja 模板缓存，须重启）后线上复验 dim/score/rp/dash 卡均已变量化。刷新页面即生效。

## [2026-08-28] 多主题皮肤系统：浅色/深色/护眼/暖阳四套一键切换（021BD）

- 用户需求：系统支持多种皮肤颜色可选。
- **架构**（CSS 变量 + 三层适配，原样式整体可回滚）：
  1. **变量层**（`app.css` 顶部）：`:root` 定义 15 个外观变量（bg/surface×3/border×2/text×3/nav×4/input/shadow），`html[data-theme='dark'|'green'|'warm']` 覆盖为各主题值。**语义色全主题恒定不翻转**——红涨绿跌（#e74c3c/#27ae60）、主蓝 #1a73e8、告警橙是 A 股工具的领域约定，不属于皮肤范畴；
  2. **适配层**（`app.css` 末尾，注释明确"必须保持在文件末尾"）：body/card/input/select/th/td/topnav/铃铛/下拉/筛选取变量重声明——同特异性靠源顺序覆盖原硬编码规则，原 1359 行规则零改动，删除本层即整体回退浅色；
  3. **内联样式迁移**：`app.js` + `index.html` 共 **322 处**中性色机械迁移为 `var(--xxx, 原值)`（带原值回退，浅色主题视觉零变化）——背景 10 色、文字 7 色、边框 6 色；正负向断言防误伤长 hex；语义色（涨跌/评级徽章/状态色）刻意不迁移。
- **主题**：🎨 浅色（默认，与原版一致）/ 🌙 深色（暗蓝灰面）/ 🌿 护眼（豆沙绿）/ ☀️ 暖阳（米黄）。切换器在导航栏右侧，localStorage 持久化（`sa_theme`），`<head>` 内联脚本在 CSS 加载前恢复主题防首帧闪白。
- **图表适配**（ECharts 为 canvas 渲染，CSS 变量不生效）：新增 `_themeCol(var, fallback)` 运行时取色；行业饼图 label、评级柱状图坐标轴、四维雷达图（轴名/分隔线/分隔区/轴线）、K线图（图例/坐标轴/dataZoom 底色）全部改接主题实色；`themechange` 事件触发看板图表重渲染（雷达/K线弹窗随下次打开刷新——已注明）。
- 深色主题下 4 类语义提示条（info/success/warning/error）换深底浅字专用配色，避免浅色透明底刺眼。
- 验证：`node --check` 通过；替换计数逐一核对（JS 274 + HTML 48）；`_dashData`/`dashRenderCharts` 作用域与双参调用核实；纯前端改动（CSS/JS/HTML），无后端变更，刷新页面即生效（no-cache + mtime 版本号自动生效）。
- 已知边界（诚实记录）：JS 内零星低频浅色 tint（如 fold hover #f0f4ff）未迁移，深色下观感稍亮但不影响可读性；打开中的雷达/K线弹窗不即时重绘，重开即新主题。

## [2026-08-28] 评级准确性策略优化：消息面非对称化 + 降权 + 动态优化器重跑（021BC）

- 用户诉求：策略上有什么能优化的，让评级更准。基于全量 699 条 v5 回测样本（021BB 归因修复后）完成三项诊断，用户选定 ①消息面非对称化 + ③重跑动态优化器 两项实施。
- **诊断证据**（只读分析，三组交叉验证）：
  1. **分档胜率**：A股"建议减仓"是最强信号（方向 61%/超额 71%）；"推荐买入"方向仅 53%（≈随机）且 1 月均收益 -2.23%——A股买入档失效（港股推荐买入 65%/1 月 +6.16% 正常）；
  2. **分数-胜率倒挂**：A股 55-69 分组方向准确率 68%，70+ 高分组反降至 44%——高分区被"动量热度"而非后续收益驱动；
  3. **维度归因**（analysis_results 逐维分 × 回测结果，n=410）：资金面 +0.153 / 基本面 +0.117 / 技术面 +0.027（近零）/ **消息面 -0.098（四维唯一负相关）**；高分组内部判错样本消息分 76.8 vs 判对 67.1——正面情绪是制造假高分的主因之一。
- **实施 ①（消息面非对称化，走 §6 豁免登记）**：
  - `scoring_engine.score_sentiment` 映射改非对称：s≤0 保持 (s+1)×48 全额惩罚（-1→0）；s>0 斜率减半 48+s×24 封顶 72（原对称曲线极多 95）。002 校准档位、子项结构、评级边界零改动；
  - `config_weights.json`：A股 news 0.1504→0.08，腾出 0.0704 按现值比例回填 k/f/c（0.2715/0.2171/0.4314）；7 个行业覆盖 news 等比降至 0.08 同口径；热加载即生效；
  - 语义：正面情绪按已定价噪音处理只给半分，负面情绪才全额扣分——评分对"利好出尽"型假信号脱敏。
- **实施 ③（动态优化器重跑）**：`scripts/run_dynamic_optimizer.py --apply`（021AI 动态窗口：维度分重放 + 网格 + 前 20/后 10 切分前向验证 + 数据门槛，双达标才写权）。021BB 归因修复后 v5 纯净样本首达可用线。**运行结果（2026-08-28）：门槛未达标，按设计拒绝写权**——v5 纯净评级日 272 < 300、测试段窗口 24 < 100；但诊断方向明确：当前权重（021BC 调整后）训练 57.8%/测试 70.8%，最优候选 {kline 0.5, fundamental 0.05, capital_flow 0.4, news 0.05} 测试段 +8.0pp，且前五候选无一例外把 news 压到 0.05~0.1——**独立佐证了消息面降权方向**。每月积累 20~25 个评级日，约一个月后达标重估。
- 验证：`tests/test_scoring_engine.py` 情绪映射断言更新（极多 95→72、偏多 72→60）+ 新增非对称性测试（等幅正负情绪负面惩罚更大）；**scoring+redlines 176 passed**；**红线核验 28/28**；权重 JSON 无 BOM、主权重与 7 行业 sum=1.0；豁免已登记 RED_LINES.md §6（用户 021BC 批准）。回退：权重还原 config_weights.json 旧值；引擎还原 `(s+1)*48` 对称曲线。
- 未实施但留档的策略选项：买入档多维共振确认制（改变评级语义，待用户后续决策）；减仓档预警强化；技术面低相关观察项（+0.027，样本期仅 21 个交易日，攒足一季度样本再评估）。

## [2026-08-28] 回测中心引擎归因修复：全部样本归位 v5 单引擎（021BB）

- 用户诉求：回测中心只显示 v5 引擎的数据。
- 排查发现"历史引擎（未标记）"行并非经典引擎样本——**全库不存在任何 legacy 标记**——而是两类归因缺陷把 v5 时代样本错标成了"未标记(历史)"：
  1. `ratings_history.engine_version` 标记 2026-08-14 才上线，此前 7/16 起的 514 条 v5 评级该列为 NULL；而系统自 7/16 起评级即由 v5 生成（021AU 记录），经典引擎 021AE 已整体退役，当前代码恒写 'v5'（advisor.py 021AE 注释）；
  2. `ratings_history` 用 INSERT OR REPLACE（同股同日重评换新 id），`backtest_results` 按 `rh.id = br.rating_id` JOIN 产生 **338 条孤儿 rating_id**，归因断链。
- 修复（三层）：
  1. **JOIN 改自然键**（`backtest_engine.compute_market_report`）：`rh.stock_id = br.stock_id AND rh.rating_date = br.rating_date`——自然键在 REPLACE 下稳定，孤儿 id 不再导致归因丢失（附带收益：动态优化器只认 v5 标记样本，此前因归因断链被排除的纯净样本得以回归统计）；
  2. **一次性标记回填**（`scripts/backfill_engine_version_021bb.py`，先经在线 backup 备份）：514 条 NULL → 'v5'，属事实性回填而非口径放宽；全表 823 条评级标记齐全；
  3. 前端"引擎分层统计"表说明同步更新。
- 效果：回测中心引擎构成仅剩 **v5（当前规则）** 一行（A股 564 / 港股 135 全样本保留，而非修复前误标的 134/37）；"v5 样本不足，结论主要反映历史引擎"的警示语随构成声明自动消失。
- 验证：新增 `tests/test_backtest_021bb.py` 2 条（孤儿 rating_id 经自然键归因 v5 / REPLACE 换 id 后归因不丢+无匹配诚实降级）；回测相关存量 56 条全过；**pytest 690 passed**；改动文件 ruff/mypy 干净；执行 `python scripts/backfill_engine_version_021bb.py` 回填 514 条（备份 `db_backup_20260828_220356_*.db`）。

## [2026-08-28] 看板行业"未分类"治理：加自选股识别增强 + 补采调度器行业自愈（021BA）

- 现象：总览看板 3 只 A股（300034/002716/301269）行业显示 ⚠️ 未分类。库内 32 只自选股中 3 只 industry='未分类'，且恰好是 id 最新的三只。
- 根因（两层叠加）：
  1. 加自选股时行业识别虽已存在（INDUSTRY-DYNAMIC），但主源是 akshare `stock_individual_info_em` **裸调**——无子域轮换/轮间退避，遇东财 WAF 窗口式断连即失败，失败值 '未分类' 被永久写库（本地映射仅 25 只，覆盖极小）；
  2. 失败后无主动自愈路径——仅批量分析时顺带补取，用户不跑批量分析则永不修复。
- 修复：
  1. **`fetch_stock_industry` 主源升级**（`data_collector.py`）：改为东财 push2 `qt/stock/get` 字段 f127 直连，**复用项目 `_http_get_em` 请求层**（编号子域轮换/轮间退避/UA池，抗 WAF 能力与资金面采集同级）；f127 与行业资金流（`market_overview`）、`config_weights` industry_overrides 同为申万 2021 口径，命名体系一致不污染权重匹配。akshare 降为备源，本地映射最后兜底。新增 `patient` 参数：同步场景（加自选股/批量分析）EM 直连 1 轮快速失败不拖长请求；后台场景（调度器）2 轮容忍轮间退避。尊重资金面熔断冷却（`_em_banned` 时跳过直连）。
  2. **补采调度器行业自愈**（`backfill_scheduler._maybe_backfill_industry`）：每轮 tick 检测 industry 为空/'未分类' 的 A股，随机抽样限量 3 只（防单只持续失败卡死队首）补取，30 分钟~4 小时巡检节奏直到分类齐全；港股不走此路径（恒返'港股'）。
- 实测（诚实记录）：部署时东财整体断连（akshare 与新直连路径均 RemoteDisconnected，与 021AX 记录的断连同族），3 只存量未能当场补齐、优雅降级为'未分类'——服务重启后由行业自愈在 EM 恢复后自动收敛，无需人工干预。
- 验证：新增 `tests/test_industry_backfill_021ba.py` 10 条（EM 直连成功/patient 轮数/f127 无效降级/本地映射/全挂返未分类/港股零请求/无缺口零请求/成功写库失败不写/限量抽样/单只异常隔离）；**pytest 688 passed**；改动文件 ruff 干净（全项目 13 个存量项未触碰）；mypy 改动行零报错。

## [2026-08-28] 删除「一键分析」按钮，单只/批量分析统一走 /api/batch-analyze（021AZ）

- 背景：自选股页并存两个分析入口——行内「⚡ 一键分析」与顶部「⚡ 批量分析+评级」，用户询问区别后确认冗余。核查发现一键分析是 U6 时代前端三步串行链（`/api/collect` → `/api/stocks/<id>/analyze` → `/api/stocks/<id>/advise`），而 **analyze 与 advise 两个端点内部都调用 `generate_advice()`**——点一次一键分析，完整评分实际计算两遍（ratings_history 同日 `INSERT OR REPLACE` 覆盖，第 3 步仅多取一个展示用分数），纯浪费 10-30 秒/只。
- 结论：批量分析（勾选 1 只即可）落库效果完全等价且更强（同花顺资金面批量预取、行业缺失自动补取、单只失败不阻塞、价格建议后处理），删除一键分析零功能损失。
- 改动（仅 `static/js/app.js`，后端端点保留不删——`/analyze`、`/advise` 仍服务报告页等调用方）：
  1. 删除股票行内「⚡ 一键分析」按钮与 `oneClickAnalyze` 函数（含三步进度 UI，-76 行）；
  2. 首次使用提示改指「⚡ 批量分析+评级」；
  3. 看板「待评分」卡片点击直达改为新增 `quickBatchAnalyze(stockId)`：自动勾选该股（清除其他勾选）后调用 `batchAnalyze()`，保留单击分析体验且与批量路径同源；
  4. 看板"待评分"文案与 `refreshDashboardIfLoaded` 注释同步去"一键分析"提法。
- 验证：`node --check` 通过；全库无 `oneClickAnalyze` 残留引用（仅变更说明注释）；后端零改动，pytest/红线不受影响。

## [2026-08-26] 启动补跑：睡眠错过批次钟点自愈（021AY）

- 现象：用户 20:50 发现"今天的报告没生成完"——当日仅 8/31 只有日报。
- 根因（系统事件日志实证）：电脑 **15:39 入睡、20:46 才唤醒**——15:54 A股批次与 16:10 港股批次的执行时刻整机在睡眠，计划任务不运行；20:48 Watchdog 拉起服务后只注册了明天的批次（`下次定时报告: 2026-08-27 15:30`），当天缺口无人补。这是笔记本用户的常态场景（021H 曾修过电池供电下 Watchdog 停摆，睡眠是同族问题的另一半）。
- 修复（`daily_report.py`）：新增**启动补跑**——`start_scheduler` 注册一次性 90 秒延迟 Timer；回调 `_catchup_tick` 自查三条件：交易日 && 已过 16:15（A股 15:54 / 港股 16:10 批次均已过点+缓冲）&& 当日报告覆盖不齐 → 自动补跑完整批次（`generate_daily_report(force=True)`，A股+港股一起），并同批执行指数刷新 + 行业资金流快照（与港股批次挂载项一致）。正常运行日批次已完成 → 覆盖齐 → 自动跳过零开销；`stop_scheduler` 防御性取消未触发 Timer。
- 实证：21:00 部署重启 → 21:01:34 自动检出"报告不齐（8/31）"→ 21:07 补跑完成 **31/31 只全部生成**（本次缺口由新机制现场自愈）。行业资金流刷新时东财仍限流（此前 021AX 补数触发），由 021AX 巡检自愈在夜间自动重试，不影响报告数据。
- 验证：新增 `tests/test_report_catchup_021ay.py` 6 条（过点不齐→补跑+挂载项同批 / 已齐跳过 / 未到点跳过 / 周末跳过 / 生成异常隔离 / Timer 注册与防御取消）；**pytest 678 passed**；红线核验 **28/28**；ruff 干净。

## [2026-08-26] 修复市场行情(行业资金流) 8/25 整天缺失 + 三层自愈（021AX）

- 现象（用户二次澄清后定位）：市场行情页行业资金流 **8/25 无数据**——个股/指数（021AW 已修）之外，市场行情页还有行业资金流快照这一独立数据源，8/25 整天缺失（8/24 有 200 板块、8/26 有 496，唯独 8/25 为 0）。
- 根因：行业资金流每日唯一落库时机在 15:56 日报批次后；8/25 该时点东财 5 个域名×直连/代理共 14 次请求全部 Connection aborted（与 08-18 事故同族），失败后**无任何重试路径**（10 分钟冷却只防硬闯，不负责补抓）→ 缺口永久滞留。今天 13:48 用户打开页面触发实时刷新才恢复当日。
- 修复（三层防线）：
  1. **16:10 港股批次追加行业资金流二次刷新**（`daily_report._hk_report_tick`）：15:56 失败后 15 分钟内二次尝试；尊重 10 分钟冷却（冷却中说明刚失败过，硬闯加重熔断）；
  2. **补采调度器行业资金流自愈**（`backfill_scheduler`）：16:15 后每轮 tick 检测快照"缺失当日"或"当日快照保存于 15:00 前（盘中未定稿）"→ 触发刷新兜底，随 30 分钟~4 小时巡检节奏重试；
  3. **港股指数 EM 列漂移降级**（`index_collector`）：补数脚本实测 EM 偶发返回非空但缺 `close` 列（旧代码此时直接返回空表、降级失效），改为列不完整即视同不可用 → 降级新浪源。
- 8/25 数据恢复（尽力而为）：复用 `scripts/backfill_industry_flow_0818.py`（参数化日期）+ 临时补充脚本（以 8/26 全量板块清单为基准），从 push2his 历史接口逐板块回补；**8/25 最终 312/496 板块**——86 个核心行业板块全部补齐，缺的 184 个为概念板块（东财限流连挂三轮后收手，避免延长熔断影响晚间定时任务）。诚实说明：概念板块 8/25 无数据，相关"连续流入/流出天数"在 8/25 处会断。
- 验证：新增 `tests/test_market_flow_021ax.py` 11 条（四态判定/盘中不触发/缺失触发/冷却跳过/异常吞掉/港股批次顺序=报告→指数→行业流/冷却跳过/EM 列漂移降级新浪）；**pytest 672 passed**；红线核验 **28/28**；ruff 干净；服务重启 health 200。

## [2026-08-26] 修复指数数据 8/25 起缺失 + 指数采集双防线（021AW）

- 现象：用户发现"昨天的行情数据没有获取到"——诊断确认**个股 K 线/资金面/日报均正常**（8/25、8/26 各 31 只全有），唯独 **7 只指数 K 线与指数评级停在 8/24**。
- 根因：指数刷新只挂在 15:54 主批次的日报流程后（约 15:56 执行）。8/25 那次东财指数接口当天 bar 尚未发布（返回的最新行止于 8/24，日志可见"获取 300 条/写入 300 条"但都是旧数据），此后系统再无任何调度路径补采指数 → 缺口永久滞留，指数评级也只能按 8/24 数据计算。
- 修复（双防线，缺一不可）：
  1. **16:10 港股批次后追加指数二次刷新**（`daily_report._hk_report_tick`）：此时 A股(15:00收盘)/港股(16:00收盘)当天 bar 必已发布，`INSERT OR REPLACE` 幂等覆盖；异常隔离不阻塞次日批次注册；
  2. **补采调度器指数滞后自愈**（`backfill_scheduler`）：每轮 tick（30 分钟~4 小时巡检）在 16:15 后检测 `index_kline` 最新日是否落后于个股 K 线并集最新日，落后即触发 `refresh_all()` 兜底（覆盖批次刷新失败、或刷新时 EM 当日 bar 仍未发布两种残余情形）；盘中不检查（个股当日行盘中即存在而指数 bar 收盘后才发布，盘中比较必"假滞后"）。
- 数据恢复：手动触发 `/api/index-ratings/refresh`，7 只指数 K 线与评级全部恢复至 8/25；8/26 当日 bar 由今晚 16:10/16:15 新防线自动补齐（修复时未收盘）。
- 验证：新增 `tests/test_index_refresh_021aw.py` 8 条（滞后判定三态/盘中不检查/收盘后触发/刷新异常吞掉不抛/港股批次顺序=报告→指数刷新/空转巡检自愈仍生效）；**pytest 661 passed**；红线核验 **28/28**；ruff 干净；服务重启 health 200。

## [2026-08-26] 价格回测口径升级：锚点真实评级 + 持仓流水倒推（021AU）

- 用户需求（确认采纳）：回测中心"有持仓样本"表只有持有观望一档——把历史各评级的真实样本显示出来有什么好处？→ 确认改为历史真实评级口径。
- 旧口径三处未来函数/快照冻结：回放评级=重跑时点实时评分（同一段历史今天跑明天跑结果不同）；持仓状态与成本="现在是否持有/现在成本"回放全部历史；分评级统计混入重建点。
- 新口径（`price_backtest.py`）：
  1. **评级**：每个回放点先查 ±5 天内最近的真实评级（ratings_history，归一化兼容旧 A/B/C/D），有锚点即用**锚点当日评级**（无未来函数、重跑稳定）；无锚点的早期时段退回实时评分（历史重建点，anchor 为空，仅作参照）；
  2. **持仓**：新增 `_historical_position_state`——从 buy/sell 流水按平均成本法倒推至回放日时点（分红/补税不影响数量），跨账户聚合；原"预读当前成本"移除；
  3. **分评级统计**：改为仅统计真实锚点样本（rating_stats 与主指标同口径）；删除被替代的 `_mark_rating_confidence`（mismatched 类别自此不再产生，报告键保留兼容）；
  4. 前端口径说明同步更新（有持仓表/分评级表注释）。
- 真库重跑（3398+240 行零错误）后**有持仓样本表覆盖各评级**（A股：减仓12/观望15/推荐买入9；港股：减仓1/观望3/推荐买入2 等）——"某评级下持有时的建议表现"首次可验证。主指标：A股 N=149 目标 31.9%/止损 8.7%/RR 3.65；港股 N=31 目标 48%/止损 16.1%/RR 2.98（评级按锚点取值后自然微调，基线以本次为准）。
- 限制说明：早期无评级记录的时段（系统 7/16 起 v5 评级）只能作为重建点；持仓倒推依赖交易流水完整性（未补录的时期按"当时无持仓"处理，符合事实）。
- 验证：新增 `tests/test_price_backtest_021au.py` 7 条（未来函数防线/加权成本/清仓/空流水/锚点窗口/集成回放行评级与持仓/分评级仅真实样本）；**pytest 653 passed**；红线核验 **28/28**；`node --check` 通过；ruff 干净；服务重启 health 200。

## [2026-08-26] 回测中心有持仓样本口径说明 + 修复跨市场清表（021AT）

- 用户疑问：回测中心"有持仓样本"表只有"持有观望"一档有样本，很奇怪。
- 查明三层原因：
  1. **口径使然（非 bug）**：价格回放的"有持仓"=重跑时点**当前持仓**的股票回放历史，评级也取重跑时点的实时评分——当前 8 只持仓股实时评分全部落在"持有观望"档（钢研高纳日报评级曾为建议减仓，但回放时实时评分 51.6 仍属观望档），故该表只有观望行有样本；未持仓股票的建议减仓/推荐买入样本全部落在"无持仓表"；
  2. **发现真 bug：跨市场清表**——`run_price_backtest(market='a_stock', force=True)` 原实现全表清空后仅重建所选市场，用户在回测中心选"A股"点"重新运行回测"会把港股结果全部抹掉（实测 170 条港股行被误清，本次已恢复）；
  3. 评级快照冻结：回放评级=重跑当日实时评分，与日报评级（含迟滞）可能不同档（锚点机制以 mismatched 标记）。
- 修复：
  - `price_backtest.run_price_backtest` force 清表改为**市场定向**：指定 market 只清该市场行，market=None 才全表清空；
  - 前端"有持仓样本"表标题下加口径说明（"仅统计当前持仓股票的历史回放，通常只有持仓股当前评级有样本"）。
- 重跑两市场恢复数据（3398+240 行零错误）。**指标随新样本自然漂移（非本次改动引起）**：A股目标命中 39.6%→33.3%（新增 8/24-25 样本）、止损 4.8%→8.7%；港股目标 52.2%→48.0%、止损 10.7%→16.1%（港股行 170→240，新增股票 K线达标入库）——A股目标与港股止损两条 ⚠️ 预警重现，属数据滚动变化，按既定计划样本积累后复核校准常数（港股 ≥100 条时）。
- 待办（未做，列为后续选项）：回放评级可改为"锚点当日真实评级"（而非重跑时点实时评分），使有持仓表能出现历史各档评级——语义更真但会整体改变回测口径，需单独评估。
- 验证：新增 `tests/test_price_backtest_021at.py` 3 条（定向清表保留另一市场/全量清空不变/force=False 不清）；**pytest 646 passed**；红线核验 **28/28**；`node --check` 通过；改动文件 ruff 干净；服务重启 health 200。

## [2026-08-23] 减仓/清仓评级给出建议减仓价格区间（021AS）

- 用户需求：评级是"建议减仓"时，能否给一个建议的减仓价格区间。
- 实现（`price_advisor` 新增 `_build_reduce_range`，仅减仓语义评级生效）：
  - **建议减仓**：`减仓区间（建议减50%）= [现价, 现价+0.6ATR]`——下限=现价（越早越主动、不追求卖在最高），上限=最近反弹位（与网格第一止盈位同 0.6ATR 间距；无 ATR 回退 现价×1.03）；上限再被 max(现价, 成本价) 封顶——浮盈时不超过成本价（成本 10.1/现价 10 → 上限压至 10.1），浮亏时不诱导"等回本再减"（回本减仓是浮亏网格的档位语义，不混入）；
  - **强烈建议卖出**：区间退化为 `[现价, 现价]（尽快）`，清仓 100%——风险优先，不等反弹；
  - 其他评级（买入/观望系）返回 None，前端不显示该行。
- 展示：分析报告价格建议卡（持仓中）在止盈/止损行后新增"减仓区间（建议减X%）"行（橙色加粗，hover 说明"区间内分批执行，越早越主动"）；买入/观望评级不受影响。
- 字段：`price_advice.reduce_range = {low, high, pct}`（非减仓评级为 null）；数据新增字段向后兼容，price_backtest 不消费该字段（回测口径零影响）。
- 验证：新增 `tests/test_price_advisor_021as.py` 7 条（减仓区间/清仓退化/其他评级None/无ATR回退/浮盈封顶/集成字段/观望评级None）；**pytest 643 passed**；红线核验 **28/28**；`node --check` 通过；改动文件 ruff 干净；服务重启 health 200。

## [2026-08-23] 综合评分卡片去重（021AR）

- 用户需求：分析报告综合评分卡片重复——"评级 持有观望 / 持有观望 / 建议：持有观望"三行同词。
- 根因：RATING-ALIGN-004 起中文5档 **key=label 恒等**（scoring_engine._map_rating 返回 `(grade, grade)`），而操作建议矩阵在部分持仓组合下返回与评级同名的词（持有观望+持仓浮亏 → '持有观望'），三行信息完全相同。
- 实现（仅展示层，条件显示——有增量信息才出现）：
  - `app.js` 评分卡：`rating_label` 行仅在 ≠ 评级时显示；"建议："行仅在 ≠ 评级时显示。例：持有观望+浮亏 → 只留"评级 持有观望"徽章；持有观望+浮盈 → 徽章 + "建议：持有"；
  - `advisor._build_markdown_single`（日报 markdown 拼装函数，非 generate_advice，无 R13 约束）：`（X级 · label）`中 label=评级时省略后缀；`操作建议`行与评级同词时不单列。
- 不变：`rating_label`/`action_advice` 字段值与 API 返回（数据契约不动，仅显示端收敛）；历史快照里旧引擎 A/B/C 标签 ≠ 评级，仍正常显示。
- 验证：**pytest 636 passed**；红线核验 **28/28**；`node --check` 通过；advisor ruff 干净；服务重启 health 200。

## [2026-08-23] 浮亏持仓网格重构：两级止损+分批补仓+分批回本（021AQ）

- 用户需求：对已经亏损的个股，操作网格计划不能简单地只给"一个补仓位、回本清仓"。
- 原设计问题（浮亏时）：① 补仓只有一档（10%），跌得深没有第二档、也没有下行风险档；② 止盈目标低于回本价时建议"回本清仓 100%"一把梭——死等回本可能永远等不到，等到了又全卖踏空反弹；③ 止损线只是卡片上一条隐形价，网格里没有对应动作档。
- 新设计（`price_advisor._build_grid` 有持仓浮亏路径，全部档位仍自下而上递增）：
  - **两级止损**：止损清仓位（100% @止损价−1ATR）→ 破位减仓位（50% @止损价）——跌破止损先减半控损，减半后仍下行才清仓离场；
  - **分批补仓**：补仓一档（10% @max(止损+0.5ATR, 现价−1ATR)，**公式为回测补仓区间口径锚点，未改**）→ 补仓二档（15% @现价−2.2ATR，与一档拉开 ≥0.3ATR 间距，被地板抬平时自动跳过，合计 ≤25%）；
  - **分批回本**（止盈目标 < 回本价场景）：回本减仓位（50% @回本价）→ 回本清仓位（100% @max(回本+0.6ATR, 回本×1.02)）——回本先落袋一半，剩余在略上方清掉；
  - 止盈目标 ≥ 回本价的浮亏场景保持原"回本减仓30% → 第一止盈50% → 最终止盈100%"；浮盈路径不变；
  - S4 已破止损防御分支改为纯离场档（止损清仓 + 反抽减仓@现价上方，无补仓档）——注：现价锚定止损（021AJ 后止损=现价×0.89/0.84）下 S4 常态不可达，此为防御代码。
- 消费端口径对齐（补仓区间取**最高档**=最先触发的补仓机会）：`app.js` 021AP 补仓区间行取 max(补仓档价)；`price_backtest._check_hit` grid 回退路径同样取 max（主路径 add_price 标量即一档公式，回测指标不受影响）。
- 修复实施中发现的档序 bug：补仓二档（更低价）最初追加在一档之后破坏递增约定，已改为按价格升序列出（二档在前、一档在后，执行时价格从高到低自然先触发一档）。
- 验证：新增 `tests/test_price_advisor_021aq.py` 9 条（分批回本/两级止损/两档补仓及升序/地板抬平跳二档/浮亏目标≥回本保留阶梯/浮盈不变/无ATR回退/S4防御）；**pytest 636 passed**；红线核验 **28/28**；`node --check` 通过；改动文件 ruff 干净；服务重启 health 200。

## [2026-08-23] 分析报告价格建议卡补齐"补仓区间/持有区间"（021AP）

- 用户需求：回测中心的有持仓统计表里有"补仓区间""持有区间"两列，个股分析报告里却没有。
- 分析：两个概念的数据在价格建议里都已存在——补仓位 = 网格计划 type='add' 档（现价下方 max(止损+0.5ATR, 现价−1ATR)，10% 仓位）；持有区间 = [止损价, 止盈价]（股价在其中应继续持有）。只是报告卡片没有展示。
- 实现（纯前端，`app.js` renderFullReport 持仓中分支，止损价行后新增两行）：
  - **补仓区间**：`≤ X.XX`（紫色，与网格表补仓位同色；hover 提示"回落到该价以下出现补仓机会，10% 仓位档；已破止损时不提供"）——S4 破止损时网格无 add 档，该行自动不显示（与回测"无 add 价不计入"口径一致）；
  - **持有区间**：`止损价 ~ 止盈价`（hover 提示"区间内继续持有；跌破下沿止损、涨过上沿止盈"）。
- 无持仓分支不加（回测中心这两列本就只统计"有持仓样本"，无持仓侧的买入区间/目标价/止损价卡片已齐）。
- 验证：`node --check` 通过；**pytest 627 passed**；服务重启 health 200。

## [2026-08-23] 分组两侧独立 + Tab 自动隐藏空组（021AO）

- 用户需求：持仓管理和自选股的分组如何设计好一点——持仓页有很多用不到的分组。
- 诊断：`POST /api/groups` 与前端建组默认 `sync_to_other_type=true`，在任一侧建组都会自动在另一侧建同名空组（实测持仓侧 6 组中 5 组为 0 条：港股/科创板/创业板/沪A/深A，均为自选股建组的被动产物）。
- 设计（两侧分组彻底独立）：
  1. **建组默认只建当前侧**：同步创建改为显式勾选（分组管理弹窗新增"两侧同步建"复选框，默认不勾）；`POST /api/groups` 默认 `sync_to_other_type=False`；兼容端点 `POST /api/portfolio/groups` 移除自动同步；
  2. **改名默认只改当前侧**：`PUT /api/groups/<id>` 默认 `sync_to_other_type=False`（前端不再硬编码 true）；两侧同名时仍可显式传 True 联动改名；
  3. **Tab 栏自动隐藏空组**：持仓/自选股两页 Tab 只显示有条目的组（选中组变空时自动回"全部"）；空组仍在"管理分组"弹窗中可见可删，删除后不影响另一侧；
  4. **一次性清理**（备份 `db_backup_20260823_110130_pre021ao.db`）：删除持仓侧 5 个空组，持仓页现在只有"全部 + 核心持仓(8)"；自选股侧 6 组均在用，未动。
- 验证：新增 `tests/test_groups_021ao.py` 4 条（默认不同步建/显式同步建/默认不同步改名·同名联动/兼容端点不再同步）；**pytest 627 passed**；红线核验 **28/28**；`node --check` 通过；改动文件 ruff 干净；服务重启 health 200。

## [2026-08-23] 分组完全自定义——移除预置默认组（021AN）

- 用户需求：持仓管理和自选股页面的"短线关注/观察池"等组是系统默认建的，希望组全部自定义创建，不要默认组（或默认组可删除）。
- 根因：`db_manager.init_database()` 每次启动都 `INSERT OR IGNORE` 三个默认组（核心持仓/观察池/短线关注 × 自选股/持仓两类型）——组虽可在"管理分组"里删除，但**删后下次重启会复活**。
- 方案（取用户首选：组全部自定义）：
  1. 删除默认组播种代码——新装环境零预置组；
  2. 存量库一次性归一：`UPDATE groups SET is_default=0`（先备份 `db_backup_20260823_104751_pre021an.db`；6 个默认组标记清除，组与其内股票原样保留，不想要的可随时在"管理分组"里删，删后不再复活）；
  3. 前端删除确认文案改为"移至「未分组」"（原"移至默认分组"表述已失效）。
- 不变：分组删除仍只删当前类型（另一类型同名组不动，与创建时的双向同步对称）；组内记录删除组后 `group_id` 置 NULL 进"未分组"；`is_default` 列保留（历史遗留，分组域无任何逻辑依赖它）。
- 验证：新增 `tests/test_groups_021an.py` 3 条（全新库零预置组 / 删除后重启不复活 / 存量 is_default=1 组可删且记录移未分组）；**pytest 623 passed**；红线核验 **28/28**；`node --check` 通过；改动文件 ruff 干净；服务重启 health 200，`/api/groups` 两类型各 8 组且 `is_default` 全为 0。

## [2026-08-23] 交易流水新增"红利补税"类型（021AM）

- 用户需求：流水的分红是派息金额，但股息红利差异扣税与派息日不同天发生（A股按持股期限在卖出时补扣 10%/20%；港股通派息时即代扣 20%），没有地方记录，已实现盈亏失真。
- 设计：新增独立流水类型 `dividend_tax`（红利补税）——一条独立流水、独立日期（实际扣税日）、独立金额（补扣税款，正数存储），与派息分红行解耦。不建外键关联（备注注明关联派息日即可，零代码友好）。
- 实现：
  - `portfolio.py`：`_recalculate_holding` 新增分支 `realized_pnl -= (amount + commission)`；新增/编辑/筛选的类型枚举加入 `dividend_tax`；全局流水汇总新增 `total_dividend_tax` 且 `net_amount = 卖出 + 分红 − 补税 − 买入`；
  - `index.html`：流水弹窗类型下拉加"红利补税"（title 注明两种税制）；筛选下拉同步；表单新增"金额(可选)"直填框（分红/补税直接填总额，留空回退 成交价×数量）；
  - `app.js`：`tradeTypeLabel/tradeTypeColor` 统一映射（买入红/卖出绿/分红橙/补税紫，弹窗与全局列表两处复用）；编辑回填 amount；汇总卡新增"红利补税"；分红/补税未填金额时前端拦截提示；
  - 编辑的二次验证重试分支同步携带 amount。
- 兼容性：既有 buy/sell/dividend 行为零变化（仅枚举扩展）；T+1 锁定/大额二次验证/清算限制对补税行同样生效；无 DB 结构变更（trade_type 为自由 TEXT 列）。
- 验证：新增 `tests/test_dividend_tax_021am.py` 6 条（补税扣减盈亏/独立补录/编辑重算/删除回滚/汇总与筛选/非法类型拒绝）；**pytest 620 passed**；红线核验 **28/28**；`node --check` 通过；改动文件 ruff 干净；服务重启 health 200。

## [2026-08-23] 港股价格建议市场校准（021AL）

- 用户需求：港股价格建议报告——真实样本仅 28 个（<30）；目标价命中 T+20 17%（偏乐观）；止损触发 18%（偏紧）；风险收益比 0.97。
- 背景：021AJ 校准时港股尚无真实样本（常量只含 a_stock），此后港股积累 28 条 anchor 样本，预警出现。
- 诊断（28 条真实样本归因）：
  - 空仓/持有观望 18 条：目标距中位 **+25%**（`max(布林上轨,60日线)` 在港股大波动下推得过远）vs 实际 20 日高点中位 +10.4% → 命中 17%；
  - 持仓/持有观望 5 条：止损 -5%（评级分档）→ 触发 40%；
  - 港股波幅约 A股 1.5 倍（20日高点 P75 21.4% vs A股 7.7%，与 021P 观望带比 1.75 同量级）。
- 实现（与 021AJ 同构，双模块同步）：
  - `TARGET_CAP['hk_stock']=0.11`（目标封顶 +11%）、`POSITION_STOP_PCT['hk_stock']=0.16`（持仓止损统一 -16%）——查表逻辑 021AJ 已就位，港股值自动生效；
  - 更新过时注释与测试（`test_hk_capped_11`/`test_hk_floor_kept`/`test_hk_uniform_16` 替换原"港股不封顶/沿用分档"断言）。
- 重跑验证（force=True 全市场，自动备份 `db_backup_20260823_102214`，3568/3568 成功）：
  - **港股：目标命中 17% → 52.2%（≥35% 达标）、止损触发 18% → 10.7%（≤15% 达标）、RR 0.97 → 4.87（≥1 达标）——三条预警全部解除**；
  - A股逐位不变（39.6%/4.8%/8.25，常量未动）；
  - 样本提醒（已写入常量注释）：港股 28 条（持仓侧仅 5 条）为波动率推算初值，≥100 条后应复核；"样本不足 28<30"提示会随数据积累自行消失。
- 验证：**pytest 614 passed**；红线核验 **28/28**；改动文件 ruff 干净；服务重启 health 200。

## [2026-08-22] 港股动态判定波动率标定（021AK）

- 用户需求：港股动态准确率 33%（70条）低于随机——怎么解决？
- 诊断（港股 70 条动态样本全量归因）：
  - **引擎构成**：65/70 条来自已退役旧引擎（v5 仅 5 条，2/5=40%）——33% 主体是给旧引擎"验尸"，非现行规则表现（报告的引擎构成声明已会明示）；
  - **带宽与窗口错配（主因）**：±3.5% 观望带按固定 1 周标定（021P），而港股动态窗口中位 7 天、最长 23 天，收益极度分散（P10 -16.6% / P90 +21.5%，64% 落 ±4.5% 带外）——无涨跌停+T+0 下窗口越长噪声越大，固定带装不下；
  - 逐条重判模拟：按 σ·√t 标定带宽 33% → 57%。
- 实现（`backtest_engine.py`，仅港股动态路径，A股固定带口径不动）：
  - `_judge` 新增 `vol_scale` 参数：观望带宽/方向档阈值/边界容忍均按系数缩放；方向档保留最小有效幅度守卫（up ≥0.3% / down 判错 ≥0.5%），防系数过小时噪声判定；
  - `_sigma_daily`（日收益率σ）+ `BacktestEngine._calc_dynamic_vol_scale`：scale = σ(评级日前20日)×√(窗口天数)/3.5，夹 [0.5, 4.0]；A股/日期异常/K线不足返回 None 退回固定带；
  - 港股市场报告口径说明改为波动率标定专属文案（021P 的固定 ±3.5% 声明仅保留在固定周期段）；
  - 新增 `tests/test_backtest_021ak.py`（14 条：观望/方向档缩放、最小幅度守卫、σ 计算、门控分支、s=1 等价性）。
- 重跑验证（先备份 `db_backup_20260822_*_pre021ak.db`，667/667 成功）：
  - **港股动态 33%（70条）→ 56.9%（51条）**：19 条模糊样本按同比例容忍不计入；观望档 34%→69%、推荐买入 31%→47%；"低于随机"预警解除（落入 45~60% 谨慎带，口径说明已附）；
  - A股不受影响（164/296=55.4%，与改前逐位一致——vol_scale 仅港股路径传入）；
  - 诚实提示：港股 v5 样本仅 5 条，57% 主要基于历史样本的口径修正；现行规则的港股验证仍需数据积累（引擎构成声明会明示）。
- 验证：**pytest 613 passed**；红线核验 **28/28**；改动文件 ruff 干净；服务重启 health 200。

## [2026-08-22] 价格建议市场校准：目标封顶+止损放宽+命中口径修复（021AJ）

- 用户需求：目标价命中 T+20 15%（偏乐观）/ 止损触发 18%（偏紧）/ 风险收益比 0.83——有改善方案吗？（用户选 C 完整方案）
- 诊断（price_backtest 真实样本 125 条，2026-07-16 起）：
  - **口径 bug**：`_check_hit` 把无目标价的持仓行也计为"目标未命中"（take_profit 有同坑防护注释，target 漏防）——29/125 条持仓行全被算成未达标；
  - **距离错配**：空仓目标价中位 +9.8~12.1%，而实际 20 日高点 P75 仅 +7.7% → 命中仅 15~20%；持仓止损按评级分档 3~8%（建议减仓 -4%），而 20 日低点中位 -5.7% → 触发 66%（止损 18% 几乎全由该档贡献，19/22 次）；
  - 评级分档止损无数据支撑（125 条持仓侧仅"建议减仓"一档有样本）。
- 实现（C 完整方案，price_advisor 与 price_backtest 双向同步）：
  - `_check_hit` 口径修复：buy_range/target/stop_loss 仅在对应价位存在时才计"未命中"（与 take_profit 对齐）；
  - 新常量 `TARGET_CAP={'a_stock': 0.075}`（无持仓目标价封顶 +7.5%，20 日高点 P75 定位）与 `POSITION_STOP_PCT={'a_stock': 0.11}`（持仓止损统一 -11%，波幅 P15 定位）；港股暂不入表（无真实样本，沿用旧逻辑，待样本按 021P 波幅比 ~1.75× 复核）；
  - `_gen_no_position`/`_gen_with_position` 新增 market 参数（默认 a_stock 向后兼容），`generate_price_advice` 与回测主循环传入股票市场；
  - 新增 `tests/test_price_backtest_021aj.py`（14 条：口径修复三态/封顶与下限/A股统一止损/港股分档保持/双模块常量与输出同步）。
- 重跑验证（force=True 自动备份 `db_backup_20260822_234259`，3331/3331 成功，真实样本 125 条构成不变）：
  - **目标命中 15% → 39.6%**（≥35% 达"合理"线）；**止损触发 18% → 4.8%**（≤15% 达"适中"线）；**风险收益比 0.83 → 8.25**；持仓-建议减仓止损触发 66% → 7%；三条预警全部解除；
  - 代价（已向用户说明）：空仓目标距 +10~12% → +5.3~6.8%（单次止盈赚得更少）；持仓止损 -4% → -11%（真破位单次亏得更多，换不被日常波动洗出）。
- 验证：**pytest 599 passed**；红线核验 **28/28**；改动文件 ruff 干净；服务重启后 health 200。

## [2026-08-22] 动态窗口权重优化器：重放+前向验证+数据门槛（021AI，C 阶段）

- 用户需求：承接 021AG/021AH——用户确认"先修尺子再跑 C"；021AH 修完尺子后启动 C（以动态窗口指标为目标重配权重）。
- 预研发现（决定方案形态的三个硬事实）：
  - M9 周度优化器（optimizer_engine）以 T+1 日准确率为代理调权，且"安全阀"比较写入权重前后的**存量回测行**——权重写入不改变存量行，阀门永远不触发（strategy_params 史料：07-23/07-26 两次优化 before==after 全等；当前 A股权重正是 M9 基于旧引擎时代数据调出的）；
  - 维度分跨引擎切换点（08-14）均值漂移 7~11 分（tech 59.5→52.6、fund 51.4→57.3）——两代数据混池搜权重必然引入口径噪声；
  - 基座权重只影响 10 只股票（14/24 只A股命中行业覆盖用固定行业权重），有效样本天然减半。
- 实现（新模块，不动 R7 保护的 scoring_engine，不动 M9 旧路径）：
  - `modules/dynamic_optimizer.py`：维度分重放（候选权重→总分→市场差异化档位→021AG 迟滞→改评点→动态窗口→021AH 判定）+ 四维网格（0.05 步长，617 向量）+ 前向验证（70/30 按日期切分，测试段不参与选择）+ 三重数据门槛（v5 纯净评级日 ≥300 / 训练测试段各 ≥100 可判定窗口 / 测试段提升 ≥3pp），任一不过不改权重；
  - `scripts/run_dynamic_optimizer.py`：零代码中文报告入口（默认只读；`--apply` 且门槛全过才写 `config_weights.json` + strategy_params 审计）；
  - `tests/test_dynamic_optimizer.py`：13 条核心函数测试（档位映射同序/网格完备约束/迟滞重放/窗口构建分段/边界排除/门槛常量）。
- 首跑结果（A股，10 只非覆盖股票 × 216 评级日，v5 纯净仅 48 天）：
  - 当前权重：训练段 55.6% / 测试段 85.2%；最优候选（kline 0.35/fund 0.25/cap 0.35/news 0.05）训练段 64.1% / 测试段 80.0%——**测试段反而 −5.2pp，前向验证门正确拦截**（训练段选优在未见数据上失效，典型过拟合签名）；
  - 裁决：维持当前权重，不改。v5 纯净样本约一个月后（每日 +20~25）达 300 门槛重估；期间可周期性重跑观察。
- 验证：**pytest 586 passed**；红线核验 **28/28**；新文件 ruff 干净。

## [2026-08-22] 动态回测度量修复：观望档边界模糊不计入（021AH）

- 用户需求：承接 021AG 诊断（动态准确率接近随机）——用户确认"先修尺子再跑 C"。
- 根因（021AG 仿真实证）：动态窗口终点=下次改评日，而改评主要由价格跑出区带触发——观望档 55% 的窗口以"刚跑出 ±2% 带"收尾，属口径结构性偏置而非方向判断错误；B（迟滞）上线后窗口变长，该偏置反而放大（仿真 54.2%→49.2%），必须先修度量。
- 实现：
  - `backtest_engine._judge` 新增 `neutral_borderline` 参数（默认 False，全部既有调用方零影响）：观望档跑出带外幅度 < `NEUTRAL_BORDERLINE_TOL=1.0%` 时返回 None（模糊不计入）；港股 ±3.5% 带同样叠加 1% 容忍；涨/跌档不受影响；
  - 仅 `_compute_dynamic`（动态路径）启用——固定周期口径无"终点=跑出"的相关性，不启用；
  - 市场报告动态准确率三条评语各附口径说明（021AH，中性色调）；
  - 新增 `tests/test_backtest_021ah.py`（20 条：动态/固定口径分离、边界精确值、港股叠加、涨跌档不受影响、常量约束）。
- 重跑验证（先备份 `backups/db_backup_20260822_230453_pre021ah.db`）：`batch_backtest(force=True)` 667/667 成功；**动态准确率 46.6% → 51.1%**（观望档 47.5% → 55.1%，30 条模糊样本不再硬判错）；真实短板仍在（推荐买入 41.4%）——正是 C（权重优化）要对症的目标。
- 验证：**pytest 573 passed**；红线核验 **28/28**；改动文件 ruff 干净。

## [2026-08-22] 评级变更迟滞：抗边界抖动（021AG）

- 用户需求：动态准确率 50%（313条）接近随机，如何提升？（诊断见 021AG 前置分析）
- 诊断（backtest_results 全量 380 条 + ratings_history 7-8 月重放）：
  - 动态准确率 46.6%，观望档占 58% 样本且 55% 以"跑出 ±2% 带"收尾——窗口终点=改评日≈跑出区间日，判定结构性偏置；
  - 改评抖动严重：67% 改评间隔 ≤3 天、42% 评级日分数距档位边界 ≤3 分——大量换挡是边界蹭线（whipsaw）；
  - 口径模拟：换 alpha 口径无效（47.3%）；观望边界 ±1% 模糊不计入 +4pp（50.6%）；
  - 真实短板：买入档 42%/减仓档 46%，中期方向辨别力不足（T+1 日 60.3% vs 拉长即衰减）。
- 实现（方案 B，用户确认"先做B再做C"）：
  - 新模块 `modules/rating_hysteresis.py`：纯函数迟滞（升档需 ≥目标档 min+MARGIN；降档需 <原档 min−MARGIN；多档跳变天然满足立即换挡；首次评级不受影响）；边界与评分引擎同源（A股 RATING_THRESHOLDS / 港股 hk_stock.rating_overrides 热加载）；
  - `config.py` 新增 `RATING_HYSTERESIS_ENABLED=True` / `RATING_HYSTERESIS_MARGIN=3.0`（开关回退无需改代码）；
  - `advisor.generate_advice` 步骤 2b 接入：迟滞后评级贯穿操作建议/风险提示/is_change/写库/日报全链路；`_save_analysis_results_for_v5` 移至迟滞后（analysis_results 与 ratings_history 必须同档）；结果新增 `rating_hysteresis` 字段、markdown 附说明（R13 行为锁，§6 豁免已登记）；
  - 新增 `tests/test_rating_hysteresis.py`（升/降/带内/带外/精确边界/多档跳变/港股门槛/开关/未知档/文案）。
- 历史回放仿真（2026-07-01 起 30 股 × 667 评级日，只读）：
  - 改评次数 263 → 162（**−38%**，抖动显著抑制，设计目标达成）；
  - 动态准确率 54.2% → 49.2%——**指标口径问题被放大**：窗口拉长后观望档更多时间跑出 ±2% 带（53%→45%）；
  - 叠加"观望边界 ±1% 模糊不计入"：实际 59.1% / 仿真 53.4%——度量偏置是主导项，迟滞单独看不抬指标；
  - 结论：B 的价值=换挡即高置信信号+窗口变长，指标修复需 C（或 A 口径）配合，已向用户如实呈现。
- 验证：**pytest 562 passed 全绿**（含 19 条新增迟滞测试）；红线核验 **28/28**；改动文件 ruff 干净；服务经 Watchdog 重启后 health 200。

## [2026-08-22] 经典引擎整体退役：v5 单引擎化（021AE）

- 用户需求：经典引擎和 v5 引擎有什么区别？有必要保留经典引擎吗？没必要就彻底删除经典引擎的有关内容。
- 调查结论：
  - 两引擎区别——经典引擎（`analysis_engine.py`，v4，直接读库的量化因子打分，维度级粗粒度降级）；v5（`scoring_engine.py`，经 StockData 契约，子项级 A/B/C 类降级+归一化，含港股差异化门槛与置信收缩）。v5 全面更优。
  - 保留必要性——**无**：灰度 `config_engine_switch.json` 早已 `mode=all_v5`；`ratings_history` 实证 153 行 v5 / **0 行 legacy**（最新 v5 行 08-21）；近一周日志 v5 降级仅 1 次（08-20 stock_id=1，次日批次自愈）。经典引擎仅为"v5 失败回退"而存活，双引擎徒增维护面（评分纯净四处过滤点、三处评级一致、两套因子文案兼容）。
- 删除内容：
  - `modules/analysis_engine.py`（1502 行经典引擎）、`modules/engine_switcher.py`（468 行灰度切换/熔断）、`config_engine_switch.json`、`test_engine_compare.py` / `tests/test_engine_compare.py`（对比脚本，对比对象已不存在）及对应 pyc；
  - `blueprints/system.py` 的 `/api/engine/status`、`/api/engine/rollback-all`（仅运维口，前端无入口；回退目标已删，误触发有害）。
- 迁移与改造：
  - `validate_rating_config`（三处评级一致性自检）→ 新模块 `modules/rating_config.py`（`app.py` 启动自检 + `check_redlines.py` R6 核验改引新家；不放入 R7 保护的 scoring_engine）；
  - `_dedup_news`（新闻两级去重）→ `modules/news_detail.py`（唯一消费方 watchlist 个股新闻卡）；
  - `advisor.generate_advice`：灰度分流/legacy 回退/熔断记录块移除，v5 失败直接返回 `success=False`；**签名、返回结构、ratings_history/analysis_results/daily_reports 写库不变量全部不变**（R13 行为锁，已走 RED_LINES.md §6 豁免登记）；`engine_version` 恒为 'v5'；
  - `daily_report.py` 删除 v5 fallback 检测块；看板 `engine_stats` 口径 `{'v5','legacy'}` → `{'v5','history'}`（portfolio.py + 前端同步，历史 NULL/legacy 行归入 history）；
  - 前端：引擎筛选仅剩 v5 选项，表格/标签"经典引擎"文案改"历史引擎"（历史快照仍可读）；
  - 测试：`tests/test_routes.py` 删 engine/status 冒烟项；`tests/qa_019f_isolation_test.py` 评分纯净隔离验收对象收敛为 data_adapter（原 analysis_engine 两项测试随对象删除，data_adapter 等价覆盖）；
  - 文档：AGENTS.md / docs/PROJECT_INDEX.md / docs/PROJECT_STRUCTURE.md 同步；docs/reports、docs/tasks 历史验收文档**不改**（对当时事实的记录）。
- 验证：`node --check` 通过；**pytest 全绿（543 passed）**；改动文件 `ruff` 干净（全仓存量 14 项编码声明/格式问题非本次引入）；红线核验 `check_redlines.py` **28/28 通过**；服务重启（Watchdog 自动拉起，health 200）后 report-latest 正常返回（engine_version=v5，65.0/推荐买入）、`/api/engine/status` 已 404。
- 效果：modules 净减约 1970 行死代码，评分链路单一化（评分纯净过滤点从 4 处收敛为现役 3 处），评级口径自检与新闻去重各归独立小模块。

## [2026-08-22] 持仓页「刷新」按钮并入「刷新价格」（021AD）

- 用户需求：持仓管理的「刷新价格」和「刷新」有什么区别？重复的话删掉一个。
- 调查结论：两按钮高度重复且「刷新」是弱子集——
  - 「🔄 刷新价格」`refreshPrices()`：POST `/api/portfolio/refresh-prices` **联网**拉腾讯实时行情写 `price_cache`（失败回退 K 线收盘价、保留旧价不归零），成功后重载持仓表+汇总卡+自选股列表，但**不刷分组Tab计数**；
  - 「刷新」`loadPortfolioGroups()`：**纯本地**重读（分组Tab+持仓表+汇总卡），**不联网取价**；且所有改数据操作（流水/持仓/账户/分组增删、切账户、切分组）本就自动调用相应重载，该按钮无独立价值。
- 改动：
  1. `templates/index.html`：删除持仓工具行的「刷新」按钮；
  2. `static/js/app.js` `refreshPrices()`：成功路径改调 `loadPortfolioGroups()`（级联持仓表/汇总卡/状态标签，**补齐分组Tab计数**，消除原两按钮各刷一半的差异）；失败/网络错误路径也做本地重载，完整承接原「刷新」的兜底职责；按钮 tooltip 更新说明新行为。
- 验证：`node --check` 通过；**547 passed** 全绿；重启服务（杀进程后 Watchdog 1 分钟内自动拉起，health 200）确认线上 HTML 已无「刷新」按钮、JS 已含合并逻辑。刷新页面即见。
- 生效：页面刷新后，持仓页仅剩「⚙ 管理分组」「🔄 刷新价格」两个操作按钮。

## [2026-08-22] 雷达图窗口 resize 漂移二次修复：CSS 反馈循环 + ResizeObserver（021AC）

- 用户反馈（021AB 后仍复现）：调整窗口大小，雷达图卡片里的雷达图不居中，必须刷新页面才恢复居中。
- 根因一（CSS，主因）：`#radarChart` 原 `flex: 1 1 auto`——flex-basis `auto` 以**内容高度**为基准，而其内容恰是 ECharts canvas（**固定像素尺寸**）。刷新页面时 DOM 重建、容器是空 div，高度纯由 flex 布局决定 → init 几何正确、居中；调整窗口时旧 canvas 高度把容器（连带卡片/grid 行高）**粘在旧尺寸上**，`resize()` 与圆心重算读到的都是被污染的几何 → 不居中。021AB 只修了"监听缺失"，重算链路读到的几何本身是错的，故仍漂移。
- 根因二（时机）：window resize 事件回调时 CSS Grid stretch 可能尚在过渡态，读到的几何非终态。
- 改动：
  1. `static/css/app.css`：`#radarChart` `flex: 1 1 auto` → **`flex: 1 1 0`**（basis 归零，容器高度只由卡片 flex 布局决定，与 canvas 内容彻底解耦，切断反馈循环；`min-height:230px` 下限保留）；
  2. `static/js/app.js` `_bindRadarResize(chartDom)` 升级：**ResizeObserver** 监听容器（布局完成后才回调，几何必然是终态，且覆盖断点切换等一切布局变化来源）+ window resize 兜底（老浏览器降级）；每次渲染以当前 DOM 重新 observe（innerHTML 重建后旧节点已脱离，断开重挂）；rAF 合并高频触发去抖。
- 验证：`node --check` 通过；确认运行中服务 JS/CSS 均已吐出修复后内容（020S no-cache + mtime 版本号），**刷新页面即生效，无需重启**。后端零改动。
- 关联：021AA（dispose 置 null）、021AB（resize 监听独立绑定）——本条补齐几何污染与监听时机两环。

## [2026-08-22] 报告页雷达图窗口 resize 漂移修复（021AB）

- 用户反馈：分析报告点刷新后，调整窗口大小雷达图会漂移。
- 根因（`static/js/app.js`）：全应用唯一的窗口 resize 监听绑定在 `_renderKlineChart` 体内（`window._klineResizeBound`），内含雷达图的 resize + 圆心重算；但 **020R 移除报告页 K 线卡后 `_renderKlineChart` 已无任何调用方（死代码）**，监听从未绑定——窗口尺寸变化时 `.radar-card`（CSS Grid）跟随伸缩而 ECharts canvas 保持 init 时的固定像素尺寸，雷达图形锚定画布左上角，相对卡片中心漂移。次因：`_ensureVisible` 渲染后 50ms 的补排只调 `resize()` 不重算圆心，init 时按 Grid stretch 过渡态几何算出的 center 百分比过期，同样造成偏移。
- 改动（纯前端，`_renderRadarChart` 相关重构）：
  1. 圆心计算提为共享函数 `_radarCalcCenter(chartDom)`（公式不变：卡片几何中心补偿标题偏移，边界保护 5%~95%），渲染与 resize 复用同一口径；
  2. 新增 `_radarResizeRecenter()`：先 `resize()` 画布 → rAF + 120ms 双触发按新几何重算 center（兜底 CSS Grid stretch 延迟稳定），全程 `isDisposed()` 守卫（021AA 实例生命周期）；
  3. 新增 `_bindRadarResize()`：雷达图**独立**的窗口 resize 监听（幂等，`window._radarResizeBound`），随 `_renderRadarChart` 挂载，不再依赖 K 线图渲染；
  4. `_ensureVisible` 渲染后 50ms 补排改调 `_radarResizeRecenter`（resize 后重算圆心，消除过期百分比）；
  5. 清理 `_renderKlineChart`（死代码）resize 处理器中的雷达分支，留注释指明雷达 resize 已独立绑定、勿再挂回（该误导性结构正是本 bug 隐藏至今的原因）。
- 验证：`node --check` 通过；确认运行中服务 `/static/js/app.js` 已吐出修复后内容（020S no-cache + mtime 版本号），**刷新页面即生效，无需重启**。后端零改动。
- 关联：021AA（同文件上一修复——dispose 后置 null + isDisposed 兜底）。

## [2026-08-22] 分析报告刷新后雷达图空白修复（021AA）

- 用户反馈：分析报告页面一点「🔄 刷新报告」，四维评分雷达图就不显示了。
- 根因（`static/js/app.js` `_renderRadarChart`）：`renderFullReport` 每次用 `innerHTML` 整体重建 DOM（`#radarChart` 换成新节点），随后 `_renderRadarChart` 先 `_radarChart.dispose()` **但未置 null**——`_applyOption` 的 `if (!_radarChart)` 判定失效（已销毁实例仍为 truthy），跳过 `echarts.init(chartDom)`，转而对已 dispose 的实例调 `setOption`；经下载 ECharts 5.5.0 源码核实，实例级 `setOption` 对 `_disposed` 实例是**静默 no-op**（仅告警即返回），故雷达图永远空白。首次打开正常（此时 `_radarChart` 为 null 走 init），点刷新/快照过旧触发 021G 自动重评/切换股票再进报告页均复现。同文件 `_renderKlineChart` 的"dispose 后无条件 init"是正确参照；5530 行"保留实例避免闪屏"注释与 5458 行无条件 dispose 本身互相矛盾（DOM 重建后实例不可复用），一并修正。
- 改动（纯前端 2 处，`_renderRadarChart` 内）：
  1. dispose 后同步 `_radarChart = null`，保证在新 DOM 上重新 `echarts.init`；
  2. `_applyOption` 增加 `_radarChart.isDisposed()` 兜底——实例一旦被销毁即重新 init（自愈保险）。
- 验证：`node --check` 通过；确认运行中服务 `/static/js/app.js` 已吐出修复后内容（Flask 每请求直读磁盘 + 020S no-cache/mtime 版本号），**刷新页面即生效，无需重启服务**。后端零改动，pytest 不受影响。
- 关联：021K 实时/快照两路径数据结构均带 `dimensions[*].score`，本问题与数据无关，纯前端实例生命周期 bug。

## [2026-08-21] 港股报告独立批次：16:00 收盘后再生成（021Z）

- 用户需求：港股的报告晚点生成——港股 16:00 收盘，晚于 A股主批次（15:54），主批次生成时港股尚未收盘、数据非定稿。
- 改动（`modules/daily_report.py`）：
  - `generate_daily_report` 新增 `market_filter` 参数（None=全部/'a_stock'/'hk_stock'），手动 API 不受影响；
  - A股主批次（窗3 后 ~15:54）改为仅生成 `market_filter='a_stock'`；
  - 新增港股独立批次 `_HK_REPORT_TIME=(16,10)`：港股收盘后 10 分钟单独重算全部港股当日报告（交易日 force=True 与 021F 口径一致；非交易日默认复用）；一次性 daemon Timer 链式注册次日，`finally` 保证异常不断链；`start_scheduler` 启动时按今日是否已过 16:10 注册今天/明天；`stop_scheduler` 同步取消。
- 职责边界：预警扫描/指数刷新/行业快照/延迟补采仍只在 A股主批次挂载一次，港股批次不重复。
- 测试：`test_scheduler_force.py` 扩展 4 项（主批次市场过滤 / 港股交易日 force / 周末复用 / 异常不断链），**547 passed** 全绿；ruff 干净。
- 生效：重启服务后即时生效；当日已生成的港股报告由一次性脚本按新口径补算。

## [2026-08-21] 收盘后采集与日报批次整体前移 75 分钟（021Y）

- 用户需求：每天收盘后的采集和报告生成还能提前吗？
- 现状取证（08-20 日志）：三窗实际采集仅数秒（8.6s/0.1s/0.13s），报告批次 145s，最终 ~17:12 完成——30 分钟窗间距是 019X 东财限频时代的历史包袱，021L 主源换腾讯系批量后已无意义。
- 改动（`modules/daily_report.py`）：`_CAPITAL_WINDOW_TIMES` 由 (16:10/16:40/17:10) 前移为 **(15:30/15:42/15:54)**；`_schedule_next` 次日窗1、`start_scheduler` 的"今日三窗已结束"判断线同步改为从 `_CAPITAL_WINDOW_TIMES` 派生（消除硬编码）；启动日志/补采日志文案同步。
- 时间依据：A股 15:00 收盘，15:30 起步为资金面定稿保留 30 分钟缓冲；报告约 **15:57 完成**（原 ~17:12）。
- 提前代价（已知悉）：个别股票资金面若未定稿，当日分数按当时数据计算，由次日批次纠正；补采链路（30 分钟延迟补采 + 缺口检测调度器）兜底不变。
- 实测（当日生效）：15:38 重启服务，窗1 错过钟点 1 秒补触发成功（8 只采集完成），窗2 15:42 / 窗3 15:54 正常注册；543 passed 全绿。
- 生效：已重启服务即时生效。

## [2026-08-21] 交易流水支持直接填写手续费（021X）

- 用户需求：交易流水填写时能直接填写手续费。
- 数据库：`trade_records` 新增 `commission REAL DEFAULT 0` 列（`_migrate_columns` 幂等迁移，存量行自动补 0；真实库副本演练通过）。
- 重算口径（`blueprints/portfolio.py` `_recalculate_holding`，按账户维度不变）：
  - **买入**：手续费计入成本基数——`total_cost += 数量×成交价 + 手续费`，摊高加权平均成本；
  - **卖出**：手续费扣减已实现盈亏——`realized_pnl += (卖出价-均价)×数量 - 手续费`；
  - **分红**：一并扣除（如划扣费）；
  - `amount` 字段保持纯成交金额口径不变，手续费独立成列便于审计；大额二次验证阈值仍按 `amount` 判定不受影响。
- API：新增/编辑流水均接受 `commission`（非负数字校验）；流水列表 `tr.*` 自动带出。
- 前端：流水弹窗新增「手续费(可选)」输入框（悬停提示口径）；流水表新增手续费列；编辑流水回填、保存/取消/清空全链路覆盖。
- 测试：新增 4 项（买入计入成本 / 卖出扣盈亏 / 负数拒绝 / 编辑回填+T+1 回填绕过），**543 passed** 全绿；ruff 干净。
- 生效：重启服务自动迁移，刷新页面即见。

## [2026-08-21] 多交易账户支持：持仓域按账户隔离 + 全账户汇总视图（021W）

- 用户需求：支持多交易账户。确认范围：仅持仓域隔离（自选股/分析/预警保持全局共享）；同一股票允许在多个账户分别持仓；需要「全部账户」汇总视图。
- 数据库（`database/db_manager.py`）：
  - 新增 `accounts` 表（name 唯一 / broker / notes / is_default / display_order）；
  - `holdings` 唯一约束 `UNIQUE(stock_id)` → `UNIQUE(account_id, stock_id)`——SQLite 无法 ALTER 唯一约束，采用标准表重建迁移 `_migrate_holdings_multi_account`（DROP 前自动备份，备份失败中止，红线 R11）；
  - `trade_records` 新增 `account_id` 列（`_migrate_columns` 登记 + 迁移回填：优先取关联持仓账户，断链流水归默认账户）；
  - 默认账户「默认账户」幂等创建（is_default=1，存量数据归属锚点）。实测真实库副本演练：7 条持仓 + 33 条流水全部正确归入，幂等重跑通过。
- 后端 API（`blueprints/portfolio.py`）：
  - 账户 CRUD：`GET/POST /api/accounts`、`PUT/DELETE /api/accounts/<id>`；删除守卫三重——默认账户禁删、最后账户禁删、有持仓需 `force_confirm=true`（删除时流水保留并归入默认账户）；
  - 存量端点全部兼容升级：holdings/trades/summary/trade-suggestion 支持 `?account_id=` 过滤（缺省/'all'=全部账户）；持仓 upsert 冲突键改 `(account_id, stock_id)`；多账户同股删除未指定账户时 409 并列出候选；
  - 核心重算函数 `_recalculate_holding(cursor, stock_id, account_id)` 按（股票, 账户）维度严格隔离，两账户成本/盈亏互不影响；流水编辑支持跨账户移动并双向重算；
  - summary 全账户视图新增 `accounts_breakdown` 分账户汇总（市值/浮动盈亏/已实现/仓数），ETag 口径随内容自动更新；
  - 风控阈值不变：T+1 锁定、大额二次验证、成本修正冷却照旧，清算判定改为按账户维度。
- 多行持仓兼容（同股多仓不再重复行/取错行）：`blueprints/watchlist.py` 自选股列表与 `modules/export_engine.py` 导出改「最大持仓子查询」JOIN；`blueprints/portfolio.py` 批量评分看板同法；`modules/advisor.py` `_read_position`、`price_advisor.py`、`price_backtest.py` 持仓读取加 `ORDER BY quantity DESC LIMIT 1`（advisor 仅数据读取辅助函数，B24 红线 `generate_advice` 未触碰）。
- 前端（`static/js/app.js` + `templates/index.html`）：
  - 持仓页顶部账户切换条（全部账户/各账户，含在仓计数）+ ⚙ 管理账户弹窗（列表/新建/改名/删除）；
  - 持仓表全账户视图显示「账户」列；汇总卡下方分账户汇总条（点击即切换到该账户）；
  - 添加持仓弹窗可选归属账户（编辑模式锁定不可变更）；流水弹窗新增交易可选账户、流水表显示账户列；全局流水页加账户列。
- 测试：新增 `tests/test_accounts.py` 18 项（默认账户幂等、CRUD 守卫、同股双仓独立成本、重算隔离、汇总聚合口径、409 歧义、旧调用兼容）；**539 passed** 全绿；`node --check` 通过；mypy/ruff 无新增错误。
- 生效：重启服务后自动迁移（已有数据零丢失，全部归入「默认账户」），刷新页面即见账户切换条。

## [2026-08-20] 基本面/资金面/消息面按技术面样式改造：打分子项分组折叠（021V）

- 用户需求：三个维度参考技术面（021S）的显示效果改造——打分子项完整展示 + 分组折叠。
- 后端（`blueprints/analysis.py` 新增 `_attach_scoring_subitems`，纯展示层、引擎零改动）：
  - 只读复用评分引擎（`score_dimension` + `FUNDAMENTAL_SUBITEMS`/`CAPITAL_SUBITEMS`/`NEWS_SUBITEMS`），一次 `load_stockdata_from_db` 供三维共用；
  - 各维明细 dict 附带 `scoring_score`/`scoring_subitems`（name/score/normalized_weight/completeness/degradation/detail）；
  - **两路径同源**：`_enrich_advice_result`（实时）与 report-latest 快照路径都调用，杜绝 021E~K 式口径漂移；
  - 失败静默降级——前端退回纯指标读数形态。
- 前端（`static/js/app.js`）：
  - 新增通用折叠工具（`_dimFoldRow`/`_dimFoldSubRow`/`_dimFoldStats`/`_dimFoldGroup`），与技术面同视觉语言（▶ 箭头旋转、组头权重+组内加权得分、子项行得分着色+权重%+引擎明细）；
  - **基本面 3 组**：📊 估值与盈利（55%：估值+盈利能力）/ 📈 成长性（25%，含业绩预告/快报）/ 🛡️ 财务质量（20%：现金流+财务健康度）；
  - **资金面 3 组**：💰 主力资金（50%，含主力5日均/南下·个股）/ 🏦 机构与筹码（30%：机构持仓+股东人数/港股机构股东数）/ ⚖️ 杠杆资金（20%，仅 A股——港股沿 021U 制度性缺失整组隐藏）；
  - **消息面 2 组**：📰 市场情绪（70%，含新闻概览/重要新闻）/ 👥 股东行为（30%三态）；
  - 组权重按基础权重显示，组内得分按归一化权重加权（自适应数据缺失降级，如港股估值28%/盈利33%重归一）。
- 实测（服务重启后）：A股#11 基本面 5 子项（估值78.5/盈利62/成长57/现金流88/财务47.5）+ 资金面 4 子项 + 消息面 2 子项全量返回；阿里巴巴（HK）资金面 主力20分60%/机构68分24%/杠杆50分17%（中性锚）/股东0%（归零）权重自适应正确。
- 验证：`node --check` 通过；`ruff` 干净；**515 passed** 全绿。
- 生效：刷新报告页即见。

## [2026-08-20] 港股报告显示修复：两融故障提示 + 完整度口径标注 + 021Q 字段断链（021U-2）

- 用户反馈（阿里巴巴）：资金完整度显示 50%、维度亮点提示"两融数据源暂不可用"是否有问题。
- 三个问题三种处理：
  1. **两融故障提示对港股是误导**（`modules/scoring_engine.py`）：该警告原对所有市场无条件输出——港股两融属制度性无披露源（非故障），现仅 A股缺失时提示（可能是数据源故障，值得暴露）。
  2. **完整度 50% 需要口径说明**（`static/js/app.js` 两处）：数值本身真实且驱动 021R 置信收缩（不能改数字），但港股资金面 2/4 子项是"制度性不存在"而非"数据丢了"——顶部完整度行显示"资金 50%(港股口径)"（悬停说明），维度亮点卡同口径标注"港股口径：两融/股东人数无披露源，主力+机构持仓已全覆盖"。
  3. **021Q 字段展示断链**（`blueprints/analysis.py`，本次真正 bug）：蓝图的 `_capital_detail_for_stock` 有独立的显式列清单 SQL，021Q 只改了 `data_adapter` 路径，漏了这条——导致报告页南下资金/机构股东数明明入库却显示缺失。补齐 4 列（south_net_buy/south_hold_ratio/inst_count/inst_count_change_pct）。
- 实测（阿里巴巴，服务重启后）：南下 -11.18亿港元·大幅减持·占比 10.16%、机构 1999 家·环比 -4.85%·机构数量减少，全部正常呈现；两融故障提示已消除；31 项回归测试通过。
- 生效：刷新报告页即见。

## [2026-08-20] 港股资金面恒缺子项行隐藏（021U）

- 用户反馈：港股资金面卡显示"杠杆资金：数据缺失（A股两融）""股东人数：数据缺失"——这些数据拿不到就删掉。
- 背景确认（021Q 调研结论）：港股两融无统一披露源（各券商自记，akshare 54 个港股接口均无）、股东人数港交所不强制披露——两字段对港股**永久缺失**，展示"数据缺失"行只制造噪音（真实替代数据：南下资金/机构股东数量已在 021Q 接入并展示）。
- 改动（纯前端 `static/js/app.js`）：
  - 新增 `_reportMarket` 缓存（随四维明细一并装配）；
  - 港股（`hk_stock`）：杠杆资金行、股东人数兜底行**整行不渲染**；A股缺失时保留提示行（可能是数据源故障，值得暴露）；
  - 港股资金面卡行序变为：主力资金 → 主力5日均 → 南下资金·个股 → 机构持仓 → 机构股东数(港股) → 南向资金(参考)——全部为有数据的行。
- 评分侧不动：杠杆资金子项在评分中的中性锚（50分×16.7%）与 021R 置信收缩刚完成校准（港股准确率 61.8%），等 021Q 数据积累后以"南下资金/机构数量顶替空位"的方式重构，届时一并消除幻影锚（直接删锚会整体上移 1~4 分，需回测证据支持）。
- 验证：`node --check` 通过；`pytest tests/test_routes.py` 18 passed；刷新报告页即生效。

## [2026-08-20] 市场行情·行业资金流表排序功能（021T）

- 用户需求：市场行情资金流向增加降序/升序功能。
- 改动（纯前端 `static/js/app.js`，后端零改动）：
  - 9 个数值列全部可排序：涨跌幅 / 主力净流入 / 5日累计 / 连续天数 / 主力净占比 / 超大单 / 大单 / 中单 / 小单；
  - 表头三态点击：首次点击 **降序 ↓** → 再点 **升序 ↑** → 再点恢复原始序（后端返回序），当前排序列高亮蓝色；
  - None/缺失值统一排最后（与排序方向无关）；行号随排序重排；
  - 顶部汇总卡与"持续流入/流出榜单"仍基于原始序（与后端口径一致），仅表格本体重排；
  - 切换交易日/刷新后排序状态保留（`_flowSortState` 缓存 + 数据缓存重渲染）。
- 验证：`node --check` 通过；刷新浏览器市场行情页即生效。

## [2026-08-19] 技术面打分子项按周期分组折叠展示（021S）

- 用户反馈：四维评分详情技术面打分子项未完整显示，要求按日线/周线/月线分组展示并加折叠效果。
- 背景：评分引擎技术面 7 子项（020R-48：月线方向 25% + 周线波段 45% + 日线择时 30%）的得分/权重/明细此前未传到前端，报告页只显示指标读数（均线/MACD/RSI 等），看不到"每个子项打了多少分"。
- 后端（`blueprints/analysis.py` `_technical_detail_for_stock`，纯展示层、021K 后处理链）：
  - 复用评分引擎同口径计算（只读调用 `score_dimension` + `TECHNICAL_SUBITEMS`，引擎零改动）；
  - `technical_detail` 新增 `scoring_score`（技术面分）/ `scoring_subitems`（7 子项：name/score/normalized_weight/completeness/degradation/detail）/ `monthly_penalty`（月线空头惩罚标记，与 analyze() 同口径文案）；
  - 失败静默降级：前端退回纯指标读数形态，不影响报告主流程。
- 前端（`static/js/app.js` `_renderTechRows` 重写）：
  - 三个 `<details>` 折叠组：**📈 月线方向层（25%）** / **📊 周线波段层（45%）** / **📉 日线择时层（30%）**，默认展开、点击标题折叠；
  - 组头显示层名 + 基础权重 + 层内加权得分（子项按实际归一化权重层内归一，跨层可比）；
  - 组内先列评分子项（得分着色 + 实际权重% + 引擎明细文案，如"MA5>MA10(月线多头)"），再列对应周期指标读数（月/周均线、MACD、RSI、布林、量能等）；
  - 子项缺失显示"数据缺失（不占权重）"；月线空头惩罚以 ⚠ 提示行呈现。
- 补充（同日用户反馈"月线的内容这么少吗"）：月线/周线读数行补齐六类指标全量——
  - **月线组**：均线（MA5/10/20 + 排列状态）· MACD（DIF/DEA/柱/多空）· RSI(14) · KDJ · 布林（位置/上下轨）· 量能（当月量/20月均量）· 月线收盘；实测 A股 44 根月线产出全部 23 个 monthly_ 字段；次新股（11 根）按各指标内部窗口守卫自动降级，前端条件渲染适应；
  - **周线组**：补 KDJ 与量能（本周量/20周均量），布林补上下轨；
  - 说明：月线评分子项只有 1 个（月线方向，020R-48 设计——月线只看大方向，层内 25% 权重全给它）；**展示读数与评分子项是两回事**，读数现已全量展示。
- 折叠符号优化（同日用户反馈"换个折叠符号，这个太丑了"）：隐藏 `<details>` 原生三角（`::marker`/`::-webkit-details-marker`），改用自定义 **▶ 小箭头**（灰蓝色 #8a94a6、10px、0.15s 过渡），展开时 CSS `details[open]` 自动旋转 90° 变 ▾；summary 改 flex 布局，层得分右对齐（替代 float）。
- 验证：`node --check` 通过；`pytest tests/test_routes.py` 18 passed；服务重启后实测 A股/港股 advise 接口均返回 7 子项完整数据（A股#11：月线方向 82.5 分/周线趋势 86.5 分…；港股#39：月线方向 25.0 分空头…）。
- 生效：服务已重启（health 200），**浏览器刷新报告页即见**。

## [2026-08-19] 港股评分准确率三连修：判定口径 / 数据底座 / 评级门槛（021P/021Q/021R）

- 背景：用户提出"港股评分准确率不如 A股，策略是否要不一样"。实证定位三个不同性质的问题：
  1. **判定口径不公**：港股观望档 1 周落点仅 12% 留在 A股标定的 ±2% 区间（A股 34%）——无涨跌停日常波幅天然装不下，不是选股错；
  2. **数据底座残缺**：港股资金面 4 子项恒缺 2（两融无披露源、户数无披露源），归一化放大剩余子项 → 资金面 >70 分占比 63% vs 完整度仅 50% → 推荐买入占比 39%（A股 16%）系统性偏多；
  3. **评级门槛单一**：全市场共用 80/65/50/30。
- **021P 判定口径差异化**（`modules/backtest_engine.py` + `tests/test_backtest_021p.py`）：
  - `_judge` 新增 market 参数，港股「持有观望」档 ±2%→±3.5%；三个调用点（固定周期/alpha/动态）同步；港股报告解读附口径声明；
  - 强制重跑港股存量回测：**准确率 45%→61.8%**，与 A股（61.9%）持平——印证"被口径压低"判断；
  - A股口径、`normalize_rating`、JUDGEMENT_MATRIX 档位零改动。
- **021Q 数据底座补强**（无新增依赖，全部复用现有 westock 命令）：
  - `raw_capital_flow` 新增 `south_net_buy`/`south_hold_ratio`：hkfund 独有 `_lgtHoldInfo` 块（南下当日净增持市值+持股占比）——港股无两融汇总，南下资金是最重要的杠杆/聪明钱指标；
  - `holder_structure` 新增 `inst_count`/`inst_count_change_pct`：shareholder 机构持仓统计 `instCount` 季度环比——港股无股东人数披露的最接近代理（注意方向语义与 A股户数相反）；
  - 契约新增 3 展示字段（**不入完整度集合、不参与评分**——021R 校准后再接入）；R2 写入模式保持 UPDATE+INSERT OR IGNORE，南下列 COALESCE 保旧值；
  - 前端资金面明细卡新增「南下资金·个股」「机构股东数(港股)」两行 + 状态着色；
  - 实测 6 只港股全部采到真实数据（如腾讯南下 +11.1 亿港元/占比 11.78%、MINI MAX 机构数环比 +73%）。
- **021R 评级门槛 + 置信收缩**（`modules/scoring_engine.py` + `config_weights.json` + `tests/test_scoring_hk_021r.py`）：
  - `_map_rating` 新增 market 参数：港股走 `hk_stock.rating_overrides`（推荐买入 65→70）热加载；全局 rating_mapping 受红线锁定不动；
  - analyze() 港股资金面完整度 <75% 时维度分向中性 50 收缩（系数=完整度/0.75），收缩说明进 data_warnings；**A股不启用**（回归测试锁定：A股 mock 基线不变）；
  - 002 校准档位（margin 68/88、main 85、vol_ratio 80）保持不变。
- 豁免登记：RED_LINES.md §6 新增 021P/021Q/021R 三条（R7/R8 涉及点均已登记）。
- 验证：`pytest tests/` **515 passed**（新增 37 项：021P 29 + 021Q 13 + 021R 12，另修复 fixture 表结构）；`ruff check`（改动文件）零告警；红线核验 28/28 通过。
- 生效：评分侧下次分析自动生效（权重/门槛热加载）；前端展示刷新页面即见。

## [2026-08-19] 总览看板移除大盘指数区（021O）

- 用户需求：看板去掉大盘指数，到市场行情页看（指数区与行业资金流同页更聚焦；看板聚焦持仓/自选）。
- 改动（纯前端，`static/js/app.js`，后端零改动）：
  - `loadDashboard` 移除 `/api/index-ratings` 并行请求与 `_dashData` 的 indices/indexUpdatedAt 字段——看板少一次网络请求，加载更快；
  - `renderDashboard` 移除大盘指数区渲染（`renderIndexSection`/`_indexRatingColor` 保留，市场行情页 `loadMarketIndexSection` 在用）；
  - `refreshIndexRatings` 成功回调：非市场页不再刷新看板（改为提示到市场行情页查看）。
- 行为：总览看板自「概览卡片」起渲染；大盘指数/评级入口唯一化为市场行情页顶部区。
- 验证：`node --check static/js/app.js` 通过；纯前端改动，刷新页面即生效。

## [2026-08-18] 港股实时价格修复：HK 前缀代码构造 bug（021M）

- 用户反馈：腾讯返回的港股价格不对。
- 实证诊断（2026-08-18 盘中 14:02）：`price_cache` 港股价格更新于当日 14:02:14 但数值全部等于 **08-17（上周五）K线收盘价**（美团 88.0/+0.92%），而腾讯实时实为 84.95/-3.08%——差近 4%；涨跌幅同样是昨日的。
- 根因：`blueprints/portfolio.py` `_fetch_realtime_price_batch` 港股代码构造用 `symbol.zfill(5)`——对库内带前缀形态 `'HK3690'`（已 6 字符）**完全无效**，生成错误代码 `hkHK3690` → 腾讯返回空行 → 港股在批量结果中全部 miss（mootdx 降级仅覆盖 A股）→ 落入 K线收盘价 fallback → **盘中刷新永远写入昨收**。注释"港股：5位数字"暗示原实现假设 symbol 为纯数字，与库内实际形态不符。
- 修复：改用权威实现 `data_collector._normalize_hk_symbol`（剥离 HK/hk 前缀+左补零至 5 位）构造 `hk03690`；其余 5 处 `zfill(5)` 用法逐一核查均先剥离前缀，安全。
- 端到端验证（修复后真实接口实测）：美团 84.95/-3.08%、小米 25.84/-0.15%、阿里 127.10/+4.01% 全部命中实时价（修复前美团 88.0/+0.92% 为昨收）。
- 验证：pytest 480 通过（+1 回归测试：HK 前缀代码归一化 + 错误代码 hkHK3690 禁止出现）；check_redlines 28/28；ruff（portfolio.py 337 行 I001 为存量问题，非本次引入）。
- 注意：需重启服务生效；生效后首次刷新价格即恢复实时。

## [2026-08-18] 资金面链路重构：腾讯 westock 提为主源，东财三层降为兜底（021L）

- 背景与实证（DB data_status 近 30 天取证）：资金面是唯一不稳定重灾区——7 月 EM 失败率 66%（212/319），8 月"失败归零"实为容错链在扛（EM 直接成功仅约 1/3，westock 顶替 70 次成为实际主力）；其余东财数据域（个股新闻/预告/快报/解禁）全部 0 失败。用户裁定：东财不稳定的高频路径撤下，唯一点保留。
- 改动（`modules/data_collector.py` `fetch_capital_flow`）：
  - 链序由「东财三层 → westock → 新浪 → 估算」改为「**腾讯 westock（主源）** → 东财三层（兜底）→ 新浪 lscjfb(sina_main) → 估算兜底」；westock 成功即短路东财（三层全部 `if saved_count == 0` 守卫）。
  - westock 行（capital_source='westock'，is_estimated=0，与东财同口径：主力=超大+大，020A 探针实证精确相等）自此计为"当日已完成"：前置校验跳过 SQL、批量补采清单 SQL、延迟补采缺口 SQL（daily_report）三处同步移出 NOT IN 排除列表——**东财日常请求密度归零**，仅 westock 失败时兜底触达。
  - 防覆盖跳过消息匹配扩展至 '腾讯自选股' 开头（与东财同待遇）；sina_main/ths_total 行仍不触发跳过（主源链可覆盖升级，测试锁定）。
  - 熔断语义修正：仅"westock 失败且东财三层也未写入"才累计熔断计数（旧序中 westock 成功前东财已挨过失败故需计入；新序 westock 成功时东财未被尝试，不构成失败证据）。
  - 历史缺口回补：补采调度器 020I 链（westock --date → 新浪 lscjfb，严格日期匹配 M-2 红线不变）为历史缺口主通道；东财 push2his 120 天批量仅在 westock 失败的兜底路径偶发覆盖。
- 不变（红线保持）：R1 估算标记/评分过滤、R3 日期严格匹配、R4 周末守卫、R15 签名锁定 `(symbol, market)`、估算层来源守卫（估算永不覆盖 westock/sina_main 行）、写入模式 UPDATE + INSERT OR IGNORE。
- 红线治理：R2 链序文本更新 + §6 豁免登记（021L，用户批准）。
- 效果预期：交易日 16:10/16:40/17:10 三窗与日报批次的东财逐只请求基本消失（清单仅含真实数据缺失股）；EM 熔断/冷却/重试对日常体验的扰动同步消失；东财保留低频唯一点（个股新闻 stock_news_em、业绩预告/快报、解禁）——这些接口实测 0 失败。
- 验证：pytest 479 通过（+6：westock 主源短路东财 / EM 兜底回退 / westock 行同日跳过 / sina 行覆盖升级 / 补采清单排除 westock 行 / 无数据仍补采）；`python scripts/check_redlines.py` 28/28；ruff 通过。

## [2026-08-18] 报告打开与手动刷新同源化：统一增强 + 过旧快照服务端实时重评（021K）

- 用户反馈：打开报告"不是最新的，点下刷新才是新版"——具体为**四维评分详情卡片、综合分析、维度亮点**的内容旧，分数却是新的。
- 根因（口径漂移，021I 原则典型案例）：「打开」（GET report-latest）与「手动刷新」（POST /advise）两条路径各自演化了十几批补丁（005/009/019L/020R-35~54），增强字段早已不同步：
  - /advise（刷新）全量增强：price_advice、结构化 advice_detail、technical/fundamental/capital/news_detail 四维明细、industry_flow_bg 等；
  - report-latest 首开（B11）只增强一半——**缺四维明细/行业背景**，卡片渲染不完整；
  - report-latest 有当日行时直接供快照——dimensions/advice_detail/亮点全部停留在报告生成时刻，**只有指标明细是实时的**，造成"分数新、内容旧"的割裂。
- 修复（`blueprints/analysis.py`，读路径重构，**不触碰 generate_advice**）：
  - ① 抽取 `_enrich_advice_result()`：实时结果的统一增强后处理，`/advise` 与 report-latest 实时路径共用——两端口径从机制上不可能再漂移。
  - ② 抽取 `_generate_fresh_advice()`：generate_advice + 统一增强，失败返回 None（调用方回落快照）。
  - ③ report-latest 新增**过旧快照服务端实时重评**：当日行 `generated_at` 超 15 分钟（`_REPORT_STALE_MINUTES`，与 021G 客户端阈值一致）且工作日 → 服务端直接重评返回（与手动刷新同源同副作用）；失败回落快照不影响打开。B11 分支同步改走统一函数。
  - 服务保证新鲜度（不依赖浏览器 JS 版本），021G 客户端自动刷新降级为冗余保险。
- 实测（2026-08-18 09:08，恒瑞 08:35 快照过旧）：① GET → 服务端重评，generated_at=09:08:34，四维明细 69 键/综合分析/亮点/行业背景/data_warnings 全量，耗时 0.20s；② 立即再 GET → 0.07s 直接用新快照（阈值生效不重复计算）；③ report-latest 与 /advise **字段集完全一致**（双向差集为空）。
- 验证：pytest 473 通过；服务已重启加载新代码（watchdog 兜底）；未触碰受保护对象（R13 generate_advice 仅被调用，R9/R10 写库不变量由 advisor 本体保证）。

## [2026-08-18] 报告新鲜度可见化：头部生成时间徽标（021J）

- 用户反馈：点开报告"还不是最新的"，点刷新才"变成最新的"。
- 实证诊断（日志+DB，2026-08-18 盘前）：打开报告时服务端 B11 **已经实时重评**（恒瑞 08:35:38 打开即算出 53.8 并写入三表；用户 14 秒后手动刷新重算仍得 53.8；爱尔 54.5→54.5 同理）——盘前底层数据未变，分数自然相同。**真正缺的是新鲜度可见性**：报告头部无任何生成时间标识，用户无法区分"刚算的新报告"与"旧快照"，只能靠点刷新求安心（021I 原则的典型案例：契约层数据是新的，UX 上不可感知等于没做）。
- 修复（纯前端，`static/js/app.js`）：
  - 报告头部新增**新鲜度徽标**：<5 分钟绿色"✓ 刚刚重新计算 HH:MM:SS"（悬停解释"分数相同=数据未变，非旧版本"）；今日灰色"生成于今日 HH:MM:SS"；更早橙色"生成于 M-D HH:MM（历史快照）"（悬停提示已自动触发重评）。
  - 新增 `_parseIsoTs()` 稳健时间解析（Python isoformat 6 位微秒截断到毫秒，兼容各浏览器 Date.parse），`_autoRefreshReportIfStale`（021G）同步换用。
- 效果：B11 服务端即时重评（~300ms）/ 021G 自动重评 / 手动刷新三条路径的计算结果都带上可见时间戳；分数与昨日相同时用户可凭绿色徽标确认"这是刚算的"。
- 验证：`node --check static/js/app.js` 通过；后端零改动。

## [2026-08-17] 分析报告页自动重评：快照过旧时进入即后台刷新（021G）

- 用户反馈：分析报告页显示的内容是之前版本的（如早上批次生成的快照），每次都要手动点「🔄 刷新报告」才是新的。
- 根因：P3-A 设计报告页优先读 `daily_reports` 快照（与列表/看板同源），快照停留在生成时刻；仅 B11 分支（当日无报告）会自动实时分析，已有快照时永远展示旧内容。
- 修复（纯前端，`static/js/app.js`，零后端改动）：
  - 新增 `_autoRefreshReportIfStale()`：快照 `generated_at` 超过 **15 分钟** 即静默 POST `/advise` 重评（与手动点「🔄 刷新报告」完全同路径、同副作用），成功后重渲染报告并同步看板（021F 钩子）；失败则保留快照不打断浏览。
  - 守卫：周末不自动重评（与后端 020M 同口径，避免写出周末日期报告行扰动看板口径）；`_reportAutoRefreshing` 防重入；重评返回前用户切走（`_reportStockId` 变化）则丢弃结果。
  - `loadReport` 的 report-latest 请求加 `cache:'no-store'`（与 021E 同理，防浏览器 HTTP 缓存）。
  - 首屏仍先渲染快照（秒开），后台重评完成后无缝替换——不增加等待。
- 副作用说明：自动重评与手动刷新一致——交易时段内对当日尚无 daily 行的股票会把 intraday 顶替为 daily（advise 既有语义，R9/R13 不变量不变）；15 分钟阈值防止频繁进出页面时重复重评。
- 验证：`node --check static/js/app.js` 通过；pytest 473 通过（后端零改动）；未触碰受保护对象；**用户实测验收通过**（2026-08-17：点开分析报告即显示最新内容，无需手动刷新；看板同步链路 021E/F/G 端到端走通）。

## [2026-08-17] 工作约定新增：UI/UX 反向校验契约层（021I）

- 用户确立开发原则并要求固化：为支撑 UI/UX 而设计的契约层（数据建模、接口口径、缓存策略、前端状态管理）**不是开工前不可动摇的终点，必须接受真实 UI/UX 端到端验收的反向校验**；必要时优先实现最小可用 UI/UX 用于真实验收，并接受反馈随时调整契约层。
- 已写入 [AGENTS.md](AGENTS.md) §9 第 6 条工作约定，附 021E/F/G 反例（三项契约各自"正确"、叠加致用户看到旧数据）：契约层自洽 ≠ 用户体验正确，判定标准是用户真实操作路径下的端到端表现。
- 同步记录：021G 已通过用户真实使用验收（详见上条验证行）。

## [2026-08-17] 看板批量评分表自动同步：分析完成即刷新 + 重进看板轻刷新（021F）

- 用户反馈：须「分析报告里点刷新 → 再到总览看板点刷新」两步操作看板评分才更新。
- 根因（与 021E 互补）：021E 解决"刷新请求被 ETag 304 吞掉"；本条解决"刷新根本不自动触发"——看板数据源为 daily_reports 快照，hash 路由 `_viewLoaded` 使看板每会话仅首次加载，且各分析入口（一键分析/批量分析/报告页刷新/建议生成）完成后均不通知看板。
- 修复（纯前端，`static/js/app.js`，零后端改动）：
  - ① 新增 `refreshDashboardIfLoaded()`：看板已加载过（`_dashData` 就绪）则后台重拉并重渲染表格/建议/图表；未访问过看板则跳过（不发多余请求）。
  - ② 四个分析完成点挂钩：一键分析成功、批量分析 finally、报告页「🔄 刷新报告」重评成功（`_loadReportFromAdvise`）、自选股「评级与建议生成」成功。
  - ③ `handleHashChange`：重进 `#dashboard`（非首次）且 `_dashData` 就绪时自动 `refreshDashboardData()` 轻刷新。
- 效果：任一处分析完成 → 看板若在前台即时更新；切回看板 Tab 也自动轻刷新，无需再手动点「🔄 刷新」。与 021E 的 no-store 协同：自动触发的刷新请求也能拿到 200 全量数据。
- 行为不变：盘中快报不覆盖 daily 展示（019D）；复用式「生成今日报告」本就不产生新评分（020R-58）。
- 验证：`node --check static/js/app.js` 通过（含 021E 合并后状态）；pytest 466 通过（后端零改动）。

## [2026-08-17] 看板批量评分表生成后不自动刷新修复（021E）

- 用户反馈：报告已生成，但总览看板「批量评分表」没有自动刷新。
- 根因：`/api/portfolio/watchlist-scores`（及 summary）带 ETag 条件缓存；生成完成后的轻量刷新 fetch 走默认缓存模式，命中 304 空响应（日志实证：18:05:56 生成完成，18:05:56/18:06:11 的刷新请求均 304）——`safeJson` 对空体判定"非 JSON"返回 success:false，刷新逻辑静默放弃，表格保持旧数据。
- 修复（前端）：`loadDashboard` / `refreshDashboardData` 的 summary + watchlist-scores + index-ratings 请求显式 `cache:'no-store'`（直连网络恒 200 全量）；刷新失败时在生成状态区显示可见提示（不再静默）。
- 语义锁定（后端测试）：`tests/test_routes.py` 新增 `test_watchlist_scores_etag_semantics`——匹配 If-None-Match → 304 空体；无匹配 → 200+ETag，防止未来误改。
- 验证：node --check 通过；pytest 全绿；ruff 通过。无需重启服务（Flask 静态文件按请求读盘，页面刷新即生效新 JS）。

## [2026-08-17] 自选股列表按钮优化：合并刷新/删冗余/批量改造（020R-61）

- 用户确认实施按钮优化方案。
- ① 刷新列表升级：`refreshStockList()` = 刷新最新价（`/api/portfolio/refresh-prices`，网络）+ 重读列表，一步到位；原纯读库的 loadStocks 保留供内部自动刷新用。
- ② 删除「刷新分组」按钮：分组增删改后前端已自动重绘 Tab（4 处自动触发），按钮纯冗余。
- ③ 批量分析+评级改造：前端删除手写"仅分析"串行循环（~80 行），改调用后端现成的 `/api/batch-analyze`（采集+分析+评级+价格建议+资金面批量预取+行业自动补取，上限 20 只前端先行校验）；行为对齐「一键分析 × N」，进度 UI 改为静态提示 + 完成后渲染结果表并自动刷新列表。
- 说明：持仓管理页的「刷新价格」按钮保留（该视图仍需独立价格刷新，且其本就会刷新两个列表）。
- 验证：pytest 465 通过（后端零改动）；check_redlines 28/28。

## [2026-08-17] 盘中采集优化：腾讯行情刷K线 + 消息面旁路 + 预告快报门控（020R-60）

- 用户批准实施第一批优化（①②③），④资金面快照经周一盘中实测后取消。
- ① K线盘中刷新改腾讯行情接口：`qt.gtimg.cn` 一个请求取今开/最高/最低/现价/成交量（不再下载 250 根历史），UPSERT 当日行，amount/turnover 保留；实测腾讯接口 25/25 稳。
- ② 消息面盘中旁路"当日跳过"：交易时段内允许重采，午间公告进入盘中快报；非盘中维持跳过。
- ③ 业绩预告/快报"当日门控"（`_dimension_done_today`）：结果日内不变，当日已采集即跳过，不再重复查询全市场表。
- ④ 资金面快照调研结论（周一 14:13 盘中实测）：东财 push2 quote f62 盘中仍返回占位值 1（不可用）；daykline 接口盘中挂起超时（180s）。结论：**维持现有 lmt=10 旁路 + westock/新浪兜底**，不新增 quote 快照端点（盘中东财资金面由现有兜底链保证）。
- 验证：pytest 465 通过（+5）；check_redlines 28/28。

## [2026-08-17] 盘中刷新：一键分析/盘中快报交易时段内取最新盘中价（020R-59）

- 用户确认实施：盘中重复使用一键分析时，K线/资金面的「同日跳过」导致评分沿用当天第一次采集的旧盘中价。
- K线：交易时段内（A股 9:30-11:30/13:00-15:00，港股 9:30-12:00/13:00-16:00，周一~五）同日跳过前先刷新**今日未收盘 bar**（腾讯接口轻量拉取 → INSERT OR REPLACE 当日行，历史数据不动，amount/turnover 保留）；非盘中时段维持原同日跳过。
- 资金面：两处同日跳过（已有真实数据/防覆盖）在盘中时段旁路，走主源路径 UPSERT 当日行（盘中累计值随点击时刻刷新）；非盘中不变。
- 节假日无法感知：节假日命中时段时刷新拿不到当日行 → 安全回落到原跳过。
- 验证：pytest 460 通过（+11）；check_redlines 28/28。

## [2026-08-16] 报告复用保护：无当日采集记录时补采（020R-58）

- 背景（小米 08-16 案例）：凌晨 advise 先于调度批次生成当日报告 → 批次按「当日已有有效报告」跳过该股全部采集 → 当日消息面等数据缺失。
- 修复：`_process_single_stock` 复用分支增加保护——当日已有有效报告但该股**无任何当日采集记录**（新助手 `_has_collection_today` 查 data_status 当日行）时，仍补充采集并重新分析刷新报告；仅当已有当日采集记录或 skip_collect 时才沿用复用捷径。
- 验证：pytest 449 通过（+3）；check_redlines 28/28。

## [2026-08-16] 数据完整度提示治理：新闻语义修正 + 看板三态（020R-57）

- 用户确认建议实施：①新闻提示语义修正；②看板二元→三态；③tooltip 标注检查时间。
- ① 语义修正（`_build_data_freshness` 消息面段）：区分「采集滞后」与「无新消息」——情绪表覆盖最新交易日（采集系统正常）时，原文超 7 天无更新只做中性提示「最近个股新闻 X（近N日无新消息，情绪每日更新）」**不标 ⚠️**；仅情绪表本身停更才标 ⚠️。顺丰（新闻稀疏股）不再被冤枉报警。
- ② 看板三态：新增共享分类器 `_classifyDataWarning`（bad/warn/good/info 四档，与报告页维度亮点卡逻辑合并同源）；总览看板与评分概览表的「数据」列由 ⚠️/✓ 二元升级为 🔴异常/🟡滞后/✓正常，悬停列出异常与滞后明细。
- ③ 快照时效：悬停 tooltip 附加「检查于 {报告生成时间}」，消除看板快照与报告页实时判定的困惑。
- 验证：pytest 446 通过（445+1）；check_redlines 28/28；顺丰实测：消息面行变为「最近个股新闻 2026-08-07（近9日无新消息，情绪每日更新）」，has_issue 不再含消息面。
- 热修复（020R-57-HF1）：①分类器「暂无→🟡」规则误伤「业绩预期：最近三期暂无（预告/快报）」——没有预告/快报属正常状态，现仅当行不含「业绩预期」时才按暂无告警；②基本面缺失行补 ⚠️（此前 has_issue=True 但行内无标记，看板显示 ✓ 不一致）。

## [2026-08-16] 港股估值兜底修复：失败可重试 + 总市值（020R-56）

- 调研结论（2026-08-16 实测）：港股估值候选源中 百度港股/东财quote/新浪/亿牛 均失效或停更（亿牛止于 2022-07），**腾讯行情 5/5 可用**（PE_TTM [39] / PB [43] / 总市值 [45] 亿港元，021C 已实现 PE/PB 兜底）。
- 修复①「同日跳过」bug：估值同日跳过此前只看 data_status 日期、不区分 success/failed——失败记录会导致当日不再重试，腾讯兜底需等次日才生效；现改为仅「真实成功」记录跳过（status=success 且 message 非「同日跳过」），且跳过本身写 `skipped` 状态——同时根治历史「跳过链」自延续问题（小米/阿里曾因连续同日跳过从未真正采集）。
- 增强②：腾讯兜底补采总市值（[45] 优先、[44] 兜底，亿港元→元），写入 stock_valuation.total_mv；`_fetch_valuation_tencent` 返回改为三元组，A股/港股两处调用点同步适配。
- 数据修复：全部 6 只港股（美团/小米/阿里/智谱/MINIMAX/快手）force 补采，估值恢复（source=tencent，PE/PB/总市值齐全）。
- 验证：pytest 445 通过（+3）；check_redlines 28/28。

## [2026-08-16] 行业资金流向数据利用：温度计/持续性/个股关联（020R-54）

- 用户提问并批准（A+B+C 方案）：如何利用行业资金流向（时间维度已就绪）。
- A 市场资金温度计：接口新增 `summary`（全行业主力净流入合计 + 流入/流出/持平家数），市场行情页顶部温度条（红流入/蓝流出比例条）。
- B 持续性信号：`compute_streaks` 计算每个行业截至所选日的连续净流入/流出天数；表格新增「连续」列；榜单区展示「持续净流入≥3日 / 持续净流出≥3日」行业 chips。
- C 个股关联（纯展示层，不碰评分引擎）：自选股行业名→东财板块名别名映射（同花顺旧口径→申万 2021，如 医药制造→医药生物、旅游酒店→社会服务）；建议接口新增 `industry_flow_bg`（所属行业当日净流入/流出 + 排名 + 连续方向）；每日报告数据完整度新增「行业资金背景」行；自选股看板行业列附加资金徽标（▲/▼ 亿 + 连续天数，悬停看排名）。
- 验证：pytest 442 通过（+5）；check_redlines 28/28；港股/无匹配行业静默跳过。

## [2026-08-16] 市场行情-行业资金流向补时间维度（020R-53）

- 用户反馈：行业资金流向缺少时间维度（此前仅展示最新交易日快照，且快照只靠手动刷新，无自动积累）。
- 采集自动化：每日报告调度完成后自动落库当日行业资金快照（挂载点与指数定时刷新同模式，异常隔离不阻塞调度；东财失败走既有冷却兜底）——时间维度从此自然积累。
- 接口：`GET /api/market/industry-fund-flow` 支持 `?date=YYYY-MM-DD` 历史回看，响应新增 `dates`（可用交易日列表）与每行 `main_net_5d`（截至该日的前 5 个交易日主力净流入累计，SQL 窗口计算）；刷新接口同步返回。
- 前端：交易日下拉选择器（默认最新）+ 表格新增「5日累计」列（含口径提示），冷却提示与日期选择共存。
- 验证：pytest 437 通过（+3）；check_redlines 28/28；实盘接口验证 dates/5日累计正确。

## [2026-08-16] 回测中心真实性治理：真实样本主口径 + 引擎断点声明（020R-52）

- 体检发现：① 价格回测 3265 个回测点中 3163 个是历史重建点（未来函数偏差），头号结论建立在带偏差数据上（真实样本止损触发仅 8.7%，全样本口径却报 35%「止损偏紧」，结论反转）；② 评级回测 423 条样本中 v5 仅 1 条，"63% 有效"实际验证的是旧引擎；③ 解读文案「周期衰减」硬编码；④ 无显著性/成本提示。
- 价格回测（主口径治理）：真实样本 = `anchor_rating_date` 非空（当前 102 条，无未来函数）；报告新增 `real_hit_rates`/`real_avg_days`/`real_sample`/`real_risk_reward`/`real_composite_score`；解读与前端指标卡/对比表以真实样本为主口径，历史重建点降级为灰色参照行；无真实样本时退全样本但前置警示。
- 评级解读：新增引擎构成声明（v5 样本 <30 时明示「当前评分规则尚未积累足够回测样本、未被验证」）；总体准确率附二项检验 p 值 + 「未扣除交易成本」提示；周期趋势条件化（衰减/增强/无单调三措辞）。
- 前端：评级报告新增「引擎分层统计」表；价格回测页数据质量说明置顶、真实样本徽标、全样本参照行、分评级表标注全样本口径。
- 验证：pytest 434 通过（427+7）；check_redlines 28/28；未触碰受保护对象。

## [2026-08-16] 回测新基线：评级引擎版本标记 + 技术面专项历史回测（020R-51）

- 用户提问（已按建议实施）：能否用当前评分规则重写历史评分并重做回测。判断：**全历史重评不可行**——基本面/资金面/消息面缺少时点快照（raw_fundamental 无披露日期、资金面仅约 120 日、消息面无历史），重算必带前视偏差；故分两步：A 新基线从现在起 + 评级打引擎标记；B 技术面维度（K线时点完备）专项历史回测。
- A 新基线：`ratings_history` 新增 `engine_version` 列（建表 + 幂等迁移，历史行 NULL）；`advisor._save_rating` 写入 v5/legacy（v5 经 `_convert_v5_to_legacy` 显式携带；legacy 与 v5 降级路径自动标记 legacy；`generate_advice` 未改动）；`backtest_engine.compute_market_report` 新增 `engine_stats` 分层统计（主/动态准确率、T+20 平均收益按引擎分组，历史行显示「未标记(历史)」）。
- B 技术面专项回测：新模块 `modules/technical_backtest.py` + 一键脚本 `scripts/run_technical_backtest.py`——按历史时点逐日重算当前技术面 7 子项得分（复用 data_adapter 指标函数与 `scoring_engine.score_dimension`，日线窗口 60 根/周线 ISO 周/月线自然月/月线空头×0.85 惩罚全部与实盘一致，同源性校验 3/3 MATCH），统计 T+5/T+20 区间收益与方向命中率，报告写入 `reports/technical_backtest_YYYYMMDD.md`（已归档 docs/reports/）。
- 首次结果（16778 观测，2023-04→2026-08，29 只全样本）：**分档信号弱**——偏多(≥65) T+20 平均 +0.08%（方向命中 41.3%）、中性 +0.25%、偏空(≤45) +1.11%（52.1%），均未跑赢全部观测基准 +0.42%；当前技术面分档在该样本期无稳定方向区分度，作为基线存档、滚动观察。
- 红线：未触碰受保护对象（scoring_engine/data_contract/generate_advice 均未改）；ratings_history 不变量（UNIQUE + INSERT OR REPLACE）保持。
- 验证：pytest 427 通过；check_redlines 28/28。

## [2026-08-16] 业绩快报并入业绩预期（020R-50）

- 用户要求：中国中免发的是业绩快报（2026-07-15 公告，H1 归母净利 31.06 亿、同比 +19.49%）而非业绩预告，020R-49 只接预告接口（stock_yjyg_em）导致漏采。
- 采集：新增东财 `stock_yjkb_em` 快报采集 + `raw_express` 新表（报告期/每股收益/营收/净利/同比/ROE/公告日期），并入每日采集流程（港股跳过）；`/api/stocks/<id>/forecast` 响应新增 `express` 数组，个股详情弹窗新增「业绩快报」表格。
- 取用优先级（适配层统一 `get_latest_forecast_info`，评分与展示同一套逻辑）：正式财报 > 业绩快报 > 业绩预告——同报告期快报胜出；仅报告期晚于最新正式财报期生效（财报披露后自动失效）；快报置信度 0.9（预告明确 0.8/模糊 0.6 不变），仍按「预期×0.6×置信度」折价融合，`score_growth` 公式不变。
- 契约：新增 `forecast_type` 字段（44→45）；基本面卡「业绩预告」行按类型切换为「业绩快报」行（快报增红/减绿、标注快报月份）。
- 红线豁免：RED_LINES.md §6 已登记（R7/R8/adapter/collector/db/blueprints/daily_report/tests，批准人=用户；R7 仅展示文案区分「含快报/预告折价」，融合公式未改）。
- 验证：pytest 421 通过；check_redlines 28/28；实测中国中免快报 +19.49% 以置信 0.9 折价入分。

## [2026-08-16] 业绩预告纳入基本面成长性评分（020R-49）

- 用户要求：业绩预告（预报）出来就参与打分。
- 盘点：raw_forecast 共 55 条、16 只 A 股有预告（预增22/预减12/续盈6/略增5/首亏4/续亏3/略减3），报告期以 20260630 中报为主；13 只（含全部港股）无预告。
- 方案 A 折价融合（不新增子项、权重不变）：契约新增 `forecast_np_yoy`/`forecast_confidence`（42→44）；`score_growth` 净利同比 = 预告×0.6×置信度(明确0.8/模糊0.6) + 正式×(1-…)；**预告期晚于最新正式财报期才生效**（财报披露后预告自动失效）；首亏/续亏无幅度按 -100% 处理，扭亏幅度未知不采信。
- 展示：基本面卡新增「业绩预告」行（类型+同比中值+报告期，预增/略增/续盈/扭亏红、预减/略减/首亏/续亏绿）。
- 红线豁免：RED_LINES.md §6 已登记（R7/R8/adapter/tests，批准人=用户）。
- 验证：pytest 420 通过；check_redlines 28/28；实测海光信息（中报预告预增 +47.2%）折价入分、无预告股票不受影响。

## [2026-08-16] 多周期技术面二期：评分结构切换（020R-48B）

- 一期铺垫（020R-48）：周/月线聚合与展示；本批完成历史补采与评分切换。
- 历史补采：`backfill_kline_history_mootdx`——21 只 A 股经 mootdx 前复权补齐至 800 根日线（自 2023-04，月线 41 根，月线 MACD 可算）；2 只次新股（摩尔线程/沐曦 2025-12 上市）为真实全量；港股不适用（月线 MACD 保持缺失→子项按可用字段打分）。
- 契约：StockData 新增 10 个周/月线字段（32→42）；技术面完整度集合 12→22 字段。
- 评分重构（技术面 7 子项三层）：月线方向 0.25（MA5/MA10 排列 + 月线 MACD，空头时技术面总分 ×0.85 惩罚）+ 周线波段 0.45（趋势 0.25/超买超卖 0.10/波动 0.10）+ 日线择时 0.30（超买超卖 0.10/量价 0.10/量比 0.10）；原日线均线/趋势/波动率三子项移除（函数保留供测试）。
- 降级链：月线字段全缺失→月线层权重归零；成对不全→中性分；周线同理 reduce。
- 红线豁免：RED_LINES.md §6 已登记（R7/R8/adapter/mock/tests，批准人=用户）。
- 验证：pytest 417 通过；check_redlines 28/28；实测中芯技术面 68.8（月线多头排列/月线MACD多头、周线MACD空头、完整度 22/22=1.0）；小米（港股）技术面 42.5（月线 MACD 缺失自动降级）。
- 注意：所有股票技术面分数按新口径变化，下次「生成今日报告」后快照同步。

## [2026-08-16] 无效接口清理（021D）

- 背景：全量审计 91 个路由 × 前端三种 URL 构造方式 × 测试 × 运维引用交叉比对。
- A 类（无任何调用者）删除 11 项：`POST /api/stocks/<id>/refresh-full`、`POST /api/backtest/simulate`、`POST/PUT/DELETE /api/watchlist/groups(/<id>)` ×3、`PUT /api/alerts/rules/<id>`、`POST /api/alerts/scan`、`GET /api/positions/<holding_id>/cost-adjustments`、`PUT/DELETE /api/portfolio/groups/<id>` ×2、`GET /api/daily-report/<report_date>`。
- B 类（仅测试引用，功能已被取代）删除 6 项：`GET /api/stocks/<id>/analysis`、`GET /api/stocks/<id>/ratings`、`GET /api/ratings`、`GET /api/backtest/status`、`GET /api/daily-report/history`、`GET /api/portfolio/realized-pnl`；`POST /api/batch-analyze` 保留（qa_019f 验收脚本核心入口）。
- 关联孤儿函数清理：`daily_report.get_reports_by_date`、`backtest_engine.run_historical_simulation`（price_backtest 注释同步改为同口径说明）。
- C 类（运维/调试口，保留并加「仅运维/调试」标注）：`/api/health`、`/api/engine/status`、`/api/engine/rollback-all`、`/api/v5/scoring-demo|analyze|validation`。
- 同步：`tests/test_routes.py` 冒烟清单更新；`AGENTS.md` §3 端点清单修正（原 `POST /api/analyze/<id>` 为过期文档）并附删除记录。
- 净减：17 个路由函数 + 2 个模块函数，约 430 行代码，零前端改动。
- 验证：pytest 全绿；check_redlines 28/28；ruff 通过；服务重启后健康 200。

## [2026-08-16] 多周期技术面一期：周线/月线聚合与展示（020R-48）

- 用户要求（路线已批）：月线定大方向、周线做买卖点参考、弱化日线——一期先采集/聚合+展示，二期切评分。
- 采集：新增 `aggregate_period_klines`（由日线 resample：周线=ISO自然周、月线=自然月，open首/high最高/low最低/close末/volume合计），日线采集后自动聚合；新表 `raw_kline_weekly`/`raw_kline_monthly`（幂等）。
- 展示：技术指标明细卡新增「多周期参考（暂不参评）」区——月线方向（MA5/MA10 排列，历史短时降级为「价在MA5上/下方」）、周线均线/MACD/RSI/布林；状态着色与日线一致。
- 回填：29 只股票全部聚合（周线 32-58 根、月线 8-15 根）。
- 关键发现：现有日线历史仅约 14 个月（最多 268 根），**月线 MACD（需 26 个月）当前无法计算**——二期前需补采更长日线历史（腾讯接口深度受限，拟用 mootdx 全量历史补采）。
- 验证：pytest 418 通过；check_redlines 28/28 通过；实测中芯月线「短期多头」（MA5 134.86>MA10 124.28）、周线 MACD 空头/RSI 71.9；次新股智谱月线「价在MA5下方」。

## [2026-08-16] 昨日数据获取检查整改（021C）

- 背景：检查 08-15（周六）数据获取，发现 3 个问题并修复。
- 问题1 盘口周末脏行：mootdx 周末返回上一交易日快照却盖当日日期，08-15/08-16 共 40 行周末脏行。修复：`fetch_orderbook` 增加周末守卫（与资金面 020L 同原则，非交易日跳过）；数据清理：备份 `db_backup_20260816_090305_cleanup_orderbook_weekend_021C.db` 后删除 40 行，盘口快照回退到 08-14 最新交易日。
- 问题2 行业资金流冷却失效：08-15 晚 23:14~23:28 刷新失败后 1 分钟内重复硬闯东财 7 次——冷却状态原为纯内存态，服务重启（当天 41 次联调重启）即清零。修复：`market_overview` 冷却状态改为内存 + 落盘双写（`logs/industry_ff_cooldown.json`），重启后冷却仍生效。
- 问题3 港股估值源缺口：HK3690 等港股估值恒失败（akshare 港股 baidu 接口已失效——实测 JSON 解析错误；baostock 不支持港股；亿牛网接口数据停在 2022 年不可用）。修复：港股估值链新增腾讯行情 PE(TTM)/PB 备源（复用 `_fetch_valuation_tencent`），交易日戳记取该股最新K线日期防周末脏日期；仅补 PE/PB 两字段，其余保持缺失（诚实标注来源）。
- 红线同步：R4 周末守卫扩展至盘口；`check_redlines.py` 新增 `orderbook_weekend_guard`（28 项）；RED_LINES R7/R8 与 020R-47 互联互通移除同步更新。
- 新增测试：`tests/test_market_overview.py`（冷却持久化 4 项）+ `tests/test_data_collector.py`（盘口守卫 2 项 + 港股腾讯估值兜底 1 项）。
- 验证：pytest 全绿；check_redlines 28/28；ruff 通过。

## [2026-08-16] 互联互通子项移除 + 南向资金展示（020R-47，路线 A）

- 调研结论：北向个股级数据 2024-08 政策性断供且无替代源（实测 6 类接口：个股停更、大盘空壳、排行失效）；南向（港股通）大盘数据正常更新至 2026-08-14。
- 评分：**删除互联互通子项**（A/H 统一）——资金面 4 子项：主力资金 0.50（承接原 0.10）/机构持仓 0.20/杠杆资金 0.20/股东人数 0.10；删除 `score_north_capital` 与注册；`north_net_buy` 移出资金面完整度集合（capital 5→4 字段）；资金面提示改为两融口径。
- 展示：港股资金面卡新增「南向资金（参考，不参评）」行——今日净买/持股市值（东财南向历史资金流，快照入 `south_fund_flow` 表，2 小时新鲜度）；A 股不显示。
- 红线豁免：RED_LINES.md §6 已登记（R7/R8/advisor/tests，批准人=用户）。
- 测试：pytest 411 通过（北向子项测试随功能移除）；check_redlines 27/27 通过。
- 验证：小米（港股）资金面 85.1、南向行「今日净买 -13.16 亿元 · 持股市值 12.0 万亿港元（2026-08-14）」；中芯（A股）资金面 81.1、无南向行；两融提示文案已更新。

## [2026-08-16] 股东人数与机构持仓纳入资金面评分（020R-45）

- 用户要求：股东人数与机构持仓比例相关数据纳入评分（此前完全未处理）。
- 数据源：股东户数=东财 `stock_zh_a_gdhs_detail_em`（升序接口取最新一期 iloc[-1] 户数环比）；机构持仓=东财 `stock_report_fund_hold` 五类机构（基金/QFII/社保/券商/保险）持股总数汇总 ÷ 总股本（阳光私募接口不支持已剔除）；快照入新表 `holder_structure`（按统计截止日幂等，保留最近 12 期）。
- 契约：StockData 新增 `holder_count_change_pct` / `institution_hold_ratio`（30→32 字段，缺失=权重归零型降级）；港股不采集恒为缺失。
- 评分（资金面 5 子项，权重重排）：主力资金 0.55→0.40、杠杆资金 0.35→0.20、新增机构持仓 0.20、股东人数 0.10、互联互通 0.10 不变；机构持仓档位（重仓95/高配82/中等68/低配55/极少40），股东人数档位（户数大幅减少92/减少78/略降62/持平50/略增40/大幅增加25）；002 校准档位保持不变。
- 展示：资金面卡新增「机构持仓」「股东人数」两行（含状态着色与报告期标注）。
- 红线豁免：已按 RED_LINES.md §6 登记（R7 引擎/R8 契约，批准人=用户）。
- 回填：23 只 A 股全部采集成功（机构比例 0.67%~17.79%，户数环比 -21.2%~+52.0%）。
- 验证：pytest 418/418 通过；check_redlines 27/27 通过；中芯实测资金面 77.4（机构 17.79% 低配/户数持平参与评分）、港股小米自动降级（资金面数据完整度 0.2，两新子项权重归零）。

## [2026-08-16] 红线治理二期：提案 P1~P5 全部落地（021B）

- P1 行为锁迁移：R13/R14/R15（generate_advice / _build_capital_factors / fetch_capital_flow）从"函数体文本锁"改为"签名锁 + 行为契约锁"——`tests/test_redlines.py` 新增 3 项签名锁定测试（inspect 校验），写库不变量与输出契约由既有 test_advisor/test_scoring_engine/test_data_collector 覆盖；行为级变更仍须 RED_LINES.md §6 豁免登记。
- P2 备份守卫：`db_manager.py` 013 迁移（DROP daily_reports）与 B12 迁移（DELETE ratings_history）调用点检查 `backup_database()` 返回值，备份失败抛错中止破坏性操作（红线 R11 从"机制存在"升级为"失败必中止"）。
- P3 依赖治理确认：westock npm CLI 的 Node 可用性守卫（`_westock_cli_query` 检测 npx/npm，不可用优雅跳过落回新浪/估算）已存在，纳入红线核验（westock_node_guard）+ RED_LINES R17 登记。
- P4 核验扩充：check_redlines.py 新增 7 项检查——R3 westock EndDate 精确匹配 / R3 新浪 lscjfb opendate 精确匹配 / R4 周末守卫 / R8 业务模块无直接 akshare 耦合 / R8 StockData 契约存在 / R11 备份失败中止守卫 / R17 westock Node 守卫，总计 27 项。
- P5 CI 门禁：`.github/workflows/tests.yml` 在 pytest 后新增「红线自动核验」步骤。
- RED_LINES.md 升级 v1.1：§4 行为锁化、§7 覆盖清单、§8 提案状态全部更新为已落地。
- 验证：pytest 418 passed；check_redlines 27/27 通过；ruff 通过。

## [2026-08-16] 红线治理：统一文档 + 自动核验 + 存量测试清零（021A）

- 背景：红线清单分散 4+ 处且版本漂移（行号引用失效、"8 包"过时、"三处 if False"已名存实亡）；6 个存量测试失败长期挂着（测试门禁失灵），红线却靠人肉 grep 核验。
- 统一文档：新增 `docs/RED_LINES.md` —— 红线定义唯一事实来源（治理原则/红线分类 R1~R22/豁免登记表/修订提案 P1~P5/依赖白名单/已作废红线），锚点全部为语义锚点（函数签名/唯一约束/过滤表达式），不用行号。
- 自动核验（行为锁）：新增 `scripts/check_redlines.py`（20 项检查，只读不触网）与 `tests/test_redlines.py`（随 pytest 执行）；`AGENTS.md` §5/§7/§9 同步更新指向。
- 存量测试清零（408→415 passed，0 failed）：
  - `test_backfill_scheduler.py`：fixture 对齐 020H 新语义（交易日历并集 + ths 维度）；fake_collect 补 `missing_cap_dates` 关键字参数（此前 TypeError 被吞、部分用例"意外通过"）；同花顺批量调用网络隔离。
  - `test_data_collector.py`：`_TradingDayDateTime` 把 datetime.now() 固定为交易日，规避 019G 周末守卫（周末跑测试必挂的环境依赖）。
  - `test_data_freshness.py`：K线滞后测试对齐 020R-19"全市场最新交易日"基准（需基准股提供最新日历日）。
- 修复并发编辑产生的语法错误：`data_adapter.py` `_read_stock_info` docstring 与 `conn = get_connection()` 被误合并到一行（SyntaxError，服务重启即崩溃），已恢复为两行。
- 020R-45 并发护航：`load_stockdata_from_db` 已先行接入 `_read_holder_structure` 调用点但函数未定义（NameError，v5 引擎加载全断），补最小实现（读 `holder_structure` 最新一期，无数据→None→契约权重归零型降级，带表缺失容错）——如与 020R-45 最终实现冲突以最终版本为准。
- 修订提案（RED_LINES.md §8，待决策）：P1 文本锁→行为锁迁移；P2 破坏性操作调用点检查备份返回值；P3 依赖白名单 11 项 + npm CLI 治理；P4 核验自动化扩充；P5 CI 门禁。
- 验证：`python scripts/check_redlines.py` 20/20 通过；`python -m pytest tests/` 415 passed 0 failed；py_compile 通过。

## [2026-08-15] 股东行为三态语义（020R-44）

- 用户确认：股东行为数据"真实没有"（接口正常，自选股近30天无增减持披露），要求三态区分。
- 采集（data_collector.fetch_holder_increase）三态：True=近30天有增持；False=近30天无增持（有减持、或30天内/全市场无披露记录）；None=接口不可用/返回空（港股不采集恒为 None）。
- 展示（消息面卡「股东行为」）：增持·利好（红）→ 近30天无增持（绿）→ 数据缺失（接口不可用或港股未采集，灰）。
- 评分不变口径：False 走 35 分「未增持/减持」档且权重生效（此前 None 权重归零）；消息面分数将在下次报告生成时按新口径更新。
- 回填：23 只 A 股自选股已全部回填 False（预加载全市场 24,856 条记录校验）；港股 6 只保持 None。
- 验证：中芯（A股）news_detail.holder=False、小米（港股）=None；线上 JS 三态显示生效。

## [2026-08-15] 综合分析刷新前后格式统一（020R-43）

- 用户反馈（中芯国际对比）：刷之前综合分析正文是结构化 markdown 但固定「风险提示」区显示暂无；刷之后正文变成一段纯文本段落，格式变了。
- 根因：① 快照路径 report-latest 未返回 risk_warnings 字段（风险只在 markdown 里）；② 实时路径 advice_detail 是段落文本（_build_detail_text），而快照是结构化 markdown。
- 修复：/advise、/analyze、/refresh-full 的 advice_detail 统一改用 `_build_markdown_single` 生成结构化 markdown（与快照路径同款）；report-latest 从日报 markdown 解析出 risk_warnings 字段返回；前端正文剥离「风险提示」块（与固定风险提示区去重）。
- 效果：两条路径综合分析结构完全一致——正文（评分/操作建议/四维）+ 消息面摘要 + 风险提示（同 3 条）。
- 验证：中芯国际实测——快照与实时均返回 3 条风险提示，advice_detail 均为 `### 中芯国际 (688981) — 🚀 v5引擎` 结构化 markdown。

## [2026-08-15] 刷新报告综合分析内容变化修复（020R-42）

- 用户反馈：顺丰控股点刷新后综合分析卡片发生变化（操作建议变空、风险提示消失）。
- 根因：generate_advice 内部写库时用的是未组装完的 analysis 字典，markdown 所需的 action_advice/risk_warnings 在组装后的 result 里才有——刷新路径写出的 markdown 缺「操作建议」值与「风险提示」段；每日报告路径用组装后的 advice 构建 markdown，两者不一致。
- 修复：`advisor._save_daily_report_for_advice` 写库前按同一口径补齐 action_advice（评级×持仓×盈亏矩阵）与 risk_warnings（_detect_risks），markdown 与每日报告路径一致（不改 generate_advice，B24 红线）。
- 验证：顺丰实测——刷新后 markdown 恢复「操作建议：持有观望」+「风险提示：MA5低于MA20 均线空头排列」；写库快照与 08-15 日报内容一致。

## [2026-08-15] 刷新报告数据完整度提示丢失修复（020R-41）

- 用户反馈：小米原本有多项数据滞后提示，点「刷新报告」后只剩一条（资金面提示）。
- 根因：每日报告路径生成前做数据新鲜度检查并追加「数据完整度：…」行；刷新报告（advise/analyze）路径无此步骤，且会把缺行的新报告写回 daily_reports，覆盖原快照。
- 修复：`advisor._save_daily_report_for_advice` 写库前按 `daily_report._build_data_freshness` 补齐完整度行（与每日报告同口径）；`/advise`、`/analyze`、`/refresh-full` 三个接口响应同步补齐。
- 验证：小米实测——刷新后 data_warnings 恢复 6 条（资金面提示 + K线/基本面/资金面/消息面/业绩预告 5 条完整度），快照与实时路径一致。

## [2026-08-15] 综合分析/维度亮点子项固定必显（020R-40）

- 用户反馈：贵州茅台维度亮点看不到数据完整度、综合分析比其他股少几项、点刷新报告后格式变化。
- 根因：数据完整度/消息面摘要/风险提示等按「有数据才渲染」处理——茅台当日无告警、无消息摘要时整段消失；快照路径 key_factors 的 data_completeness 因子排在优先级末尾永远取不到，导致 data_quality 全空。
- 综合分析卡固定子项（缺失自动兜底）：综合分析文本（无则「暂无综合分析文本」）、消息面摘要（无则说明文案）、风险提示（无则「暂无风险提示」）——两条路径结构完全一致，不再随数据增减。
- 维度亮点卡固定子项：最强/最弱维度（后端缺失时从前端四维得分兜底计算）、数据完整度与提示（有告警→分级列表；无告警→按 data_quality 逐维度展示；旧快照无统计→「ℹ️ 未统计完整度，点击刷新报告获取」）。
- 后端：advisor._pick_top_factors 将 data_completeness 固定纳入关键因子（快照路径 data_quality 恢复真实值，与实时路径一致）；下次生成每日报告后历史快照同步生效。
- 验证：pytest 408 通过（6 个失败均为既有无关模块 backfill_scheduler/data_collector/data_freshness）；茅台两路径实测——快照显示未统计兜底提示，刷新报告(advise)显示逐维度完整度（技术100%/基本100%/资金67%/消息80%）。

## [2026-08-15] 消息面卡片补全两个子项明细（020R-39）

- 用户要求：消息面评分卡片内能看到子项内容。
- 后端：新增 `modules/news_detail.py`（读 news_sentiment 最新聚合 + 股东增持标志：情绪 -1~+1 状态标注与 scoring_engine 对齐，新闻总数/正面占比/负面数/重要新闻标题，股东行为 True/False）；`/report-latest` 与 `/advise` 注入 `news_detail` 字段。
- 前端：消息面卡内新增「消息面指标明细」行——情绪(70%)、新闻概览、重要新闻（超40字截断）、股东行为(30%)；正面/增持红、负面/减持绿、中性灰、缺失灰；顶部小注显示新闻截至日期。
- 验证：node --check / py_compile 通过；3 只真实新闻数据离线验证状态标注正确；线上接口实测（快手：情绪 +0.19 偏正面 / 10条 正面90% 负面1，股东行为数据缺失提示）。

## [2026-08-15] 资金面卡片补全三个子项明细（020R-38）

- 用户要求：资金面评分卡片内能看到子项内容。
- 后端：新增 `modules/capital_detail.py`（读 raw_capital_flow：主力净流入最新值+5日均、北向最近非空值、两融最近两个非空值之差；状态阈值与 scoring_engine 主力/互联互通/杠杆子项对齐，单位万元）；`/report-latest` 与 `/advise` 注入 `capital_detail` 字段。
- 前端：资金面卡内新增「资金面指标明细」行——主力资金(55%)、主力5日均、互联互通(10%)、杠杆资金(35%)；流入/买入/增加红、流出/卖出/减少绿；金额自动换算万/亿；缺失显示「数据缺失（北向数据源已停更）」；顶部小注显示数据截至日期。
- 验证：node --check / py_compile 通过；3 只真实资金数据离线验证状态标注正确；线上接口实测（快手：主力 -1.36亿 大幅净流出 / 5日均 -1.83亿，北向与两融缺失提示）。

## [2026-08-15] 基本面卡片补全五类子项明细（020R-37）

- 用户要求：基本面评分卡片内能看到子项内容。
- 后端：新增 `modules/fundamental_detail.py`（读 raw_fundamental 最新一期，估值/盈利/成长/现金流/财务健康五类子项的状态标注，阈值与 scoring_engine 逐条对齐）；`/report-latest` 与 `/advise` 注入 `fundamental_detail` 字段。
- 前端：基本面卡内新增「基本面指标明细」行——PE/PB（低估→严重高估）、ROE/毛利率（优秀→亏损）、营收/净利同比（高增长→明显下滑）、经营现金流/净利润（充裕→为负警惕）、负债率/流动比率（低杠杆→极高杠杆）；红=好/低估值、绿=差/高估、灰=中性；保留「基本面趋势」行与「最新财报期」小注。
- 验证：node --check / py_compile 通过；3 只真实财务数据离线验证状态标注全部正确；线上接口实测（快手：PE 8.41 低估 / ROE 26.3 优秀 / 毛利率 55 高 / 负债率 51.6 偏高）。

## [2026-08-15] 技术指标明细并入技术面卡片（020R-36）

- 用户要求：技术指标明细合并到技术面评分卡片内。
- 布局：四维评分详情网格下方独立的「技术指标明细」条移除；六类指标（均线系统/MACD趋势/RSI/KDJ/布林带/量能）改为直接渲染在「技术面」卡内，替换掉与之重复的因子列表，并保留「近期走势」行与「K线截至日期」小注。
- 行样式复用卡内因子行（左标签右数值+彩色状态），长文本自动换行；状态着色逻辑不变。
- 验证：node --check 通过；服务重启健康 200；线上 JS 已含卡内渲染、旧独立条已移除。

## [2026-08-15] 行业资金流刷新：冷却机制 + 主机轮换 + 提示修复（020R-34）

- 用户反馈：点刷新弹出错误「行业资金流刷新失败：行业资金流刷新失败: (Connection aborted...)」（前后端重复前缀）。
- 修复：后端错误信息去掉重复前缀；前端提示只加一次前缀。
- 冷却机制：刷新失败后 10 分钟内不再硬闯东财，POST 刷新直接回放上次快照并在表头显示「限流冷却中，约 N 分钟后可重试」的琥珀色提示（不弹窗）。
- 抓取增强：东财 push2 主机轮换（push2/82.push2/push2his）×（直连 2 次 → 系统代理 2 次）。
- 验证：py_compile / node --check 通过；服务重启健康 200。

## [2026-08-15] 技术面卡片补全六类指标明细（020R-35）

- 用户反馈：四维评分详情技术面只有 3 个因子，看不到均线/MACD/RSI/KDJ/布林/量能。
- 后端：新增 `modules/technical_detail.py`（纯函数，从 raw_kline 全量计算 MA5/10/20 排列、MACD(12,26,9) 多空/金叉死叉、RSI(14)、KDJ(9,3,3)、BOLL(20,2) 位置、量比/放缩量）；`/report-latest` 与 `/advise` 两接口均注入 `technical_detail` 字段（失败返回 None 不影响主流程）。
- 前端：四维评分详情卡内技术面区新增「📈 技术指标明细」六宫格——均线系统、MACD 趋势、RSI(14) 超买超卖、KDJ 超买超卖、布林带波动、量能配合；状态按语义着色（多头/金叉红、空头/死叉绿、超买/触上轨橙、超卖/触下轨蓝、放量橙红）。
- 验证：node --check / py_compile 通过；用 3 只真实 K 线离线验证六类指标全部计算正确；线上接口返回 technical_detail（快手：均线纠缠/MACD 空头/RSI 47.8 健康/KDJ 超卖/布林触下轨/量比 1.25 平量）；布林位置已钳制 0-100。

## [2026-08-15] 新增市场行情页：大盘指数 + 行业资金流向（020R-33）

- 用户要求：新增页面显示大盘指数与行业资金流向。
- 页面：导航新增「🌐 市场行情」（`#market`），两张卡片——「📊 大盘指数」（复用指数评级，含刷新指数按钮）与「💰 行业资金流向」。
- 行业资金流向：新增 `modules/market_overview.py` 直连东财行业资金流排行（多页抓取、直连优先/系统代理回退、逐页重试）；新增 `blueprints/market.py`（GET 读库快照 / POST 实时刷新）；新增 `industry_fund_flow` 表按交易日幂等缓存（限流/断网时页面仍显示上次快照）。
- 表格列：排名、行业、涨跌幅、主力净流入/净占比、超大单、大单、中单、小单、领涨股；金额自动换算万/亿，红流入绿流出。
- 验证：node --check / py_compile 通过；服务重启健康 200；新路由与页面线上生效；东财当日限流时刷新接口优雅报错并保留快照。

## [2026-08-15] 数据完整度提示去掉「数据完整度：」前缀（020R-32）

- 用户要求：维度亮点卡「数据完整度与提示」每条文案删掉「数据完整度：」前缀（如「数据完整度：消息面：…」→「消息面：…」），只保留维度名与状态；「资金面提示：」等独立文案不受影响。
- 验证：node --check 通过；服务重启健康 200；线上 JS 已生效。

## [2026-08-15] 数据完整度与提示按状态分级提示效果（020R-31）

- 用户要求：分析报告「维度亮点」卡中「数据完整度与提示」对不同状态的数据给出不同提示效果。
- 四级状态与样式：🔴 异常（红底+红左边框：⚠️ 标记、滞后≥5天）→ 🟡 滞后（黄底+琥珀左边框：滞后1-4天、业绩预告暂无、数据源暂不可用）→ ℹ️ 提示（灰底：常规说明）→ ✅ 正常（绿底：数据最新/滞后0天）；每条带图标与右侧状态标签，按严重程度排序。
- 标题附状态摘要：「🔴 N 项异常 · 🟡 N 项滞后」或「✅ 数据状态正常」。
- 验证：node --check 通过；用线上 22 类真实告警文案跑分类规则全部符合预期；服务重启健康 200。

## [2026-08-15] 操作建议卡片按紧急程度分级（020R-30）

- 用户要求：操作建议按操作紧急程度分级显示。
- 建议胶囊分组为五级：🔴 紧急处理（止损/减仓，级内亏损幅度大的排前）→ 🟠 考虑行动（买入/加仓/考虑减仓）→ 🟡 保持关注（观望类）→ ⚪ 继续持有（持有/继续持有）→ ⏳ 待评分（暂无报告）。
- 每组带图标标题、说明文案与数量，空级自动隐藏。
- 验证：node --check 通过；服务重启健康 200；线上 JS 已含分级渲染。

## [2026-08-15] 总览看板新增操作建议卡片（020R-29）

- 用户要求：总览看板加操作建议卡片。
- 卡片「📌 操作建议」位于概览卡片下方：按「评级 × 持仓盈亏」矩阵（与 advisor 操作建议口径一致）为每只自选股生成一个建议胶囊——买入/加仓/持有/继续持有/持有观望/考虑减仓/减仓/建议止损/回避/关注/待评分。
- 排序：风险动作（止损/减仓）→ 机会动作（买入/加仓）→ 持有观望类 → 待评分；红=买入类、绿=卖出/风控类、蓝=持有、橙=关注/待评分。
- 每个胶囊含：建议标签、股票名、评级徽标、评分、持仓盈亏百分比；点击跳转个股报告，无评分股票点击触发一键分析。
- 底部免责声明；生成日报/快报后随表格一起轻量刷新。
- 验证：node --check 通过；服务重启健康 200；看板数据 29 只全部有评级、5 只持仓，卡片正常填充。

## [2026-08-15] 日报表并入看板评分表，生成按钮移到表头（020R-28）

- 用户要求：每日报告表与看板股票表合并，生成按钮放表头。
- 看板「📋 批量评分表」即合并后的总表：新增「数据」列（数据完整度 ⚠️/✓，原日报表列，后端 `watchlist-scores` 补充 `data_warnings` 字段）；原日报评分概览表不再单独渲染。
- 表头（卡片标题行右侧）放置「🚀 生成今日报告」「📊 盘中快报」按钮与「强制全量刷新」勾选框（带悬停说明）；生成完成后在表格上方显示紧凑结果提示（复用/新分析/失败），并自动刷新表格与图表（轻量刷新，不整页重渲染）。
- 「日报Excel」导出日期自动取看板当前报告日期。
- 验证：node --check / py_compile 通过；服务重启健康 200；watchlist-scores 返回 29 条且含 data_warnings。

## [2026-08-15] 每日报告融合到总览看板（020R-27）

- 用户要求：每日报告页面与总览看板内容重复，合并为一页。
- 页面：删除「📅 每日报告」导航与独立视图；总览看板底部新增「📅 每日报告」卡片（功能说明 + 生成今日报告/盘中快报/强制全量刷新勾选框 + 最新报告评分概览表），与看板的大盘指数、概览卡片、批量评分表、图表同页。
- 导出：看板导出栏升级为「📥 看板Excel」「📥 日报Excel」「🖨️ 导出PDF」，原每日报告 Excel 导出能力保留。
- 路由：旧链接 `#daily` 自动重定向到 `#dashboard`，收藏夹不受影响。
- 验证：node --check 通过；服务重启健康 200；日报接口返回 29 条（2026-08-15）正常渲染。

## [2026-08-15] 自选股列表有持仓的自动排前面（020R-26）

- 自选股列表默认排序：有持仓（持仓数量 > 0）的股票自动排在前面，无持仓的排后面，组内保持接口原始顺序。
- 点列表头手动排序时以所选列为准（三态：降序 → 升序 → 恢复默认有持仓优先）。
- 验证：node --check 通过；服务重启健康 200；线上 JS 已更新。

## [2026-08-15] 自选股列表持仓数量为 0 显示 --（020R-25）

- 自选股列表「持仓数量」列：无持仓或数量为 0 时显示 `--`（灰色占位，与持仓成本列一致），不再显示「0」。
- 验证：node --check 通过；服务重启健康 200；线上 JS 已更新。

## [2026-08-15] 自选股列表列序与按钮调整（020R-24）

- 自选股列表「最新价」列移到「持仓成本」之前（紧随「分组」之后），查看行情与成本顺序更顺手。
- 「📋 查看数据」从「⋯ 更多」下拉菜单移出，改为操作列独立按钮（蓝色），与「⚡ 一键分析」「📊 报告」并排；「⋯ 更多」菜单保留加入持仓/编辑/删除。
- 验证：node --check 通过；服务重启健康 200；线上 JS 已含新列序与独立按钮。

## [2026-08-15] 交易流水与成本修正合并为一页（020R-23）

- 用户要求：交易流水、成本修正两个页面合并成一个页面。
- 页面：导航合并为单一入口「📝 交易流水」，`#trades` 视图内上下两张卡片——「📝 全部交易流水」（含类型筛选与汇总）与「✎ 成本修正历史」（审计留痕）；各自刷新按钮独立。
- 路由：移除 `#adjustments` 路由与导航项；旧链接 `#adjustments` 自动重定向到 `#trades`，收藏夹旧地址不受影响。
- 数据：进入页面同时加载两接口（`/api/portfolio/trades` 与 `/api/portfolio/cost-adjustments`），无行为变化。

## [2026-08-15] 成本修正历史孤儿记录修复（020R-22）

- 用户反馈：成本修正页面有两条历史记录没有股票代码和名称。
- 根因：接口经 holdings 关联取股票，持仓被删除后断链；且 `h.stock_id`（NULL）在行字典中覆盖了修正记录自带的 `pca.stock_id`。
- 修复：全局修正历史接口改为直接用 `position_cost_adjustments.stock_id` 关联 stocks——两条孤儿记录恢复显示（000333 美的集团、603501 豪威集团）。
- 验证：10 条修正记录全部带股票代码与名称。

## [2026-08-15] 客观解读条目色调标注（020R-21）

- 用户裁定：解读报告好的要提示、不好的要预警。
- 后端：评级解读与价格建议解读逐条附色调 `interpretation_tones`（good/bad/neutral，与 interpretation_parts 等长）——准确率≥60%、命中率达标、风险收益比≥1 等为 good；接近随机、周期衰减、命中率偏低、样本不足、未来函数偏差提示等为 bad；样本量/免责等中性。
- 前端：客观解读卡逐条着色——**good：绿色 ✓**、**bad：橙色 ⚠️**、中性：默认圆点；分档点评拆为「最可信」（提示）与「最弱」（预警）两条。
- 验证：py_compile/ruff/node --check 通过；两接口各返回 7-8 条色调；服务重启健康 200。

## [2026-08-15] 回测中心客观解读卡片化逐条展示（020R-20）

- 用户裁定：客观解读单独成卡，一条一个观点逐条列出（原先一大段不便于阅读）；解读需包含评级有效性与价格建议命中率两部分。
- 后端：`compute_market_report` 新增 `interpretation_parts`（评级有效性逐条观点）；`compute_price_backtest_report` 新增 `interpretation_parts`（价格建议命中率逐条观点：样本量/买入区间命中/目标价命中/止损触发/风险收益比/综合得分/数据质量提示）。
- 前端：回测中心新增「📋 客观解读」独立卡片（琥珀色左边框），按「评级有效性」「价格建议命中率」分组，每条一个圆点条目；原报告内的解读段落移除。
- 验证：py_compile/ruff/node --check 通过；两接口各返回 7 条解读；服务重启健康 200。

## [2026-08-15] 数据完整度提示修复（020R-19）

- 休市日（周末/节假日）K线/资金面至最新交易日被按自然日误报"滞后N天"；改为与市场最新交易日（全部自选股K线最大日期）比较，0 天显示「最新」；westock 来源标注修正为「腾讯自选股」（原先标成东财真实）；9 天报告快照重生成。

## [2026-08-15] 投资建议详情拆两卡 + 综合分析 markdown 渲染（020R-14）

- 用户裁定：投资建议详情卡拆为「📝 综合分析」与「🌟 维度亮点」两张卡片；综合分析改用 markdown 渲染（引入 marked@12 CDN，未加载时降级纯文本）；仓位建议段移除（网格计划卡已含各档位仓位比例）。
- 综合分析卡内附：消息面摘要、风险提示、数据完整度与提示（原卡片其余小节并入）。
- 验证：node --check 通过；页面已引用 marked；服务重启健康 200。

## [2026-08-15] 价格建议+网格计划移入首屏雷达图右侧（020R-7）

- 用户裁定：价格建议与操作网格计划从「投资建议详情」抽出，做成独立卡片放在雷达图旁边；空间不足时雷达图卡片收窄。
- 布局：首屏三列 = 评分卡(280px) + 雷达卡(320px) + 价格建议卡(380-460px，含价格建议键值行+网格计划表+资金面信号+交易分析)；≤1320px 价格建议卡换行占满整行、≤900px 全部单列堆叠。
- 「投资建议详情」卡片保留：综合分析、仓位建议、维度亮点、风险提示、消息面摘要、数据完整度提示。

## [2026-08-15] 四维因子长文本截断修复（020R-6）

- 用户反馈：中芯国际基本面因子"基本面较上期恶化（…净利率较上期恶化(10.71%→9..."显示不完整。
- 根因：advisor `_pick_top_factors` 对 >50 字符的因子值截断加省略号（前端不截断），另有新闻标题 30 字符截断。
- 修复：移除全部因子值截断（`_pick_top_factors` 两处 + `top_news` 标题一处），长文本完整入参，前端换行完整显示（020R-5 已支持换行）。
- 数据联动：08-06～08-15 全部报告快照重生成（新代码落库）；评级回测 521/521 重跑。
- 验证：29 只股票 report-latest 全因子复查，含省略号因子 = 0。

## [2026-08-15] 前端改动不可见的缓存问题根治（020S）

- 背景：用户反馈多次前端修改后页面无变化——根因是浏览器缓存了旧 HTML 页面（其引用的静态资源版本号停留旧值），改 CSS/JS 文件后用户始终加载旧资源。
- 根治（服务端）：① `after_request` 对 `/` 与 `/static/*` 响应强制 `Cache-Control: no-cache, no-store, must-revalidate`；② 首页静态资源版本号改为模板变量，自动取文件修改时间（mtime），每次改动自动换版本、无需人工升级。
- 使用方式：用户只需普通刷新（或首次关闭标签页重开），之后每次改动即时可见。
- 验证：HTML/JS 响应头均为 no-cache；JS 内容含全部最新改动（K线卡片已移除、紧凑类生效）。

## [2026-08-15] 报告页布局紧凑化 + 移除K线卡片（020R）

- 用户反馈：四维评分雷达图与四维评分详情空白过多；K线走势卡片不再需要。
- 雷达图：高度 220→190px、多边形半径 65%→72%（填满画布）、坐标轴字号 13→12、雷达卡内边距 20→12px。
- 四维评分详情：2×2 网格间距 16→10px、卡片内边距 16→12px、大分数 36→30px、维度名 16→14px、状态徽章与关键因子行高收紧、区块外边距 20→12px（`dim-detail-card` 局部生效，不影响其他页卡片）。
- 首屏评分+雷达区：列间距 20→12px、评分列 320→300px。
- **移除「K线走势（最近20个交易日）」卡片**（用户裁定）——K线数据仍可在个股「数据」页查看；报告页顺序变为 评分卡+雷达图 → 四维详情 → 投资建议详情。

## [2026-08-15] 价格建议与网格计划表格显示优化（020Q）

- 用户反馈：表格平铺太宽、内容不多应紧凑易读；列框与文字对齐由设计统一。
- 价格建议：由全宽 2×2 表格改为**紧凑卡片式键值行**（max-width 440px 圆角卡片）——标签靠左灰色小字、数值靠右对齐（tabular-nums 等宽数字）、操作建议行淡橙底强调；涨红跌绿保持。
- 网格计划：改为**紧凑窄表**（max-width 440px）——灰底表头、细边框；档位居中、价位右对齐（等宽数字）、仓位居中、说明靠左；买入/减仓/补仓类型色保留。
- 验证：node --check 通过；静态资源已生效（强刷页面查看）。

## [2026-08-15] 价格建议止盈/止损改为现价锚定（020P）

- 用户裁定：止盈价/止损价与个人成本无关，应只由 **评级档位 + 现价 + 技术指标** 决定（市场不看个人成本）。
- 有持仓公式改为：
  - 止盈价 = max(现价×(1+保底涨幅), min(现价×(1+评级目标涨幅), 技术阻力位))（阻力位 = min(BOLL上轨、MA60 中高于现价者)，无则现价×1.10）
  - 止损价 = 现价 × (1 - 评级止损比例)
  - 成本仅保留用于：浮盈/浮亏展示、状态机浮亏判定、网格回本/补仓位（其语义天然与成本相关）
- 行为变化：止盈价恒在现价上方（未来目标），状态机 S1「已超目标」不再可达；止盈提示改由 评级降档/跌破止损/网格分批止盈位 触发。示例：美团 现价87.20 止盈96.46 止损82.84（原 65.98/78.48 为成本锚定）。
- 同步修改 `price_backtest._gen_with_position`（双向同步约定），价格回测 1046/1046 全量重跑。
- 网格计划同步重构（020P 一致性）：卖出档位严格自下而上递增、锚定现价与成本孰高——浮盈：补仓位→第一止盈位(现价+0.6ATR)→最终止盈位；浮亏且止盈目标<回本价：补仓位→回本清仓位；浮亏且止盈目标≥回本价：补仓位→回本减仓位(30%)→第一止盈位(50%)→最终止盈位。修复了 020P 后"回本减仓/第一止盈低于现价、最终止盈低于回本价"的档位倒挂。
- 验证：py_compile/ruff 通过；5 只有持仓股票报告页实时建议全部现价锚定、数值复算一致。

## [2026-08-15] 港股全资金净流入 + 主力净流入占比补齐（020O）

- 回答用户"全资金净流入能否从腾讯获取"：**港股能、A股不能**——腾讯 hkfund（港股）返回 TotalNetFlow（主力+散户主动净额，有实际意义，实测美团 08-13 为 -28744.31 万港元）；asfund（A股）散户为被动镜像（散户净额与主力净额分毫不差互为相反数），全口径恒等 0，无法合成。
- 落地：`raw_capital_flow` 新增 `total_net_inflow` 列（仅港股有值）；westock 层解析 TotalNetFlow 与主力净流入占比（占比=主力净额÷成交额，成交额=主力买+主力卖+散户买+散户卖，A股 MainInFlow 系/港股 MainIn 系字段自动适配）；实盘写入与逐日回填路径同步携带两字段；新增 `backfill_hk_total_net`（只补 total/pct 不降级覆盖港股东财主力）。
- 数据：6 只港股 × 10 交易日 total+pct 全量回填 60/60；A股 64 个 westock 行占比逐日回填。
- 前端：资金面表按数据形态条件渲染——A股显示四档分解（超大/大/中/小），港股显示「全资金净流入(腾讯)」列；占比列港股同步有值。
- 验证：node --check / py_compile / ruff 通过；HK3690 10/10 行含 total 与 pct，A股不显示全净额列。

## [2026-08-15] 资金面表移除同花顺净额列，改为完整四档展示（020N）

- 背景：用户指出中国中免等个股"同花顺净额"只显示 4 条——同花顺历史逐日接口不可获取（探测实证：akshare 仅当日全市场快照；官网历史 URL 变体/stockpage SPA/basic.10jqka API 全部无效或反爬），缺失日无法补齐。
- 关键发现：腾讯/东财的四档净额**互补恒等**（超大+大+中+小 ≡ 0，散户=被动方口径），合成不出同花顺「净额」口径（同花顺净额含散户主动部分，如 601888 08-14 同花顺 -22300 万 vs 主力 -18459 万），"近似补齐"数学上不成立（会得到全 0 序列）。
- 按用户裁定：前端资金面表移除「同花顺净额」辅助列，改展示中单+小单两列——四档（超大/大/中/小）来自同一数据源、近 10 交易日逐日完整，无缺口；来源标注同步改为「东方财富/腾讯自选股」逐行混合诚实标注。
- 影响面：ths_net_inflow 仅前端表格消费（评分不依赖），采集与调度器保留（数据留存备用，不展示）。
- 验证：node --check 通过；601888 资金面 10 行 × 6 列全部有值。

## [2026-08-15] 个股报告页显示完整性修复（020M）

- 背景：周六查看个股报告页显示不完整——当日(非交易日)无报告时，report-latest 走实时生成路径，且回退查询用全表 MAX(report_date)，部分股票已有当日行时其余股票回退失败。
- 修复 1：周末/休市日（weekday>=5）跳过实时生成，直接回退该股票最新日报快照（含综合文本 markdown）；交易日实时路径补齐 `advice_detail`（与日报同源 `_build_markdown_single`）。
- 修复 2：回退查询改为按股票取 `MAX(report_date)`，杜绝"别的股票才有报告的日期"导致本股票查无报告。
- 修复 3：快照响应补齐 `action_advice`（取价格建议操作）、`latest_close`/`latest_close_date`（查最新K线）——评分卡"建议"行与"最新收盘"行不再缺失。
- 数据清理（先备份 db_backup_20260815_133847_pre_0815_cleanup.db）：删除 10 只股票由实时生成意外写入的 08-15（周六）daily_reports 行，统一回到 08-14 真实交易日口径。
- 验证：29/29 只股票 report-latest 字段齐全（advice_detail/action_advice/latest_close/latest_close_date/rating_date=08-14）。

## [2026-08-15] 前端数据完整性修复（020L：周末守卫 + 来源升级 + 同花顺回溯）

- 前端审计发现三类问题：① 23 只 A股各有一条 08-09（周日）估算脏行，挤占资金面 LIMIT 10 展示名额；② 近 10 交易日窗口内 33 格新浪顶替 + 6 格估算兜底；③ 同花顺净额全窗口仅 71/230 格有值。
- 修复 1（预防）：`fetch_capital_flow` 新增周末守卫——周六/周日全链路跳过（与 019G 同花顺同原则），根治定时日报在周末写入非交易日脏行；`backfill_capital_history` 升级时同步置空 `main_net_inflow_pct`（westock/新浪不提供占比，避免估算旧占比残留）。
- 修复 2（数据，先备份 db_backup_20260815_132302_pre_020L_frontend_fix.db）：删除 23 条周日估算脏行；39 个估算/新浪格子全部经腾讯 westock --date 升级为真实数据（窗口来源分布：东财 213 / 腾讯 77 / 新浪 0 / 估算 0 / 缺失 0）。
- 修复 3（同花顺回溯）：从历史备份恢复可追回的 30 格 ths（08-05/08-06 来自 019S 备份、08-13 2 格来自 08-13 备份），仅填空洞不覆盖；剩余 129 格历史数据源从未存在（08-03/08-04/08-07/08-10 同花顺接口全市场失败、08-11/12 部分股票接口未返回），无法追回——自 08-14 起每日 23/23 采集，10 个交易日后窗口自然满 10 条。
- 联动：39 格升级后 08-07～08-14 报告重生成（skip_collect，29/29×7 天）+ 评级回测 521/521 + 价格回测 1046/1046 重跑。
- 验证：脏行 0；K线/主力资金 10 交易日全窗口无缺口；ths 101/230（历史可追回部分已全部追回）。

## [2026-08-15] 回测中心数据同步重算 + 孤儿回测行自愈（020K）

- 背景：报告重生成会经 advisor.py 的 `INSERT OR REPLACE` 重写 `ratings_history` 换掉 rating id（B24 红线模块，不改），导致 `backtest_results` 累计 595 条孤儿行（旧 id 失去引用），污染回测中心市场报告统计。
- 落地：`BacktestEngine.batch_backtest` 开头新增自愈清理——删除 `rating_id` 非空且不在 `ratings_history` 中的孤儿行（排除 `rating_id=-1` 的历史模拟行），每次回测运行自动收敛，无需人工维护。
- 数据修复（先备份 db_backup_20260815_130323_pre_backtest_regen.db）：清理 595 条孤儿行 → 评级回测全量重跑 521/521 成功（真实样本与 ratings_history 一一对应）→ 价格建议回测全量重跑 1046/1046 成功（force 模式自带备份）。
- 说明：07-16 遗留的 77 行 `ratings_history` 字母档（B/C/D）与回测表中文标签不一致属历史表示差异，回测统计口径统一、不受影响。
- 验证：孤儿行 0；08-06～08-14 每天 29 行真实回测覆盖；价格回测 1046 行 created_at 全部刷新。

## [2026-08-15] 报告重生成支持跳过采集（020J：skip_collect）

- 背景：数据回填完成后需重生成历史报告，但 `_process_single_stock` 写死"先采集后分析"，历史 8 天重生成会重复打外部接口（每轮 3-4 分钟）。
- 落地：`generate_daily_report` / `_process_single_stock` 新增 `skip_collect` 参数（默认 False，行为零变化）；True 时跳过同花顺批量预取与逐只采集，纯用库内已有数据重新分析。API `POST /api/daily-report/generate` 新增 `skip_collect` 请求字段。
- 兼容性：18:00 定时调度、intraday 端点、CLI、tests 等既有调用点均走默认值，不受影响。
- 验证：py_compile/ruff 通过；08-06～08-13 共 7 天历史报告以 `{date, force, skip_collect}` 全部重生成成功，回测评级历史（ratings_history）同步修正。

## [2026-08-15] 资金面历史回填补强：腾讯 --date 逐日层 + 新浪窗口扩大（020I）

- 背景：020H 上线首日实测发现两类补不上：港股历史缺口（新浪 lscjfb 仅 A股）与超出新浪 5 日窗口的旧缺口（如 000977 的 08-06）。
- 探针实证：westock CLI 的 `--date` 参数对 A股 asfund / 港股 hkfund 均支持历史逐日查询（港股 08-13 主力 4460.21 万港元、A股 08-06 主力 -26050.95 万，四档自洽）。
- 落地：`backfill_capital_history` 链序改为 **腾讯 westock --date（A股+港股）→ 新浪 lscjfb（仅A股）**；`_fetch_capital_flow_westock` 新增 `date_str` 参数并严格校验 `EndDate == date_str`（M-2 同款红线，不匹配只记日志、不计 westock 连续失败，避免回填拖垮实时链路）；新浪回补窗口 num 5→15（覆盖近 10 交易日口径）。
- 验证：py_compile/ruff 通过；HK3690 6 天 + 688981/002714 08-11 + 000977 08-06 全部回填成功（westock 源）。

## [2026-08-15] 补采调度器重写：近 10 个交易日完整性补采（020H）

- 背景：用户确认补采口径 =「近 10 个交易日，只要不完整就补采，同花顺净额也要采」。
- 交易日历：以全部自选股 K 线日期并集推导（不含周末/节假日），窗口为最近 10 个交易日。
- 缺口检测四维：K 线缺口（缺日即补）、资金面缺口（该日 `main_net_inflow` 为空）、同花顺净额（A股最近交易日 `ths_net_inflow` 为空）、基本面/消息面（沿用原逻辑）。
- 资金缺口补采：新增 `data_collector.backfill_capital_history()`，逐日按 东财三层 → 腾讯 westock → 新浪 lscjfb 链补采（东财熔断期直接走 westock/新浪），写库 `capital_source` 标注实际来源；同花顺净额经 `fetch_capital_flow_batch` 批量刷新（沿用 019G 周末跳过规则）。
- 轮次控制：每轮最多 5 只、30 分钟基础周期、失败退避至 120 分钟，与原有 K 线/基本面/消息面补采共用轮次。
- 验证：py_compile/ruff 通过；干跑识别 15 只有缺口股票（12 资金 + 2 K线 + 1 双缺口）；端到端实测 000858 的 08-11 资金缺口经新浪补采成功（主力 -47244.98 万，`capital_source='sina_main'`，非估算、参与评分）。

## [2026-08-14] 新增腾讯自选股资金面备用层（020A：westock）

- 背景：东财资金面接口频繁不可用；搜索发现腾讯自选股（westock-data-clawhub npm CLI）提供 A股/港股主力净流入（含南下持仓、两融、大宗），社区实测腾讯不封 IP。
- 探针审计：CLI 经 proxy.finance.qq.com 签名网关交付，仅访问单域名；实测 A股主力口径=超大+大（与东财精确同概念：600276 主力 -72451.11 万 = 超大 -63472.32 + 大 -8978.79）；港股返回主力净额+南下持仓。
- 落地：`_fetch_capital_flow_westock` 层插入 东财三层 → **腾讯 westock** → 新浪 → 估算 之间；npx 经 cmd /c 调用（Windows 批处理兼容），Markdown 表解析，45s 超时，连续失败 3 次冷却 30 分钟；写库 `capital_source='westock'`、`is_estimated=0`（参与评分），防覆盖/补采清单 SQL 已含 'westock'（东财恢复后可覆盖回补）。
- 验证：直连测试（A股四档+港股主力）、端到端熔断链路测试（600276/HK3690 落库成功）、py_compile/ruff 通过、服务重启健康 200。

## [2026-08-14] 数据源韧性增强（019Z：东财熔断冷却 + 编号子域轮换 + 请求节流）

- 依据社区实测情报（a-stock-data SKILL，2026-06）：东财封禁阈值与"push2 被封 ≠ 全站不可用"、编号子域可绕部分 WAF 拦截。
- 新增进程级"东财熔断冷却"：批量回退循环触发熔断（连续失败 5 只）后进入 2 小时冷却期，期间 `_fetch_capital_flow_em_individual` / `_fetch_capital_flow_em` / akshare 备用源**直接跳过**（省去每只约 2.5 分钟空等，链路自动落新浪主力口径/估算）；期间任意一次东财成功即提前解除。
- `_http_get_em` 增强：第 3 轮起 push2/push2his 编号子域（1~99）轮换；全局最小请求间隔 0.5s（社区阈值 <5 次/秒）。
- 验证：py_compile/ruff 通过；熔断状态机与子域轮换逻辑自检通过；服务重启后健康检查 200。

## [2026-08-14] 服务自愈看门狗（补充：巡检间隔降至 1 分钟）

- 背景：同日 17:38/17:44 两次注销导致服务中断，5 分钟巡检的恢复窗口过长；ONLOGON 触发器被本机策略拒绝（Access denied）。
- 调整：`StockAnalyst Watchdog` 计划任务改为 `/SC MINUTE /MO 1`（每分钟巡检，登录后 ≤60 秒自动恢复）。
- 实测：杀进程 → 17:48:01 巡检刻度自动复活，health 200；任务每分钟正常触发（IgnoreNew 策略不影响后续巡检，看门狗拉起的是分离子进程）。
- 说明：PowerShell `Set-ScheduledTask`/`Register-ScheduledTask` 在本机会话被拒，改用 `schtasks /Create`（经 cmd 中转处理引号）注册。

## [2026-08-14] 数据修复：每天仅一份最终报告（回测依据统一）

### 背景
- 需求：每天的最终报告只有一份，且是回测中心"评级有效性报告 / 价格建议命中率"的依据。
- 盘点结论：写入路径已有保护（daily 生成时清掉当天 intraday；ratings_history `UNIQUE(stock_id, rating_date)` + `INSERT OR REPLACE`），问题仅存在于 013 迁移前的历史存量数据。

### 数据修复（备份：db_backup_20260814_170133_enforce_single_daily_report.db）
- 删除被 daily 顶替的历史 intraday 行 **106 行**（08-13/08-11/08-06/08-04/07-31）。
- 归一化旧状态 `success` → `ok` **27 行**（07-24 的报告此前因状态值不符而被所有读取路径"隐形"）。
- 校验：daily+intraday 共存 = 0；每股每天 ok 报告恰好 1 份；ratings_history / price_backtest_results / backtest_results（真实行）均无重复。
- 说明：08-10 有 3 只股票当日生成失败（仅 failed 标记、无 ok 报告），可重跑当日报告补齐。

## [2026-08-14] 服务自愈看门狗 + 掉线根因修复

### 背景
- 08-13~08-14 多次服务掉线。根因：系统注销/关机/睡眠会终止用户会话进程（Windows 事件 1074/42/7002），登录自启存在失灵竞态（14:03 一次登录后未拉起）。

### 新增
- `scripts/watchdog.py`：端口检查（127.0.0.1:5000）+ pythonw 分离式静默拉起（无窗口、幂等）。
- Windows 计划任务 **StockAnalyst Watchdog**（每 5 分钟，登录会话内运行）：服务被误杀/窗口误关/登录后均 5 分钟内自动恢复。
- 端到端自愈演练通过：杀进程 → 触发任务 → 10 秒内 `/api/health` 恢复 200。

### 说明
- 托盘图标仅作状态显示，服务存续不再依赖托盘。
- 若需"注销后仍运行"，可升级为 SYSTEM 级计划任务（需管理员权限），但会与托盘/start.bat 的端口释放逻辑冲突，默认不启用。

## [2026-08-13] 盘中快报生成动效 + 蓝图路径回归修复

### 前端动效（盘中快报 / 每日报告 生成过程可视化）
- **templates/index.html**：
  - 新增步骤时间线动效（准备 → 采集数据 → 分析评分 → 写入报告 → 完成），当前阶段脉冲高亮、已完成打 ✓、失败显示 ✕
  - 流光渐变进度条 + 旋转 spinner + 实时百分比/第几只/当前股票/当前阶段
  - `generateIntradayReport()` 接入 `/api/daily-report/progress` 轮询（1.5s），替换原静态"请稍候"占位
  - `renderProgressUI` 升级为通用动效面板，按场景显示标题（每日报告 / 盘中快报）

### 回归修复（app.py 拆分为蓝图引入的 __file__ 路径偏移）
- `blueprints/report.py`：进度文件读取路径改为复用 `daily_report._REPORT_PROGRESS_PATH`（单一来源，原路径指向 blueprints/logs/ 读不到数据）
- `blueprints/system.py`：`_ROLLBACK_AUDIT_LOG` 路径补一级 dirname，回落到 `logs/rollback_audit.log`

### 验证
- `python -m pytest tests/`：392 passed，1 skipped
- 真实启动：`/api/daily-report/progress` 正确返回进度 JSON；页面包含动效代码；内联 JS `node --check` 通过

## [2026-08-13] 代码结构治理（app.py 拆分 / 脚本归档 / 路由测试）

### 结构调整（本次改造）
- **app.py 按业务域拆分为 blueprints/ 蓝图包**（4094 行 → 约 130 行入口）
  - 新增 9 个业务蓝图：watchlist（自选股/分组/采集）/ analysis（分析/评级/v5）/ portfolio（持仓/流水/成本）/ report（日报）/ system（健康/引擎）/ backtest（回测/优化）/ export（导出）/ index_ratings（指数）/ alerts（预警）
  - 共享展示层工具函数（_fmt_* / _derive_obos_signal / _resolve_report_type 等 9 个）迁至 blueprints/_utils.py
  - 函数体零改动，仅装饰器 @app.route → @bp.route；102 函数 / 77 路由与拆分前逐一对齐
- **scripts/ 诊断脚本归档**：12 个 diag_*.py（东财反爬/数据源排障等历史一次性脚本）移入 scripts/archive/diag/
- **新增 tests/test_routes.py 路由层冒烟测试**：16 个用例，覆盖全部 9 个蓝图的核心端点（隔离临时库，不触网）
- **analysis_engine.py 标注 LEGACY 状态**：灰度已完成 all_v5，但作为 advisor 回退路径与 engine_switcher 熔断依赖暂不可删，docstring 注明清理条件
- **.gitignore 完善**：补充 .pytest_cache/ .mypy_cache/ .reasonix/ 等，清除误提交的 31 个 .reasonix 环境文件

### 验证
- `python -m pytest tests/`：392 passed（原 376 + 新增 16），1 skipped
- `ruff check .`：通过
- 真实启动冒烟：/api/health、首页、db-stats、ratings、engine/status、stocks 全部 200

## [2026-07-29] 009 价格建议增强（全栈开发）

### 009: 价格建议增强模块（glm5.2）
- **重写 modules/price_advisor.py**：状态机+动态止盈+网格价位+资金面转化+交易流水分析
  - 操作建议状态机（S1-S4 × 5评级矩阵，S4破止损禁止加仓）
  - 止盈价动态化（双约束：max(最低止盈, min(固定止盈, 技术阻力位))）
  - 网格价位（无持仓3档买入，有持仓1补+3减，ATR动态间距）
  - 资金面信号转化（7档修饰词，正则解析资金面文本）
  - 交易流水分析（加仓节奏/成本趋势/买卖时机，数据不足静默跳过）
- **app.py**：4处调用点追加 position_advice 覆盖逻辑（+13行）
  - /analyze, /advise, report-latest(实时+自动触发)
  - 当 price_advice 有动态操作建议时覆盖旧 position_advice
- **templates/index.html**：价格建议section重写为网格表格+资金面+交易分析+状态颜色编码（+86/-21行）
- 红线零触碰：advisor.py / data_collector.py / db_manager.py / daily_report.py / config_weights.json

## [2026-07-28] 005 价格建议（全栈开发）

### 005: 价格建议模块（glm5.2）
- **新建 modules/price_advisor.py**：ATR + MA/BOLL 组合算法，生成买入区间/目标价/止损价/止盈价/建议仓位
- **app.py**：/advise + /analyze + 批量分析 3处端点后处理集成（generate_advice 返回后追加 price_advice，不修改 generate_advice）+ report-latest 返回 price_advice
- **modules/daily_report.py**：_save_report 新增 price_advice 参数，日报持久化价格建议 JSON
- **database/db_manager.py**：daily_reports 表新增 price_advice TEXT 列
- **templates/index.html**：投资建议详情区域新增价格建议 section（无持仓/有持仓两种表格 + 免责声明 + CSS）
- 无持仓输出：买入区间/目标价/止损价/建议仓位/预期涨幅/最大回撤
- 有持仓输出：止盈价/止损价/成本价/浮盈/操作建议
- 数据不足时返回 available=false 优雅降级

## [2026-07-26] 数据完整度提升 + 四维因子明细 + 回测改四维 + 文档整理

### B19-1: analysis_results 日期对齐（kimi k3）
- 修复 analysis_results.analysis_date 与 daily_reports.report_date 非交易日不对齐
- advisor.py: generate_advice 增加 report_date 参数，_save_analysis_results_for_v5 支持 report_date 覆盖
- daily_report.py: 调用 generate_advice 传 target_date
- 删除 28 个历史遗留临时脚本（_sync_daily_reports.py / _fix_db_scores.py / _check_*.py 等）

### B20: v5 引擎四维因子明细（glm5.2）
- advisor.py: 重写 _build_v5_factors，新增 _build_kline_factors/_build_fundamental_factors/_build_capital_factors/_build_news_factors
- templates/index.html: 修复 var dims 变量遮蔽 bug（B15-T4 引入，L3862 var dims → var dqDims）

### B21: PE/PB 聚合回退防御兜底（glm5.2）
- data_adapter.py: _read_fundamental_data 增加聚合回退（pe_ratio/pb_ratio/holder_increase 最新行 NULL 时取次新非空行）

### B22: 消息面数据维度扩展（glm5.2）
- data_contract.py: StockData 新增 news_count/news_positive_ratio/news_negative_count，NEWS 集合从 2→5 字段，news_total 从 2→5
- data_adapter.py: 从 news_sentiment 表映射 total_count/positive_count/negative_count
- 消息面完整度从 50% 提升到 80%

### B23: 回测模拟改四维评分（glm5.2）
- backtest_engine.py: run_historical_simulation 从技术面单维度改为调用 scoring_engine.analyze 四维综合评分

### B24: 前端消息面因子展示（glm5.2）
- templates/index.html: _factorPriority.news 和 _dimFactorLabels.news 新增 news_count

### B25: 用户使用说明文档更新（minimax m3）
- 用户使用说明.md: 从 226 行扩展到 587 行，新增每日报告/看板/回测/导出/四维详情章节

### 文档整理
- 新增 docs/PROJECT_INDEX.md（项目文档索引）

## [2026-07-15] 市值与已实现盈亏精确计算升级

### 盈亏计算逻辑变更（审计追溯）

**变更前**：
- 市值 = cost_price × quantity（基于成本价估算，表头标注「市值估计」）
- 已实现盈亏 = holdings.realized_pnl（流水编辑时重算，但无独立查询接口）

**变更后**：
- 市值 = quantity × price_cache.latest_price（基于实时行情精确计算）
  - latest_price 为 NULL 时显示 "--"（不显示0）
  - 银行家舍入法（ROUND_HALF_EVEN），保留2位小数
  - 向后兼容：旧字段 `estimated_market_value` 保留（@deprecated），与 `market_value` 值相同
- 已实现盈亏：新增 `/api/portfolio/realized-pnl` 独立接口
  - 计算方法：加权平均法（Weighted Average Cost）
  - 数据源：从 trade_records 逐笔计算
  - 不含交易手续费（与券商对账单口径一致）
  - 支持按日/周/月聚合查询

### 数据库变更
- 新增索引：`idx_trade_records_stock_date` (trade_records.stock_id, trade_date, created_at)
- 新增索引：`idx_price_cache_stock` (price_cache.stock_id)

### API 变更
| 字段 | 变更类型 | 说明 |
|------|---------|------|
| `market_value` | 新增 | 精确市值 = quantity × latest_price |
| `estimated_market_value` | @deprecated | 向后兼容字段，值与 market_value 相同 |
| `data_status` | 新增 | realtime / cache / offline |

### 新增接口
- `GET /api/portfolio/realized-pnl` — 已实现盈亏精确查询（支持 stock_id / period / start_date / end_date 参数）
