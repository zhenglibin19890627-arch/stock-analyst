akshare>=1.16.0
Flask>=3.0.0
pandas>=2.1.0
numpy>=1.26.0
python-dateutil>=2.8.0
pydantic>=2.12.0
# P1-5: requests 2.28 与新版 urllib3/chardet 存在兼容警告
# （RequestsDependencyWarning: urllib3 2.6.3 / chardet 7.x 不匹配），
# 升级到 2.32+（改用 charset_normalizer，兼容 urllib3<3）消除警告。
# 修改后需执行: pip install -r requirements.txt 才在本机生效。
requests>=2.32.0,<3
openpyxl>=3.1.0
pytest>=8.0.0
# 系统托盘图标（scripts/tray.py）
pystray>=0.19.5
Pillow>=10.0.0
