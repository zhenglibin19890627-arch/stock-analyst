# 开发任务书（2026-07-21 签发）

| 项目 | 内容 |
|---|---|
| **文档编号** | DEV-TASKS-20260721 |
| **签发人** | AI 产品经理 |
| **签发日期** | 2026-07-21 |
| **执行方** | 开发（GLM） |
| **监理方** | 用户（监理） |
| **需求基线** | `docs/requirements_v1.1.md`（唯一权威，v1.1） |
| **最高约束** | 零代码用户可独立运行（`python app.py` 一键启动不变，禁止引入需用户手动配置的依赖） |
| **状态** | FIX-A/B+ISSUE-1/2 **监理已批准关闭**（2026-07-22）；RATING-ALIGN-004 验收关闭；M8-BACKTEST-003 待补算31条后最终验收；P0观察期继续跟踪（capital待数据源恢复后连续3日复核） |

---

## 任务总览

| 任务ID | 任务名称 | 优先级 | 预估工时 | 前置依赖 | 阻塞下游 |
|---|---|---|---|---|---|
| P0-CAPITAL-001 | P0 资金面数据完整度提升 | P0 | 3-5 人天 | 无 | M8 回测框架 |
| P0-HK-FUND-002 | 港股基本面数据源补齐 | P0 | 2-3 人天 | 无 | M8 回测框架 |
| M8-BACKTEST-003 | M8 评级有效性监测（回测）框架 | P1 | 8-12 人天 | 任务1 + 任务2 | M9 自动优化 |
| RATING-ALIGN-004 | 评级档位对齐改造 | P1 | 1-2 人天 | P3-A 整体验收通过 | M8 双档位兼容 |
| M9-OPTIMIZE-005 | M9 自动优化模块 | P2（暂缓） | 待评估 | M8 输出稳定 | 无 |

**本周启动建议**：任务1（P0-CAPITAL-001）+ 任务2（P0-HK-FUND-002）并行开发。

---

## 任务依赖关系图

```
任务1 (P0资金面) ──┐
                  ├──> 任务3 (M8回测) ──> 任务5 (M9优化)
任务2 (港股基本面) ──┘       ↑
                            │
任务4 (档位对齐) ────────────┘ (M8需兼容双档位)
```

---

## 任务卡 1：P0 资金面数据完整度提升

| 字段 | 内容 |
|---|---|
| **任务ID** | P0-CAPITAL-001 |
| **优先级** | P0（阻塞 M8） |
| **依据文档** | `docs/p0_data_completeness_plan_20260720.md` §2.1 候选源#1（已原则通过 + 2项补强） |
| **现状问题** | A股资金面成功率 50.3%（78/157 失败），11/12 白名单使用 T-2/T-3 历史数据参与评分 |
| **根因** | 东方财富批量采集触发反爬限流 |
| **核心动作** | 1. 接入同花顺 `stock_fund_flow_individual()` 全市场批量源（5197只/次，11.37s）<br>2. 采用**双源协同策略**：同花顺主源 + 东方财富备源<br>3. 字段口径：仅含主净额，不含分层（监理已裁定） |
| **涉及文件** | `modules/data_collector.py` |
| **预估工时** | 3-5 人天 |
| **验收标准** | ① 12 只白名单资金面 T-0 覆盖率 ≥90%<br>② 批量采集不触发限流<br>③ `data_status` 表 capital 维度成功率 ≥95%<br>④ 无估算值污染（B1 禁用规则保持） |
| **零代码约束** | 数据源切换对用户透明，无需手动配置；若需新依赖必须并入 `requirements.txt` 一键安装 |

---

## 任务卡 2：港股基本面数据源补齐

| 字段 | 内容 |
|---|---|
| **任务ID** | P0-HK-FUND-002 |
| **优先级** | P0（结构性缺失） |
| **依据文档** | `docs/p0_data_completeness_plan_20260720.md` §1.3 |
| **现状问题** | HK3690 fundamental 维度 **0% 成功率（NEVER_SUCCESS）**，17 次采集全部失败 |
| **核心动作** | 1. 调研并接入港股财务数据源（候选：akshare 港股财务接口 / 东方财富港股 / 新浪港股）<br>2. 字段映射对齐 v5.0 契约（pe_ttm / pb / roe / gross_margin / revenue_yoy / net_profit_yoy / ocf_to_profit / debt_to_asset / current_ratio 共 9 字段）<br>3. 港币汇率处理沿用 Q01 决策（固定 0.92，适配器层转换） |
| **涉及文件** | `modules/data_collector.py`、`modules/data_adapter.py` |
| **预估工时** | 2-3 人天 |
| **验收标准** | ① HK3690 fundamental 成功率 >0% 且稳定 3 个交易日<br>② 9 个基本面字段至少 6 个有真实值<br>③ `data_status` 表 hk_stock / fundamental 不再是 NEVER_SUCCESS |
| **零代码约束** | 若新源需额外依赖，必须并入 `requirements.txt` 一键安装 |

---

## 任务卡 3：M8 评级有效性监测（回测）框架

| 字段 | 内容 |
|---|---|
| **任务ID** | M8-BACKTEST-003 |
| **优先级** | P1（P0 完成后启动） |
| **依据文档** | `docs/m8_backtest_framework_plan_20260720.md`（510+ 行，已原则通过 + 1项补强） |
| **需求映射** | US-09 / 需求基线 §2.8 |
| **前置依赖** | 任务1（P0 资金面）+ 任务2（港股基本面）完成 |
| **核心动作** | 1. **3层架构**：数据采集层 → 回测计算层 → 结果存储层<br>2. **固定周期回测**：评级后 1d / 1w / 1m 股价表现（price_1d / price_1w / price_1m + return_*）<br>3. **动态周期回测**：评级发布至下一次变更期间表现（基于 `ratings_history.is_change` 字段）<br>4. **5档判定矩阵**：买入评级后涨=正确，卖出评级后跌=正确（仅对比个股自身，不比大盘/行业指数）<br>5. **A/H 双市场独立**：分别回测、分别存储、分别报告<br>6. **触发机制**：评级变更自动触发 + 手动重跑 |
| **涉及文件** | 新建 `modules/backtest_engine.py`，填充 `backtest_results` 表（已建，0行） |
| **预估工时** | 8-12 人天（监理已排期） |
| **关键预留** | **D4 裁定**：消息面权重 0→20% 作为首批自动优化实验场景，方案须预留权重实验接口 |
| **双档位兼容** | 历史数据 A/B+/B/C/D + 新数据中文5档，回测判定矩阵须兼容 |
| **验收标准** | ① 429 行 `ratings_history` 全量回测完成<br>② `backtest_results` 表填充率 100%<br>③ A/H 双市场独立回测报告可查看<br>④ 评级变更自动触发回测验证通过 |
| **零代码约束** | 回测结果在 Web 界面可视化展示，用户无需查数据库 |

---

## 任务卡 4：评级档位对齐改造

| 字段 | 内容 |
|---|---|
| **任务ID** | RATING-ALIGN-004 |
| **优先级** | P1（需求硬性偏差，Batch-2 前置） |
| **依据文档** | `docs/requirements_v1.1.md` §2.3.1 |
| **偏差内容** | 当前：A / B+ / B / C / D<br>需求：强烈推荐买入（85-100）/ 推荐买入（70-84）/ 持有观望（50-69）/ 建议减仓（30-49）/ 强烈建议卖出（0-29） |
| **前置** | P3-A 整体验收通过（观察期 2026-07-20 ~ 07-21 结束） |
| **核心动作** | 1. 评分→档位映射规则改造（5档中文 + 分数边界 85/70/50/30）<br>2. 历史数据兼容：保留旧档位字段，新增新档位字段（或视图映射）<br>3. 前端展示同步更新（报告页 / 看板 / 日志）<br>4. M8 回测判定矩阵同步切换 |
| **涉及文件** | `modules/scoring_engine.py`、`modules/advisor.py`、前端模板 |
| **预估工时** | 1-2 人天 |
| **验收标准** | ① 新评级严格按 85/70/50/30 边界划分<br>② 历史 A/B+/B/C/D 数据可追溯<br>③ 用户界面全量切换为中文5档<br>④ M8 回测兼容双档位 |
| **零代码约束** | 档位切换对用户透明，无需手动迁移数据 |

---

## 任务卡 5：M9 自动优化模块（暂缓）

| 字段 | 内容 |
|---|---|
| **任务ID** | M9-OPTIMIZE-005 |
| **优先级** | P2（**暂缓启动**，等 M8 输出稳定） |
| **依据文档** | `docs/requirements_v1.1.md` §2.9 |
| **需求映射** | US-10 |
| **硬依赖** | M8 回测结果作为输入（M8→M9 顺序不可颠倒，监理已裁定） |
| **核心动作** | 1. 自动调整四维权重配比<br>2. 自动调整各维度内部打分规则<br>3. 自动调整评级阈值<br>4. A/H 双市场独立优化 |
| **首批实验** | D4 裁定：消息面权重 0→20% |
| **启动条件** | M8 回测框架上线 + 至少 2 周回测数据积累 |
| **预估工时** | 待 M8 完成后评估 |

---

## 开发方执行要求

1. **需求基线唯一权威**：任何实现偏差必须对照 `docs/requirements_v1.1.md`，偏差需监理确认后更新版本号。
2. **零代码用户最高优先级**：所有新数据源接入、新依赖引入必须保持 `pip install -r requirements.txt` + `python app.py` 一键启动不变。
3. **M8 → M9 顺序不可颠倒**：M8 必须先于 M9 启动，M8 产出供 M9 输入。
4. **验收留痕**：每个任务完成后，须在 `reports/` 目录生成对应日期的验收报告，列明验收标准逐项达标情况。
5. **任务领用确认**：开发方领用任务后，请在本文件"状态"字段更新为"开发中"，并在完成后更新为"待验收"。

---

**文档版本**：v1.0  
**编制日期**：2026-07-21  
**编制人**：AI 产品经理

---

## 附录：开发方只读摸排实施预案（2026-07-21 观察期内编制）

> 本附录由开发执行方在 P3-A 静默期（仅允许只读操作与文档编制）内编制。已完成两个 P0 任务的全代码路径摸排，形成可执行实施预案，待 07-21 18:30 观察期结束、监理授权后据此执行。整个预案严守「零代码用户最高优先级」（`pip install -r requirements.txt` + `python app.py` 一键启动不变）。

### A. P0-CAPITAL-001 实施预案（资金面·同花顺批量源）

**现状根因（已复核）**：[fetch_capital_flow](file://c:\Users\zlb19\Desktop\Qoder%20cn\stock_analyst\modules\data_collector.py) 三层链路主源 `_fetch_capital_flow_em_individual`(push2his) 与备源 `_fetch_capital_flow_em`(push2) 均受东方财富批量限流；akshare 备源仅 A 股且底层同源。L1235/L1274/L1307 三处 `if False` 估算源为 B1 红线禁用。

**改动点（3 处，`modules/data_collector.py` + `app.py`）**：
1. 新增 `fetch_capital_flow_batch(a_symbols)`：调用 `ak.stock_fund_flow_individual()` 全市场批量（实证 5197 只/11.37s/3 次全成功），1 小时缓存；字段映射 `净额`→`main_net_inflow`(元÷1e4 转万元)、`净额/成交额`→`main_net_inflow_pct`；处理 int64 代码坑点（`int(symbol)` 转换）；仅 A 股。
2. 防覆盖前置校验层（[L1079](file://c:\Users\zlb19\Desktop\Qoder%20cn\stock_analyst\modules\data_collector.py) 防覆盖之前插入）：若今日 `raw_capital_flow` 已有该 stock_id 真实数据（不区分来源）则跳过采集。符合红线「仅可前置校验层」，不改动 L1091/L1092/L1225。
3. [app.py batch-analyze L947](file://c:\Users\zlb19\Desktop\Qoder%20cn\stock_analyst\app.py) 循环前插入：提取所有 a_stock symbol 预取资金面；港股 HK3690 仍走原东财港股 secid 路径。

**红线遵守**：不修改 `fetch_capital_flow` 签名；不改动 L1091/L1092/L1225；不恢复三处 `if False` 估算源；不写入任何估算值。

**验收对应**：① 12 只白名单 T-0 覆盖率≥90%（11 只 A 股走同花顺≈91.7%+，港股走东财）；② 同花顺 1 次调用替代东财 12×3=36 次，根因消除限流；③ `data_status` capital 成功率≥95%；④ B1 禁用规则保持。

**风险与回退**：同花顺被封→回退东财逐只（现状）；防覆盖误判→基于数据存在性，可配置。

### B. P0-HK-FUND-002 实施预案（港股基本面·数据源补齐）

**现状根因（已复核）**：[fetch_hk_fundamental](file://c:\Users\zlb19\Desktop\Qoder%20cn\stock_analyst\modules\data_collector.py) 调 `ak.stock_financial_hk_analysis_indicator_em`(L590) 返回空/异常（17 次全失败=NEVER_SUCCESS）；字段映射仅 7 字段，缺 `ocf_to_net_profit` 写入（表有此列）；PE/PB 走腾讯可用但 saved_count=0 时整体判 failed。

**改动点（2 处，`modules/data_collector.py` + `modules/data_adapter.py`）**：
1. 港股财务源替换（[_fetch_hk_fundamental_em L585](file://c:\Users\zlb19\Desktop\Qoder%20cn\stock_analyst\modules\data_collector.py)）：候选 `ak.stock_financial_hk_report_em` / 新浪港股 / 腾讯扩展字段，须先对 HK3690/HK00700/HK09988 三只实证非空后选定；补齐 `ocf_to_net_profit` 等 9 字段映射对齐 v5 契约。
2. [data_adapter.py L362-L370](file://c:\Users\zlb19\Desktop\Qoder%20cn\stock_analyst\modules\data_adapter.py) 字段映射确认/补齐；港币汇率 Q01（固定 0.92）确认转换位置。

**红线遵守**：不修改 v5 契约 `StockData`；财务数据必须真实，不引入估算。

**验收对应**：① HK3690 fundamental 成功率>0% 且稳定 3 交易日；② 9 字段至少 6 个真实值（PE/PB 腾讯 + 财务新源≥4）；③ `data_status` hk_stock/fundamental 不再 NEVER_SUCCESS。

**风险与回退**：所有港股财务源失败→维持现状并标注结构性受限（P0 方案 §7.4 建议港股 fundamental 降权）；实证为关键前置。

### C. 共性约束

- **零代码**：本次仅可能新增 `akshare` 已有接口调用（已在 requirements.txt），不引入新 pip 依赖；启动方式不变。
- **编码顺序**：A（资金面）与 B（港股基本面）可并行，互不依赖；均在观察期结束后启动。
- **回归范围**：batch-analyze 12 只白名单全量、`data_status` 四维度统计、v5 评分一致性（score_diff=0）。

> 领用确认：开发执行方已于 2026-07-21（观察期内）领用 P0-CAPITAL-001 与 P0-HK-FUND-002，只读摸排与预案编制完成。正式编码待监理授权。