# QA-TASKS-20260805-019L："刷新报告"时间显示修复（/advise 补 generated_at）— 独立验收任务书

> **签发人**：PM | **签发日期**：2026-08-05 | **状态**：待 QA 执行

---

## 角色定义（内嵌，无需额外窗口提示词）

### 你的角色：QA（独立验收）

**职责边界**：
- 独立验收 019L 开发成果，**不依赖开发自验报告结论**（自验报告 `reports/dev_selftest_019L_advise_generated_at_20260805.md` 仅作对照参考）
- 静态代码核查 + 受控实测（mock 场景），逐条用例出 PASS/FAIL + 证据
- **不修改任何功能代码**；mock 测试须在独立临时脚本中执行，验收结束前删除
- 交付物：`reports/qa_accept_019L_advise_generated_at_20260805.md`

### 独立性原则
- 各角色独立不兼职：PM 不兼架构、架构师不编码、开发不验收、QA 独立测试
- QA 自行构造 mock 测试，不复用开发临时脚本（`.dev_019L_work/selftest_019L.py` 仅可对照不可采信）

### 项目背景摘要
| 项 | 内容 |
|---|---|
| 项目路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格，命令行需引号） |
| 数据库路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst\stock_analyst.db` |
| Python 解释器 | `C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe` |
| 技术栈 | Python + Flask + SQLite + akshare + Jinja2 单页应用 |
| 任务书 | `docs/tasks/dev_tasks_20260805_019L_advise_generated_at.md`（v2 定稿，M-1/M-2 已并入） |
| 评审报告 | `docs/reviews/review_019L_advise_generated_at_20260805.md`（架构师 ⚠️ 有条件通过，M-1/M-2） |
| 自验报告 | `reports/dev_selftest_019L_advise_generated_at_20260805.md`（仅对照参考，不采信结论） |
| PM 核验结论 | PM 独立核验通过：代码抽查（成功分支补 generated_at、失败/异常分支无）+ py_compile PASS + **功能独立复跑**（test_client mock：HTTP 200 + generated_at 存在（时间偏差 0.0s）+ 失败态无 generated_at + price_advice 保持）+ 范围外 10 文件哈希与基线一致 |

### 环境约束（硬性，违反将导致执行失败）
1. **项目在 IDE 工区外**：路径含空格，Write 工具直写会报错，须"工作区 Copy + SearchReplace 编辑 + Copy-Item 覆盖回项目目录"
2. **PowerShell 中文**：追加中文到文件用 `[System.IO.File]::ReadAllText + WriteAllText`（UTF-8），禁止 Add-Content/Out-File（乱码）
3. **PowerShell 内联 Python**：含 `*` 的 SQL 会被通配符解析破坏，须用 `chr(39)` 包裹字符串或写临时 .py 脚本；含 f-string 的 Python 代码也须用临时文件
4. **临时脚本清理**：mock 测试脚本须在验收结束后删除，不留存仓库
5. **真实数据库保护**：mock 测试严禁向真实 `stock_analyst.db` 写入任何业务行；零真实网络请求

---

## 一、改动概述（1 文件 1 端点）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `app.py` | `api_advise_stock`（L1117-1140）成功分支补充 `generated_at`（东八区 `datetime.now(_CN_TZ).isoformat()`，与 `/report-latest` L939-940 019D 同型）；`from datetime` 函数内 import（L1133-1137）|

**核心语义**：`generated_at` 仅当 `result.get('success')` 时补充（失败/异常分支不加）；格式 `YYYY-MM-DDTHH:MM:SS.mmm+08:00`（前端 `_fmtGenTime` slice(0,16) 兼容）。

---

## 二、关键代码位置（QA 必读）

| 位置 | 说明 |
|---|---|
| `app.py` L1117-1140 | `api_advise_stock` — 本批次唯一改动端点 |
| `app.py` L1132-1137 | 019L 改动（generated_at 补充，成功分支 005/009 之后）|
| `app.py` L939-940 | 019D 先例（/report-latest 同型）|
| `app.py` L836-871 | `/api/stocks/<id>/refresh-full`（M-1 登记备查，前端零调用，**本批次不处理**）|
| `templates/index.html` L4185 / L4213 / L5412-5415 | 前端链路（刷新按钮→渲染→_fmtGenTime，**本批次不修改**，仅验证联动）|

---

## 三、验收用例（按任务书 v2 + M-2，逐条 PASS/FAIL + 证据）

### V1：代码级核查

| 核查项 | 验证方法 |
|---|---|
| `api_advise_stock` 成功分支含 `result['generated_at'] = datetime.now(_CN_TZ).isoformat()` | Read L1132-1137 |
| 失败分支（success=False）无该行 | Read L1138-1140 |
| 异常分支（except → 500）无该行 | Read L1139-1140 |
| 与 019D 先例 L939-940 同型（格式/时区/函数内 import 一致）| Read 对照 |
| `refresh-full` 端点未改动（M-1 备查）| Read L836-871 |

### V2：编译验证

```
python -m py_compile app.py
```
→ 无错误

### V3：成功态功能验证（M-2 细化——QA 重点）

**mock 方式（M-2）**：
- **Flask test_client POST `/api/stocks/1/advise`**（`jsonify` 需应用上下文，勿直接调视图函数）
- **须同时 patch** `modules.advisor.generate_advice` 与 `modules.price_advisor.generate_price_advice`（防 price_advisor 真实执行查库/持仓破坏 mock 隔离）

| 断言 | 预期 |
|---|---|
| HTTP 200 | 命中 |
| success=True | 命中 |
| **generated_at 存在** | 命中 |
| 格式 `YYYY-MM-DDTHH:MM:SS` 前缀（前端 slice(0,16) 兼容）| 命中 |
| **时间偏差 <60s**（`datetime.fromisoformat` 与东八区 now 求差）| 命中 |
| 005 行为保持（price_advice.action_suggestion 回传）| 命中 |
| 009 行为保持（position_advice 被动态操作建议覆盖）| 命中 |

### V4：失败态验证

**mock 组合**：`generate_advice` 返回 `{'success': False, 'message': '...'}`（并 patch price_advisor 防御）

| 断言 | 预期 |
|---|---|
| HTTP 200（语义与 /report-latest 一致）| 命中 |
| success=False | 命中 |
| **无 generated_at 字段**（语义红线）| 命中 |
| message 含错误信息 | 命中 |

### V5：异常态验证

**mock 组合**：`generate_advice` 抛异常

| 断言 | 预期 |
|---|---|
| HTTP 500 | 命中 |
| **500 响应无 generated_at** | 命中 |
| message 含"建议生成失败" | 命中 |

### V6：前端联动验证（QA 可选，纯 JS 断言）

`_fmtGenTime('2026-08-05T23:11:21.558722+08:00')` → `'2026-08-05 23:11'`（slice(0,16).replace('T',' ')，无需浏览器）

### V7：零改动确认

| 文件 | 哈希（019K 基线，PM 已复算）|
|---|---|
| app.py | `5C73F6EA320D838D`（**本批次改动**）|
| templates/index.html | `79F3F330F7148D49`（不变）|
| modules/advisor.py | `CA1857B0F6452B20`（不变）|
| modules/data_collector.py | `B2CACC622E2A9ABA`（**019N 预期改动**，非本批次）|
| modules/analysis_engine.py | `DF71A6FE4FD7685D`（不变）|
| modules/alert_engine.py | `053F0CDB4DA62385`（不变）|
| modules/scoring_engine.py | `DD9DBFBBD005B35D`（不变）|
| modules/data_adapter.py | `0792E5006D7DCED9`（不变）|
| config.py | `F6CE1F84B8DDACDA`（不变）|
| requirements.txt | `DBE076A7458C5788`（不变）|
| database/db_manager.py | `2D222BE42F298258`（不变）|

---

## 四、红线遵守核查清单

| 红线 | 核查方法 |
|---|---|
| B24（不改 advisor）| V1 + V7（advisor.py 哈希不变）|
| 范围（仅 app.py 一端点）| V1 + V7 |
| 语义（失败不补 generated_at）| V4/V5 |
| 零代码（标准库）| V1（datetime/timezone/timedelta 标准库）|
| 并行隔离（019N）| V7（data_collector.py 变化为 019N 预期，非本批次）|

---

## 五、QA 交付物要求

`reports/qa_accept_019L_advise_generated_at_20260805.md`，含：
1. 逐用例 PASS/FAIL + 证据（实测数据、日志文本）
2. 红线核查清单
3. 新发现问题（如有）
4. 验收结论：**通过 / 不通过**
5. QA 签署

---

## 六、PM 备注

1. **M-2 验收要点**：① mock 走 Flask test_client（勿直接调视图函数）；② 成功态须同时 patch price_advisor（防真实执行破坏隔离）；③ 时间偏差断言 <60s。
2. **低风险小批次**：单文件单端点 6 行改动，风险极低；评审确认 `/refresh-full` 前端零调用（备查不处理）。
3. **运行实例提醒**：当前运行中 app.py 为旧代码，修复需重启后生效；QA 基于 mock 验收不依赖运行实例。
4. **并行批次说明**：019N（data_collector.py）与 019L（app.py）文件零重叠，QA 可并行验收；双方各自独立 mock，不混用。
