# 开发任务书（2026-07-22 签发·第二批）

| 项目 | 内容 |
|---|---|
| **文档编号** | DEV-TASKS-20260722-B2 |
| **签发人** | AI 产品经理 |
| **签发日期** | 2026-07-22 |
| **执行方** | 开发（GLM） |
| **监理方** | 用户（监理） |
| **需求基线** | `docs/requirements_v1.1.md`（唯一权威，v1.1） |
| **最高约束** | 零代码用户可独立运行（`python app.py` 一键启动不变，禁止引入需用户手动配置的依赖） |
| **状态** | 监理已于 2026-07-22 批准全部方案；开发已完成，待验收 |

---

## 任务总览

| 任务ID | 任务名称 | 优先级 | 预估工时 | 前置依赖 | 阻塞下游 |
|---|---|---|---|---|---|
| M8-BT-FILL | M8 回测补算（31条 pending） | P1 紧急 | 0.5人天 | FIX-A/B 已验收通过 | M8-BACKTEST-003 最终关闭 |
| DATASRC-C | 资金面结构补齐（north + margin） | P1 | 2-3人天 | 无 | capital 完整度 33%→66%+ |

**启动建议**：两任务可并行，互不依赖。M8-BT-FILL 优先完成（工时短、闭环价值高）。

---

## 任务卡 1：M8 回测补算（M8-BT-FILL）

| 字段 | 内容 |
|---|---|
| **任务ID** | M8-BT-FILL |
| **优先级** | P1 紧急 |
| **依据文档** | `reports/pm_accept_20260722.md` 遗留项#2；`docs/test_cases/tc_m8_backtest_003.md` TC-M8-FL-01/02 |
| **背景** | M8 回测框架已实现并验收通过，但 `backtest_results` 覆盖率仅 94.2%，存在 31 条待补算记录（评级时 T+1 数据尚不可用，需后续补填） |
| **核心动作** | 执行 `fill_pending_backtests()` 补算所有到期未回测记录 |

### 执行步骤

```python
# 开发方在 Python 环境中执行（或通过 /api/backtest/rerun 触发）：
from modules.backtest_engine import BacktestEngine

engine = BacktestEngine()
result = engine.fill_pending_backtests()
print(f'补算完成: pending={result["pending"]}, filled={result["filled"]}')

# 补算后验证覆盖率
# 预期: coverage = total_backtests / total_ratings_with_price >= 0.99
```

### 涉及文件

- `modules/backtest_engine.py`（`fill_pending_backtests()` 函数，已实现）
- 无需修改代码，仅需执行补算 + 验证

### 验收标准

| # | 标准 | 核验方法 |
|---|---|---|
| 1 | `fill_pending_backtests()` 执行成功，返回 `{pending, filled}` | 开发自验截图/日志 |
| 2 | 补算后 `/api/backtest/status` 的 coverage >= 99% | QA 调用 API 核验 |
| 3 | 连续调用两次，第二次 pending=0（幂等性） | TC-M8-FL-02 |
| 4 | 补算不产生重复行（UPSERT 机制） | TC-M8-FU-04 回归 |
| 5 | 补算不删除/覆盖 `ratings_history` 原始数据 | TC-M8-RG-02 回归 |
| 6 | 零代码约束不变 | `python app.py` 一键启动 |

### 完成标志

补算完成 + 覆盖率达标后，**M8-BACKTEST-003 任务最终关闭**。

---

## 任务卡 2：资金面结构补齐（DATASRC-C）

| 字段 | 内容 |
|---|---|
| **任务ID** | DATASRC-C |
| **优先级** | P1 |
| **依据文档** | `docs/tasks/pm_ruling_20260722.md` 方案C；`docs/reviews/review_capital_33pct_20260722.md` |
| **背景** | 当前 capital 数据完整度恒定 33%（仅含 main_net_inflow 1/3 字段），缺少 `north_net_buy`（北向资金净买入）和 `margin_balance_chg`（融资余额变化） |
| **核心动作** | 补齐北向资金 + 融资融券两个数据源，将 capital 完整度从 33% 提升至 66%+ |

### 技术方案

#### 子任务 2.1：北向资金净买入（north_net_buy）

| 字段 | 内容 |
|---|---|
| **数据源** | akshare `ak.stock_hsgt_individual_em(symbol)` 或 `ak.stock_hsgt_north_net_flow_in_em()` |
| **字段映射** | 北向净买入额 -> `north_net_buy`（单位：万元） |
| **覆盖范围** | 仅沪深港通标的（非全部A股），非标的填 None 并标注 |
| **采集频率** | 每日1次（T+0 收盘后） |

#### 子任务 2.2：融资余额变化（margin_balance_chg）

| 字段 | 内容 |
|---|---|
| **数据源** | akshare `ak.stock_margin_detail_szse(date)` / `ak.stock_margin_detail_sse(date)` |
| **字段映射** | 融资余额日变化 -> `margin_balance_chg`（单位：万元） |
| **覆盖范围** | 仅融资融券标的（约2200只），非标的填 None 并标注 |
| **采集频率** | 每日1次（T+1 公布） |

### 涉及文件

- `modules/data_collector.py`（新增采集函数）
- `database/db_manager.py`（确认/新增列）
- `modules/scoring_engine.py` 或 `modules/analysis_engine.py`（资金面评分消费新字段）

### 验收标准

| # | 标准 | 核验方法 |
|---|---|---|
| 1 | 沪深港通标的（600276/000333/300750）`north_net_buy` 有真实值 | SQL 核验 |
| 2 | 融资融券标的 `margin_balance_chg` 有真实值 | SQL 核验 |
| 3 | 非标的股票对应字段为 None（不填0，不估算） | SQL 核验 |
| 4 | `data_quality` 表 capital 完整度从 33% 提升至 >= 66% | 采集后核验 |
| 5 | 数据源失败时容错（不阻塞主流程，记录 warning） | 断网/超时测试 |
| 6 | 零代码约束不变（仅用 akshare 已有接口，无新 pip 依赖） | 一键启动验证 |
| 7 | 防覆盖机制不破坏 | 回归测试 |
| 8 | 禁用估算值红线保持（三处 `if False` 不恢复） | 代码审查 |

### 红线核验

| 红线 | 状态 |
|---|---|
| 零代码约束 | 仅调用 akshare 已有接口 |
| 需求基线 | 需求 2.1.1 资金面要求"融资融券余额变化、北向资金流向" |
| 禁用估算值 | 非标的填 None，不填估算值 |
| 防覆盖机制 | 沿用现有防覆盖逻辑 |
| A/H 双市场独立 | 北向资金仅 A 股，港股不受影响 |

### 风险与回退

| 风险 | 概率 | 缓解 |
|---|---|---|
| akshare 北向资金接口变更/限流 | 中 | 容错处理 + 失败不阻塞 |
| 融资融券数据 T+1 延迟 | 高（设计如此） | 标注数据时效性，评分引擎兼容 None |
| 非沪深港通/非两融标的无数据 | 预期行为 | 填 None + 评分引擎降权处理 |

---

## 开发方执行要求

1. **需求基线唯一权威**：任何实现偏差必须对照 `docs/requirements_v1.1.md`
2. **零代码用户最高优先级**：保持 `pip install -r requirements.txt` + `python app.py` 一键启动不变
3. **验收留痕**：每个任务完成后，在 `reports/` 目录生成自验报告
4. **任务领用确认**：领用后更新本文件"状态"为"开发中"，完成后更新为"待验收"
5. **回归范围**：26只全量 batch-analyze、data_status 四维度统计、v5 评分一致性

---

## 验收流转

```
M8-BT-FILL:
  开发执行补算 -> 自验覆盖率 -> QA 核验（TC-M8-FL-01/02 + TC-M8-FU-04）
      -> 产品经理+QA 双签 -> 监理批准 -> M8-BACKTEST-003 最终关闭

DATASRC-C:
  开发编码+自验 -> QA 测试（数据完整性 + 容错 + 回归）
      -> 产品经理+QA 双签 -> 监理批准关闭
      -> capital 完整度 >= 66% 纳入 P0 观察期跟踪
```

---

## 待监理决策清单

| # | 决策项 | 产品经理建议 | 工时 |
|---|---|---|---|
| 1 | M8-BT-FILL（回测补算）批准 | 紧急批准（闭环 M8 验收） | 0.5人天 |
| 2 | DATASRC-C（north + margin 补齐）批准 | 批准并行开发 | 2-3人天 |

---

**文档版本**：v1.0 | **编制日期**：2026-07-22 | **编制人**：AI 产品经理
