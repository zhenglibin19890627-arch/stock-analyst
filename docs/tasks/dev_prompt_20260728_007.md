# 007 价格建议回测验证 — 开发人员窗口提示词

> 监理操作：新开 Quests 窗口，选择 **glm5.2** 模型，将下方分割线之间的全部内容粘贴为首条消息。

---

# 开发人员激活 + 任务指令（粘贴以下全部内容）

你是「智能个股分析与评级系统（Stock Analyst）」项目的 **开发人员（Developer）**。

**核心职责**：编码实现、自验、红线遵守、状态更新。
**独立性原则**：不负责方案评审，不负责正式验收，专注编码实现。

---

## 项目概况

| 项 | 内容 |
|---|---|
| 项目路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格） |
| 技术栈 | Python + Flask + SQLite + akshare + Jinja2 单页应用 |
| 最高约束 | 零代码用户可独立运行 |
| 数据库 | `stock_analyst\stock_analyst.db`（在 stock_analyst 子目录内） |
| Python 解释器 | `C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe` |

---

## 本次任务：007 价格建议回测验证 — 全栈开发

**任务书**：`docs/tasks/dev_tasks_20260728_007_dev.md`（请先完整阅读）
**方案设计报告**：`docs/reviews/review_007_price_backtest_design_20260728.md`（方案依据）

**目标**：验证 005 价格建议的命中率，输出命中率报告。
**工作量**：~520行（新建1文件 + 修改3文件）

---

## 技术红线（绝对遵守）

| # | 红线 | 说明 |
|---|---|---|
| 1 | **price_advisor.py 不可改** | 007 仅验证，不改算法 |
| 2 | **generate_advice 不可改** | advisor.py 红线 |
| 3 | **data_collector 三处 if False** | L1645/L1684/L1717 不可恢复 |
| 4 | **零代码约束** | 无新 pip 依赖 |
| 5 | **不回写** | 回测验证不修改生产数据 |
| 6 | **backtest_engine.py 不可改** | 现有回测引擎不修改 |

---

## 任务详细分解（4个子任务）

### 任务1：新建 modules/price_backtest.py（~400行）

#### 1.1 历史技术指标计算

复用 data_adapter 的计算函数（接受 closes 列表参数）：

```python
from modules.data_adapter import _calc_ma, _calc_bollinger


def _calc_historical_indicators(stock_id, end_date):
    # SQL: SELECT close FROM raw_kline WHERE stock_id=? AND trade_date<=? ORDER BY trade_date ASC
    closes = [row['close'] for row in ...]
    ma20 = _calc_ma(closes, 20)
    ma60 = _calc_ma(closes, 60)
    boll_upper, _, boll_lower = _calc_bollinger(closes, 20)
    atr = _calc_historical_atr(stock_id, end_date)
    return {ma20, ma60, boll_upper, boll_lower, close, atr}
```

#### 1.2 历史ATR计算

参考 price_advisor._calc_atr，但取 end_date 之前的数据：

```python
def _calc_historical_atr(stock_id, end_date, period=14):
    # SQL: SELECT high, low, close FROM raw_kline WHERE stock_id=? AND trade_date<=? ORDER BY trade_date DESC LIMIT 15
    # TR = max(high-low, |high-prev_close|, |low-prev_close|)
    # ATR = TR 的 14日 SMA
```

#### 1.3 历史价格建议生成

在指定日期生成价格建议。不调用 price_advisor.generate_price_advice（它用最新数据），
而是用历史指标自行计算。参考 price_advisor.py L153-263 的算法逻辑：

**无持仓算法**（参考 price_advisor._gen_no_position）：
- 买入中枢 = MA20（若可用）否则 close
- 买入下限 = 中枢 - ATR*0.5
- 买入上限 = 中枢 + ATR*0.3
- 约束1: 若 boll_lower < 买入下限 -> 下限 = boll_lower
- 约束2: 买入上限不超过 close*1.05
- 目标价 = max(boll_upper, ma60)，不低于 close*1.05
- 止损价 = 买入下限 - ATR*1.0

**有持仓算法**（参考 price_advisor._gen_with_position）：
- 止盈价 = cost_price * (1 + 目标涨幅%)
- 止损价 = cost_price * (1 - 止损比例%)

#### 1.4 命中判定（核心）

基于日内 high/low 触及判定：

| 建议项 | 命中条件 |
|---|---|
| 买入区间命中 | T+N内存在任意一天 low<=buy_range_high 且 high>=buy_range_low |
| 目标价命中 | T+N内存在任意一天 high>=target_price |
| 止损价命中 | T+N内存在任意一天 low<=stop_loss |
| 止盈价命中 | T+N内存在任意一天 high>=take_profit |

同时记录首次命中天数、T+N内 max_high/min_low。

#### 1.5 主回测函数

```python
def run_price_backtest(market=None, force=False):
    # 对每只股票，从第35个交易日开始，每隔5天取一个回测点
    # 每个回测点：
    #   1. 生成价格建议（历史指标+当前评级+当前持仓）
    #   2. 取后续T+5/T+20的K线（含high/low/close）
    #   3. 命中判定
    #   4. 写入 price_backtest_results 表
    # force=True 时先清除旧记录再重跑
```

#### 1.6 报告生成

```python
def compute_price_backtest_report(market='a_stock'):
    # 从 price_backtest_results 表读取，计算：
    # - 核心命中率: 买入区间/目标价/止损价/止盈价的T+5和T+20命中率
    # - 时间效率: 平均达到天数
    # - 偏差分析: 未命中案例的平均偏差
    # - 分组统计: 分评级/分市场
    # - 综合评估: 风险收益比 = 目标价命中率 / 止损价命中率
```

#### 评级/参数映射（从 price_advisor.py 复制常量，不import修改）

```python
RATING_POSITION_PCT = {
    '强烈推荐买入': 80,
    '推荐买入': 50,
    '持有观望': 20,
    '建议减仓': 0,
    '强烈建议卖出': 0,
}
RATING_TARGET_GAIN = {
    '强烈推荐买入': 0.25,
    '推荐买入': 0.20,
    '持有观望': 0.12,
    '建议减仓': 0.08,
    '强烈建议卖出': 0.05,
}
RATING_STOP_LOSS = {
    '强烈推荐买入': 0.08,
    '推荐买入': 0.07,
    '持有观望': 0.05,
    '建议减仓': 0.04,
    '强烈建议卖出': 0.03,
}
```

> 注意：这些常量从 price_advisor.py 复制到 price_backtest.py，不 import price_advisor，保持参数同步即可。

---

### 任务2：修改 database/db_manager.py — 新建表（+40行）

在 init_database() 函数索引区域（L527附近）之前追加：

表名：price_backtest_results
字段：stock_id, backtest_date, rating, market, has_position,
      buy_range_low, buy_range_high, target_price, stop_loss, take_profit, position_pct,
      close_at_backtest, ma20, ma60, boll_upper, boll_lower, atr,
      t5_hit_buy_range, t5_hit_target, t5_hit_stop_loss, t5_hit_take_profit,
      t5_days_to_buy_range, t5_days_to_target, t5_days_to_stop_loss, t5_days_to_take_profit,
      t5_max_high, t5_min_low,
      t20_hit_buy_range, t20_hit_target, t20_hit_stop_loss, t20_hit_take_profit,
      t20_days_to_buy_range, t20_days_to_target, t20_days_to_stop_loss, t20_days_to_take_profit,
      t20_max_high, t20_min_low,
      created_at

索引：idx_price_backtest_stock_date(stock_id, backtest_date), idx_price_backtest_market(market)

---

### 任务3：修改 app.py — 新增2个API端点（+30行）

在回测API区域（L3060附近）追加：

- POST /api/price-backtest/run：触发回测（参数：market, force）
- GET /api/price-backtest/report：获取回测报告（参数：market）

---

### 任务4：修改 templates/index.html — 前端展示（+50行）

在回测报告页面'分级准确率'表格之后，新增'价格建议命中率'section：
- 标题：'价格建议命中率'
- 核心指标卡片：买入区间命中率/目标价命中率/止损价命中率/风险收益比
- T+5 vs T+20对比表
- 页面加载时自动请求 /api/price-backtest/report

---

## 自验标准

### 自验命令

```
cd "C:\Users\zlb19\Desktop\Qoder cn\stock_analyst"

# 验证1：模块可独立import
C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe -c "from modules.price_backtest import run_price_backtest; print('import OK')"

# 验证2：执行回测
C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe -c "from modules.price_backtest import run_price_backtest; print(run_price_backtest(market='a_stock', force=True))"

# 验证3：生成报告
C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe -c "from modules.price_backtest import compute_price_backtest_report; import json; print(json.dumps(compute_price_backtest_report('a_stock'), ensure_ascii=False, indent=2))"

# 验证4：表结构
C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe -c "import sqlite3; c=sqlite3.connect('stock_analyst/stock_analyst.db'); print([d[1] for d in c.execute('PRAGMA table_info(price_backtest_results)').fetchall()])"
```

### 自验检查清单

- [ ] price_backtest.py 可独立 import 无报错
- [ ] run_price_backtest 可遍历股票生成回测点
- [ ] 历史技术指标计算正确（MA20/BOLL/ATR）
- [ ] 命中判定逻辑正确（基于 high/low）
- [ ] 回测结果写入 price_backtest_results 表
- [ ] compute_price_backtest_report 返回完整指标
- [ ] price_backtest_results 表结构正确
- [ ] /api/price-backtest/run 端点工作正常
- [ ] /api/price-backtest/report 端点工作正常
- [ ] 前端命中率卡片展示
- [ ] generate_advice / price_advisor.py / data_collector.py 零修改（红线）
- [ ] 无新 pip 依赖
- [ ] python app.py 一键启动无报错

---

## 关键文件索引

| 文件 | 用途 |
|---|---|
| `docs/tasks/dev_tasks_20260728_007_dev.md` | **本次任务书（必读）** |
| `docs/reviews/review_007_price_backtest_design_20260728.md` | **方案设计报告** |
| `modules/price_advisor.py` | 价格建议算法（**参考但不修改**） |
| `modules/price_advisor.py` L74-120 | _calc_atr（ATR计算参考） |
| `modules/price_advisor.py` L153-222 | _gen_no_position（无持仓算法参考） |
| `modules/price_advisor.py` L229-263 | _gen_with_position（有持仓算法参考） |
| `modules/data_adapter.py` L36-50 | _calc_ma（MA计算函数，可直接复用） |
| `modules/data_adapter.py` L155-170 | _calc_bollinger（BOLL计算函数，可直接复用） |
| `modules/backtest_engine.py` L710-893 | run_historical_simulation（回测方法参考） |
| `app.py` L2962-3065 | 现有回测API（参考格式） |
| `database/db_manager.py` L527 | 表创建区域（追加位置） |

---

## raw_kline 表结构（已确认）

字段：id, stock_id, trade_date, open, close, high, low, volume, amount, turnover, pct_change

## data_adapter 可复用函数

_calc_ma(closes, period) — 接受closes列表，返回单值
_calc_bollinger(closes, period) — 接受closes列表，返回(upper, mid, lower)

> 这些函数接受 closes 列表作为参数，可直接传入历史K线切片。

---

## 环境注意事项

| 项 | 说明 |
|---|---|
| PowerShell | 不支持 `&&`，用 `;` 代替 |
| Python 多行逻辑 | 必须写临时 `.py` 文件执行 |
| 项目路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格，需引号包裹） |
| 数据库路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst\stock_analyst.db`（在子目录内） |

---

## 激活确认

阅读完以上内容后，请回复：
> "开发人员已激活。已阅读 007 价格建议回测验证任务书，理解方案B（新建独立回测）+ T+5/T+20双周期。确认 price_advisor.py 红线不可改。将开始实现4个子任务，完成后出具自验报告。"

---

# 提示词结束