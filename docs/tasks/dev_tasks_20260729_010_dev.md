# DEV-TASKS-20260729-010-DEV：010 回测引擎方法论修复与评级有效性诊断 — 开发任务书

> **签发人**：PM  | **签发日期**：2026-07-29 | **状态**：待监理批准后执行
> **架构评审**：`docs/reviews/review_010_backtest_methodology_20260729.md`（已通过）

---

## 执行信息（PM 标注）

| 项 | 内容 |
|---|---|
| 任务类型 | 后端开发（回测引擎修复 + 数据诊断标记 + 报告增强） |
| 推荐模型 | **glm5.2** |
| 窗口类型 | **Quests 独立窗口** |
| 执行模式 | 单代理 agent |
| 预估工作量 | ~170行（修改2文件，可选改1文件） |

> **PM 裁定**：架构师建议4个子任务顺序执行，PM合并为一个任务。理由：4个子任务全集中在 price_backtest.py 同一文件，拆分反而增加衔接成本。

---

## 一、方案概述（架构师推荐，已通过）

| 决策点 | 方案 |
|---|---|
| 1. 评级锚点法 | 降级为辅助验证 + 新增高偏差风险标记 + 分时段统计 |
| 2. 动态止盈同步 | 复制 MIN_TARGET_GAIN + _calc_resistance + 动态止盈公式，force=True 重跑 |
| 3. 稀释 Bug 修复 | 在 _check_hit 中 take_profit 为 None 时保持 None |
| 4. 表结构设计 | 新增 5 列（rating_confidence/anchor_rating_date/anchor_rating/bias_risk/days_since_rating） |
| 5. 可信样本报告 | 现有报告新增 confidence_report + period_comparison 字段 |
| 6. 影响面 | 修改2文件（price_backtest.py + db_manager.py），可选改1文件（index.html） |

---

## 二、技术红线

| # | 红线 | 说明 |
|---|---|---|
| 1 | generate_advice 不可改 | advisor.py L869 零修改 |
| 2 | _build_capital_factors 不可改 | advisor.py 资金面因子构建函数不可改 |
| 3 | data_collector 三处 if False | L1645/L1684/L1717 不可恢复 |
| 4 | 零代码约束 | 无新 pip 依赖（当前8包） |
| 5 | 不回写 | 不修改数据采集逻辑，不自动写入 ratings_history |
| 6 | config_weights.json | rating_mapping 不可改 |
| 7 | scoring_engine.py | v5引擎不可修改 |
| 8 | price_advisor.py | 本次不修改（仅复制其常量到 price_backtest） |
| 9 | price_backtest.py | 本次**核心修改对象**，可修改 |
| 10 | backtest_engine.py | M8引擎，方法论正确，不修改 |

---

## 三、详细实现要求

### 任务1：010-1 同步 009 动态止盈（P0）

#### 1.1 新增常量（price_backtest.py，与 price_advisor.py L64-70 同步）

```python
# 与 price_advisor.py L64-70 同步，修改时需双向同步
MIN_TARGET_GAIN = {
    '强烈推荐买入': 0.08,
    '推荐买入': 0.06,
    '持有观望': 0.04,
    '建议减仓': 0.03,
    '强烈建议卖出': 0.02,
}
```

#### 1.2 新增函数（price_backtest.py，与 price_advisor.py L223-236 同步）

```python
def _calc_resistance(close, ma60, boll_upper):
    # 计算技术面阻力位（与 price_advisor._calc_resistance 逻辑一致）
    # 取 boll_upper 和 ma60 中 > close 的最小值（最近阻力位）
    # 都不可用时降级为 close * 1.10
    candidates = []
    if boll_upper and boll_upper > close:
        candidates.append(boll_upper)
    if ma60 and ma60 > close:
        candidates.append(ma60)
    if candidates:
        return min(candidates)
    return close * 1.10
```

#### 1.3 重写 _gen_with_position（price_backtest.py L202-230）

当前旧代码：
```python
def _gen_with_position(close, cost_price, rating):
    take_profit = cost_price * (1 + target_gain)  # 旧固定公式
    # ...
```

改为 009 动态止盈版（与 price_advisor.py L749-764 同步，修改时需双向同步）：

```python
def _gen_with_position(close, cost_price, rating, ma60=None, boll_upper=None):
    # 有持仓：止盈价 / 止损价（009 动态止盈版）
    target_gain = RATING_TARGET_GAIN.get(rating, 0.12)
    min_target_gain = MIN_TARGET_GAIN.get(rating, 0.04)
    stop_loss_pct = RATING_STOP_LOSS.get(rating, 0.05)

    # 动态止盈：max(min_tp, min(fixed_tp, resistance))
    fixed_tp = cost_price * (1 + target_gain)
    resistance = _calc_resistance(close, ma60, boll_upper)
    min_tp = cost_price * (1 + min_target_gain)
    take_profit = max(min_tp, min(fixed_tp, resistance))

    # 止损价
    stop_loss = cost_price * (1 - stop_loss_pct)
    min_stop = close * 0.90
    if stop_loss < min_stop:
        stop_loss = min_stop

    return {
        'available': True,
        'has_position': True,
        'position_pct': None,
        'buy_range_low': None,
        'buy_range_high': None,
        'target_price': None,
        'stop_loss': round(stop_loss, 2),
        'take_profit': round(take_profit, 2),
    }
```

#### 1.4 修改调用方 _gen_price_advice_at_date

当前 L254-256 调用 `_gen_with_position(close, cost_price, rating)`，需改为：
```python
if cost_price and cost_price > 0:
    return _gen_with_position(close, cost_price, rating, ma60, boll_upper)
```

---

### 任务2：010-2 修复稀释 Bug（P0）

#### 2.1 修复位置：_check_hit 函数末尾（L340-348附近）

当前代码（Bug）：将所有 hit 字段统一在循环中设为 0，包括 take_profit。

修复为（将 take_profit 从统一循环中拆出）：

1. buy_range/target/stop_loss 的 hit 字段：如为 None 则设为 0（保持原逻辑）
2. take_profit_hit 特殊处理：仅当 advice 中 take_profit 不为 None 时才设为 0，否则保持 None

实现要点：
- 从现有的统一循环 `for key in [buy_range, target, stop_loss, take_profit]` 中移除 take_profit
- 新增单独判断：`if take_profit is not None: if result[take_profit_key] is None: result[take_profit_key] = 0`

---

### 任务3：010-3 锚点标记 + 高偏差风险标记（P1）

#### 3.1 新增表结构（database/db_manager.py）

新增 _ensure_price_backtest_columns 函数（参考 backtest_engine.py L83-103 的 _ensure_columns 模式），幂等添加 5 列：

| 字段名 | 类型 | 说明 |
|---|---|---|
| rating_confidence | TEXT | 锚点可信度：confirmed/mismatched/unknown |
| anchor_rating_date | DATE | 匹配到的历史评级日期 |
| anchor_rating | TEXT | 匹配到的历史评级值 |
| bias_risk | TEXT | 偏差风险：high/medium/low |
| days_since_rating | INTEGER | 回测日距最近评级日的天数 |

在 run_price_backtest 函数开头调用此函数。

#### 3.2 锚点标记逻辑（price_backtest.py 新增函数）

新增 _mark_rating_confidence(cursor, stock_id, bt_date, rating) 函数：

1. 从 ratings_history 中查找该股票在回测日前后5天内的最近评级记录
2. 如找到：归一化评级后比较，一致则 confirmed，不一致则 mismatched
3. 如未找到：标记 unknown
4. 同时记录 anchor_rating_date、anchor_rating、days_since_rating

需新增评级归一化辅助函数 _normalize_rating_for_compare，兼容历史A/B+/B/C/D与中文5档：

| 历史评级 | 归一化为 |
|---|---|
| A / A+ | 强烈推荐买入 |
| B+ / B | 推荐买入 |
| C | 持有观望 |
| D | 建议减仓 |
| 中文5档 | 原值不变 |

#### 3.3 高偏差风险标记逻辑

新增 _calc_bias_risk(all_kline, bt_idx, rating) 函数：

1. 当前评级为"建议减仓"或"强烈建议卖出"时才评估
2. 计算近60日涨幅（bt_idx 处收盘价 vs bt_idx-60 处收盘价）
3. 涨幅>30% -> high，涨幅>15% -> medium，否则 low
4. 非减仓/卖出评级 -> 直接返回 low

#### 3.4 在主回测循环中集成

在 run_price_backtest 的写入逻辑中：
- 调用 _mark_rating_confidence 获取锚点信息
- 调用 _calc_bias_risk 获取偏差风险
- INSERT 语句中新增5个字段的值

---

### 任务4：010-4 可信样本报告（P1）

#### 4.1 修改 compute_price_backtest_report 函数

在现有报告返回结构中新增两个字段：

**confidence_report**（按可信度分组统计）：

| 分组 | 统计内容 | 特殊标注 |
|---|---|---|
| confirmed | total + T20目标价/止损价/止盈价命中率 | 样本量<30时标注"样本量不足（<30），仅供参考" |
| mismatched | total + 同上 | 样本量<20时标注"样本量极少（<20），仅作定性参考" |
| unknown | total + 同上 | 正常展示 |
| bias_risk_high | total + T20目标价/止损价命中率 | 标注"高偏差风险样本，命中率可能虚高" |

**period_comparison**（分时段统计）：

| 时段 | 说明 |
|---|---|
| recent_12d | backtest_date >= 2026-07-16 的样本（有真实评级数据） |
| earlier | backtest_date < 2026-07-16 的样本（存在未来函数偏差） |

标注说明："近12天有真实评级数据，命中率可能更准确；之前时段存在未来函数偏差"

---

### 任务5（可选）：前端展示

在 templates/index.html 回测报告区域，新增"可信样本统计"卡片。展示：
- 锚点覆盖率：confirmed X条 / mismatched Y条 / unknown Z条
- 高偏差风险样本数及提示
- 分时段命中率对比

若时间紧张可降级为仅API返回新字段。

---

## 四、开发顺序（架构师建议）

```
步骤1：010-1 同步动态止盈 + 010-2 修复稀释Bug（可同时进行）
步骤2：修改 db_manager.py 新增 _ensure_price_backtest_columns
步骤3：force=True 重跑全部938条（确保数据口径一致）
步骤4：010-3 锚点标记 + 高偏差风险标记（基于修复后数据）
步骤5：010-4 可信样本报告（基于标记后数据）
步骤6：（可选）前端展示
```

---

## 五、自验清单

### 5.1 010-1 验证
- [ ] price_backtest.py 新增 MIN_TARGET_GAIN 常量（5个评级值与 price_advisor.py 一致）
- [ ] 新增 _calc_resistance 函数（与 price_advisor.py L223-236 一致）
- [ ] _gen_with_position 重写为动态止盈公式，新增 ma60/boll_upper 参数
- [ ] _gen_price_advice_at_date 调用时传递 ma60 和 boll_upper

### 5.2 010-2 验证
- [ ] _check_hit 函数中 take_profit 为 None 时 take_profit_hit 保持 None
- [ ] buy_range/target/stop_loss 的 hit 字段不受影响

### 5.3 force=True 重跑验证
- [ ] 执行 run_price_backtest(force=True) 成功，938条记录重新生成
- [ ] has_position=0 的记录中 t20_hit_take_profit 全为 NULL
- [ ] 有持仓样本的 take_profit 使用动态止盈公式（非旧的固定公式）

### 5.4 010-3 验证
- [ ] price_backtest_results 新增5列
- [ ] rating_confidence 字段有 confirmed/mismatched/unknown 三种值
- [ ] bias_risk 字段有 high/medium/low 三种值
- [ ] days_since_rating 字段为整数或 NULL

### 5.5 010-4 验证
- [ ] compute_price_backtest_report 返回 confidence_report 字段
- [ ] compute_price_backtest_report 返回 period_comparison 字段
- [ ] confirmed 样本量 < 30 时有 "样本量不足" 提示

### 5.6 红线验证
- [ ] advisor.py 未修改
- [ ] price_advisor.py 未修改
- [ ] backtest_engine.py 未修改
- [ ] scoring_engine.py 未修改
- [ ] data_collector.py 未修改
- [ ] 无新 pip 依赖

---

## 六、验收标准（PM/QA 参考）

| 子任务 | 验收项 | 标准 |
|---|---|---|
| 010-1 | 动态止盈同步 | 有持仓样本 take_profit 与 price_advisor 计算结果一致（误差<1%） |
| 010-2 | 稀释Bug修复 | has_position=0 的 713 条 t20_hit_take_profit 全为 NULL |
| 010-2 | 止盈命中率修正 | "推荐买入"止盈命中率从 8.0% 修正为 ~18.5%（仅统计有持仓） |
| 010-3 | 锚点标记 | rating_confidence 三级分类正确，confirmed+mismatched+unknown 覆盖全部样本 |
| 010-3 | 偏差风险标记 | bias_risk=high 的样本特征：评级为减仓/卖出 + 近60日涨幅>30% |
| 010-4 | 可信样本报告 | confidence_report + period_comparison 字段存在且数据正确 |
| 全局 | 红线合规 | 10条红线零触碰 |

---

## 七、参考文档

| 文档 | 用途 |
|---|---|
| `docs/reviews/review_010_backtest_methodology_20260729.md` | **本次架构评审报告（656行，含完整代码方案）** |
| `docs/reviews/review_007_price_backtest_design_20260728.md` | 007回测方案设计 |
| `modules/price_backtest.py` | 本次核心修改对象（766行） |
| `modules/price_advisor.py` | 动态止盈算法参考源（L64-70/L223-236/L749-764） |
| `modules/backtest_engine.py` | _ensure_columns 幂等模式参考（L83-103） |

---

## 八、监理确认

> 监理确认后，将本任务书内容粘贴到 Quests 独立窗口（glm5.2 模型）执行。
