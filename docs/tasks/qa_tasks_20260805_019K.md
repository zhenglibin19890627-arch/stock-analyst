# QA-TASKS-20260805-019K：东财失败时 THS 资金数据顶替（方案一）— 独立验收任务书

> **签发人**：PM | **签发日期**：2026-08-05 | **状态**：待 QA 执行

---

## 角色定义（内嵌，无需额外窗口提示词）

### 你的角色：QA（独立验收）

**职责边界**：
- 独立验收 019K 开发成果，**不依赖开发自验报告结论**（自验报告 `reports/dev_selftest_019K_ths_capital_fallback_20260805.md` 仅作对照参考）
- 静态代码核查 + 受控实测（mock 场景），逐条用例出 PASS/FAIL + 证据
- **不修改任何功能代码**；mock 测试须在独立临时脚本中执行，验收结束前删除
- 交付物：`reports/qa_accept_019K_ths_capital_fallback_20260805.md`

### 独立性原则
- 各角色独立不兼职：PM 不兼架构、架构师不编码、开发不验收、QA 独立测试
- QA 自行构造 mock 测试，不复用开发临时脚本（开发自验脚本在 `.dev_019K_work/`，仅可对照不可采信）

### 项目背景摘要
| 项 | 内容 |
|---|---|
| 项目路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格，命令行需引号） |
| 数据库路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst\stock_analyst.db` |
| Python 解释器 | `C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe` |
| 技术栈 | Python + Flask + SQLite + akshare + Jinja2 单页应用 |
| 任务书 | `docs/tasks/dev_tasks_20260805_019K_ths_capital_fallback.md`（v2 定稿，M-1~M-12 已并入） |
| 评审报告 | `docs/reviews/review_019K_ths_capital_fallback_20260805.md`（架构师 ⚠️ 有条件通过） |
| 自验报告 | `reports/dev_selftest_019K_ths_capital_fallback_20260805.md`（仅对照参考，不采信结论） |
| PM 核验结论 | PM 独立核验通过：顶替逻辑（插入点①、UPDATE+INSERT OR IGNORE、仅写 3 字段）代码级确认；4 处防覆盖落地确认；迁移列 DB 实测存在；py_compile PASS；范围外 9 文件哈希与 019J 基线一致；生产库仅迁移无测试污染（ths_total 行=0，daily_reports 387 行未变） |

### 环境约束（硬性，违反将导致执行失败）
1. **项目在 IDE 工区外**：路径含空格，Write 工具直写会报错，须"工作区 Copy + SearchReplace 编辑 + Copy-Item 覆盖回项目目录"
2. **PowerShell 中文**：追加中文到文件用 `[System.IO.File]::ReadAllText + WriteAllText`（UTF-8），禁止 Add-Content/Out-File（乱码）
3. **PowerShell 内联 Python**：含 `*` 的 SQL 会被通配符解析破坏，须用 `chr(39)` 包裹字符串或写临时 .py 脚本；含 f-string 的 Python 代码也须用临时文件
4. **临时脚本清理**：mock 测试脚本须在验收结束后删除，不留存仓库
5. **真实数据库保护**：mock 测试须用临时库（%TEMP%），严禁向真实 `stock_analyst.db` 写入业务数据（注意：真实库已被开发执行过一次启动迁移新增 capital_source 列——这是预期内 schema 变更，非污染；QA 不得再写入任何行）

---

## 一、改动概述（3 文件）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `database/db_manager.py` | 迁移列表追加 `('raw_capital_flow', 'capital_source', 'TEXT DEFAULT NULL')`（走 `_safe_add_columns` 幂等自动迁移） |
| 2 | `modules/data_collector.py` | ① THS 顶替写入（em_all_failed 块内、估算前，L2140-2188）② 前置跳过 SQL 排除 ths_total（L1930-1935）③ 补采清单 SQL 排除 ths_total（L1501-1505）④ EM 三层写入显式 capital_source=NULL（L1991-1996/L2044-2049/L2095-2100）⑤ 估算三处 UPDATE 来源守卫（L2209/L2247/L2285）⑥ docstring 修订 |
| 3 | `templates/index.html` | ① 表头动态来源标注（L2483-2488）② 行内"同花顺"`<sup>` 标注（L2499）③ 两处状态三元链 'fallback'→'⚠️顶替'（L2068-2075/L2558-2560） |

**核心实现要点（M-4/M-5）**：
- 顶替源 = 库内 `ths_net_inflow`（零网络）；写入 `main_net_inflow=ths 值, is_estimated=0, capital_source='ths_total'`，**不写** pct/分单字段，仅当日 1 行
- UPDATE + INSERT OR IGNORE（禁 INSERT OR REPLACE）
- 成功返回 `('fallback', '同花顺顶替(全部资金口径，非主力；东财恢复后自动回补)')`；`data_status.status='fallback'`
- 顶替失败（ths NULL）→ 落回估算兜底

---

## 二、关键代码位置（QA 必读）

| 位置 | 说明 |
|---|---|
| `modules/data_collector.py` L2133-2188 | `em_all_failed` 块 + THS 顶替逻辑（本批次核心） |
| `modules/data_collector.py` L1930-1935 | 前置跳过 SQL 排除 ths_total（Task 3①） |
| `modules/data_collector.py` L1501-1505 | 补采清单 SQL 排除 ths_total（Task 3②） |
| `modules/data_collector.py` L1991/2044/2095 附近 | EM 三层 INSERT OR REPLACE 带 capital_source=NULL（Task 3③） |
| `modules/data_collector.py` L2209/2247/2285 附近 | 估算三处 UPDATE 来源守卫（Task 3④） |
| `modules/data_collector.py` L1327 附近 | `_em_batch_collect` 的 `result[0]=='success'` 判定（'fallback' 不计成功） |
| `database/db_manager.py` L965 | 迁移列表 capital_source 项 |
| `templates/index.html` L2483-2499 / L2068-2075 / L2558-2560 | 标注与 fallback 映射 |

---

## 三、验收用例（按任务书 v2 验收标准，逐条 PASS/FAIL + 证据）

### V1：代码级核查

| 核查项 | 验证方法 |
|---|---|
| `capital_source` 列在迁移列表（db_manager.py） | Read 迁移列表 |
| 顶替逻辑位于 `em_all_failed` 块内、估算源之前 | Read L2133-2190 |
| 顶替写入仅 3 字段（main_net_inflow/is_estimated=0/capital_source='ths_total'），无 pct/分单字段 | Read L2162-2173 |
| 使用 UPDATE + INSERT OR IGNORE，无 INSERT OR REPLACE | Read L2162-2173 + grep 该函数体内 |
| 4 处防覆盖全部落地（①前置跳过 ②补采清单 ③EM 三层 ④估算守卫） | Read 对应行号 + grep `capital_source` |
| `_em_batch_collect` L1327 判定语义（'fallback' ≠ 'success'） | Read |
| index.html 标注与 fallback 映射 3 处 | Read L2483-2499/L2068-2075/L2558-2560 |

### V2：编译验证

```
python -m py_compile modules/data_collector.py database/db_manager.py
```
→ 无错误

### V3：THS 顶替路径（核心功能）

**mock 组合**：mock EM 三层全失败（`_fetch_capital_flow_em_individual`/`_fetch_capital_flow_em`/akshare 全返回 None）+ 临时库预置当日行 `ths_net_inflow=-11800` + 其余依赖（get_connection 指向临时库、save_data_status 捕获）

| 断言 | 预期 |
|---|---|
| 返回 `('fallback', '同花顺顶替(全部资金口径，非主力；东财恢复后自动回补)')` | 命中 |
| DB 写入 main_net_inflow=-11800 | 命中 |
| is_estimated=0 | 命中 |
| capital_source='ths_total' | 命中 |
| data_status.status='fallback'、message 含"同花顺顶替/全部资金口径" | 命中 |
| 仅当日 1 行（trade_date=今日） | rows=1 |
| **不写** main_net_inflow_pct / super_large_net / large_net / medium_net / small_net | 均 NULL |
| ths_net_inflow 保留原值 | 命中 |

### V4：EM 成功正常路径零干扰

**mock 组合**：EM 三层正常返回（模拟 2 天数据）→ 调用 fetch_capital_flow

| 断言 | 预期 |
|---|---|
| 返回 success | 命中 |
| EM 值写入 main_net_inflow（非 THS 顶替） | 命中 |
| capital_source 保持 NULL | 命中 |
| THS 顶替逻辑未触发（无 'fallback' 返回） | 命中 |

### V5：EM 恢复回补（防覆盖闭环——QA 重点）

**mock 组合**：临时库预置 THS 顶替行（main=-11800, is_estimated=0, capital_source='ths_total'）→ mock EM 恢复成功 → 再次调用 fetch_capital_flow

| 断言 | 预期 |
|---|---|
| 前置跳过**不阻塞** ths_total 行（同日重采未直接跳过） | 命中 |
| EM 重采成功覆盖 main_net_inflow=EM 值（如 12345.67） | 命中 |
| capital_source 归位 NULL | 命中 |
| is_estimated=0 | 命中 |
| 补采清单 SQL（fetch_capital_flow_batch 019E 补采）不命中 ths_total 行（应进补采）/命中 EM 真实行（排除出补采） | 双向断言 |

### V6：THS 为 NULL 落回估算兜底

**mock 组合**：EM 全失败 + 库内无 ths_net_inflow（NULL）→ 估算源 mock 返回

| 断言 | 预期 |
|---|---|
| 返回 ('estimated', ...)（估算兜底链路不变） | 命中 |
| 写入 is_estimated=1、capital_source=NULL | 命中 |
| 状态为 estimated | 命中 |

### V7：估算不覆盖 THS 顶替（来源守卫）

**mock 组合**：临时库预置 ths_total 行 → 估算路径 UPDATE 执行

| 断言 | 预期 |
|---|---|
| ths_total 行 UPDATE rowcount=0（守卫禁止覆盖） | 命中 |
| 对照 NULL 来源行 UPDATE rowcount=1（可覆盖） | 命中 |

### V8：状态消费方（`_em_batch_collect`）

**mock 组合**：mock 单只 fetch_capital_flow 返回 ('fallback', ...)

| 断言 | 预期 |
|---|---|
| 'fallback' 计 fail_count=1、success_count=0 | 命中 |
| EM 熔断计数不重置 | 命中 |
| 对照 'success' 返回 → 计成功并重置计数 | 命中 |

### V9：评分进入验证

**mock 组合**：临时库预置 THS 顶替行（main=-11800, is_estimated=0, capital_source='ths_total'）→ 调用 data_adapter._read_capital_data + scoring_engine.score_main_capital

| 断言 | 预期 |
|---|---|
| `_read_capital_data` 读到顶替行（is_estimated=0 过滤放行） | main=-11800 |
| `score_main_capital` 以 -11800 计分（大幅净流出分，非缺失中性 85） | 命中 |
| 对照无数据行 → 中性 85 | 命中 |

### V10：零改动确认

| 文件 | 哈希（019J 基线，PM 已复算） |
|---|---|
| modules/data_collector.py | `4C847FAD888F20BA`（本批次改动） |
| database/db_manager.py | `2D222BE42F298258`（本批次改动） |
| templates/index.html | `79F3F330F7148D49`（本批次改动） |
| modules/advisor.py | `CA1857B0F6452B20`（不变） |
| modules/analysis_engine.py | `DF71A6FE4FD7685D`（不变） |
| modules/alert_engine.py | `053F0CDB4DA62385`（不变） |
| modules/scoring_engine.py | `DD9DBFBBD005B35D`（不变） |
| modules/data_adapter.py | `0792E5006D7DCED9`（不变） |
| modules/daily_report.py | `94C20A5CB7C78A7C`（不变） |
| app.py | `8F8373C029E76390`（不变） |
| config.py | `F6CE1F84B8DDACDA`（不变） |
| requirements.txt | `DBE076A7458C5788`（不变） |

验证方法：SHA256 前 16 位比对。

---

## 四、红线遵守核查清单

| 红线 | 核查方法 |
|---|---|
| 1. 功能红线：EM 全失败资金面因子不缺失 | V3 + V9（顶替真实数据参与评分） |
| 2. 口径红线：全链路标注 + 监理知情 | V3 message 断言 + V1 index.html 标注核查 |
| 3. 来源标注红线 | V1 index.html 表头/行内标注 + V3 message |
| 4. EM 回补红线：pre-check + 补采清单排除 ths_total | V5 |
| 5. 状态消费红线：'fallback' 适配前端 + 不重置熔断 | V1 + V8 |
| 6. 范围红线：仅 3 文件 | V10 哈希 |
| 7. 零代码约束：无新依赖、config.py 不碰、schema 仅新增列 | V10 + V1 迁移列表 |
| 8. 防覆盖红线：估算↛THS、THS↛EM、EM→THS 可回补 | V5/V7 |
| 9. 评分纯净红线：is_estimated=1 永不进评分 | V6 + V9 对照 |
| 10. 超时红线：顶替链路零网络调用 | V1 代码核查（无新增网络调用） |

---

## 五、QA 交付物要求

`reports/qa_accept_019K_ths_capital_fallback_20260805.md`，含：
1. 逐用例 PASS/FAIL + 证据（实测数据、日志文本）
2. 红线核查清单
3. 新发现问题（如有）
4. 验收结论：**通过 / 不通过**
5. QA 签署

---

## 六、PM 备注

1. **核心风险点 = 防覆盖闭环（V5）**：架构师发现 3（THS 顶替行阻塞 EM 回补）是本批次阻塞项，M-6 四向修改必须全部落地且 QA 实测闭环。
2. **口径偏差知情**：THS 全部资金口径与主力口径同日符号可相反（DB 实证 600276/300146）。本批次按监理方案一"参与评分 + 标注"验收；如监理后续启用"口径纯净开关"（4 处 WHERE 单行修改）则另立批次。
3. **真实库状态**：开发已执行过一次启动迁移（capital_source 列已存在，属预期 schema 变更）；QA 不得向真实库写入任何业务行（每日报告 387 行基线不得变化）。
4. **开发自验脚本可对照**（`.dev_019K_work/selftest_019K.py`，42 断言）但 QA 须独立构造，不采信其结论。
5. **运行实例提醒**：当前运行中 app.py（PID 16764，20:04 启动）为 019J 代码，019K 改动需重启后生效；QA 验收基于 mock 测试，不依赖运行实例。
6. **R-7 登记**：EM 恢复覆盖时 ths_net_inflow 被 INSERT OR REPLACE 清空为既有 018 行为（评审接受登记），QA 在 V5 中记录实测即可，不算缺陷。
