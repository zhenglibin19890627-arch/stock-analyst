# QA 验收任务书 QA-TASKS-20260803-019D

## 任务：019D 报告生成时间分钟级展示与三入口评分同源对齐 — QA 独立验收

| 项 | 内容 |
|---|---|
| 编号 | QA-TASKS-20260803-019D |
| 关联开发任务 | DEV-TASKS-20260803-019D（定稿，监理已批准 2026-08-03，开发已完成并自验） |
| 签发日期 | 2026-08-03 |
| 签发人 | PM |
| 优先级 | P1 |
| 推荐模型 | qwen3.8-max-preview / glm5.2（并列优先） |
| 窗口类型 | Quests 独立窗口 |
| 执行模式 | 智能体（单代理） |

---

## 〇、角色定义（内嵌，监理新窗口直接粘贴本任务书即可）

**你是本项目的 QA 验收人员**，职责为：独立设计测试用例 + 独立执行验收。

**独立性原则**：
- QA **不依赖开发自验报告**，独立设计测试用例并独立执行（开发自验报告仅供交叉参考，不得作为 PASS 依据）
- QA **不修改代码**（只验证、不修复）
- QA 发现问题记录为 FAIL，由开发修复后复验
- PM 已完成交付物完整性核查（`_resolve_report_type` 抽取+四处调用、实时路径 generated_at、`finished_at` 返回值、`_fmtGenTime`+五处展示点均已就位）；QA 负责功能正确性独立验证

**项目背景摘要**：
- 项目路径：`C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格，命令需引号）
- 数据库路径：`C:\Users\zlb19\Desktop\Qoder cn\stock_analyst\stock_analyst.db`
- Python 解释器：`C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe`
- 技术栈：Python + Flask + SQLite + Jinja2 单页应用（前端全部在 `templates/index.html`）
- 启动方式：`python app.py`（默认端口 5000）
- 最高约束：零代码用户可独立运行
- PowerShell 不支持 `&&`，用 `;` 代替；多行 Python 逻辑必须写临时 `.py` 文件执行；中文输出需 `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8', errors='replace')`

---

## 一、批次 019D 功能概述（供 QA 独立设计用例参考，不替代 QA 自行阅读代码）

**目标**：解决「分析报告点刷新和总览看板、每日报告的评分对不上 + 报告时间精确到分钟」两项用户反馈。

| 任务 | 改动 | 位置（开发报告口径，以实际代码为准） |
|---|---|---|
| 任务 1 | 抽取共享函数 `_resolve_report_type`（daily 优先/intraday 降级/status='ok' 口径）+ app.py 内 4 处读取口径统一（看板 JOIN / MAX generated_at / report-latest 当日查询 / report-latest 回退查询）；`/api/ratings` 改为调用该函数（行为等价） | app.py 约 L1402 起 |
| 任务 1b | `get_latest_reports` / `get_reports_by_date` 三段 SQL 补 `status='ok'` 过滤 | daily_report.py |
| 任务 2 | report-latest 响应保证含 `generated_at`；B11-DETAIL-LOAD 实时路径补 `advice['generated_at'] = datetime.now(_CN_TZ).isoformat()` | app.py 约 L940 |
| 任务 3 | 前端 `_fmtGenTime(s)` 辅助函数（slice(0,16).replace('T',' ')）+ 四处展示点（看板顶部 / 日报列表表头+行级 / 生成汇总视图 / 详情页"报告生成于"） | index.html 约 L5393 起 |
| 任务 3-3b | `generate_daily_report()` 返回值追加 `finished_at`（空格分隔格式） | daily_report.py 约 L675 |

**正确口径标杆**：`/api/ratings`——先判定当日是否存在 `status='ok' AND report_type='daily'`，有则取 daily，否则 intraday；读取限定 `status='ok' AND report_type=?`。

详细背景见：`docs/tasks/dev_tasks_20260803_019D_score_time_alignment.md`（定稿）与 `docs/reviews/review_019D_score_time_alignment_20260803.md`（架构评审报告）。

---

## 二、QA 独立验收要求

### 验收方法
1. **静态核查 + 受控实测并重**：构造 daily+intraday 并存场景必须用临时 `.py` 脚本直接 INSERT 测试行，验收完成后**必须清理全部测试行**（以 stock_id + report_date 精确删除，严禁误删真实报告）
2. 前端展示验证以 API 响应 JSON + 代码核查为主；如有条件启动 app 用浏览器核对渲染效果（非强制截图）
3. 下述测试用例指引为 PM 提供的参考方向，QA 应**自行扩充边界用例**

### ⚠️ 数据前置条件（开测前必查）

本验收依赖当日资金面数据完整。2026-08-03 曾有 19 只股票 `raw_capital_flow.main_net_inflow` 为 NULL（旧代码污染+新代码无补采触发点所致），PM 已批准 EM 逐只补采。**QA 开测前须确认 29 只股票当日 `main_net_inflow` 全部非 NULL**（SQL 见下），否则 TC-019D-2 涉及资金面评分的对比无效，需等待补采完成。

```sql
SELECT COUNT(*) FROM raw_capital_flow
WHERE trade_date='2026-08-03' AND main_net_inflow IS NULL;
-- 预期结果：0
```

### 测试用例

#### TC-019D-1 — `_resolve_report_type` 与四处调用核查（风险：低）
- [ ] `_resolve_report_type` 逻辑与 `/api/ratings` 原判定逻辑等价（daily 优先，COUNT>0 判定）
- [ ] 看板 JOIN、看板 MAX(generated_at)、report-latest 当日查询、report-latest 回退查询四处均调用该函数并限定 `report_type=? AND status='ok'`
- [ ] `/api/ratings` 改为调用该函数后响应结构与行为不变（rating_time/created_at 兼容字段仍在）

#### TC-019D-2 — daily+intraday 并存场景实测（风险：中）★★核心验收项
- [ ] 构造方法：选 1 只自选股，临时 INSERT 同日同 stock_id 的 daily 行（status='ok'）与 intraday 行（status='ok'），两行 total_score 故意不同
- [ ] 预期：总览看板（watchlist-scores API）该股票仅 1 行，评分=daily 行分值
- [ ] 预期：`/api/ratings` 该股票取 daily 分值
- [ ] 预期：report-latest 返回 daily 行（含 generated_at）
- [ ] 预期：每日报告列表（get_latest_reports）返回 daily 行
- [ ] 变体：删除 daily 行仅留 intraday 行 → 各入口正确降级展示 intraday（不出现空列表误报）
- [ ] 清理：验收后删除全部构造行并复核

#### TC-019D-3 — failed 行过滤核查（风险：低）
- [ ] 构造 status='failed' 行，验证 get_latest_reports / get_reports_by_date / 看板 JOIN / MAX(generated_at) 均不返回/不计入
- [ ] 代码核查：failed 行的前端渲染分支保留未删（R-5 防御性保留），并确认用户无"在每日报告页查看 failed 行 error_msg"的依赖（结论记录于报告）

#### TC-019D-4 — 三入口分钟级时间展示（风险：中）★重点
- [ ] `_fmtGenTime` 函数存在且逻辑为 slice(0,16).replace('T',' ')，对 undefined/null/非字符串返回 `—`
- [ ] 看板顶部「生成时间」、日报列表表头「本批生成时间」+行级「生成于」、生成汇总「生成时间」、详情页「报告生成于」四处均调用 `_fmtGenTime`
- [ ] 详情页「报告生成于」数据源为 `adviseData.generated_at`，**不是 rating_date**（rating_date 行保留，语义为数据截至日）
- [ ] 生成汇总视图数据源为返回值 `finished_at`（任务 3-3b）

#### TC-019D-5 — generated_at 回写链路贯通（风险：中）
- [ ] 触发一次单只分析（刷新报告），验证详情页「报告生成于」更新为当前分钟
- [ ] B11-DETAIL-LOAD 实时路径（当日无报告直接 generate_advice）响应含 generated_at（代码核查 L940 附近）

#### TC-019D-6 — finished_at 返回值核查（风险：低）
- [ ] `generate_daily_report()` 返回 dict 含 `finished_at`，格式 `YYYY-MM-DD HH:MM:SS`，取值时机为批次完成后
- [ ] 无 INSERT/UPDATE/DELETE 写入逻辑被改动（仅返回值扩展）

---

## 三、红线核验（QA 需独立复核）

| 红线项 | 核验方法 |
|---|---|
| `generate_advice()` 签名未变 | advisor.py 未在本批次 diff 中 |
| `_save_report` / `_save_daily_report_for_advice` 写入语义未改 | daily_report.py 写入逻辑与 generated_at ISO 格式不变 |
| `config_weights.json` 未改（含 BOM 检查）；`config.py` 不动 | 文件比对 |
| 无新增 pip 依赖 | requirements.txt 维持 9 包；前端无第三方时间库 |
| 改动限于 app.py / index.html / daily_report.py（三处限定） | git diff 核查 |
| watchlist-scores ETag 机制保留 | 排除 generated_at 的既有做法不变 |
| 口径唯一性 | 全仓 grep 不得出现第三套 report_type/status 口径 |

---

## 四、PM 提示 QA 关注/知悉项（不构成 FAIL，需记录）

| # | 项目 | 说明与 QA 操作 |
|---|---|---|
| 1 | R-5 failed 行渲染分支 | 修复后每日报告列表不再返回 failed 行，渲染分支保留不触发；QA 确认用户无相关依赖并记录结论 |
| 2 | 历史并存脏行不迁移 | 评审 D-2 裁定：读取侧过滤隔离即可；QA 若发现历史脏行，属预期，不判 FAIL |
| 3 | ETag 值变化 | 修复后 stocks 数组内容变化导致 ETag 变化属预期（客户端重新拉取正确数据） |
| 4 | finished_at 与 generated_at 格式差异 | 前者空格分隔、后者 ISO（T 分隔）；`_fmtGenTime` 对两者均正确（无 T 时 replace 为 no-op），QA 可构造两种输入验证 |
| 5 | 构造测试行注意事项 | daily_reports 表唯一约束含 (stock_id, report_date, report_type)，构造并存行用不同 report_type；清理时按构造行精确删除 |

---

## 五、验收报告交付物

| 交付物 | 说明 |
|---|---|
| `reports/qa_accept_019D_score_time_20260803.md` | 019D 批次 QA 验收报告（TC-019D-1~6 + 红线 + 知悉项确认 + 结论） |

### 验收报告格式要求
- 逐条 TC 列出 PASS/FAIL，FAIL 项需附问题描述 + 复现步骤 + 预期/实际对比
- 红线核验逐项列出结论
- 构造测试行的插入/清理记录单列一节（含清理后复核结果）
- 最终结论：`全部 PASS 可双签` / `有 FAIL 需修复后复验`
- 注明验收环境（测试时间、验证方式：代码核查/受控实测/API 响应核对）

---

**QA 完成独立验收后，将报告提交 PM，由 PM+QA 双签后报监理关闭。**
