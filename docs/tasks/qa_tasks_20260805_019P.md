# QA-TASKS-20260805-019P：基本面数据完善（毛利率补全+来源标注+趋势分析+港股占位行修复）— 独立验收任务书

> **签发人**：PM | **签发日期**：2026-08-05 | **状态**：待 QA 执行

---

## 角色定义（内嵌，无需额外窗口提示词）

### 你的角色：QA（独立验收）

**职责边界**：
- 独立验收 019P 开发成果，**不依赖开发自验报告结论**（自验报告 `reports/dev_selftest_019P_fundamental_enhance_20260805.md` 仅作对照参考）
- 静态代码核查 + 受控实测（mock 场景），逐条用例出 PASS/FAIL + 证据
- **不修改任何功能代码**；mock 测试须在独立临时脚本中执行，验收结束前删除
- 交付物：`reports/qa_accept_019P_fundamental_enhance_20260805.md`

### 独立性原则
- 各角色独立不兼职：PM 不兼架构、架构师不编码、开发不验收、QA 独立测试
- QA 自行构造 mock 测试，不复用开发临时脚本（`.dev_019P_work/selftest_019P.py` 仅可对照不可采信）
- **QA 验收对象基线**：当前工作树（00:06 git reset 后状态，PM 已核验各批次代码标记完好、主库数据 387/315 与 019P 迁移备份一致）

### 项目背景摘要
| 项 | 内容 |
|---|---|
| 项目路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格，命令行需引号） |
| 数据库路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst\stock_analyst.db` |
| Python 解释器 | `C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe` |
| 技术栈 | Python + Flask + SQLite + akshare + Jinja2 单页应用 |
| 任务书 | `docs/tasks/dev_tasks_20260805_019P_fundamental_enhance.md`（v2 定稿，M-1~M-10 已并入） |
| 评审报告 | `docs/reviews/review_019P_fundamental_enhance_20260805.md`（架构师 ⚠️ 有条件通过，M-1~M-10） |
| 自验报告 | `reports/dev_selftest_019P_fundamental_enhance_20260805.md`（仅对照参考，不采信结论） |
| PM 核验结论 | PM 独立核验通过：abstract 主源/P1/P2/P3/data_source 迁移（DB 实测列存在）/TTL 完整性检查/港股占位行修复/趋势四输出位置代码级确认；py_compile PASS；pytest 343 passed；**异常核查**：00:06 git reset（mixed 仅 index，工作树无损——各批次代码标记在位）；23:35 backups 出现 drop/delete 备份文件（空表快照，主库 387/315 行与 019P 迁移备份一致，无数据损失） |

### 环境约束（硬性，违反将导致执行失败）
1. **项目在 IDE 工区外**：路径含空格，Write 工具直写会报错，须"工作区 Copy + SearchReplace 编辑 + Copy-Item 覆盖回项目目录"
2. **PowerShell 中文**：追加中文到文件用 `[System.IO.File]::ReadAllText + WriteAllText`（UTF-8），禁止 Add-Content/Out-File（乱码）
3. **PowerShell 内联 Python**：含 `*` 的 SQL 会被通配符解析破坏，须用 `chr(39)` 包裹字符串或写临时 .py 脚本；含 f-string 的 Python 代码也须用临时文件
4. **临时脚本清理**：mock 测试脚本须在验收结束后删除，不留存仓库
5. **真实数据库保护**：mock 测试须用临时库（%TEMP%），严禁向真实 `stock_analyst.db` 写入任何业务行；零真实网络请求（QA 验收不得触发真实 akshare 调用）

---

## 一、改动概述（4 文件）

| # | 文件 | 任务 | 改动区域（函数名区域，M-9）|
|---|---|---|---|
| 1 | `modules/data_collector.py` | 1/3/4/5 | `_fetch_a_fundamental_sina` 系 + `fetch_a_fundamental` + `fetch_hk_fundamental` + 新增辅助函数（基本面区 L537-1330）|
| 2 | `modules/advisor.py` | 2 | `_build_fundamental_factors` + `_describe_dimension` + `_detect_risks` + `_pick_top_factors` + 新增 `_build_fund_trend` |
| 3 | `templates/index.html` | 2/3 | L2469-2495 基本面展示区 + L5170-5241 因子卡 |
| 4 | `database/db_manager.py` | 3 | `_migrate_columns` 追加 `raw_fundamental.data_source TEXT DEFAULT NULL` |

**核心规格（v2）**：
- **A' 方案**：abstract 主源（写 8 期）+ P1 ocf 保留 + P2 现接口降级 + P3 超时闭包
- **趋势仅展示不进评分**（口径双轨制：ROE 仅同比、期间/时点型指标环比、增速"加快/放缓"）
- **来源标注三通道**：data_source 列 + message 前缀 + 前端动态表头
- **港股占位行修复** + **存量自动回补**（TTL 完整性检查）

---

## 二、关键代码位置（QA 必读）

| 位置 | 说明 |
|---|---|
| `modules/data_collector.py` L542-606 | `_fetch_a_fundamental_sina`（abstract 主源）+ `_call_ak_with_timeout`（P3）|
| `modules/data_collector.py` L787-940 | `fetch_a_fundamental` 主源切换 + P1 ocf 保留 + data_source 写入 |
| `modules/data_collector.py` L723-760 | TTL 完整性检查（任务 5）|
| `modules/data_collector.py` L1100-1340 | `fetch_hk_fundamental`（占位行修复 + data_source='em_hk'）|
| `modules/advisor.py` L1080-1132 | `_build_fund_trend`（趋势计算）|
| `modules/advisor.py` L212-221 / L404-415 / L449-460 | 四输出位置（highlights/risks/pick_top）|
| `database/db_manager.py` L965-967 | data_source 迁移 |
| `templates/index.html` L2469-2495 / L5170-5241 | 前端标注 + fund_trend 因子卡 |

---

## 三、验收用例（按任务书 v2 §四 + M-8，逐条 PASS/FAIL + 证据）

### V1：代码级核查

| 核查项 | 验证方法 |
|---|---|
| abstract 主源：`_fetch_a_fundamental_sina` 调 `ak.stock_financial_abstract` | Read |
| `data_source` 迁移存在（db_manager.py）+ 主库列存在 | Read + DB PRAGMA |
| P1 ocf 保留实现 | Read（REPLACE 前查既有 ocf）|
| P2 降级实现（abstract 异常→现接口 + message 标注）| Read |
| P3 超时闭包 `_call_ak_with_timeout`（daemon 线程模式，不 import THS 闭包）| Read |
| 港股占位行修复（清理 + PE/PB 合并真实行）| Read |
| TTL 完整性检查（最新期 gross_margin NULL → 不跳过）| Read |
| 趋势四输出位置（highlights/risks/pick_top/前端因子卡）| Read + grep |
| **范围隔离**：019N 资金面函数（`_safe_num`/`fetch_capital_flow`/`_em_batch_collect`）函数体未动 | grep 函数签名在位 |

### V2：编译验证

```
python -m py_compile modules/data_collector.py modules/advisor.py database/db_manager.py
```
→ 无错误

### V3：abstract 结构适配 + 毛利率补全（M-8①）

**mock 组合**：mock `ak.stock_financial_abstract` 返回（行=指标、列=['选项','指标']+报告期列**最新在前**、'毛利率' 同名两行）

| 断言 | 预期 |
|---|---|
| 写 8 期（非全历史）| 命中 |
| report_date 格式 `YYYY-MM-DD` | 命中 |
| **gross_margin 8 期全非空**（核心修复）| 命中 |
| 同名去重（R-1）：'毛利率' 取常用指标组 | 命中 |
| ROE=常用指标组口径（4.15 量级，值域 0<v<100 断言）| 命中 |
| data_source='sina_abstract' 全行标注 | 命中 |
| PE/PB 合并到最新行 | 命中 |

### V4：P1 ocf 保留

**mock 组合**：abstract 该期 ocf=NaN + 临时库预置同 (stock_id, report_date) ocf=0.3448

| 断言 | 预期 |
|---|---|
| ocf 保留 0.3448（不被 NaN 覆盖）| 命中 |
| abstract 有值期正常写入 | 命中 |

### V5：P2 降级 + P3 超时（M-8②）

| 场景 | 断言 | 预期 |
|---|---|---|
| abstract 抛异常 | 降级现接口写 2 期 + data_source='sina_analysis_indicator' + message 含"降级" | 命中 |
| abstract 挂起超时（注入超时 1s）| 返回 <2s 不挂死 + 降级现接口 | 命中 |

### V6：TTL 三态（M-8③）

| 场景 | 断言 | 预期 |
|---|---|---|
| 最新期 gross_margin NULL（TTL 内）| 不跳过 + message '财报补全(毛利率缺失触发)' + 回补后二次调用'同日跳过'（收敛）| 命中 |
| 最新期有值（TTL 内）| '同日跳过(财报80天TTL内+PE/PB 24h内)'（回归）| 命中 |
| 最新期 100 天前 | 正常采集（回补标记不误触发）| 命中 |

### V7：趋势分析（M-8④——R-5 环比陷阱重点）

**mock 组合**：Q1（ROE=4.15）与年报（ROE=14.26）混排 + 多期毛利率/净利率/负债率

| 断言 | 预期 |
|---|---|
| **不输出 ROE 环比**（'ROE较上期' 不在明细）| 命中 |
| ROE 同比正确（有同期数据时）| 命中 |
| 环比指标（毛利率/净利率/负债率/流动/速动）较上期 | 命中 |
| 负债率下降=改善 | 命中 |
| 增速指标"加快/放缓"表述 | 命中 |
| \|Δ\|<1pct → 平稳 | 命中 |
| 单期/无数据 → '历史数据不足，暂无趋势判断'（不崩溃）| 命中 |
| **四输出位置**：highlights（改善）/ risks（恶化）/ 因子卡首位 fund_trend / _pick_top_factors 首位 | 命中 |

### V8：港股占位行修复（M-8⑤）

**mock 组合**：临时库预置占位行（今日 PE/PB 全指标 NULL）+ 真实财报行（2025-12-31 全指标有值）

| 断言 | 预期 |
|---|---|
| 全指标 NULL 占位行清理（0 残留）| 命中 |
| 最新真实财报行可读、含全部指标 | 命中 |
| PE/PB 合并到最新真实财报行 | 命中 |
| data_source='em_hk' + message '港股EM财报+腾讯估值: …' | 命中 |
| 收敛性：二次调用'同日跳过'（不每日触发回补）| 命中 |

### V9：来源标注（M-8⑥）

| 断言 | 预期 |
|---|---|
| data_source 迁移幂等（重复启动不报错）| 命中 |
| data_status message 前缀（abstract/降级/港股/PE/PB 仅更新四态）| 命中 |
| 前端动态表头（'来源：新浪关键指标(abstract)…'+'；估值：腾讯行情'）+ 行级 sup 标注 | 命中 |

### V10：评分纯净 + 零改动（M-8⑦）

| 断言 | 预期 |
|---|---|
| scoring_engine / config_weights / data_contract **零改动**（趋势不进评分）| 哈希比对 |
| 资金面区（019N）函数体未动 | 哈希/grep |
| app.py / data_adapter / analysis_engine / alert_engine 哈希不变 | 哈希比对 |
| `python -m pytest tests/` 343 passed | 回归 |

### V11：真实库保护

- QA 全程零真实网络、零生产库写入（验收前后 size/mtime 比对）
- 主库 daily_reports=387 / ratings_history=315 基线不变

---

## 四、红线遵守核查清单

| 红线 | 核查方法 |
|---|---|
| B24（不碰 generate_advice）| V1 + V10 |
| 范围（4 文件 + 区域隔离）| V1 + V10 |
| 超时红线（P3 自建闭包）| V1 + V5 |
| ocf 保留红线（P1）| V4 |
| 评分纯净红线（趋势不进评分）| V10 |
| 零代码约束 | V1（akshare 内置接口）+ V10 |
| 增量红线（TTL 门控不变）| V6 |
| 真实数据源红线 | V1（无估算公式）|
| ROE 口径迁移文档化 | V3（值域断言非精确值）|

---

## 五、QA 交付物要求

`reports/qa_accept_019P_fundamental_enhance_20260805.md`，含：
1. 逐用例 PASS/FAIL + 证据（实测数据、日志文本）
2. 红线核查清单
3. 新发现问题（如有）
4. 验收结论：**通过 / 不通过**
5. QA 签署

---

## 六、PM 备注

1. **核心风险点**：V7 趋势口径（R-5 ROE 环比陷阱——Q1 vs 年报混排必须不输出环比）、V3 abstract 结构适配（同名去重 R-1 + 最新在前列序 R-2）、V8 港股占位行（R-3 高优先级）。
2. **异常登记（监理已知情，来源未明）**：① 23:35 `backups/` 出现 drop_daily_reports_rebuild / delete_ratings_history_duplicates 备份文件（空表快照，主库 387/315 行与 019P 迁移备份一致，无数据损失——QA 验收前后须复验基线）；② 00:06 git reset（mixed 仅 index，工作树无损，各批次代码标记在位）。QA 若发现主库基线变化须立即报告 PM。
3. **QA 验收基线**：当前工作树（00:06 后状态）。QA 哈希比对以 PM 提供基线为准（见 V10）。
4. **运行实例提醒**：当前 5000 端口 app 为旧代码；019P 需重启（start.bat）后生效；存量 21 只 A 股毛利率经"完整性回补+abstract 重采"自动补全（零代码），港股占位行自动清理。
5. **开发自验脚本可对照**（`.dev_019P_work/selftest_019P.py` + `realrun_019P.py`，75 断言）但 QA 须独立构造，不采信其结论；**QA 验收严禁触发真实网络**（realrun 的联调模式不可复制）。
