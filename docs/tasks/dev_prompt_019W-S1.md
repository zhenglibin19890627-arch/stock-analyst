# 开发提示词 019W-S1：诊断报告失败分布数字修正

**返工单**：`docs/tasks/dev_tasks_20260810_019W-S1_report_numbers_fix.md`（权威依据，与本提示词冲突时以返工单为准）
**批次性质**：**纯文档修订**——只改两份报告中的 3 处数字，不重做诊断、不改生产代码、不产生任何东财请求。

---

## 一、你的任务

019W 诊断主体已被 PM 核验通过（红线全守、核心结论可信），但报告中「失败按小时分布」数字 **403 / 293 / 642** 对不上任何日志口径，PM 重数后无法复现，被打回修订。

你要做的是：**把这 3 处数字改为可复现的数字，并注明统计口径**。

## 二、修订位置（共 3 处，返工单第二节有原文定位）

1. `docs/reports/diag_019W_em_anti_crawl_20260810.md` 第一节「四类根因判定」表「频率级」行；
2. 同文档第二节 2.1 第 4 条；
3. `docs/reports/dev_selftest_019W_em_anti_crawl_20260810.md` 第十一节第 4 条。

## 三、正确数字（PM 已核验，你写入前必须自行复核一遍）

采用**口径 A**（与报告其余各处引用的「78 次」一致）：

| 时段 | 次数 |
|---|---|
| 08-09 11 时 | 23 |
| 08-10 16 时 | 19 |
| 08-10 17 时 | 36 |
| 两日合计 | **78** |

复核方法（UTF-8 逐行匹配，两个日志文件）：
- 文件：`logs\app.log.2026-08-09`、`logs\app.log`
- 匹配：含「akshare备用源失败」的行，按行首时间戳第 11-12 位取小时分组
- 参考口径 B（备查，含重试的全部 RemoteDisconnected 行）：258 / 190 / 412，合计 860——**不采用**，但可写入报告脚注说明口径差异（可选）

## 四、硬性要求

1. **只改上述 3 处**及其口径说明，其余内容、结论、建议一律不动；
2. 在数字首次出现处注明口径说明（统计对象 = 哪条日志文案、哪两个文件）与复核命令，保证任何人可复现；
3. 零生产改动（不碰 modules/*.py、app.py、config_*.json、requirements.txt）、零数据库写入、**零新增东财请求**；
4. 自测报告末尾补「修订记录」一节：修订位置、改前/改后数字、你的复核命令与复核输出原文。

## 五、交付物

| 交付物 | 路径 |
|---|---|
| 修订后的诊断报告 | `docs/reports/diag_019W_em_anti_crawl_20260810.md`（原地修订） |
| 修订后的自测报告（含修订记录） | `docs/reports/dev_selftest_019W_em_anti_crawl_20260810.md`（原地修订） |

## 六、移交

修订完成后回件，PM 按口径 A 重数复核：三处数字与日志一致、内部自洽（合计 78）、红线无违反，即收口。
