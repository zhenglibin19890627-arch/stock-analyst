# QA-TASKS-20260805-019N：EM 资金流 NaN 防护与假成功修正 — 独立验收任务书

> **签发人**：PM | **签发日期**：2026-08-05 | **状态**：待 QA 执行

---

## 角色定义（内嵌，无需额外窗口提示词）

### 你的角色：QA（独立验收）

**职责边界**：
- 独立验收 019N 开发成果，**不依赖开发自验报告结论**（自验报告 `reports/dev_selftest_019N_em_nan_fix_20260805.md` 仅作对照参考）
- 静态代码核查 + 受控实测（mock 场景），逐条用例出 PASS/FAIL + 证据
- **不修改任何功能代码**；mock 测试须在独立临时脚本中执行，验收结束前删除
- 交付物：`reports/qa_accept_019N_em_nan_fix_20260805.md`

### 独立性原则
- 各角色独立不兼职：PM 不兼架构、架构师不编码、开发不验收、QA 独立测试
- QA 自行构造 mock 测试，不复用开发临时脚本（`.dev_019N_work/selftest_019N.py` 仅可对照不可采信）

### 项目背景摘要
| 项 | 内容 |
|---|---|
| 项目路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格，命令行需引号） |
| 数据库路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst\stock_analyst.db` |
| Python 解释器 | `C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe` |
| 技术栈 | Python + Flask + SQLite + akshare + Jinja2 单页应用 |
| 任务书 | `docs/tasks/dev_tasks_20260805_019N_em_nan_fix.md`（v2 定稿，M-1~M-4 已并入） |
| 评审报告 | `docs/reviews/review_019N_em_nan_fix_20260805.md`（架构师 ⚠️ 有条件通过，M-1~M-4） |
| 自验报告 | `reports/dev_selftest_019N_em_nan_fix_20260805.md`（仅对照参考，不采信结论） |
| PM 核验结论 | PM 独立核验通过：`_safe_num` 13 用例独立单测 PASS（'nan'/'-'/''/None/±Inf→None；正常值保留）；三层转换/行跳过/saved_count 代码级确认；M-2 解析层确认；py_compile PASS；`pytest tests/test_data_collector.py` 82 passed（含新增 33）；范围外文件哈希与基线一致（app.py 变化=019L 预期改动） |

### 环境约束（硬性，违反将导致执行失败）
1. **项目在 IDE 工区外**：路径含空格，Write 工具直写会报错，须"工作区 Copy + SearchReplace 编辑 + Copy-Item 覆盖回项目目录"
2. **PowerShell 中文**：追加中文到文件用 `[System.IO.File]::ReadAllText + WriteAllText`（UTF-8），禁止 Add-Content/Out-File（乱码）
3. **PowerShell 内联 Python**：含 `*` 的 SQL 会被通配符解析破坏，须用 `chr(39)` 包裹字符串或写临时 .py 脚本；含 f-string 的 Python 代码也须用临时文件
4. **临时脚本清理**：mock 测试脚本须在验收结束后删除，不留存仓库
5. **真实数据库保护**：mock 测试须用临时库（%TEMP%），严禁向真实 `stock_analyst.db` 写入任何业务行；零真实网络请求

---

## 一、改动概述（2 文件）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `modules/data_collector.py` | ① `import math` ② `_safe_num/_safe_float_wan/_safe_float_pct` 模块级辅助函数（L1560-1591）③ M-2 解析层 `_fetch_capital_flow_em_individual` 13 字段改用 `_safe_num`（L1634-1646）④ Layer 1 六字段安全转换 + 全 None 行跳过 + saved_count 仅计 main 非 None + skipped 计数（L2019-2061）⑤ Layer 2 五字段同语义（L2083-2119）⑥ Layer 3 六字段同语义（L2145-2176）⑦ 成功消息含有效/跳过数 |
| 2 | `tests/test_data_collector.py` | 新增 TestSafeNum（21）/TestSafeFloatWan（7）/TestSafeFloatPct（6）单测（L179-283） |

**核心语义**：EM 三层字段 None/NaN/'nan'/'None'/'-'/±Inf → None（不写 NULL 占位）；六字段全 None → 跳过该行；saved_count 仅计 main 非 None；全 NaN 日 message 不以"东方财富"开头 → 防覆盖不锁 → 走 THS 顶替/估算降级。

---

## 二、关键代码位置（QA 必读）

| 位置 | 说明 |
|---|---|
| `modules/data_collector.py` L1560-1591 | `_safe_num` / `_safe_float_wan` / `_safe_float_pct`（本批次核心） |
| `modules/data_collector.py` L1634-1646 | M-2 解析层（'-' 炸批修复） |
| `modules/data_collector.py` L2019-2061 | Layer 1：安全转换 + 跳过 + 计数 |
| `modules/data_collector.py` L2083-2119 | Layer 2：同语义（五字段） |
| `modules/data_collector.py` L2145-2176 | Layer 3：同语义 |
| `modules/data_collector.py` L2430 附近 | 成功消息（有效/跳过行数） |
| `modules/data_collector.py` L1926-1967 | pre-check + 防覆盖（**本批次不修改**，仅验证行为） |

---

## 三、验收用例（按任务书 v2 + M-4，逐条 PASS/FAIL + 证据）

### V1：代码级核查

| 核查项 | 验证方法 |
|---|---|
| `_safe_num/_safe_float_wan/_safe_float_pct` 存在（模块级，`_parse_cn_amount` 附近）| Read L1560-1591 |
| EM 三层 **17 个字段表达式**全部使用安全函数（Layer1 六 / Layer2 五 / Layer3 六）| Read L2020-2025/L2084-2088/L2146-2151 |
| M-2 解析层 13 字段用 `_safe_num` | Read L1634-1646 |
| **无 `or 0` 残留于 EM 三层**（L2006-2200 区间）| grep |
| 六字段全 None → `skipped += 1; continue`（Layer 2 为五字段）| Read |
| saved_count 仅当 main 非 None 才 +1（三层）| Read L2053-2054/L2117-2119/L2166-2167 |
| `import math`（标准库）| Read L18-22 |
| 防覆盖查询逻辑未修改（本批次不碰）| Read L1926-1967 |

### V2：编译验证

```
python -m py_compile modules/data_collector.py
```
→ 无错误

### V3：全 NaN 场景（防假成功 + THS 顶替衔接）

**mock 组合**：EM 三层全返回 'nan' 字符串/NaN（Layer1 108 行 'nan'、Layer2 'nan'、Layer3 空）+ 临时库预置当日行 `ths_net_inflow=-11800`

| 断言 | 预期 |
|---|---|
| 返回 ('fallback', ...)（THS 顶替触发）| 命中 |
| 顶替写 main=-11800、is_estimated=0、capital_source='ths_total' | 命中 |
| **无 NULL main 行**（全 NaN 行被跳过，不写占位）| 命中 |
| data_status.status='fallback' 且 message **非"东方财富"开头**（防覆盖不锁）| 命中 |
| 019K 衔接：顶替行可被 EM 恢复回补（capital_source 归位 NULL）| 命中 |

### V4：全 NaN + ths 无值（估算兜底）

**mock 组合**：EM 三层全 NaN + ths_net_inflow=NULL + 估算源 mock 返回

| 断言 | 预期 |
|---|---|
| 返回 ('estimated', ...) | 命中 |
| 估算行 is_estimated=1、capital_source=NULL | 命中 |
| message 非"东方财富"开头 | 命中 |
| Layer2 'nan' 字符串不写 NULL 行（当日仅估算 1 行）| 命中 |

### V5：部分 NaN（当日 NaN + 历史正常）——M-4 重点

**mock 组合**：Layer1 108 行历史正常 + 当日（第 109 行）'nan' 字符串

| 断言 | 预期 |
|---|---|
| 返回 success | 命中 |
| message 精确含"写入 108 天有效数据（跳过 1 天异常数据）" | 命中 |
| 仅 108 行写入（NaN 当日无占位行）| 命中 |
| 无全 NULL 行 | 命中 |
| 值零回归（历史值逐行与旧公式一致）| 命中 |
| message 以"东方财富"开头（有效日防覆盖锁等价，A-4 场景 5 文档化）| 命中 |

### V6：正常路径零回归（M-4-f）

**mock 组合**：EM 全正常 120 天

| 断言 | 预期 |
|---|---|
| 返回 success、"写入 120 天有效数据（跳过 0 天异常数据）" | 命中 |
| 120 行数值与旧公式 `round(float(x)/1e4,2)` **逐行完全一致** | 命中 |

### V7：M-2 解析层 '-' 修复（R-1）

**mock 组合**：EM 历史 120 天中 1 行含 '-' 占位

| 断言 | 预期 |
|---|---|
| **不炸批**（120 天不丢，不降级 Layer 2）| 命中 |
| 该 '-' 行 main=None、其余子字段保留（REPLACE 未清空）| 命中 |

### V8：存量自愈（A-5 方案 A 验证）

**mock 组合**：临时库预置 10 行历史 main NULL + margin 有值 → mock EM 正常全量 120 天

| 断言 | 预期 |
|---|---|
| REPLACE 后 **0 行 NULL 残留**（历史 NULL 被真实值替换）| 命中 |

### V9：防覆盖三态（M-4-c）

| 场景 | 预期 |
|---|---|
| 有效日（message"东方财富"开头）→ 同日二次采集被拦截 | 命中 |
| 全 NaN 估算日 → 不锁（同日二次采集仍走估算）| 命中 |
| fallback 日 → 不锁（同日二次采集仍走顶替）| 命中 |

### V10：单元测试与回归

```
python -m pytest tests/test_data_collector.py -q   → 82 passed（含新增 33）
python -m pytest tests/ -q                          → 343 passed
```

### V11：零改动确认

| 文件 | 哈希（019K 基线，PM 已复算）|
|---|---|
| modules/data_collector.py | `B2CACC622E2A9ABA`（**本批次改动**）|
| tests/test_data_collector.py | （本批次改动，QA 实测记录）|
| modules/advisor.py | `CA1857B0F6452B20`（不变）|
| modules/analysis_engine.py | `DF71A6FE4FD7685D`（不变）|
| modules/alert_engine.py | `053F0CDB4DA62385`（不变）|
| modules/scoring_engine.py | `DD9DBFBBD005B35D`（不变）|
| modules/data_adapter.py | `0792E5006D7DCED9`（不变）|
| app.py | `5C73F6EA320D838D`（**019L 预期改动**，非本批次）|
| config.py | `F6CE1F84B8DDACDA`（不变）|
| requirements.txt | `DBE076A7458C5788`（不变）|
| templates/index.html | `79F3F330F7148D49`（不变）|
| database/db_manager.py | `2D222BE42F298258`（不变）|

---

## 四、红线遵守核查清单

| 红线 | 核查方法 |
|---|---|
| 1. 功能红线：NaN 不假成功 | V3/V5（不写 NULL 占位、saved_count 修正、message 语义）|
| 2. 范围红线：仅 data_collector.py + tests | V11 |
| 3. 语义红线：正常路径零变化 | V6（逐行一致）|
| 4. 零代码约束：无新依赖 | V1（math 标准库）+ V11 |
| 5. 评分纯净红线（019E）| V4（估算 is_estimated=1 行为不变）|
| 6. 降级链路红线（019K）| V3（顶替）/V4（估算）顺序不变 |
| 7. 超时红线 | V1（零新增网络调用）|
| 8. 存量红线 | V8（方案 A 零操作）|

---

## 五、QA 交付物要求

`reports/qa_accept_019N_em_nan_fix_20260805.md`，含：
1. 逐用例 PASS/FAIL + 证据（实测数据、日志文本）
2. 红线核查清单
3. 新发现问题（如有）
4. 验收结论：**通过 / 不通过**
5. QA 签署

---

## 六、PM 备注

1. **核心风险点 = 假成功闭环（V3/V5）**：全 NaN 日 message 不以"东方财富"开头是防覆盖不锁的关键；有效日 message 以"东方财富"开头（A-4 等价性）。QA 必须验证 message 语义双态。
2. **M-2 解析层（V7）**：'-' 炸批是架构师新发现（R-1），比 NaN 更凶（整批 120 天丢失）——V7 是验收重点。
3. **019K 衔接（V3 末条）**：顶替→EM 恢复回补闭环（capital_source 归位 NULL）——019K 已验收，本批次回归确认。
4. **app.py 哈希变化 = 019L 预期改动**（并行批次），非本批次引入——QA 不应判为违规。
5. **运行实例提醒**：当前运行中 app.py 为旧代码，019N/019L 修复均需重启后生效；QA 基于 mock 验收不依赖运行实例。
6. **开发自验脚本可对照**（`.dev_019N_work/selftest_019N.py`，48 断言）但 QA 须独立构造，不采信其结论。
