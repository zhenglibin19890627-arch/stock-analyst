# 开发提示词 019W：东方财富反爬阻断诊断

**任务书**：`docs/tasks/dev_tasks_20260810_019W_em_anti_crawl_diagnosis.md`（权威依据，与本提示词冲突时以任务书为准）
**批次性质**：**只诊断、不落地**——本批次不改任何生产代码、不写数据库，产出诊断报告与策略建议供监理裁定。

---

## 一、你的任务

2026-08-09 / 08-10 两日，项目对东方财富域名的资金流请求持续全失败（直连三层 + akshare 备用源，共 78 次 akshare 失败**100% 为 RemoteDisconnected**，连接层被掐断；无任何 KeyError/JSONDecodeError 解析错误）。同期新浪系接口全部正常（资金流 sina_main 正常入库、HSI 指数降级新浪成功），证明拦截**特定于东财域名**。

你要做的是：**定位拦截根因 + 验证可行的恢复策略 + 产出诊断报告**。具体诊断项见任务书第三节（T1 基线复现 / T2 策略矩阵 / T3 本机网络环境只读排查 / T4 akshare 层对照），此处不复述细节，开工前务必通读任务书全文。

## 二、项目环境

- 项目路径：`C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（**含空格，命令行必须加引号**）
- Python：`C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe`（3.12.9），akshare 1.18.53（本批次**不升级**，升级另行立项）
- Flask 服务正在运行（端口 5000），**不得重启、不得干扰**
- 日志：`logs\app.log`（当日）；数据库：`stock_analyst.db`（**本批次禁止写**，如需对照用只读 URI：`sqlite3.connect("file:...stock_analyst.db?mode=ro", uri=True)`）

## 三、关键代码位置（只读参考，不许改）

| 位置 | 内容 |
|---|---|
| `modules/data_collector.py` L55-83 | `_UA_POOL` UA 池与随机选取 |
| `modules/data_collector.py` L187-250 | `_http_get_em()`：东财专用请求（多轮重试 + UA 池 + 随机延迟 1.5~3.5s + 直连/代理回退），019B 批次产物 |
| `modules/data_collector.py` L1903 / L1972 | fflow 两个端点：`push2his.eastmoney.com/api/qt/stock/fflow/daykline/get`、`push2.eastmoney.com/...` |
| `modules/data_collector.py` L2564-2627 | akshare 备用源调用点（`ak.stock_individual_fund_flow`），失败日志文案「akshare备用源失败」 |
| `logs/app.log.2026-08-09`、`logs/app.log` | 故障原始证据（搜「东方财富三层全失败」「akshare备用源失败」） |

## 四、红线（违反即批次作废）

1. **零生产改动**：不修改 `modules/*.py`、`app.py`、`config*.json`、`*.bat`、`requirements.txt`、前端模板；结束前用 `git status` 自证全净（新增诊断脚本除外）。
2. **零数据库写入**：任何脚本不得对 `stock_analyst.db` 做写操作。
3. **请求量封顶**：对东财域名测试请求全批次总量 **≤150 次**，全部串行，相邻请求间隔 **≥2 秒**；若 T1 复现阶段即触发长时间全断，立即停止并如实上报，不硬试。
4. **时间窗口**：避开每日 16:00-17:00（16:10 采集批次 + 16:30 定时任务）与周日 20:00 周批窗口。
5. **代理边界**：仅允许进程内代理设置（`requests` 的 proxies 参数 / 临时 `os.environ` 且脚本退出前恢复）；**禁止修改 Windows 系统代理注册表、禁止 pip 安装任何新依赖**。
6. **脚本落位**：诊断脚本一律放 `scripts/`，命名 `diag_019w_*.py`；批次结束清理或显式登记保留。
7. **禁止行为**：不得点击前端个股详情（B11 效应会写库）、不得手动触发补采/采集、不得运行 app.py。

## 五、交付物（三件）

1. **诊断脚本**：`scripts/diag_019w_*.py`（含策略矩阵执行逻辑，脚本自身打印结构化结果）；
2. **自测报告（强制，独立成文）**：`docs/reports/dev_selftest_019W_em_anti_crawl_20260810.md`——必须含每个策略的**原始测试记录**：请求次数 / 成功数 / 错误类型 / 代表性响应或异常片段原文。**只给结论不给原始记录 = 验收不通过**；
3. **诊断报告**：`docs/reports/diag_019W_em_anti_crawl_20260810.md`——根因分类结论（IP 级 / 请求头指纹级 / 频率级 / 本机网络环境，四者定主因）+ 证据链 + 各策略成功率数据表 + 明确建议（a 请求策略修复含具体参数 / b 网络环境修复含具体操作 / c 建议等待自然恢复含监测方案 / d 其他）。**建议只供监理裁定，本批次不得实施任何修复。**

## 六、如实性要求

若所有策略验证后东财仍不可达，**如实写"现有手段不可恢复"** 就是合格交付，R-2 风险条已为此兜底。严禁为交差编造"有效策略"或挑选性展示数据。

## 七、自验清单（提交前逐项打勾）

- [ ] `git status` 仅新增 scripts/diag_019w_*.py 与两份报告，生产文件零改动
- [ ] 东财测试总请求数 ≤150，脚本内有计数器可复核
- [ ] 自测报告含全部策略的原始记录（次数/成功/错误类型/响应片段）
- [ ] 诊断报告给出根因分类 + 证据 + 建议三要素
- [ ] 未触碰系统代理注册表、未安装新依赖、未写数据库

完成后将三份交付物路径回报 PM，由 PM 移交 QA 验收。
