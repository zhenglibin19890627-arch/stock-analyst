# QA-TASKS-20260805-019I：THS 批量预取超时保护（报告生成卡住修复）— 独立验收任务书

> **签发人**：PM | **签发日期**：2026-08-05 | **状态**：待 QA 执行

---

## 角色定义（内嵌，无需额外窗口提示词）

### 你的角色：QA（独立验收）

**职责边界**：
- 独立验收 019I 开发成果，**不依赖开发自验报告结论**（自验报告 `reports/dev_selftest_019I_ths_batch_timeout_20260805.md` 仅作对照参考）
- 静态代码核查 + 受控实测（mock 超时场景），逐条用例出 PASS/FAIL + 证据
- **不修改任何功能代码**；mock 测试须在独立临时脚本中执行，验收结束前删除
- 交付物：`reports/qa_accept_019I_ths_batch_timeout_20260805.md`

### 独立性原则
- 各角色独立不兼职：PM 不兼架构、架构师不编码、开发不验收、QA 独立测试
- QA 自行构造 mock 测试，不复用开发临时脚本（已删除）

### 项目背景摘要
| 项 | 内容 |
|---|---|
| 项目路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格，命令行需引号） |
| 数据库路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst\stock_analyst.db` |
| Python 解释器 | `C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe` |
| 技术栈 | Python + Flask + SQLite + akshare + Jinja2 单页应用 |
| 任务书 | `docs/tasks/dev_tasks_20260805_019I_ths_batch_timeout.md`（v2 定稿） |
| 评审报告 | `docs/reviews/review_019I_ths_batch_timeout_20260805.md`（架构师独立评审，有条件通过） |
| 自验报告 | `reports/dev_selftest_019I_ths_batch_timeout_20260805.md`（仅对照参考，不采信结论） |
| PM 核验结论 | PM 独立核验通过（10/10 项），代码级无阻断问题 |

### 环境约束（硬性，违反将导致执行失败）
1. **项目在 IDE 工区外**：路径含空格，Write 工具直写会报错，须"工作区 Copy + SearchReplace 编辑 + Copy-Item 覆盖回项目目录"
2. **PowerShell 中文**：追加中文到文件用 `[System.IO.File]::ReadAllText + WriteAllText`（UTF-8），禁止 Add-Content/Out-File（乱码）
3. **PowerShell 内联 Python**：含 `*` 的 SQL 会被通配符解析破坏，须用 `chr(39)` 包裹字符串或写临时 .py 脚本；含 f-string 的 Python 代码也须用临时文件
4. **临时脚本清理**：mock 测试脚本须在验收结束后删除，不留存仓库

---

## 一、改动概述

**唯一改动文件**：`modules/data_collector.py`

| 改动 | 位置 | 内容 |
|---|---|---|
| 常量 | L1099 | `_THS_REQUEST_TIMEOUT = 60`（THS 常量块，L1098 与 L1106 之间） |
| 核心逻辑 | L1145-1180 | `_fetch_capital_flow_ths_batch()` 内新增 `_call_with_timeout(fn, label)` daemon 线程包装函数，包装主接口/重试/备选 3 处调用 |

**实现要点**：
- **M-1 daemon 线程方案甲**（架构师推荐）：`threading.Thread(daemon=True) + t.join(timeout=60)`，超时返回 `(None, True)`
- **M-2 超时跳过重试**（开发额外实现，架构师建议项）：主接口超时（hang）时跳过 5 秒等待 + 重试，直接尝试备选接口
- **禁止 `with ThreadPoolExecutor`**（红线 6）：架构师实验实证 with 块退出时 `shutdown(wait=True)` join 挂死线程，修复无效

---

## 二、关键代码位置（QA 必读）

| 位置 | 说明 |
|---|---|
| `modules/data_collector.py` L1099 | `_THS_REQUEST_TIMEOUT = 60` 常量定义 |
| `modules/data_collector.py` L1121-1192 | `_fetch_capital_flow_ths_batch()` 全函数 |
| `modules/data_collector.py` L1145-1165 | `_call_with_timeout(fn, label)` daemon 线程包装函数定义 |
| `modules/data_collector.py` L1167-1180 | 主接口/重试/备选 3 处调用（经 `_call_with_timeout` 包装） |
| `modules/data_collector.py` L1195-1206 | `_try_ths_primary()` — **函数体应零改动**（签名+实现不变） |
| `modules/data_collector.py` L1209-1235 | `_try_ths_rank_backup()` — **函数体应零改动** |

---

## 三、验收用例（7 条，逐条 PASS/FAIL + 证据）

### V1：代码级核查

| 核查项 | 验证方法 |
|---|---|
| `_THS_REQUEST_TIMEOUT = 60` 存在于 THS 常量块（L1099） | Read L1095-1100 |
| 主/重试/备选 3 处调用均经 `_call_with_timeout` 包装 | Read L1167-1180 |
| 函数体内**无 `with ThreadPoolExecutor` 语句**（红线 6） | grep `with.*ThreadPool` 全文件，仅注释命中（L1146）为合法 |
| `_try_ths_primary` / `_try_ths_rank_backup` 函数体零改动 | Read 两函数，确认仍为 `ak.stock_fund_flow_individual()` / `ak.stock_individual_fund_flow_rank(indicator='今日')` 裸调用 |
| `_fetch_capital_flow_ths_batch()` 签名无参不变 | Read def 行 |

### V2：编译验证

```
python -m py_compile modules/data_collector.py
```
→ 无错误

### V3：超时保护验证（M-4 强化——含显式耗时断言）

**推荐 mock 组合**（开发自验报告中验证过的配置）：
- mock `_try_ths_primary` = `lambda: time.sleep(120)`（模拟挂死）
- mock `_try_ths_rank_backup` = `lambda: None`（快速失败）

| 断言 | 预期 |
|---|---|
| `_fetch_capital_flow_ths_batch` 返回 None | None |
| **耗时 < 65 秒**（M-4 显式断言——可捕获 with 块缺陷） | elapsed < 65s |
| 日志含 `主接口 超时(60s)，跳过` | 命中 |
| 日志**不含** `主接口(重试)`（M-2 生效：超时跳过重试） | 未命中 |
| 超时后 `_THS_CAPITAL_CACHE['data']` 仍为 None（红线 7） | None |
| 全失败后 `_THS_CONSECUTIVE_FAIL_COUNT == 1` | 1 |

> **注意**：mock 需同时 mock 主接口和备选接口。若仅 mock 主接口不 mock 备选，备选会真实请求网络导致耗时不可控。mock 须通过 monkeypatch `modules.data_collector._try_ths_primary` / `_try_ths_rank_backup` 实现。

### V4：降级链路完整性验证

**mock 组合**：主/备选均快速失败（返回 None，非超时，保留 FIX-B 重试路径）

| 断言 | 预期 |
|---|---|
| 日志按序出现：`主接口失败，5秒后重试1次` → `重试仍失败，尝试备选接口` → `全部接口失败，连续失败计数=1` | 全部按序命中 |
| 返回 None | None |
| `_THS_CAPITAL_CACHE['data']` 为 None | None |
| `_THS_CONSECUTIVE_FAIL_COUNT == 1` | 1 |

### V5：正常路径回归验证

**mock 组合**：主接口正常返回 DataFrame（无延迟）

| 断言 | 预期 |
|---|---|
| 返回同一 DataFrame 对象（透明传递） | 同一对象 |
| `_THS_CONSECUTIVE_FAIL_COUNT` 重置为 0 | 0 |
| `_THS_CAPITAL_CACHE['data']` 写入（缓存正常） | 非 None |
| 无额外开销（耗时 < 5s） | elapsed < 5s |

### V6：进程退出验证（M-4，方案甲自动满足）

**测试方法**：子进程启动挂死 daemon 线程（`sleep(3600)`）后主线程结束，观察进程退出时间。

| 断言 | 预期 |
|---|---|
| 进程在 10 秒内退出 | returncode=0，wall_time < 10s |

> daemon 线程不参与解释器退出 join，进程退出不被挂死线程阻塞（R-1 消除）。

### V7：零改动确认

| 断言 | 验证方法 |
|---|---|
| `daily_report.py` 文件哈希不变 | SHA256 比对（开发自验报告第三节提供快照：`A96C51CD5049B679`） |
| `app.py` 文件哈希不变 | `8F8373C029E76390` |
| `config.py` 文件哈希不变 | `F6CE1F84B8DDACDA` |
| `requirements.txt` 文件哈希不变 | `DBE076A7458C5788` |
| `templates/index.html` 文件哈希不变 | `769ECE1C80627DB7` |
| `_try_ths_primary` / `_try_ths_rank_backup` 函数体 diff 为空 | Read 两函数确认 |

---

## 四、红线遵守核查清单

| 红线 | 核查方法 |
|---|---|
| 1. 功能红线：THS 不响应时不得永久阻塞 | V3 耗时断言 < 65s |
| 2. 范围红线：仅 data_collector.py | V7 零改动确认 |
| 3. 签名红线：三函数签名不变 | V1 代码级核查 |
| 4. 零代码约束：无新依赖、不碰 config.py | V7 零改动 + grep `import` 无新包 |
| 5. 降级安全红线：超时返回 None | V3/V4 返回值断言 |
| 6. 挂死线程红线：禁止 with ThreadPoolExecutor | V1 grep 检查 |
| 7. 缓存红线：超时不写缓存 | V3/V4 缓存断言 |
| 8. 先例红线：不复制 daily_report L533 模式 | V1 确认为 daemon 线程方案 |

---

## 五、QA 交付物要求

`reports/qa_accept_019I_ths_batch_timeout_20260805.md`，含：
1. 逐用例 PASS/FAIL + 证据（实测数据、日志截图/文本）
2. 红线核查清单
3. 新发现问题（如有）
4. 验收结论：**通过 / 不通过**
5. QA 签署

---

## 六、PM 备注

1. **本批次核心风险点是 M-1 daemon 线程实现**：架构师实验实证 `with ThreadPoolExecutor` 写法无法修复缺陷（with 退出时 join 挂死线程），PM 独立复现确认。QA 验收 V3 的 65 秒耗时断言是关键——若开发者误用 with 块，该断言会失败（耗时 = mock 挂死时长）。
2. **M-2 是开发额外实现的建议项**：`_call_with_timeout` 返回 `(result, timed_out)` 二元组而非单值，超时标志用于跳过主接口重试。PM 核验认为此偏差不影响正确性，QA 应验证 M-2 逻辑：超时跳过重试 vs 普通失败仍重试。
3. **运行中 PID 46172 仍为旧代码**：QA 验收不依赖运行实例（使用 mock 测试），但验收报告中应提醒用户重启 app.py 后修复才生效。
4. **临时脚本清理红线**：QA 构造的 mock 测试脚本须在验收结束后删除，不留存仓库。
