# QA 验收任务书：009 价格建议增强

| 项目 | 内容 |
|---|---|
| **任务编号** | QA-009-20260729 |
| **推荐模型** | glm5.2 |
| **窗口类型** | Quests 独立窗口 |
| **PM 验收报告** | reports/pm_accept_009_price_enhance_20260729.md |
| **开发自验报告** | reports/dev_selftest_009_price_enhance_20260729.md |

---

## 验收测试用例（8项）

### TC1：中国中免(21) — 有持仓已破止损（核心）

验证命令：
```
cd "C:\Users\zlb19\Desktop\Qoder cn\stock_analyst"
C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe -c "from modules.advisor import generate_advice; from modules.price_advisor import generate_price_advice; a=generate_advice(21); p=generate_price_advice(21,a); import json; print(json.dumps(p,ensure_ascii=False,indent=2))"
```

预期：
- state = "S4"
- action_suggestion 含"止损"，**不含"加仓"**
- take_profit < 72.94（动态化）
- grid 含减仓位，**不含补仓位**（S4跳过补仓）
- capital_signal 有值
- trade_analysis.available = True

### TC2：茅台(18) — 无持仓

预期：
- has_position = False
- grid 3档全buy
- action_suggestion 有值（"逢低买入"/"按计划买入"/"等待回调"）
- capital_signal 有值

### TC3：美团(6) — position_advice 覆盖

验证命令：
```
C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe -c "from app import app; c=app.test_client(); r=c.post('/api/stocks/6/advise'); d=r.get_json(); print('position_advice:', d.get('position_advice')); print('price_advice.action:', d.get('price_advice',{}).get('action_suggestion'))"
```

预期：
- position_advice 被覆盖为 price_advice 的建议
- 不含"加仓"（因当前价>=止盈价）

### TC4：汤臣倍健(7) — 有持仓浮亏中

预期：
- state 在 S3（浮亏中）或 S4（已破止损）
- action_suggestion 与 state 一致

### TC5：数据不足场景

构造 latest_close=None 的 advice_result，预期 available=False。

### TC6：交易流水分析

验证 stock_id=4（恒瑞医药，14条交易记录）或 stock_id=21（中国中免，9条）：
- trade_analysis.available = True
- 含 rhythm/cost_trend/timing
- summary 有一句话摘要

验证一只交易记录<2笔的股票：
- trade_analysis.available = False

### TC7：API端点验证

```
C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe -c "from app import app; c=app.test_client(); r=c.post('/api/stocks/21/advise'); d=r.get_json(); print('position_advice:', d.get('position_advice')); print('has_grid:', 'grid' in d.get('price_advice',{})); print('has_capital:', 'capital_signal' in d.get('price_advice',{}))"
```

预期：position_advice 被覆盖，price_advice 含 grid 和 capital_signal。

### TC8：一键启动+零代码约束

```
C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe app.py
```

预期：无报错，requirements.txt 仍8包无新增。

---

## 回归测试

| # | 回归项 | 验证方式 |
|---|---|---|
| R1 | 评级功能正常 | /advise 返回 rating/total_score |
| R2 | 日报生成正常 | GET /api/daily-report/latest |
| R3 | 回测报告正常 | GET /api/price-backtest/report |
| R4 | 自选股列表正常 | GET /api/stocks |

---

## 交付物

reports/qa_accept_009_price_enhance_20260729.md
