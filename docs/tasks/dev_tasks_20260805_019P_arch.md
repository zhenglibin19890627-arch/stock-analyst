# DEV-TASKS-20260805-019P-ARCH：019P 基本面数据完善（毛利率补全+来源标注+趋势分析）— 架构方案评审任务书

> **签发人**：PM  | **签发日期**：2026-08-05 | **状态**：待架构师执行

---

## 角色定义（内嵌，无需额外窗口提示词）

### 你的角色：架构师

**职责边界**：
- 复核 PM 签发的 019P 开发任务书（`docs/tasks/dev_tasks_20260805_019P_fundamental_enhance.md`）
- 对每个决策点（A-1~A-8）给出明确裁定 + 理由
- **不编码、不验收、不写功能代码**
- 交付物：`docs/reviews/review_019P_fundamental_enhance_20260805.md`

### 独立性原则
- 各角色独立不兼职：PM 不兼架构、架构师不编码、开发不验收、QA 独立测试
- 架构师仅做方案评审，不执行任何代码修改
- PM 产出的任务书仅供参考，架构师须独立 Read 代码核验，不采信 PM 结论

### 项目背景摘要
| 项 | 内容 |
|---|---|
| 项目路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格） |
| 技术栈 | Python + Flask + SQLite + akshare + Jinja2 单页应用 |
| 最高约束 | **零代码用户可独立运行**：无新 pip 依赖（当前 9 包） |
| 前序批次 | 011（TTL 增量）/ B10（字段补全）/ 019E（评分纯净）/ 019K（来源标注先例）/ 019L/019N（并行批次） |

---

## 执行信息（PM 标注）

| 项 | 内容 |
|---|---|
| 任务类型 | 架构方案评审（只读不改，不写功能代码） |
| 交付物 | `docs/reviews/review_019P_fundamental_enhance_20260805.md` |

---

## 一、需求背景

### 1.1 问题（监理指示 + PM 核查实证）

1. **毛利率全员为空**（29 只 gross_margin 全 NULL）——现接口 `stock_financial_analysis_indicator` 毛利率列本身 NaN（PM 实测）
2. **来源未标注**——data_status 仅"基本面数据采集成功"，无数据源信息
3. **缺趋势维度**——`_build_fundamental_factors` 只用最新一期（L1068-1104），无改善/恶化判断
4. 港股基本面字段不全（EM 源限制）；次新股（摩尔/沐曦）无数据
5. 监理要求：不重复获取（TTL 已有 ✅）、标注来源

### 1.2 核心实证发现（PM 独立实验，请独立复核）

**`ak.stock_financial_abstract('600276')`（新浪关键指标）**：
- **毛利率有值**（2026-03-31: 86.60），现接口 NaN——换接口即解决
- 全历史（1997~2026，100+ 报告期）——趋势/同比数据源
- 结构：行=指标、列=报告期（`20260331` 格式）——与现接口（行=报告期）不同，需转置

### 1.3 关键代码位置（评审必读，请独立 Read 核验）

| 位置 | 说明 |
|---|---|
| `modules/data_collector.py` L542-545 | `_fetch_a_fundamental_sina` — 现接口（毛利率 NaN） |
| `modules/data_collector.py` L548-735 | `fetch_a_fundamental` — TTL 门控（L556-605）+ 财报写入（L610-675，取最近 4 期）+ PE/PB（L677-716） |
| `modules/data_collector.py` L738-812 | `fetch_fundamental_detail` / `_apply_fundamental_detail` — B10 仅填 NULL 字段先例 |
| `modules/data_collector.py` L914-1000 | `fetch_hk_fundamental` — 港股 EM 源 + 14 天 TTL |
| `modules/advisor.py` L1068-1104 | `_build_fundamental_factors` — 单期因子（本批次趋势改造点，B24 红线外） |
| `modules/advisor.py` L196-220 | highlights（评级理由）构建 — 趋势文案输出点候选 |
| `modules/advisor.py` L380-414 | risks（风险提示）构建 — 恶化提示输出点候选 |
| `modules/data_collector.py` L1153-1165 | 019I `_call_with_timeout` — 新接口超时保护复用 |
| `database/db_manager.py` L950-965 | `_safe_add_columns` 迁移列表 — data_source 列（如需） |
| `templates/index.html` | 基本面展示区域（来源标注，需定位） |

---

## 二、评审决策点（请逐项裁定）

### A-1：毛利率补全方案选型（核心）

PM 草案：方案 A 换用 `stock_financial_abstract`；方案 B 保留现接口 + abstract 仅补 NULL（B10 模式）。

**架构师请核验**：
- 独立实测 `stock_financial_abstract` 字段（毛利率/净利率/营收增长/ROE/负债率 行名与单位）
- 评估转置适配成本（行=指标、列=报告期 vs 现结构）
- 裁定：A / B / 混合；与现接口字段重合部分（净利率等）以谁为准
- 数据完整性：abstract 是否含现接口全部字段（如 ocf_to_profit/流动比率/速动比率）——若缺，需混合方案

**裁定**：方案 A / B / 混合 + 理由

### A-2：趋势分析设计与评分影响（核心）

PM 草案：趋势因子仅展示（highlights/risks），不进评分。

**架构师请核验**：
- Read advisor L1068-1104 与 L196-220/L380-414，确认趋势文案接入点
- 裁定：趋势是否进 v5 评分（基本面子项）？进/不进的理由（评分稳定性、季度性跳变、019E 评分纯净精神）
- 环比/同比口径：报告期长度不一致（Q1 vs 年报）如何处理（如环比仅同口径比较）
- 因子文案粒度：单指标（ROE 环比）vs 汇总（基本面整体改善/恶化）

**裁定**：仅展示 / 进评分 + 口径方案 + 理由

### A-3：来源标注实现（data_status + 展示层）

PM 草案：data_status message 加来源前缀；前端基本面区域标注。

**架构师请核验**：
- 是否需 raw_fundamental 加 `data_source` 列（走 _safe_add_columns）还是仅 message 标注（可追溯性权衡）
- 前端标注位置与形式（019K 资金面标注先例：表头动态文案 + 行内 sup）
- 多源混合（财报=新浪 abstract + 估值=腾讯）的标注粒度

**裁定**：列 / 仅 message + 标注形式 + 理由

### A-4：港股备用源

**背景**：港股源 = EM（当日挂停 + 字段不全）；akshare 港股财务接口多为 EM 源。

**架构师请核验**：独立评估 akshare 港股财务接口清单（`stock_financial_hk_report_em`/`stock_hk_financial_indicator_em`/新浪港股等），确认是否存在非 EM 独立源；若不存在，裁定维持现状登记。

**裁定**：纳入本批次 / 维持登记 + 理由

### A-5：官方报告数据源

监理提到"通过官方的报告来完善数据"。akshare `stock_financial_report_sina`（三大报表：资产负债表/利润表/现金流量表）。

**架构师请核验**：
- 评估三大报表可计算的指标（毛利率=毛利/营收，从利润表精确计算）
- 与关键指标接口（abstract）的互补性/重复性——是否值得纳入（数据成本 vs 收益）
- 裁定：本批次纳入 / 登记后续候选

**裁定**：纳入 / 登记 + 理由

### A-6：存量数据回补

**背景**：80 天 TTL 意味着存量股票接入新接口后 80 天内不会重新采集——毛利率存量仍为空。

**架构师请核验**：
- 是否需要存量回补（如一次性 force_full 全量刷新；或启动时检测 gross_margin 全 NULL 触发重采）
- 回补范围（29 只 / 仅缺失股票）与触发方式（自动 vs 手动）
- 注意零代码约束（用户不手动操作优先）

**裁定**：自动回补 / 手动 / 不处理（自然过期）+ 理由

### A-7：并行批次文件隔离

**背景**：019L 动 app.py；019N 动 data_collector.py 资金面区域；019P 动 data_collector.py 基本面区域 + advisor.py。

**架构师请核验**：
- 019N 与 019P 在 data_collector.py 的行号区域是否明确隔离（019N: 资金面 L1897-2300；019P: 基本面 L542-1000）
- 是否需锁定行号区间防开发冲突；QA 验收如何各自断言

**裁定**：隔离确认 / 需调整 + 理由

### A-8：范围与红线确认

任务书 v1 红线：B24（不碰 generate_advice）、范围（3 文件 + 区域隔离）、零代码、TTL 不破坏（011）、评分纯净（019E）、真实数据源、超时保护（019I/019J）。

**架构师请核验**：
- 红线是否完备；遗漏风险（如 abstract 接口自身超时/异常时的降级行为；港股字段不全时的 NULL 处理）
- 验收标准是否充分（转置适配 mock、TTL 回归、区域隔离断言）

**裁定**：完备 / 需补充 + 详情

---

## 三、交付物要求

`docs/reviews/review_019P_fundamental_enhance_20260805.md`，含：

1. **逐决策点裁定**（A-1 ~ A-8，每项采纳/修改/否决 + 理由）
2. **独立核验的代码证据**（关键结论须附 Read 到的代码行号和内容；A-1 须附 abstract 接口独立实测）
3. **新发现的风险项**（R-x 编号，如有）
4. **评审结论**（通过 / 有条件通过 / 不通过）
5. **若裁定需修订任务书**，明确列出修订项（M-x 编号），PM 将据此修订任务书后交付开发

---

## 四、PM 备注

1. **本批次 PM 未越权评审**：PM 仅完成数据核查与接口实证（abstract 毛利率有值、现接口 NaN、TTL 已有），未自行产出 review 文档。架构师请独立复核 A-1 的核心实证（abstract 字段实测）。
2. **监理原始诉求**：① 要有恶化/改善趋势判断；② 完善数据（东财失败时的其他接口/官方报告）；③ 已有数据不重复获取（TTL 已有，仅验收）；④ 标注来源。
3. **A-2 是监理最关心的点**（"就一条不够"）——趋势分析是本批次核心价值，请重点评审口径与输出位置。
4. **A-6 存量回补**：监理未明确要求回补存量，但"完善数据"隐含存量补全诉求——请裁定是否需要一次性回补机制。
5. **零代码用户视角**：所有功能应"启动即用"——新接口接入、存量回补（若需）都不应要求用户手动操作。
