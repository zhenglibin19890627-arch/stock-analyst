# QA 验收任务书：评分引擎天花板优化（DEV-TASKS-20260727-002）

**编号**：QA-TASKS-20260727-002
**关联开发任务**：DEV-TASKS-20260727-002-v2
**验收日期**：2026-07-27
**PM验收报告**：reports/pm_accept_O2_scoring_ceiling_20260727.md
**开发自验报告**：reports/dev_selftest_O2_scoring_ceiling_20260727.md

---

## 一、任务卡

| 项 | 内容 |
|---|---|
| **任务名** | 评分引擎天花板优化——QA独立验收 |
| **推荐模型** | glm5.2 |
| **窗口类型** | Quests（独立窗口） |
| **执行模式** | 单代理 |
| **项目路径** | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格） |
| **Python** | `C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe` |

---

## 二、交付物范围（QA验收对象）

本批次最终交付：**O2-A+/O2-B 保留，O2-E 已回滚**。

### 2.1 scoring_engine.py（O2-A+/O2-B，6处改动保留）

| 函数 | 档位 | 原值 | 当前值 | 行号 |
|---|---|---|---|---|
| score_vol_ratio | 温和放量(1.3<vr≤2.0) | 75 | **80** | L438 |
| score_main_capital | 小幅净流入(0≤inflow<1000) | 82 | **85** | L754 |
| score_north_capital | 缺失中性分 | 65 | **70** | L767 |
| score_north_capital | 北向大幅买入(north≥3000) | 85 | **88** | L771 |
| score_margin_capital | 缺失中性分 | 63 | **68** | L788 |
| score_margin_capital | 融资大幅增加(margin≥2000) | 85 | **88** | L792 |

### 2.2 config_weights.json（O2-E已回滚，恢复原值）

| 字段 | 当前值（已回滚） | 验证标准 |
|---|---|---|
| a_stock.weights.kline | 0.2507 | 不得为0.28 |
| a_stock.weights.fundamental | 0.2005 | 不得为0.25 |
| a_stock.weights.capital_flow | 0.3985 | 不得为0.30 |
| a_stock.weights.news | 0.1504 | 不得为0.17 |
| 半导体.capital_flow | 0.35 | 不得为0.30 |
| 计算机设备.capital_flow | 0.35 | 不得为0.30 |
| 通信设备.capital_flow | 0.35 | 不得为0.30 |
| hk_stock.weights | 不变 | 未被修改 |
| rating_mapping | 80/65/50/30 | 边界不变 |

---

## 三、QA独立验收用例（V5-V8）

> QA不依赖开发自验报告，独立设计用例+独立执行。

### V5：评分稳定性验证（回归测试）

**方法**：运行 `python -m modules.scoring_engine`，验证MockDataProvider四场景

| 场景 | 预期 |
|---|---|
| normal（600519.SH） | 正常输出总分，评级合理，无异常 |
| boundary（000001.SZ） | 正常输出，无崩溃 |
| partial 30%（00700.HK） | 正常输出，降级机制工作 |
| partial 70%（300750.SZ） | 正常输出，严重缺失不崩溃 |

**通过标准**：四场景全部正常输出，无NaN/异常/崩溃。

### V6：防虚高验证

**方法**：对27只自选股跑 `analyze_from_db`，检查低分股不异常跳档

**通过标准**：
- 评级分布合理（不应出现大量股票集中在某一档）
- 无"基本面极差但评级推荐买入"的异常case
- 各档位数量分布与改动前基本一致（O2-A+/B仅微调）

### V7：红线核验（QA独立执行，不依赖PM）

**方法**：用grep/python独立核验

| 红线 | 核验方法 | 通过标准 |
|---|---|---|
| rating_mapping边界 | 读config_weights.json | 强烈推荐买入80/推荐买入65/持有观望50/建议减仓30不变 |
| data_collector.py | grep `if False` L1645/L1684/L1717 | 三处在 |
| advisor.py | grep `def generate_advice` | 存在且未被改 |
| requirements.txt | 检查依赖数 | 8个，无新增 |
| config_weights.json BOM | `open('rb').read(3)` | 无BOM |
| O2-E已回滚 | 读a_stock.weights | 必须是原值(0.2507/0.2005/0.3985/0.1504) |

### V8：零代码运行验证

**方法**：
1. `python app.py` 启动服务
2. 浏览器打开 http://127.0.0.1:5000
3. 查看个股分析报告页，确认评分/评级展示正常

**通过标准**：服务正常启动，页面正常加载，评分数据展示无异常。

---

## 四、QA验收输出

请QA出具验收报告（`reports/qa_accept_O2_scoring_ceiling_20260727.md`），包含：
1. V5-V8 每项的执行结果（含命令/输出）
2. 27只股票评分快照（可选，如V6需要）
3. 红线核验独立执行结果
4. 总体结论：PASS / FAIL（附理由）

---

## 五、红线清单（QA验收红线时参考）

| 红线 | 说明 |
|---|---|
| rating_mapping 80/65/50/30 | 不可改 |
| data_collector.py L1645/L1684/L1717 | 三处if False不可改 |
| advisor.py | B24红线，不可改 |
| 零代码约束 | 无新pip依赖 |
| config_weights.json | O2-E已回滚，必须是原值 |

---

*QA验收完毕后，PM+QA双签，报监理关闭。*
