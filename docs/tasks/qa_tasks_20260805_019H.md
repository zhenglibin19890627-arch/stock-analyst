# QA-TASKS-20260805-019H：is_estimated 过滤补全（预警层 alert_engine.py）— 独立验收任务书

> **签发人**：PM | **签发日期**：2026-08-05 | **状态**：待 QA 执行

---

## 角色定义（内嵌，无需额外窗口提示词）

### 你的角色：QA（独立验收）

**职责边界**：
- 独立验收 019H 开发成果，**不依赖开发自验报告结论**（自验报告 `reports/dev_selftest_019H_alert_filter_20260805.md` 仅作对照参考）
- 静态代码核查 + 受控实测（构造隔离测试数据），逐条用例出 PASS/FAIL + 证据
- **不修改任何功能代码**；构造测试数据须用专用日期（如 `2099-12-31`）并在验收结束前全部清理、复核 COUNT=0
- 交付物：`reports/qa_accept_019H_alert_filter_20260805.md`

### 独立性原则
- 各角色独立不兼职：PM 不兼架构、架构师不编码、开发不验收、QA 独立测试
- QA 自行构造测试数据，不复用开发临时脚本（已删除）

### 项目背景摘要
| 项 | 内容 |
|---|---|
| 项目路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格） |
| 数据库 | `stock_analyst.db`（raw_capital_flow 已含 `is_estimated` 列，019E 迁移就位） |
| 任务书 | `docs/tasks/dev_tasks_20260805_019H_is_estimated过滤补全.md`（v2 定稿） |
| 评审报告 | `docs/reviews/review_019H_is_estimated_20260805.md`（架构师独立复核定稿） |
| 自验报告 | `reports/dev_selftest_019H_alert_filter_20260805.md`（仅对照参考，不采信结论） |
| 前序 | 019E（估算兜底+评分隔离，已关闭）、019F（评分纯净 4 处闭合，已关闭）、019G（时间展示优化，已关闭） |

---

## 执行信息（PM 标注）

| 项 | 内容 |
|---|---|
| 推荐模型 | **kimi k3**（验收类任务） |
| 窗口类型 | **Quests 独立窗口** |
| 本批次最高风险 | **预警纯净**：估算值不得参与预警判定（红线 1/5，违反即 FAIL） |
| 改动范围 | **仅 `modules/alert_engine.py` L200-207 一处 SQL**（架构评审 M-1 否决了 app.py 展示层改动） |

### 关键代码位置

| 位置 | 说明 |
|---|---|
| `modules/alert_engine.py` L183-234 | `check_capital_outflow(cursor, stock_id, n_days=3)`，预警连续净流出判定。L200 注释 + L205 新增过滤 |
| `modules/alert_engine.py` L277-279 | `_RULE_CHECKERS['capital_outflow']` lambda 调用入口 |
| `modules/alert_engine.py` L213-216 | Python 层 NULL 过滤（`main_net_inflow is not None`）+ 数据不足判定（`len(valid) < n_days` → return None） |
| app.py L768-774 | 展示层 `/api/stocks/<id>/capital`——**本批次不碰**（M-1 裁定"不改"，前端已有标注） |
| templates/index.html L2481-2490 | 前端估算标注（019E Task 4.1）——**本批次不碰** |

### 已有过滤闭合点（canonical 表达式，QA 核查一致性基准）

| # | 文件 | 行 |
|---|---|---|
| 1 | data_adapter.py | L282 |
| 2 | advisor.py | L1126 |
| 3 | data_collector.py | L1477 |
| 4 | analysis_engine.py | L132 |
| 5 | data_collector.py | L1903（019E 既有变体，含 canonical 子串） |
| **6** | **alert_engine.py** | **L205（本批次新增）** |

---

## 一、测试用例（逐条出 PASS/FAIL + 证据）

### TC-019H-1 代码级核查（验收 1）

1. **过滤表达式存在**：grep `AND (is_estimated = 0 OR is_estimated IS NULL)` 在 `alert_engine.py` **恰 1 处**（L205）
2. **逐字符一致性**：L205 表达式与 data_adapter L282 / advisor L1126 / analysis_engine L132 **逐字符一致**（QA 自行 grep 比对，不得仅凭自验报告结论）
3. **无附加 SQL 条件**（红线 7）：L205 附近 SQL **未附加** `main_net_inflow IS NOT NULL` 等额外条件（NULL 行维持由 Python L213-214 过滤）
4. **签名不变**（红线 3）：函数定义为 `check_capital_outflow(cursor, stock_id, n_days=3)`（无下划线前缀，n_days 默认值 3）
5. **参数绑定不变**（红线 8）：`(stock_id, n_days * 2)` 顺序不变，`LIMIT ?` 位置不变

### TC-019H-2 编译验证（验收 2）

```
python -m py_compile modules/alert_engine.py → 无错误
```

### TC-019H-3 预警纯净验证 ★核心（验收 3，含 R-3 正/负路径）

QA 须**自行构造隔离测试数据**（内存级 SQLite 或专用日期 `2099-12-31`，验收后清理），执行 `check_capital_outflow` 函数。

> **注意**：`check_capital_outflow` 内部调用 `_get_stock_info(cursor, stock_id)` 判断市场类型，港股直接 return None。QA 构造数据时需在 stocks 表插入 `market != 'hk_stock'` 的记录。

**场景 A（负路径 — 不误报）**：
- 构造：2 行 `is_estimated=0` 连续净流出 + 1 行 `is_estimated=1` 净流出
- 期望：`check_capital_outflow` 返回 **None**（真实数据不足 n_days=3，估算行不计入窗口）
- 断言：结果为 None

**场景 B（正路径 — 不漏报）**：
- 构造：3 行 `is_estimated=0` 连续净流出（-100/-200/-300）+ 其间穿插 1 行 `is_estimated=1`（-999）
- 期望：**正常触发**预警，且 `total_outflow == 600.0`（估算行 -999 未混入统计）
- 断言：结果非 None，`total_outflow == 600.0`，`consecutive_days == 3`

**场景 C（NULL 维持 — 红线 7 验证）**：
- 构造：3 行 `is_estimated=0` 连续净流出 + 1 行 `main_net_inflow=NULL`（is_estimated=0）
- 期望：**正常触发**预警，`total_outflow == 600.0`（NULL 行由 Python L213-214 过滤，不影响判定）
- 断言：结果非 None，`total_outflow == 600.0`

### TC-019H-4 展示层回归验证（验收 4，M-1 裁定"不改"）

1. **代码零改动确认**：app.py L770 `SELECT * FROM raw_capital_flow WHERE stock_id = ?` 仍**无 is_estimated 过滤**（有意不过滤，M-1）
2. **前端标注在位**：index.html L2481 `hasEstimated` 表头逻辑 + L2490 `estTag` 行级标注逻辑**保持 019E 既有状态**
3. **运行实例验证**（如有运行实例）：`GET /api/stocks/<id>/capital` 返回数据中 `is_estimated=1` 行**仍存在**（不被过滤）；截图核查前端估算标注正常显示

### TC-019H-5 全仓 grep 闭合确认（验收 5，R-2 口径）

canonical 子串 `AND (is_estimated = 0 OR is_estimated IS NULL)` 全仓 *.py 命中 **6 处**（modules 内）：
- data_adapter.py L282 / advisor.py L1126 / data_collector.py L1477 / analysis_engine.py L132 / data_collector.py L1903（既有变体）/ **alert_engine.py L205（本批次新增）**
- 展示层有意不过滤，不计入
- `tests/qa_019f_isolation_test.py` 中 2 处为测试断言常量定义，不计入

### TC-019H-6 019F 回归验证（验收 6）

```
python -m pytest tests/qa_019f_isolation_test.py -v → 9/9 PASSED
```

重点核查 T8/T9（过滤表达式一致性）+ T1-T4（已有 4 处评分链路过滤未被破坏）。

### TC-019H-7 零改动确认（验收 7）

QA 对以下文件做**内容比对或哈希核查**（git diff 或文件哈希），确认零改动：
- app.py / templates/index.html / modules/export_engine.py
- modules/data_adapter.py / modules/advisor.py / modules/data_collector.py / modules/analysis_engine.py / modules/scoring_engine.py / modules/daily_report.py
- database/db_manager.py / config_*.json / config.py / requirements.txt

> 注：`git status` 中可能存在 019A-019G 历史批次累积的未提交改动，QA 需区分本批次改动与历史改动。本批次唯一改动文件为 `modules/alert_engine.py`。

---

## 二、验收红线（违反即 FAIL）

| 红线 | 内容 |
|---|---|
| 1. 过滤表达式一致性 | `AND (is_estimated = 0 OR is_estimated IS NULL)` 逐字符一致，不得自创变体 |
| 2. 范围红线 | 改动仅限 `modules/alert_engine.py`（1 处 SQL），其余文件零改动 |
| 3. 签名红线 | `check_capital_outflow(cursor, stock_id, n_days=3)` 签名不变 |
| 5. 评分纯净红线 | 不影响已有 4 处评分链路过滤（019F 回归测试全通过） |
| 6. 展示层不动红线 | app.py / index.html / export_engine.py 零改动 |
| 7. SQL 变体红线 | 未附加 `main_net_inflow IS NOT NULL` 等额外 SQL 条件 |
| 8. 参数绑定红线 | `LIMIT ?` 位置不变，`(stock_id, n_days * 2)` 顺序不变 |

---

## 三、范围外已知项（本批次不处理，QA 不验收）

| 文件:行 | 用途 | 缺过滤原因 | 跟踪 |
|---|---|---|---|
| export_engine.py L278-285 | Excel 导出层 | 019H 架构评审 M-2 新发现，风险低 | 建议 019I 处理 |
| app.py L770 / index.html | 展示层 | 019E 已裁定"不过滤、前端标注"（M-1） | 维持现状 |

---

## 四、交付物要求

`reports/qa_accept_019H_alert_filter_20260805.md`，含：
1. 逐用例 PASS/FAIL + 证据（代码片段/命令输出/断言结果）
2. 测试数据清理确认（专用日期数据 COUNT=0）
3. 验收总结（X/Y PASS）
4. 验收结论（通过 / 不通过）
5. 若不通过，列出具体 FAIL 项 + 复测建议

---

> **PM 备注**：本批次改动极小（一文件一处 SQL），但预警行为验证（TC-019H-3）是核心——需验证过滤后**既不误报（负路径）也不漏报（正路径）**，且 NULL 行处理不受影响（场景 C）。测试数据构造注意 `_get_stock_info` 依赖 stocks 表，需插入非港股记录。验收完成后通知 PM 进行双签。
