# PM 交接提示词：Stock Analyst 项目（2026-08-11）

> 本文档供新 PM 窗口启动时作为首条消息粘贴使用。包含截至 2026-08-11 的完整项目状态、PM 行为纪律、当前批次进度与待办。
> 生成：PM，2026-08-11

---

## 一、项目环境

- **项目路径**：`C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（路径含空格，PowerShell 操作须加引号）
- **Python**：`C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe`（3.12.9）
- **SQLite**：`stock_analyst.db`（只读访问：`sqlite3.connect(r"file:...?mode=ro", uri=True)`）
- **日志**：`logs\app.log`（当日）+ 滚动归档（如 `app.log.2026-08-09`）
- **五步中转法**：项目目录文件操作须 Write 到工作区→Copy-Item 回写→Select-String 锚点核验→DeleteFile 删临时副本→呈报
- **git 不可用作红线核验**：git 仓库根在父目录 `Qoder cn`，全仓文件均带 ` M` 标记（autocrlf 历史污染）→ 红线核验改用**文件 mtime**
- **PowerShell 编码坑**：中文输出 GBK 乱码但数值可读；统计中文日志用 `-Encoding UTF8`；SQL 里的 `*` 会被 PowerShell 当通配符，需用临时 .py 脚本查库
- **资金流表名**：`raw_capital_flow`（非 capital_flows），字段含 `trade_date/is_estimated/capital_source`；`capital_source IS NULL` = 东财真数据；`capital_source='sina_main'` = 新浪顶替

---

## 二、PM 行为纪律（必须遵守）

1. **零代码用户最高优先级**：所有技术决策以"零代码用户可独立运行"为优先，用大白话讲解（如"保安关门"比喻）
2. **不推荐窗口类型与模型**：流程交接时只说"下一步做什么、把哪份文档交给哪个角色"，窗口与模型由监理自行决定
3. **签发任务书后止步**：不主动催开发，等回件
4. **呈报格式**：先结论→再证据→再待办/请示
5. **不直接采信任何角色结论**：独立核验（数字、mtime、查库）
6. **生产改动必须监理批准**：未经监理裁定不动生产代码

---

## 三、019X 批次状态（当前主线）

### 3.1 已完成

- **019X**（东财请求策略修复）：监理裁定选项 a，已开发完成 + PM 验收通过
  - T1：`_http_get_em` 退避改为 30/60/60s±15% 抖动、轮数 4（新增 `_EM_RETRY_BACKOFFS/_EM_RETRY_JITTER/_EM_RETRY_ROUNDS` 常量，全局 `MAX_RETRIES=3` 未动）
  - T2：daily_report.py 调度由 16:10 单批改为 16:10/16:40/17:10 三窗各采 1/3（`_scheduler_tick(window_idx)/_split_em_capital_list/_register_capital_window/_run_capital_window/_run_full_report_flow`，窗3后执行完整日报流程挂载顺序与现状一致）
  - T3：config.py 新增 `EM_USE_PROXY=False` 开关只走直连（代理代码保留）
  - 改动文件：config.py / data_collector.py / daily_report.py + scripts/selftest_019x_request_strategy.py
  - **需重启 app.py 生效**（监理尚未重启）

### 3.2 夜间补采实测（2026-08-10 23:17–23:58）

监理批准夜间跑了一次完整日报流程（force=True）。实测结果：

- **退避策略验证通过**：日志可见 30s/57s/67s 退避节奏，`EM_USE_PROXY=False 未走代理` 消息正确
- **资金流入库**：东财真数据 2 只（600276 恒瑞医药、300146 汤臣倍健，各 120 天历史）；新浪顶替 21 只（数据没丢）
- **日报评分**：26 份 ok（20 只 A 股 + 6 只港股）、3 只 failed（601012 隆基绿能、688795 摩尔线程、688802 沐曦股份，排在批次尾部被 1800 秒整体超时截断）
- **发现副作用**：per-stock 90 秒超时保护 + 退避拉长（单只全败 ~3 分钟）→ 几乎每只都超 90 秒被标"failed"，但后台线程继续跑完、实际写入数据库。**汇总统计与实际入库不一致**（汇总说 0 成功，查库有 26 份 ok）。这是 T1 退避拉长与 per-stock 90 秒超时的交互问题，T2 三窗拆分后每窗预算充裕可大幅缓解。

### 3.3 待办

1. **重启 app.py**：监理尚未操作，重启后 T2 三窗调度生效，次日 16:10/16:40/17:10 为真实验证点
2. **per-stock 90 秒超时与退避的矛盾**：当前退避拉长后单只最坏 ~3 分钟，但 per-stock 超时仍是 90 秒——三窗模式下每窗 7~8 只预算充裕时影响小，但如果一窗内多只连续碰关门仍可能大面积超时标 failed。重启后观察三窗实际表现再决定是否需要调整 per-stock 超时或另立批次修复
3. **3 只 failed 股票**：明天批次自动重新采集

---

## 四、近期批次脉络

| 批次 | 内容 | 状态 |
|---|---|---|
| 019W | 东财反爬诊断 | 收口（诊断报告 + 自测报告 + 诊断脚本） |
| 019W-S1 | 诊断报告数字修正 | 收口（3 处数字改为口径 A: 23/19/36=78） |
| 019X | 东财请求策略修复（选项 a） | 验收通过，待重启生效 |
| 019V | M2 权重重估 | 预留未动 |
| 019U | 权重调整（三段式流程） | 按 D 暂缓裁定收口关闭 |
| 019T | 评分体系校准 | 双签关闭 |

---

## 五、019W 核心诊断结论（背景知识）

东财 WAF 对本机出口 IP（183.134.206.110 杭州电信家宽）实施**窗口式连接级丢弃**（TCP 正常、TLS 完成后、HTTP 响应前被掐断 → RemoteDisconnected）。特征：非永久封禁、非接口改版、非本机网络故障；换请求头无效、代理出口同被丢弃（0/8）；30 秒间隔重试 2 分钟内可捕获开放窗口；夜间有 33~40% 直连成功率。

诊断报告四类策略建议（监理已选 a 并落地为 019X）：
- a 请求策略修复（重试退避 + 错峰分批 + 放弃代理）→ **已落地 019X**
- b 重启光猫换 IP（首选见效最快）
- c 等待自然恢复 + 监测探针
- d 其他（akshare 升级对本次故障无帮助、curl_cffi 另行立项）

---

## 六、关键记忆 ID（供检索）

- 019X 批次：`0793d4fa-bc23-4624-9d12-0bcb1a7c4a9d`
- 019W 批次：`4cf9e4fa-6286-4b2b-8a68-f0d076c6a69b`
- 调度器配置（16:10 触发）：`8f133b19-5e85-4ee7-9f5c-f020d39a069c`
- 零代码用户最高优先级：`7f162084-3116-4fcb-9ce9-9a61e7ee8f64`
- 不推荐窗口与模型：`62525f01-d723-429a-9395-7c3aeaedda53`
- PM 协作流程与角色边界：（development_practice_specification 下）
- 调度器 16:10 前重启会跳过当天批次的缺陷：（common_pitfalls_experience 下）

---

## 七、其他挂起事项

- **O-6 数据补录核验**：前会话承诺未兑现，若监理催问或自然衔接时执行只读核查
- **akshare 升级（1.18.53→1.18.84）**：监理未批，019W 报告确认其对本次故障无帮助，可作长期保健另行立项
- **CHANGELOG.md**：停留在 07-29 未随近期批次更新，既往如此

---

## 使用说明（新 PM 窗口启动）

将本文件第一节至第七节全部内容复制粘贴到新窗口作为首条消息，新 PM 即获得完整上下文。监理说"开发已完成"时，按流程独立核验；监理有新指令时，按指令执行。
