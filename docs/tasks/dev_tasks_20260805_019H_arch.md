# DEV-TASKS-20260805-019H-ARCH：019H is_estimated 过滤补全 — 架构方案评审任务书

> **签发人**：PM  | **签发日期**：2026-08-05 | **状态**：待架构师执行

---

## 角色定义（内嵌，无需额外窗口提示词）

### 你的角色：架构师

**职责边界**：
- 复核 PM 签发的 019H 开发任务书（`docs/tasks/dev_tasks_20260805_019H_is_estimated过滤补全.md`）
- **特别说明**：PM 在签发后越权进行了初步代码评审，已产出评审文档 `docs/reviews/review_019H_is_estimated_20260805.md`。架构师需**独立复核**该评审的三个核心结论（M-1/M-2/M-3），可采纳/修改/否决
- 对每个决策点给出明确裁定 + 理由
- **不编码、不验收、不写功能代码**
- 交付物：确认或修订 `docs/reviews/review_019H_is_estimated_20260805.md`

### 独立性原则
- 各角色独立不兼职：PM 不兼架构、架构师不编码、开发不验收、QA 独立测试
- 架构师仅做方案评审，不执行任何代码修改
- PM 产出的初步评审仅供架构师参考，架构师须独立 Read 代码核验，不采信 PM 结论

### 项目背景摘要
| 项 | 内容 |
|---|---|
| 项目路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格） |
| 技术栈 | Python + Flask + SQLite + Jinja2 单页应用 |
| 最高约束 | **零代码用户可独立运行**：无新 pip 依赖（当前 9 包） |
| 前序批次 | 019E（估算兜底+评分隔离，已关闭）、019F（评分纯净 4 处闭合，已关闭）、019G（时间展示优化，已关闭） |

---

## 执行信息（PM 标注）

| 项 | 内容 |
|---|---|
| 任务类型 | 架构方案评审（只读不改，不写功能代码） |
| 推荐模型 | **kimi k3**（评审类任务） |
| 窗口类型 | **Quests 独立窗口** |
| 执行模式 | 单代理 agent |
| 交付物 | 确认或修订 `docs/reviews/review_019H_is_estimated_20260805.md` |

---

## 一、需求背景

### 1.1 缺陷溯源

019E/019F 已完成评分链路 4 处 `is_estimated` 过滤闭合点（data_adapter / advisor / data_collector / analysis_engine）。019H 处理两个 019F 评审登记的范围外备查项：

1. **alert_engine.py L200-207**（中风险）：预警连续净流出查询缺 is_estimated 过滤，估算值可能导致误报
2. **app.py L768-774**（低风险）：展示层查询缺 is_estimated 过滤

### 1.2 关键代码位置（评审必读，请独立 Read 核验）

| 位置 | 说明 |
|---|---|
| `modules/alert_engine.py` L183-232 | `check_capital_outflow(cursor, stock_id, n_days=3)`，预警连续净流出判定 |
| `app.py` L757-796 | `/api/stocks/<id>/capital` 展示层路由 |
| `templates/index.html` L2479-2497 | 前端资金面渲染逻辑（**PM 越权评审的关键核验点**） |
| `modules/export_engine.py` L278-285 | Excel 导出查询（**PM 越权评审新发现**） |
| 已有 4 处过滤点 | data_adapter L282 / advisor L1126 / data_collector L1477 / analysis_engine L132 |

### 1.3 PM 越权评审的三个核心结论（请逐项独立复核）

#### M-1：否决 app.py 展示层方案 A（PM 任务书方案被否决）

**PM 越权评审结论**：不改 app.py L770。理由：前端 index.html L2481-2490 在 019E Task 4.1 中**已实现**估算行双层标注（表头级"含估算兜底数据"提示 + 行级橙色"估算"上标标签）。任务书所称"前端未做视觉区分"系 PM 核验遗漏。方案 A（直接过滤估算行）会导致时间序列缺口，有害。

**架构师请核验**：
- Read `templates/index.html` L2479-2497，确认前端是否确实已有估算标注
- 确认"方案 A 导致时间序列缺口"的风险评估是否成立
- 裁定：采纳（不改 app.py）/ 修改 / 否决

#### M-2：export_engine.py 导出层缺过滤（新发现，登记备查）

**PM 越权评审结论**：`modules/export_engine.py` L278-285 的 Excel 导出查询缺 is_estimated 过滤，风险低（导出不在评分链路，估算行仅在 EM 全失败时存在），登记备查不在本批次处理。

**架构师请核验**：
- Read `modules/export_engine.py` L278-285，确认是否确实缺过滤
- 评估风险等级是否准确
- 裁定：登记备查（范围外）/ 纳入本批次 / 其他

#### M-3：任务书事实性错误更正

**PM 越权评审结论**：任务书第一节"备查项 2"中"前端未做视觉区分"的描述需更正为"前端已实现估算标注"。

**架构师请核验**：与 M-1 联动，若 M-1 采纳则 M-3 同步采纳。

---

## 二、评审决策点（请逐项裁定）

### A-1：alert_engine.py 预警过滤方案（任务书 Task 1）

任务书建议 SQL 补 `AND (is_estimated = 0 OR is_estimated IS NULL)`，与已有 4 处逐字符一致。请裁定：采纳 / 修改 / 否决。

### A-2：app.py 展示层方案（任务书 Task 2，对应 PM 越权评审 M-1）

PM 越权评审否决了方案 A，建议不改 app.py。请独立核验后裁定：
- (a) 采纳 PM 结论——不改 app.py（前端已有标注）
- (b) 否决 PM 结论——仍按方案 A 过滤
- (c) 其他方案

### A-3：export_engine.py 备查处理（对应 PM 越权评审 M-2）

请裁定：登记备查（范围外）/ 纳入本批次 / 其他。

### A-4：全仓 raw_capital_flow 读取入口完整性

PM 越权评审做了全仓扫描（grep `FROM raw_capital_flow`），列出 14 处引用。请独立复核是否有遗漏，特别是：
- 是否存在动态拼接 SQL（非直接 `FROM raw_capital_flow` 字面量）的读取入口
- 是否有 ORM 或其他间接读取路径

### A-5：范围与红线确认

任务书第五节红线是否完备？重点核查：过滤表达式一致性、签名不变、零代码约束。

---

## 三、交付物要求

确认或修订 `docs/reviews/review_019H_is_estimated_20260805.md`，含：
1. 逐决策点裁定（采纳/修改/否决 + 理由），明确标注是否与 PM 越权评审结论一致
2. 若否决 PM 结论，须给出独立核验的代码证据
3. 新发现的风险项（R-x 编号）
4. 评审结论（通过 / 有条件通过 / 不通过）

---

> **PM 备注**：本批次 PM 在签发任务书后越权进行了初步评审（违反角色独立性原则），已坦诚声明。架构师请以独立视角复核，特别是 M-1（前端已有标注）这一关键事实判断——这直接决定了 app.py 是否需要改动。如果架构师独立核验后确认 M-1/M-2/M-3 结论正确，请在评审文档中签署确认并标注"经架构师独立复核确认"；如有异议请直接修订。实际改动预计从任务书的"两文件两处"收敛为"一文件一处"（仅 alert_engine.py）。
