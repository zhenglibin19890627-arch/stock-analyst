# DEV-TASKS-20260730-012：012 日志系统+日报诊断+采集健壮性 — 开发任务书

> **签发人**：PM  | **签发日期**：2026-07-30 | **状态**：待开发执行
> **依据**：架构评审 `docs/reviews/review_012_logging_diagnosis_20260730.md`（5决策点已裁定）

---

## 角色定义（内嵌，无需额外窗口提示词）

### 你的角色：开发人员

**职责边界**：
- 按架构师裁定方案实现 012 三个子任务
- 修复后执行自验
- 交付自验报告 `reports/dev_selftest_012_logging_20260730.md`
- **不负责正式验收**（QA 独立验收）

### 独立性原则
- 各角色独立不兼职：PM 不兼架构、架构师不编码、开发不验收、QA 独立测试
- 开发仅做编码+自验，不做正式验收判定

### 项目背景摘要
| 项 | 内容 |
|---|---|
| 项目路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格） |
| 数据库路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst\stock_analyst.db`（在stock_analyst子目录内！） |
| Python 解释器 | `C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe` |
| 技术栈 | Python + Flask + SQLite + akshare + Jinja2 单页应用 |
| 最高约束 | **零代码用户可独立运行**：无新 pip 依赖（当前8包） |

---

## 执行信息（PM 标注）

| 项 | 内容 |
|---|---|
| 任务类型 | 功能开发（3个子任务串行） |
| 推荐模型 | **glm5.2 / qwen3.8**（并列优先） |
| 窗口类型 | **Quests 独立窗口** |
| 执行模式 | 单代理 agent |
| 预计改动量 | ~120行新增（5个文件） |
| 交付物 | 功能代码 + `reports/dev_selftest_012_logging_20260730.md` |

---

## 一、子任务总览

| 子任务 | 内容 | 改动文件 | 预估行数 |
|--------|------|---------|---------|
| **012-A** | 文件日志系统 | `app.py` | +18行 |
| **012-B** | 日报进度追踪 + 超时机制 | `daily_report.py`, `config.py` | +70行 |
| **012-C** | error_logs 增强 + 失败摘要 | `data_collector.py`, `db_manager.py`, `daily_report.py` | +45行 |

**执行顺序**：A → B → C（B/C 依赖 A 的文件日志能力）

---

## 二、012-A：文件日志系统

### 改动位置：`app.py` main() 函数（L3534）

在 `main()` 函数体**第一行**（`print("=" * 60)` 之前）插入：

```python
def main():
    """启动程序"""
    # === 012-A: 全局文件日志配置 ===
    import logging
    from logging.handlers import TimedRotatingFileHandler

    log_dir = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'logs')
    os.makedirs(log_dir, exist_ok=True)

    file_handler = TimedRotatingFileHandler(
        os.path.join(log_dir, 'app.log'), when='midnight', backupCount=7, encoding='utf-8'
    )
    file_handler.setFormatter(logging.Formatter('%(asctime)s [%(name)s] %(levelname)s %(message)s'))
    file_handler.setLevel(logging.INFO)

    root = logging.getLogger()
    root.setLevel(logging.INFO)
    root.addHandler(file_handler)  # addHandler 而非 basicConfig，避免冲突

    logging.getLogger(__name__).info(f'===== Stock Analyst 启动 PID={os.getpid()} =====')
    # === 012-A END ===

    print('=' * 60)
    # ... 原有逻辑不变
```

### 架构师关键裁定
- 使用 `root.addHandler()` 而非 `basicConfig()`（避免与模块级 basicConfig 冲突）
- 各模块的 `basicConfig` 调用**不需要删除**（作为独立运行 fallback）
- 统一 `app.log`，不分模块文件
- 保留7天，INFO级别
- 启动 banner 便于定位重启起点

---

## 三、012-B：日报进度追踪 + 超时机制

### 3.1 config.py 新增配置（文件末尾追加）

```python
# ============================================================
# 七、012: 日报生成超时配置
# ============================================================
# 单只股票处理超时（秒），超时则跳过该只继续下一只
STOCK_TIMEOUT_SECONDS = 90
# 批次整体超时（秒），超过则停止处理剩余股票
BATCH_TIMEOUT_SECONDS = 1800
```

### 3.2 daily_report.py 改动

#### 3.2.1 文件顶部新增 import

```python
import time
import json
from concurrent.futures import ThreadPoolExecutor, TimeoutError as FuturesTimeout
from config import STOCK_TIMEOUT_SECONDS, BATCH_TIMEOUT_SECONDS
```

#### 3.2.2 新增辅助函数（在 generate_daily_report 之前）

```python
def _update_progress_file(data: dict):
    """012-B: 更新进度文件 logs/report_progress.json"""
    try:
        progress_path = os.path.join(
            os.path.dirname(os.path.dirname(os.path.abspath(__file__))),
            'logs',
            'report_progress.json',
        )
        os.makedirs(os.path.dirname(progress_path), exist_ok=True)
        with open(progress_path, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
    except Exception:
        pass  # 进度文件写入失败不阻塞业务


def _process_single_stock(stock, target_date, force):
    """012-B: 单只股票处理（供 ThreadPoolExecutor 调用）

    将原 for 循环体内的逻辑封装为独立函数。
    返回值与原有 results.append 结构一致。
    """
    # 注意：此函数内部逻辑 = 原 L402~L554 的循环体
    # 开发时将循环体提取到此函数，保持原有逻辑不变
    pass  # 开发时实现
```

#### 3.2.3 generate_daily_report 循环改造

**核心改动**：将 `for stock in stocks:` 循环体包裹在 ThreadPoolExecutor 超时控制中。

```python
# === 012-B: 批次超时 + 进度追踪 ===
batch_start = time.time()
total = len(stocks)

_update_progress_file(
    {
        'date': target_date,
        'total': total,
        'current': 0,
        'current_symbol': '',
        'current_name': '',
        'status': 'running',
        'started_at': datetime.now(_CN_TZ).strftime('%Y-%m-%d %H:%M:%S'),
        'last_update': '',
        'finished_at': None,
    }
)

for idx, stock in enumerate(stocks, 1):
    # 整体超时检查（软超时）
    if time.time() - batch_start > BATCH_TIMEOUT_SECONDS:
        remaining = total - idx + 1
        logger.warning(f'[日报进度] 批次整体超时({BATCH_TIMEOUT_SECONDS}s)，剩余{remaining}只跳过')
        fail_count += remaining
        break

    symbol = stock['symbol']
    name = stock.get('name', '')
    logger.info(f'[日报进度] {idx}/{total} 开始 {symbol} {name}')
    _update_progress_file(
        {
            'date': target_date,
            'total': total,
            'current': idx,
            'current_symbol': symbol,
            'current_name': name,
            'status': 'running',
            'started_at': ...,  # 保持上面的值
            'last_update': datetime.now(_CN_TZ).strftime('%Y-%m-%d %H:%M:%S'),
            'finished_at': None,
        }
    )

    # 单只超时控制
    with ThreadPoolExecutor(max_workers=1) as executor:
        future = executor.submit(_process_single_stock, stock, target_date, force)
        try:
            result = future.result(timeout=STOCK_TIMEOUT_SECONDS)
            # 处理 result（更新 success_count/fail_count/results 等）
        except FuturesTimeout:
            fail_count += 1
            logger.error(f'[日报进度] {symbol} 超时({STOCK_TIMEOUT_SECONDS}s)，跳过')
            _save_report(..., status='failed', error_msg=f'采集超时({STOCK_TIMEOUT_SECONDS}s)')
            results.append(
                {
                    'stock_id': stock['id'],
                    'symbol': symbol,
                    'name': name,
                    'status': 'failed',
                    'error': f'采集超时({STOCK_TIMEOUT_SECONDS}s)',
                }
            )
            continue

    logger.info(f'[日报进度] {symbol} 完成')

# 批次完成
elapsed = int(time.time() - batch_start)
logger.info(f'[日报进度] ===== 批次完成 成功{success_count}/失败{fail_count} 耗时{elapsed}s =====')
_update_progress_file(
    {
        'date': target_date,
        'total': total,
        'current': total,
        'current_symbol': '',
        'current_name': '',
        'status': 'done',
        'started_at': ...,
        'last_update': datetime.now(_CN_TZ).strftime('%Y-%m-%d %H:%M:%S'),
        'finished_at': datetime.now(_CN_TZ).strftime('%Y-%m-%d %H:%M:%S'),
    }
)
# === 012-B END ===
```

### 架构师关键裁定
- 单只超时 **90秒**（非120s），使用 `ThreadPoolExecutor` + `future.result(timeout=90)`
- 整体超时 **30分钟**，软超时（if 检查 + break）
- 超时后：skip + 记录失败 + 继续下一只
- 进度文件 `logs/report_progress.json` 覆盖写入（非追加）
- **否决** DB 表和进度 API（不做）
- MAX_RETRIES=3 **不改**（DP-4 否决）

### 开发注意事项
- `_process_single_stock` 封装原循环体（L408~L554），包括复用检查、采集、分析、保存
- 原有 `try/except` 逻辑保持不变，只是外层多了超时控制
- `ThreadPoolExecutor(max_workers=1)` 串行提交，同一时刻最多1个遗弃线程
- 被遗弃线程会因 REQUEST_TIMEOUT=15 自行结束，无资源泄漏风险

---

## 四、012-C：error_logs 增强 + 失败摘要

### 4.1 db_manager.py：表结构 migration

在 `init_database()` 中（建表语句之后）增加：

```python
# 012-C: error_logs 表增强（增加 dimension + traceback 字段）
try:
    cursor.execute('SELECT dimension FROM error_logs LIMIT 1')
except Exception:
    cursor.execute('ALTER TABLE error_logs ADD COLUMN dimension TEXT')
try:
    cursor.execute('SELECT traceback FROM error_logs LIMIT 1')
except Exception:
    cursor.execute('ALTER TABLE error_logs ADD COLUMN traceback TEXT')
```

### 4.2 data_collector.py：统一错误写入函数

在文件中（`save_data_status` 函数附近）新增：

```python
def _log_error_to_db(
    stock_id, module, error_type, error_message, dimension=None, traceback_str=None
):
    """012-C: 统一写入 error_logs 表"""
    try:
        conn = get_connection()
        conn.execute(
            'INSERT INTO error_logs (stock_id, module, error_type, error_message, dimension, traceback) VALUES (?,?,?,?,?,?)',
            (
                stock_id,
                module,
                error_type,
                error_message,
                dimension,
                traceback_str[:2000] if traceback_str else None,
            ),  # 截断至2000字符
        )
        conn.commit()
        conn.close()
    except Exception:
        pass  # 写日志失败不阻塞业务
```

### 4.3 data_collector.py：collect_stock_data 中各维度 catch 调用

在 `collect_stock_data` 函数（L2332）的各维度 except 块中，增加 `_log_error_to_db` 调用：

```python
except Exception as e:
    logger.warning(f'[{symbol}] K线采集失败: {e}')
    _log_error_to_db(stock_id, 'data_collector', type(e).__name__, str(e), dimension='kline', traceback_str=traceback.format_exc())
```

对 6 个维度（kline/fundamental/capital/north/margin/sentiment）的 except 块均增加此调用。

> 注：需在文件顶部 `import traceback`（如尚未导入）。

### 4.4 daily_report.py：failure_summary

在 `generate_daily_report` 返回的 `summary` dict 中增加：

```python
        # 012-C: 失败摘要
        failure_summary = None
        if fail_count > 0:
            by_reason = {}
            for r in results:
                if r.get('status') == 'failed':
                    reason = r.get('error', '未知')
                    by_reason.setdefault(reason, []).append(r.get('symbol', ''))
            failure_summary = {'total_failed': fail_count, 'by_reason': by_reason}

        summary = {
            # ... 原有字段不变 ...
            'failure_summary': failure_summary,  # 012-C 新增
        }
```

### 架构师关键裁定
- error_logs 增加 `dimension`（TEXT）+ `traceback`（TEXT）两字段
- 使用 ALTER TABLE（SQLite 支持），migration 逻辑检查列是否存在
- traceback 截断至 2000 字符
- `_log_error_to_db` 内部 try/except 保护，写 DB 失败不阻塞业务
- failure_summary 从内存 results 聚合，无额外 DB 查询
- 前端展示**延后**（本期仅提供 API 数据）

---

## 五、红线约束

| 红线 | 说明 |
|---|---|
| `advisor.py` generate_advice | 函数签名和函数体不可修改 |
| `advisor.py` _build_capital_factors | 不可修改 |
| `scoring_engine.py` | v5引擎不可修改 |
| `config_weights.json` | rating_mapping 80/65/50/30 不可修改 |
| `data_collector.py` 三处 `if False` | 硬禁用不可修改 |
| `fetch_capital_flow` 签名 | `(symbol, market)` 不可加参数 |
| 011 增量逻辑 | 不得破坏 force_full 参数和增量跳过逻辑 |
| 零代码约束 | 无新 pip 依赖（全部使用标准库：logging.handlers, concurrent.futures, json, time, traceback） |

---

## 六、自验要求

### 012-A 自验
1. 启动 `python app.py`
2. 触发任意 API（如 `GET /api/health`）
3. 检查 `logs/app.log` 存在且包含启动 banner + 请求日志
4. 确认控制台仍正常输出（双输出）

### 012-B 自验
1. 触发日报生成（`POST /api/daily-report/generate`）
2. 检查 `logs/report_progress.json` 存在且 status 从 running → done
3. 检查 `logs/app.log` 包含 `[日报进度]` 结构化日志
4. 临时设 `STOCK_TIMEOUT_SECONDS=5` 测试超时跳过（验证后改回90）

### 012-C 自验
1. 触发日报生成（含港股，确保有失败维度）
2. 查询 `SELECT * FROM error_logs ORDER BY id DESC LIMIT 5`
3. 确认新记录含 dimension 和 traceback 字段值
4. 确认日报 API 返回含 `failure_summary` 字段

### 自验报告格式
交付 `reports/dev_selftest_012_logging_20260730.md`，包含上述各项执行结果。

---

## 七、后续流程

```
开发实现(A→B→C)+自验 → PM验收(红线+完整性) → QA独立验收 → PM+QA双签 → 监理批准关闭
```

---

> **PM 备注**：本任务书已内嵌角色定义，监理可直接全文粘贴到 Quests 窗口执行。架构师评审报告为唯一技术裁定依据。
