# 开发任务书 019Q-S1 — 补采清单语义注释修订（纯文字，零功能改动）

**批次编号**：019Q-S1（019Q 收尾小任务，QA 观察项 1 裁定落实）
**优先级**：P3（文档/注释一致性，不影响任何功能）
**签发日期**：2026-08-09
**架构评审**：⏭️ 监理裁定跳过（2026-08-09，规模极小纯注释修订）
**监理批准**：✅ 已批准（2026-08-09，跳过评审直接进入开发）
**当前状态**：✅ **已关闭**（2026-08-09，QA 抽查 4/4 通过 + PM 双签；O-1 低危知情登记无需整改）

---

## 一、背景（为什么改）

019Q QA 验收发现观察项 1：代码**行为**正确，但两处注释文字描述与实际行为相反。

- **实际行为**（正确，监理已裁定保留）：`fetch_capital_flow_batch` 补采清单只把"东财真数据"（capital_source IS NULL 且非估算）视为已完成；**sina_main 行与 ths_total 行都会进入补采清单**。这给东财在 30 分钟延迟补采时恢复并覆盖回补的机会，是"东财恢复后自动回补"目标的实现；新浪重采不降级已有数据（QA F9 实证）。
- **错误文字**：两处注释写成"sina_main 行被排除→幂等"，与实际行为相反，会误导后续维护者。

监理裁定（2026-08-09）：**保留代码、改说明书**。本任务只改注释文字，一行功能代码都不许动。

## 二、改动范围（仅 2 个文件，仅注释/docstring）

### 任务 1：`modules/data_collector.py` L1787-1788 注释

将现有注释（"sina_main 行被排除 → 幂等；ths_total 行…"一句）改为与实际行为一致的描述，要点：

```
# 019Q Task 3（M-5）：补采清单 SQL 扩为 NOT IN ('ths_total','sina_main')。
# 语义：只有东财真数据（capital_source IS NULL 且非估算）才算"已完成"；
# sina_main / ths_total 行仍进入补采清单 —— 东财 30 分钟内恢复时可覆盖回补
# （"东财恢复后自动回补"的实现），新浪重采不降级已有数据（019Q QA F9 实证）。
```

### 任务 2：`modules/daily_report.py` L177-178 docstring

`_capital_retry_once` docstring 中"sina_main 行被清单 NOT IN 排除 → 幂等"一句，改为与任务 1 一致的语义描述（sina_main 行仍在补采清单，东财恢复时可覆盖回补）。

## 三、红线

1. **只允许改注释/docstring 文字**，任何可执行代码零改动（diff 中不得出现非注释行变更）。
2. 改动后 `python -m py_compile` 两文件通过；`python -m pytest tests/` 343 项全过。
3. 不动生产数据库，不发起网络请求。

## 四、验收标准

1. 两处文字与实际 SQL 语义一致（sina_main 行仍进补采清单）。
2. diff 仅含注释/docstring 行。
3. 编译 + pytest 回归通过。
4. 开发报告输出到 `docs/reports/`（改动清单 + diff 摘要 + 自验结果）。

## 五、流程路径

```
Step 1: ✅ PM 签发（2026-08-09）
Step 2: ⏭️ 架构师评审——监理裁定跳过（2026-08-09）
Step 3: ✅ 监理批准（2026-08-09）
Step 4: ✅ 开发执行 + 自验（2026-08-09，自验报告 docs/reports/dev_selftest_019Q-S1_comment_fix_20260809.md，PM 抽查通过）
Step 5: ✅ QA 抽查通过（2026-08-09，验收标准 4/4，报告 docs/reports/qa_accept_019Q-S1_comment_fix_20260809.md）→ PM+QA 双签（2026-08-09）→ 关闭
```

## 六、PM 备注

1. 本任务是 019Q 观察项 1 的裁定落实，背景与证据见 `docs/reports/qa_accept_019Q_sina_capital_fallback_20260809.md` 第三节观察项 1。
2. 019Q 主任务书已同步修订该处语义（任务 3 补采清单行），见 `docs/tasks/dev_tasks_20260809_019Q_sina_capital_fallback.md`。

---

## 七、收尾记录（2026-08-09）

| 项 | 内容 |
|---|---|
| QA 抽查 | ✅ 通过（验收标准 4/4，pytest 343/343 独立复跑，旧文案全仓零残留）；报告 `docs/reports/qa_accept_019Q-S1_comment_fix_20260809.md` |
| O-1（低危） | 知情登记：注释为简化表述，与 SQL 真值方向零矛盾零误导，无需整改 |
| PM 双签 | ✅ PM 核对抽查报告覆盖度，确认 4 条验收标准逐项对应，签署同意（2026-08-09） |
| 关闭 | ✅ 019Q-S1 关闭（2026-08-09），019Q 系列全部完结 |
