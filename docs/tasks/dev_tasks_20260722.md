# 开发任务书（2026-07-22 签发·紧急）

| 项目 | 内容 |
|---|---|
| **文档编号** | DEV-TASKS-20260722 |
| **签发人** | AI 产品经理 |
| **签发日期** | 2026-07-22 |
| **执行方** | 开发（GLM） |
| **监理方** | 用户（监理） |
| **批准方** | 监理已于 2026-07-22 批准全部方案 |
| **需求基线** | `docs/requirements_v1.1.md`（唯一权威，v1.1） |
| **最高约束** | 零代码用户可独立运行（`python app.py` 一键启动不变，禁止引入需用户手动配置的依赖） |
| **状态** | 待验收 |

---

## 任务总览

| 任务ID | 任务名称 | 优先级 | 预估工时 | 前置依赖 | 依据文档 |
|---|---|---|---|---|---|
| FIX-A | 日报流程集成数据采集（方案A） | P0 紧急 | 0.5人天 | 无 | review_capital_33pct + review_legacy_zero |
| FIX-B | THS容错增强（方案B） | P0 | 1人天 | FIX-A | review_capital_33pct §四方案B |
| ISSUE-1 | RATING_LEGACY_MAP C/D映射修复 | P1 | 0.1人天 | 无 | review_code_rating_m8 §1.2 |
| ISSUE-2 | 看板筛选器旧字母档位修复 | P1 | 0.1人天 | 无 | review_code_rating_m8 §1.3 |

**启动建议**：FIX-A 与 ISSUE-1/2 可并行；FIX-B 紧随 FIX-A。

---

## 任务卡 1：日报流程集成数据采集（FIX-A）

| 字段 | 内容 |
|---|---|
| **任务ID** | FIX-A |
| **优先级** | P0 紧急 |
| **依据文档** | `docs/reviews/review_capital_33pct_20260722.md` §四方案A + `docs/reviews/review_legacy_zero_20260722.md` §四方案A |
| **根因** | 日报 `generate_daily_report()` 循环内直接调用 `generate_advice(stock_id)`，**不触发任何数据采集**。导致：① 资金面数据不采集（capital成功率3.8%）；② 日报生成先于外部采集完成→后半段股票DB无数据→0分 |
| **核心动作** | 在 `daily_report.py` `generate_daily_report()` 中做两处改动 |

### 改动1：循环前批量预取A股资金面

```python
# === 循环前：批量预取A股资金面（新增）===
from modules.data_collector import collect_stock_data, fetch_capital_flow_batch

a_symbols = [s['symbol'] for s in stocks if s['market'] == 'a_stock']
if a_symbols:
    try:
        batch_result = fetch_capital_flow_batch(a_symbols)
        logger.info(f'[日报] 资金面批量预取: {batch_result}')
    except Exception as e:
        logger.warning(f'[日报] 资金面批量预取失败(不阻断): {e}')
```

### 改动2：循环内每只股票先采集后分析

```python
for stock in stocks:
    stock_id = stock['id']
    symbol = stock['symbol']
    market = stock['market']
    try:
        collect_stock_data(symbol, market)      # 新增：采集
        advice = generate_advice(stock_id)       # 原有：分析
```

### 涉及文件

- `modules/daily_report.py`（`generate_daily_report()` 函数，约 L335-342 区域）

### 验收标准

| # | 标准 | 核验方法 |
|---|---|---|
| 1 | 日报生成时自动触发数据采集 | 日志含 `[日报] 资金面批量预取` 记录 |
| 2 | capital data_status 成功率≥95%（26只中≥25只success） | QA SQL核验（tc_data_quality TC-DQ-L1-01） |
| 3 | 经典引擎股票不再出现"有数据却0分" | QA核验（tc_data_quality TC-DQ-L3-01~10） |
| 4 | 26只全量日报无"四维全空"股票 | QA核验（tc_data_quality TC-DQ-L3-17） |
| 5 | 零代码约束不变 | `python app.py` 一键启动 |
| 6 | 防覆盖机制不破坏 | 不改动 data_collector.py L1091/L1225 |
| 7 | 无估算值污染 | 三处 `if False` 估算源不恢复 |

### 红线核验

| 红线 | 状态 |
|---|---|
| ① 零代码约束 | ✅ 仅调用已有函数，不引入新依赖 |
| ② 需求基线 | ✅ §2.4.1 要求"用户手动触发更新后生成报告"，方案使日报自带采集 |
| ③ v5数据契约 | ✅ 无影响 |
| ④ 禁用估算值 | ✅ 采集走真实数据源 |
| ⑤ 防覆盖机制 | ✅ collect_stock_data 内部已有防覆盖逻辑 |
| ⑥ M8→M9顺序 | ✅ 无影响 |
| ⑦ A/H双市场独立 | ✅ collect_stock_data 已按 market 分流 |

---

## 任务卡 2：THS容错增强（FIX-B）

| 字段 | 内容 |
|---|---|
| **任务ID** | FIX-B |
| **优先级** | P0 |
| **依据文档** | `docs/reviews/review_capital_33pct_20260722.md` §四方案B |
| **根因** | THS 失败时（`_fetch_capital_flow_ths_batch()` 返回 None），仅记录 warning 并返回 0 成功，无重试无备选 |
| **核心动作** | 1. THS 失败时重试 1 次（间隔 5 秒）<br>2. 记录 THS 连续失败次数，达阈值时自动切换为 EM 逐只<br>3. akshare 备选接口：`ak.stock_fund_flow_individual()` 不可用时，尝试 `ak.stock_individual_fund_flow_rank()`（按涨幅/主力净额排名，含全市场） |

### 涉及文件

- `modules/data_collector.py`（`_fetch_capital_flow_ths_batch()` 函数区域）

### 验收标准

| # | 标准 |
|---|---|
| 1 | THS 失败时自动重试 1 次 |
| 2 | 重试仍失败时切换 EM 逐只 |
| 3 | 零代码约束不变（仅用 akshare 已有接口） |

---

## 任务卡 3：RATING_LEGACY_MAP 映射修复（ISSUE-1）

| 字段 | 内容 |
|---|---|
| **任务ID** | ISSUE-1 |
| **优先级** | P1 |
| **依据文档** | `docs/reviews/review_code_rating_m8_20260722.md` §1.2 |
| **问题** | `RATING_LEGACY_MAP` 中 C→持有观望（应为建议减仓）、D→建议减仓（应为强烈建议卖出），各偏移一档 |
| **涉及文件** | `modules/scoring_engine.py`（L67-73 `RATING_LEGACY_MAP`） |

### 修复内容

```python
# 修复前
RATING_LEGACY_MAP = {
    'A': '强烈推荐买入',
    'B+': '推荐买入',
    'B': '持有观望',
    'C': '持有观望',  # ← 错误
    'D': '建议减仓',  # ← 错误
}

# 修复后
RATING_LEGACY_MAP = {
    'A': '强烈推荐买入',
    'B+': '推荐买入',
    'B': '持有观望',
    'C': '建议减仓',  # ← 修正
    'D': '强烈建议卖出',  # ← 修正
}
```

### 验收标准

| # | 标准 | 核验方法 |
|---|---|---|
| 1 | C→建议减仓 | QA边界测试（tc_rating_align_004） |
| 2 | D→强烈建议卖出 | QA边界测试 |
| 3 | 有total_score的记录走精确映射路径，不受影响 | 回归测试 |

---

## 任务卡 4：看板筛选器旧字母档位修复（ISSUE-2）

| 字段 | 内容 |
|---|---|
| **任务ID** | ISSUE-2 |
| **优先级** | P1 |
| **依据文档** | `docs/reviews/review_code_rating_m8_20260722.md` §1.3 |
| **问题** | `templates/index.html` L4042 看板评级筛选器仍使用旧字母代码（B/C/D），新数据评级为中文，筛选无法命中 |
| **涉及文件** | `templates/index.html`（L4042 附近） |

### 修复内容

```html
<!-- 修复前 -->
<option value="B">B (谨慎买入)</option>
<option value="C">C (持有观望)</option>
<option value="D">D (减仓/卖出)</option>

<!-- 修复后 -->
<option value="强烈推荐买入">强烈推荐买入</option>
<option value="推荐买入">推荐买入</option>
<option value="持有观望">持有观望</option>
<option value="建议减仓">建议减仓</option>
<option value="强烈建议卖出">强烈建议卖出</option>
```

### 验收标准

| # | 标准 | 核验方法 |
|---|---|---|
| 1 | 筛选器显示中文5档 | QA前端测试 |
| 2 | 筛选"持有观望"能命中所有持有观望评级股票 | QA功能测试 |

---

## 开发方执行要求

1. **需求基线唯一权威**：任何实现偏差必须对照 `docs/requirements_v1.1.md`
2. **零代码用户最高优先级**：保持 `pip install -r requirements.txt` + `python app.py` 一键启动不变
3. **验收留痕**：每个任务完成后，在 `reports/` 目录生成对应日期的验收报告
4. **任务领用确认**：开发方领用任务后，请在本文件"状态"字段更新为"开发中"，完成后更新为"待验收"
5. **回归范围**：26只全量 batch-analyze、data_status 四维度统计、v5 评分一致性（score_diff=0）

---

## 验收流转

```
开发完成自验 → QA执行测试用例（181条）→ QA出具测试报告
    ↓
产品经理+QA双签验收 → 监理批准关闭
    ↓
RATING-ALIGN-004 + 数据修复 → 验收通过
    ↓
M8-BACKTEST-003 在数据可靠基础上重新全量回测 → 验收
```

---

**文档版本**：v1.0 | **编制日期**：2026-07-22 | **编制人**：AI 产品经理
