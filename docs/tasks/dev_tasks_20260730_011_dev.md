# DEV-TASKS-20260730-011-DEV：011 数据采集全链路增量优化 — 开发任务书

> **签发人**：PM  | **签发日期**：2026-07-30 | **状态**：待开发执行

---

## 角色定义（内嵌，无需额外窗口提示词）

### 你的角色：开发人员

**职责边界**：
- 按本任务书规格实现增量采集优化，完成编码+自验
- 不负责正式验收（QA 独立验收）
- 不修改评分引擎/advisor/红线区域
- 交付物：修改后的代码 + 自验报告 `reports/dev_selftest_011_incremental_20260730.md`

### 独立性原则
- 各角色独立不兼职：PM 不兼架构、架构师不编码、开发不验收、QA 独立测试
- 开发人员仅做编码+自验，不执行正式验收

### 项目背景摘要
| 项 | 内容 |
|---|---|
| 项目路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格，命令行需引号） |
| 数据库路径 | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst\stock_analyst.db`（在stock_analyst子目录内！） |
| Python 解释器 | `C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe` |
| 技术栈 | Python + Flask + SQLite + akshare + Jinja2 单页应用 |
| 最高约束 | **零代码用户可独立运行**：无新 pip 依赖（当前8包） |
| PowerShell | 不支持 `&&`，用 `;` 代替；多行Python须写临时.py文件执行 |

---

## 执行信息（PM 标注）

| 项 | 内容 |
|---|---|
| 任务类型 | 开发编码 + 自验 |
| 推荐模型 | **glm5.2** |
| 窗口类型 | **Quests 独立窗口** |
| 执行模式 | 单代理 agent |
| 交付物 | 修改后的代码文件 + `reports/dev_selftest_011_incremental_20260730.md` |
| 架构评审参考 | `docs/reviews/review_011_incremental_collection_20260730.md` |

---

## 一、需求背景

每次分析都全量重复采集已有数据，造成大量无效网络请求。架构师已评审通过增量优化方案（5个决策点全部裁定），本任务书为开发实现规格。

### 1.1 架构师裁定总结

| 决策点 | 裁定结果 |
|---|---|
| DP-1 K线 | **同日跳过 + 全量覆盖**（否决from增量，防止复权因子漂移） |
| DP-2 基本面 | **80天财报TTL + 24h PE/PB TTL，双门控独立** |
| DP-3 消息面 | **当日跳过**（增量保留，不因权重0%禁用） |
| DP-4 北向资金 | **30天缓存 + config.py配置项** |
| DP-5 全量刷新 | **仅API入口 + force_full参数透传**（否决全局开关+新字段） |

---

## 二、实现规格（按执行顺序）

### 2.1 第一步：config.py 新增配置项

**文件**：`config.py`
**位置**：L22 `KLINE_DAYS = 250` 之后

```python
# 011：增量采集配置
# 北向资金数据源已停更（2024-08-16），缓存有效期（天）
NORTH_CAPITAL_CACHE_DAYS = 30
# 基本面财报数据TTL（天），季度级更新（80天≈一个财报周期）
FUNDAMENTAL_REPORT_TTL_DAYS = 80
# PE/PB实时估值TTL（小时）
PE_PB_CACHE_TTL_HOURS = 24
```

**同步修改** `data_collector.py` L144 的 import：
```python
# 原：
from config import KLINE_DAYS, MAX_RETRIES

# 改为：
from config import (
    KLINE_DAYS,
    MAX_RETRIES,
    NORTH_CAPITAL_CACHE_DAYS,
    FUNDAMENTAL_REPORT_TTL_DAYS,
    PE_PB_CACHE_TTL_HOURS,
)
```

---

### 2.2 第二步：fetch_kline 增加同日跳过（DP-1）

**文件**：`modules/data_collector.py`
**函数**：`fetch_kline`（L400）
**签名变更**：`fetch_kline(symbol, market, force_full=False)`

**实现逻辑**（在 L408 `try:` 之后、L410 `df = _fetch_kline_tencent` 之前插入）：

```python
def fetch_kline(symbol, market, force_full=False):
    """
    采集K线数据并存入数据库。A股和港股统一使用此函数。
    011增量优化：同日跳过（last_date >= 今日 → 跳过），全量覆盖确保复权因子一致。
    返回: (状态字符串, 消息)
    """
    stock_id = get_stock_id(symbol, market)
    if not stock_id:
        return 'failed', f'数据库中未找到股票 {symbol}'

    # 011增量：同日跳过检查
    if not force_full:
        try:
            conn_chk = get_connection()
            cursor_chk = conn_chk.cursor()
            cursor_chk.execute(
                'SELECT MAX(trade_date) as last_date FROM raw_kline WHERE stock_id = ?', (stock_id,)
            )
            row = cursor_chk.fetchone()
            conn_chk.close()
            if row and row['last_date']:
                last_date = str(row['last_date'])[:10]
                today_str = datetime.now(_CN_TZ).strftime('%Y-%m-%d')
                if last_date >= today_str:
                    skip_msg = f'同日跳过(K线已有{last_date}数据)'
                    save_data_status(stock_id, 'kline', 'success', skip_msg)
                    logger.info(f'[{symbol}] {skip_msg}')
                    return 'success', skip_msg
        except Exception as e:
            logger.warning(f'[{symbol}] K线增量检查异常(降级为全量): {e}')

    # 以下保持现有全量采集逻辑不变 ...
```

**关键约束**：
- **不修改** `_fetch_kline_tencent` 函数（L353-397），保持全量250天获取
- **不修改** 现有的 INSERT OR REPLACE 写入逻辑（L419-440）
- `force_full=True` 时跳过增量检查，直接全量采集
- force_full 触发的 `save_data_status` 中 message 加 `[FULL_REFRESH]` 前缀

---

### 2.3 第三步：fetch_a_fundamental 双门控增量（DP-2）

**文件**：`modules/data_collector.py`
**函数**：`fetch_a_fundamental`（L467）
**签名变更**：`fetch_a_fundamental(symbol, force_full=False)`

**实现逻辑**（在 L472 `warnings = []` 之前插入门控检查）：

```python
def fetch_a_fundamental(symbol, force_full=False):
    """采集A股基本面数据：财务指标 + PE/PB估值
    011增量：80天财报TTL + 24h PE/PB TTL，双门控独立。
    """
    stock_id = get_stock_id(symbol, 'a_stock')
    if not stock_id:
        return 'failed', f'数据库中未找到A股 {symbol}'

    # 011增量门控
    skip_financial = False  # 是否跳过财报采集
    skip_pepb = False  # 是否跳过PE/PB采集

    if not force_full:
        # 门控A：财报数据TTL（80天）
        try:
            conn_chk = get_connection()
            cursor_chk = conn_chk.cursor()
            cursor_chk.execute(
                'SELECT MAX(report_date) as last_report FROM raw_fundamental WHERE stock_id = ?',
                (stock_id,),
            )
            row = cursor_chk.fetchone()
            if row and row['last_report']:
                last_report_date = str(row['last_report'])[:10]
                days_since = (
                    datetime.now(_CN_TZ) - datetime.strptime(last_report_date, '%Y-%m-%d')
                ).days
                if days_since < FUNDAMENTAL_REPORT_TTL_DAYS:
                    skip_financial = True
                    logger.info(f'[A股 {symbol}] 财报数据{days_since}天内，跳过财报采集')

                    # 门控B：PE/PB TTL（24h），仅当财报跳过时检查
                    cursor_chk.execute(
                        """SELECT fetched_at FROM data_status
                           WHERE stock_id = ? AND dimension = 'fundamental'
                           ORDER BY fetched_at DESC LIMIT 1""",
                        (stock_id,),
                    )
                    status_row = cursor_chk.fetchone()
                    if status_row and status_row['fetched_at']:
                        last_fetch = datetime.strptime(
                            str(status_row['fetched_at'])[:19], '%Y-%m-%d %H:%M:%S'
                        )
                        # 转为北京时间比较
                        hours_since = (
                            datetime.now(_CN_TZ).replace(tzinfo=None) - last_fetch
                        ).total_seconds() / 3600
                        if hours_since < PE_PB_CACHE_TTL_HOURS:
                            skip_pepb = True
            conn_chk.close()
        except Exception as e:
            logger.warning(f'[A股 {symbol}] 基本面增量检查异常(降级为全量): {e}')

    # 两门控都跳过 → 整体跳过
    if skip_financial and skip_pepb:
        skip_msg = '同日跳过(财报80天TTL内+PE/PB 24h内)'
        save_data_status(stock_id, 'fundamental', 'success', skip_msg)
        return 'success', skip_msg

    # 以下为现有逻辑，但需用 skip_financial / skip_pepb 控制执行 ...
```

**现有代码修改要点**：

1. **L476-531 财报采集块**：用 `if not skip_financial:` 包裹
   - 即 `_fetch_a_fundamental_sina` 调用 + INSERT OR REPLACE 写入

2. **L533-568 PE/PB采集块**：用 `if not skip_pepb:` 包裹
   - 即 `_fetch_valuation_tencent` 调用 + UPDATE 写入

3. **返回值逻辑**（L570-578）：调整条件判断
   - 当 skip_financial=True 且 skip_pepb=False（仅采集了PE/PB）→ 返回 partial
   - 当 skip_financial=False → 保持原有 success/partial/failed 三档

---

### 2.4 第四步：fetch_hk_fundamental 增量（DP-2 港股版）

**文件**：`modules/data_collector.py`
**函数**：`fetch_hk_fundamental`（L761）
**签名变更**：`fetch_hk_fundamental(symbol, force_full=False)`

**实现逻辑**：与 A股版门控A相同（80天财报TTL），但港股无独立PE/PB门控（港股PE/PB与财报一并采集）。

```python
def fetch_hk_fundamental(symbol, force_full=False):
    """采集港股基本面数据
    011增量：80天财报TTL。
    """
    stock_id = get_stock_id(symbol, 'hk_stock')
    if not stock_id:
        return 'failed', f'数据库中未找到港股 {symbol}'

    # 011增量：80天门控
    if not force_full:
        try:
            conn_chk = get_connection()
            cursor_chk = conn_chk.cursor()
            cursor_chk.execute(
                'SELECT MAX(report_date) as last_report FROM raw_fundamental WHERE stock_id = ?',
                (stock_id,),
            )
            row = cursor_chk.fetchone()
            conn_chk.close()
            if row and row['last_report']:
                last_report_date = str(row['last_report'])[:10]
                days_since = (
                    datetime.now(_CN_TZ) - datetime.strptime(last_report_date, '%Y-%m-%d')
                ).days
                if days_since < FUNDAMENTAL_REPORT_TTL_DAYS:
                    skip_msg = f'同日跳过(港股财报{days_since}天内)'
                    save_data_status(stock_id, 'fundamental', 'success', skip_msg)
                    return 'success', skip_msg
        except Exception as e:
            logger.warning(f'[港股 {symbol}] 基本面增量检查异常(降级为全量): {e}')

    # 以下保持现有逻辑 ...
```

---

### 2.5 第五步：fetch_sentiment 增加当日跳过（DP-3）

**文件**：`modules/data_collector.py`
**函数**：`fetch_sentiment`（L2061）
**签名变更**：`fetch_sentiment(symbol, market, force_full=False)`

**实现逻辑**（在 L2066 stock_id 检查之后、L2072 `try:` 之前插入）：

```python
def fetch_sentiment(symbol, market, force_full=False):
    """
    采集消息面数据（模块4接入）。
    011增量：当日已有 → 跳过。
    """
    stock_id = get_stock_id(symbol, market)
    if not stock_id:
        return 'failed', f'数据库中未找到股票 {symbol}'

    # 011增量：当日跳过
    if not force_full:
        try:
            today_str = datetime.now(_CN_TZ).strftime('%Y-%m-%d')
            conn_chk = get_connection()
            cursor_chk = conn_chk.cursor()
            cursor_chk.execute(
                """SELECT COUNT(*) as cnt FROM news_sentiment
                   WHERE stock_id = ? AND analysis_date LIKE ?""",
                (stock_id, today_str + '%'),
            )
            row = cursor_chk.fetchone()
            conn_chk.close()
            if row and row['cnt'] > 0:
                skip_msg = f'当日跳过(消息面已有{row["cnt"]}条记录)'
                save_data_status(stock_id, 'sentiment', 'success', skip_msg)
                logger.info(f'[{symbol}] {skip_msg}')
                return 'success', skip_msg
        except Exception as e:
            logger.warning(f'[{symbol}] 消息面增量检查异常(降级为全量): {e}')

    market_name = 'A股' if market == 'a_stock' else '港股'
    # 以下保持现有逻辑 ...
```

**注意**：检查放在 `fetch_sentiment` 中（而非 `collect_news` 中），因为 `collect_news` 可能有其他调用路径。

---

### 2.6 第六步：fetch_north_capital 增加缓存检查（DP-4）

**文件**：`modules/data_collector.py`
**函数**：`fetch_north_capital`（L1803）
**签名变更**：`fetch_north_capital(symbol, market, force_full=False)`

**实现逻辑**（在 L1814 `return 'skipped'` 之后、L1820 `try:` 之前插入）：

```python
def fetch_north_capital(symbol, market, force_full=False):
    """
    采集北向资金净买入数据（DATASRC-C 子任务2.1）。
    011增量：30天缓存（数据源自2024-08-16停更）。
    """
    if market != 'a_stock':
        return 'skipped', '北向资金仅A股标的'

    stock_id = get_stock_id(symbol, 'a_stock')
    if not stock_id:
        return 'failed', f'数据库中未找到A股 {symbol}'

    # 011增量：30天缓存检查
    if not force_full:
        try:
            conn_chk = get_connection()
            cursor_chk = conn_chk.cursor()
            cursor_chk.execute(
                """SELECT fetched_at FROM data_status
                   WHERE stock_id = ? AND dimension = ?
                   ORDER BY fetched_at DESC LIMIT 1""",
                (stock_id, 'north_capital'),
            )
            row = cursor_chk.fetchone()
            conn_chk.close()
            if row and row['fetched_at']:
                last_fetch = datetime.strptime(str(row['fetched_at'])[:19], '%Y-%m-%d %H:%M:%S')
                days_since = (datetime.now(_CN_TZ).replace(tzinfo=None) - last_fetch).days
                if days_since < NORTH_CAPITAL_CACHE_DAYS:
                    skip_msg = f'北向资金缓存有效({days_since}天/{NORTH_CAPITAL_CACHE_DAYS}天)'
                    logger.info(f'[DATASRC-C] {symbol} {skip_msg}')
                    return 'skipped', skip_msg
        except Exception as e:
            logger.warning(f'[DATASRC-C] {symbol} 北向资金缓存检查异常(降级): {e}')

    # 以下保持现有逻辑 ...
```

**注意**：当前代码中 `fetch_north_capital` 的成功/失败状态是通过 `collect_stock_data` 的 `results['north_capital']` 记录的，**但未调用 `save_data_status`**。需要补充：
- 成功时：`save_data_status(stock_id, 'north_capital', 'success', ...)`
- 失败时：`save_data_status(stock_id, 'north_capital', 'failed', ...)`
- 跳过时：`save_data_status(stock_id, 'north_capital', 'success', skip_msg)` ← 缓存有效也算success

---

### 2.7 第七步：fetch_margin_balance 增量补取（011-E 部分）

**文件**：`modules/data_collector.py`
**函数**：`fetch_margin_balance`（L1937）
**签名变更**：`fetch_margin_balance(symbol, market, force_full=False)`

**实现逻辑**（修改 L1961-1968 的日期范围计算）：

```python
def fetch_margin_balance(symbol, market, force_full=False):
    """
    采集融资余额数据。
    011增量：有数据时仅补近期1-10天；无数据时保持全量回填。
    """
    # ... 前置检查不变 ...

    try:
        logger.info(f'[DATASRC-C] 融资余额采集: {symbol}')
        is_sse = symbol.startswith('6') or symbol.startswith('9')
        today = datetime.now(_CN_TZ)

        # 011增量：确定起始日期
        if not force_full:
            conn_chk = get_connection()
            cursor_chk = conn_chk.cursor()
            cursor_chk.execute(
                '''SELECT MAX(trade_date) as last_margin_date FROM raw_capital_flow
                   WHERE stock_id = ? AND margin_balance IS NOT NULL''',
                (stock_id,)
            )
            chk_row = cursor_chk.fetchone()
            conn_chk.close()
            if chk_row and chk_row['last_margin_date']:
                last_margin = datetime.strptime(str(chk_row['last_margin_date'])[:10], '%Y-%m-%d')
                # 从上次有数据的次日开始，仅补近期（上限15天防止遗漏）
                days_to_try_max = min(15, (today - last_margin).days + 2)
                if days_to_try_max <= 0:
                    return 'skipped', '融资余额已是最新'
            else:
                days_to_try_max = 159  # 无数据时保持全量
        else:
            days_to_try_max = 159  # force_full时全量

        dates_to_try = []
        for delta in range(1, days_to_try_max + 1):
            d = today - timedelta(days=delta)
            if d.weekday() < 5:
                dates_to_try.append(d.strftime('%Y%m%d'))
        # 以下保持现有逐日匹配+写入逻辑 ...
```

---

### 2.8 第八步：collect_stock_data 增加 force_full 参数（DP-5 核心）

**文件**：`modules/data_collector.py`
**函数**：`collect_stock_data`（L2147）
**签名变更**：`collect_stock_data(symbol, market, force_full=False)`

**修改要点**：所有子函数调用都需传入 `force_full`：

```python
def collect_stock_data(symbol, market, force_full=False):
    """对外统一接口。011增加 force_full 参数透传。"""
    results = {}
    market_name = 'A股' if market == 'a_stock' else '港股'
    ff = '[FULL_REFRESH] ' if force_full else ''

    logger.info(
        f'========== 开始采集{market_name} {symbol} {"[强制全量]" if force_full else ""} =========='
    )

    # K线
    results['kline'] = fetch_kline(symbol, market, force_full=force_full)

    # 基本面
    if market == 'a_stock':
        results['fundamental'] = fetch_a_fundamental(symbol, force_full=force_full)
        # B10补全块也需检查 force_full
        if not force_full:
            # 保持现有 B10 去重+补全逻辑（L2170-2205）
            ...
        else:
            # force_full时跳过B10去重检查，直接补全
            ...
        # 股东增减持
        ...
    else:
        results['fundamental'] = fetch_hk_fundamental(symbol, force_full=force_full)

    # 资金面
    results['capital'] = fetch_capital_flow(symbol, market)  # 已有同日跳过，force_full时也需绕过
    if force_full:
        # 资金面同日跳过绕过逻辑：注意L1464-1505的前置校验层
        # 需要在 fetch_capital_flow 也增加 force_full 参数
        # 但架构师评审说"资金面不需要改动"
        # PM决定：资金面的同日跳过逻辑由其内部处理，force_full仅影响其他维度
        # 理由：资金面同日跳过是基于"已有真实数据"而非缓存，即使force_full也不应重复采集
        pass

    # 北向资金 + 融资余额
    if market == 'a_stock':
        results['north_capital'] = fetch_north_capital(symbol, market, force_full=force_full)
        results['margin_balance'] = fetch_margin_balance(symbol, market, force_full=force_full)

    # 消息面
    results['sentiment'] = fetch_sentiment(symbol, market, force_full=force_full)

    logger.info(f'========== {symbol} 数据采集完成 ==========')
    return results
```

**关键决策（PM 裁定）**：资金面 `fetch_capital_flow` **不增加 force_full 参数**。理由：资金面同日跳过基于"已有真实数据"（L1464-1505），即使强制刷新也不应重复采集同日数据（同日资金流不会变化）。

---

### 2.9 第九步：app.py 新增 /refresh-full API（DP-5）

**文件**：`app.py`
**位置**：在 L754 `api_analyze_stock` 路由之后新增

```python
@app.route('/api/stocks/<int:stock_id>/refresh-full', methods=['POST'])
def api_refresh_full(stock_id):
    """011：强制全量刷新数据 + 重新分析。
    绕过所有增量缓存，重新采集全部维度数据。
    """
    from modules.data_collector import collect_stock_data
    from modules.advisor import generate_advice

    conn = get_connection()
    cursor = conn.cursor()
    cursor.execute('SELECT symbol, name, market FROM stocks WHERE id = ?', (stock_id,))
    stock = cursor.fetchone()
    conn.close()

    if not stock:
        return jsonify({'success': False, 'message': '股票不存在'}), 404

    symbol = stock['symbol']
    market = stock['market']

    try:
        # 步骤1：强制全量采集
        collect_stock_data(symbol, market, force_full=True)

        # 步骤2：重新分析
        result = generate_advice(stock_id)
        if result.get('success'):
            from modules.price_advisor import generate_price_advice

            result['price_advice'] = generate_price_advice(stock_id, result)
            if result.get('price_advice', {}).get('action_suggestion'):
                result['position_advice'] = result['price_advice']['action_suggestion']

        return jsonify(result)
    except Exception as e:
        return jsonify({'success': False, 'message': f'全量刷新失败: {str(e)}'}), 500
```

---

### 2.10 第十步：data_status 去重优化（PM 纳入的可选项 → 011-F）

**文件**：`modules/data_collector.py`
**函数**：`save_data_status`（L261）

**当前问题**：每次 INSERT 新行，导致 data_status 表 2829 条记录中大量同日重复。

**修改方案**：改为"先删后插"模式（避免 ALTER TABLE 加唯一约束的迁移成本）：

```python
def save_data_status(stock_id, dimension, status, message=''):
    """记录数据采集状态到数据库。
    011优化：同维度同日只保留最新一条（先删后插）。
    """
    ts = now_cn()
    today_prefix = ts[:10]  # YYYY-MM-DD
    conn = get_connection()
    cursor = conn.cursor()
    # 011：删除同维度同日的旧记录，仅保留最新一条
    cursor.execute(
        """DELETE FROM data_status
           WHERE stock_id = ? AND dimension = ? AND fetched_at LIKE ?""",
        (stock_id, dimension, today_prefix + '%'),
    )
    cursor.execute(
        """
        INSERT INTO data_status (stock_id, dimension, status, message, fetched_at)
        VALUES (?, ?, ?, ?, ?)
    """,
        (stock_id, dimension, status, message, ts),
    )
    conn.commit()
    conn.close()
```

**优势**：无需 ALTER TABLE（零代码用户友好），每日仅保留最新一条状态，data_status 增速降低 ~90%。

---

## 三、红线清单（绝对不可触碰）

| 红线 | 位置 | 约束 |
|---|---|---|
| `data_collector.py` L1645 `if False` | 腾讯估算源 | **不可修改** |
| `data_collector.py` L1684 `if False` | 新浪估算源 | **不可修改** |
| `data_collector.py` L1717 `if False` | 网易估算源 | **不可修改** |
| `advisor.py` `generate_advice` (L869) | 建议生成主入口 | **签名和函数体不可修改** |
| `advisor.py` `_build_capital_factors` (L785) | 资金面因子构建 | **不可修改** |
| `scoring_engine.py` | 四维评分引擎 | **不可修改** |
| `config_weights.json` | 权重配置 | **rating_mapping 80/65/50/30 不可修改** |
| 零代码约束 | requirements.txt | **无新 pip 依赖（当前8包）** |
| `fetch_capital_flow` (L1446) | 资金面采集 | **不加 force_full 参数**（已有同日跳过，不改动） |

---

## 四、验收标准（开发自验项）

### 4.1 功能自验清单

| # | 验证项 | 验证方法 | 预期结果 |
|---|---|---|---|
| V1 | K线同日跳过 | 同一只股票连续两次分析，检查 data_status | 第二次返回 'success' + 跳过消息 |
| V2 | 基本面80天门控 | 查DB中 raw_fundamental 最新 report_date 距今 <80天 → 分析 | 财报采集被跳过 |
| V3 | PE/PB 24h门控 | V2通过后检查 PE/PB 是否在24h内跳过 | PE/PB也被跳过 |
| V4 | 消息面当日跳过 | 同一只股票当日二次分析 | 消息面采集被跳过 |
| V5 | 北向资金30天缓存 | 检查 data_status 中 north_capital 最新记录 | 30天内跳过 |
| V6 | 融资余额增量 | 查DB已有 margin_balance 数据 → 分析 | dates_to_try 数量 ≤ 15 |
| V7 | force_full 参数 | 调用 `collect_stock_data(symbol, market, force_full=True)` | 全部维度全量采集，不跳过 |
| V8 | /refresh-full API | POST `/api/stocks/<id>/refresh-full` | 返回 success + 重新分析结果 |
| V9 | data_status 去重 | 同一维度同日多次采集后查 data_status | 同维度同日仅1条记录 |
| V10 | 首次分析兜底 | 新增一只股票后首次分析 | 全量采集，不跳过 |

### 4.2 不回归验证

| # | 验证项 | 方法 |
|---|---|---|
| R1 | 评分结果不变 | 同一只股票增量优化前后，total_score / rating 一致（用 monkey-patch 同数据对比法） |
| R2 | 红线文件未被修改 | git diff 确认 scoring_engine.py / advisor.py / config_weights.json 无变更 |
| R3 | 无新 pip 依赖 | requirements.txt 行数不变（8包） |
| R4 | 资金面采集逻辑不变 | fetch_capital_flow 函数签名和内部逻辑无修改 |

### 4.3 自验报告格式

自验报告 `reports/dev_selftest_011_incremental_20260730.md` 需包含：
1. 每个验证项（V1-V10）的实际执行结果+截图/日志
2. 不回归验证（R1-R4）的检查结果
3. 修改文件清单 + 行数统计
4. 已知问题/待确认项

---

## 五、修改文件清单

| 文件 | 改动内容 | 预估改动量 |
|---|---|---|
| `config.py` | 新增3个配置项 | ~6行 |
| `modules/data_collector.py` | 7个函数增加增量检查 + force_full 参数 + save_data_status 去重 | ~180行新增/修改 |
| `app.py` | 新增 /refresh-full 路由 | ~30行 |
| `reports/dev_selftest_011_incremental_20260730.md` | 自验报告 | ~100行 |
| **合计** | | ~316行 |

---

## 六、参考资料

| 文件 | 用途 |
|---|---|
| `docs/reviews/review_011_incremental_collection_20260730.md` | 架构师评审报告（506行，5决策点裁定） |
| `docs/tasks/dev_tasks_20260730_011_arch.md` | 架构评审任务书（原始需求+5决策点） |
| `modules/data_collector.py` L400 | fetch_kline |
| `modules/data_collector.py` L467 | fetch_a_fundamental |
| `modules/data_collector.py` L761 | fetch_hk_fundamental |
| `modules/data_collector.py` L1446 | fetch_capital_flow（不改） |
| `modules/data_collector.py` L1803 | fetch_north_capital |
| `modules/data_collector.py` L1937 | fetch_margin_balance |
| `modules/data_collector.py` L2061 | fetch_sentiment |
| `modules/data_collector.py` L2147 | collect_stock_data（统一入口） |
| `modules/data_collector.py` L261 | save_data_status |
| `app.py` L754 | api_analyze_stock（参考路由格式） |
| `config.py` L22 | KLINE_DAYS 配置项参考位置 |

---

> **PM 备注**：本任务书已内嵌角色定义，监理可直接全文粘贴到 Quests 窗口。开发完成后 PM 将进行红线核验 + 交付物完整性检查，QA 在独立窗口做功能验收。
