# QA 验收任务书：超买超卖徽标 + RSI修复（003+004合并验收）

**编号**：QA-TASKS-20260727-003-004
**关联开发任务**：DEV-TASKS-20260727-003（超买超卖徽标）+ DEV-TASKS-20260727-004（RSI修复）
**验收日期**：2026-07-27
**开发自验报告**：
- reports/dev_selftest_rsi_fix_20260727.md（004 RSI修复）
- 003无独立自验报告（PM直接核验代码通过）

---

## 一、任务卡

| 项 | 内容 |
|---|---|
| **任务名** | 超买超卖徽标 + RSI修复——QA独立验收 |
| **推荐模型** | glm5.2 |
| **窗口类型** | Quests（独立窗口） |
| **执行模式** | 单代理 |
| **项目路径** | `C:\Users\zlb19\Desktop\Qoder cn\stock_analyst`（含空格） |
| **Python** | `C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe` |

---

## 二、交付物范围

### 2.1 RSI修复（004）
- 文件：`modules/data_adapter.py` 的 `_calc_rsi` 函数（L123-152）
- 改动：SMA算法 → Wilder平滑算法
- 预期：茅台RSI从71.54→~59，绿的谐波从23.24→~41

### 2.2 超买超卖徽标（003）
- 文件：`templates/index.html`（CSS徽标 + JS渲染）、`app.py`（_derive_obos_signal派生函数）
- 改动：列表页/看板页显示超买⚠️/超卖⚡徽标
- 预期：RSI正确后徽标准确显示（不再误报）

---

## 三、QA独立验收用例

### V1：RSI计算正确性验证

**方法**：用Python独立计算茅台和绿的谐波的RSI

```python
import sys, os

sys.path.insert(0, r'C:\Users\zlb19\Desktop\Qoder cn\stock_analyst')
os.chdir(r'C:\Users\zlb19\Desktop\Qoder cn\stock_analyst')
from modules.data_adapter import load_stockdata_from_db
import sqlite3

DB = r'C:\Users\zlb19\Desktop\Qoder cn\stock_analyst\stock_analyst.db'
conn = sqlite3.connect(DB)
cur = conn.cursor()

for kw, label in [('600519', '贵州茅台'), ('688017', '绿的谐波')]:
    cur.execute('SELECT id FROM stocks WHERE symbol LIKE ?', (f'{kw}%',))
    sid = cur.fetchone()[0]
    data = load_stockdata_from_db(sid)
    print(f'{label}: RSI(14)={data.rsi_14}')
conn.close()
```

| 股票 | 期望RSI(14) | 旧值(应不再出现) | 通过标准 |
|---|---|---|---|
| 贵州茅台 | 55~62 | 71.54 | RSI<70（不再超买误报） |
| 绿的谐波 | 33~42 | 23.24 | RSI>30（不再超卖误报） |

### V2：全量RSI无误报验证

**方法**：对27只股票计算RSI，检查无误报

```python
# 接V1代码
cur.execute('SELECT id, symbol, name FROM stocks ORDER BY id')
for sid, sym, name in cur.fetchall():
    data = load_stockdata_from_db(sid)
    if data and data.rsi_14:
        flag = ''
        if data.rsi_14 > 70:
            flag = '超买!'
        elif data.rsi_14 < 30:
            flag = '超卖!'
        print(f'{sym} {name}: RSI={data.rsi_14} {flag}')
```

**通过标准**：27只股票无RSI>70或<30的误报（除非真实超买超卖）。龙芯中科RSI~31接近边界但不应触发。

### V3：超买超卖徽标前端显示验证

**方法**：
1. `python app.py` 启动服务
2. 浏览器打开 http://127.0.0.1:5000
3. 查看自选股列表页和看板页

**通过标准**：
- 茅台**不再**显示⚠️超买徽标（RSI已从71.54→59，正常）
- 绿的谐波**不再**显示⚡超卖徽标（RSI已从23.24→41，正常）
- 如果有真实RSI>70或<30的股票，应正确显示对应徽标
- 徽标样式：超买=橙色圆角，超卖=蓝色圆角

### V4：红线核验（QA独立执行）

| 红线 | 核验方法 | 通过标准 |
|---|---|---|
| scoring_engine.py 未改 | grep确认score_obos逻辑不变 | 未新增改动 |
| advisor.py 未改 | grep `def generate_advice` | 存在且未改 |
| data_collector.py | grep `if False` L1645/L1684/L1717 | 3处在 |
| config_weights.json | 读a_stock.weights | capital_flow=0.3985（原值） |
| requirements.txt | 计数 | 8个无新增 |
| data_adapter.py改对 | grep `period - 1` | Wilder递推存在 |

### V5：零代码运行验证

**方法**：`python app.py` 启动，浏览器检查页面正常

**通过标准**：服务正常启动，列表页/看板页/个股报告页加载无JS报错。

---

## 四、QA验收输出

请QA出具验收报告（`reports/qa_accept_003_004_20260727.md`），包含：
1. V1-V5 每项执行结果（含输出截图/数据）
2. 红线独立核验结果
3. 总体结论：PASS / FAIL

---

## 五、红线清单

| 红线 | 说明 |
|---|---|
| scoring_engine.py | 不改（RSI正确后评分自动正确） |
| advisor.py | 不改（B24红线） |
| data_collector.py L1645/L1684/L1717 | 不改 |
| config_weights.json | 不改 |
| 无新依赖 | 不引入talib等 |

---

*QA验收完毕后，PM+QA双签，报监理关闭003+004。*
