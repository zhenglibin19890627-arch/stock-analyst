# DEV-TASKS-20260726-B24 任务书

**批次**：B24  
**签发日期**：2026-07-26  
**状态**：待监理批准  
**推荐模型**：glm5.2（GLM）— 前端 UI

---

## 一、任务卡

| 项 | 内容 |
|---|---|
| **任务名** | 前端消息面因子展示增强（B22 新增字段可视化） |
| **优先级** | P2 |
| **预估工时** | 0.5 人天 |
| **背景** | B22 在 StockData 新增了 news_count/news_positive_ratio/news_negative_count 字段，后端 `_build_news_factors`（advisor.py）已输出这些因子，但前端 `_dimFactorLabels.news`（index.html L4651-4655）缺少对应的中文标签，导致这些因子无法在四维详情卡片展示。 |

---

## 二、技术方案

### 修改文件：templates/index.html

#### 修改点1：_factorPriority.news 扩展（L4630）

```javascript
// 原：
news: ['avg_sentiment', 'positive_ratio', 'top_news', 'news_activity', 'extreme_warning']

// 改（新增 news_count 排在前面）：
news: ['avg_sentiment', 'positive_ratio', 'news_count', 'top_news', 'news_activity', 'extreme_warning']
```

#### 修改点2：_dimFactorLabels.news 补充标签（L4651-4655）

```javascript
// 原：
news: {
    avg_sentiment: '平均情绪', news_activity: '新闻活跃度',
    positive_ratio: '正面占比', top_news: '重要新闻',
    extreme_warning: '极端情绪预警'
}

// 改（新增 news_count）：
news: {
    avg_sentiment: '平均情绪', news_activity: '新闻活跃度',
    positive_ratio: '正面占比', top_news: '重要新闻',
    extreme_warning: '极端情绪预警',
    news_count: '新闻数量'    // B24 新增
}
```

### 修复边界

#### ✅ 允许修改
- `templates/index.html` — L4630 `_factorPriority.news` + L4651-4655 `_dimFactorLabels.news`

#### ❌ 禁止修改
- 其他所有文件不可修改

---

## 三、验收标准

| # | 验收项 | 预期 | 核验方法 |
|---|---|---|---|
| V1 | _factorPriority.news 含 news_count | 数组包含 | 代码审查 |
| V2 | _dimFactorLabels.news 含 news_count 标签 | '新闻数量' | 代码审查 |
| V3 | 浏览器四维详情消息面卡片显示新闻数量 | 有值 | 浏览器验证 |

---

**任务书状态：待监理批准**
