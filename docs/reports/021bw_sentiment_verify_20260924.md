# 021BW/021BX 终验报告：情绪维度深化 + 数据完整度优化（t5）

> 批次：021BW（情绪维度深化）＋021BX（数据完整度优化）｜日期：2026-09-24 13:20~13:45｜执行：checker（任务 t5，终验）
> 验证对象：t2（O3 情绪行＋O5① 换手率修复）、t4（F1 迟滞标注表缝修复＋F2 A股回测补跑）、t7（K2 港股回测补跑＋资金面回补＋K6 定案）全部交付。
> 方法声明：**全程零写生产库**——独立对账/复算一律 `sqlite3 mode=ro`；live 冒烟全部 GET 只读（未触碰 report-latest 等自带写库路径）；终验自身仅新增文档（本报告＋两份审计重跑存档＋一份完整度审计快照），**零代码改动**。
> ⚠️ 产出路径说明：任务书指定 `docs/reports/021bw_verify_20260924.md`，该文件已被**同日更早完成的「录流水费用实时预估」终验报告占用**（021BV 备忘 F-5 闭环存档，被 `021bv_verify_20260923.md` 状态标注引用）——为不破坏既有闭环记录，本报告改用现文件名落盘，请队长知悉（如需合并/改名请指示）。

---

## 0. 结论速览

**通过。** 八项终验全部达成，无阻塞 findings（2 条 P2＋3 条 info，见 §8；无 P0/P1）：

| # | 终验项 | 结果 |
|---|---|---|
| ① | pytest 全量含 slow 层完整跑完 | ✅ **1275 passed, 1 skipped in 657.40s**，exit 0（=t4 后 fast 1269＋slow 6；唯一 skip 为已知 F-3 环境项，连续四批次同口径） |
| ② | ruff + mypy + 红线 | ✅ ruff「All checks passed!」；mypy「Success: no issues found in 57 source files」；红线 **28/28 通过** |
| ③ | 独立重跑全部审计规则 | ✅ R1 selftest（含 F1 表缝用例20）全过＋实跑 **P0=0/P1=0**/P2=95；R2 selftest（含 N10）全过＋实跑 **P0=0/P1=0**；完整度审计重跑 EXIT=0（K2 新鲜度断言「差 0 天 ✅」） |
| ④ | 独立复算情绪分桶抽样对账（不采信 builder） | ✅ **ALL PASS**：验证者手写纯 SQL（CTE＋窗口函数，与查询脚本 Python 实现完全独立）复算 S1/S2a/S4 三类代理 **11/11 桶 n/up 逐格全等**＋样本基 1240=1240；3 股抽样 as-of 原始值与桶归属逐一人工复核正确 |
| ⑤ | 数据完整度轨：t6 缺口逐项复核＋修复项抽样 | ✅ K1/K2/K6/资金面/F2 全部实测吻合（§4）；**发现一处数据回归（600487 换手率 -250 行，根因已定位，可自愈，见 §5）** |
| ⑥ | 运行实例只读冒烟＋诚实边界 | ✅ 7/7（§6）：服务 13:10:18 重启后加载全部新代码（实测佐证：F1 标注与情绪行均在场）；双市场情绪行 live==常量模板；诚实边界四项扫描零泄漏 |
| ⑦ | B24/R7/classify_stage/权重裁定零触碰 git 核验 | ✅ 批次全程（2db1c88..工作区）受保护六文件 `git diff`=**0 行**（§7） |
| ⑧ | findings 报告 | 无 P0/P1；F-V1/F-V2/F-V3 三条 P2 建议跟进（不阻塞），F-V4/F-V5 info |

---

## 1. 门禁明细（复现命令与实测输出）

| 命令（项目根目录） | 实测结果 |
|---|---|
| `python -m pytest tests/ -m "slow or not slow"` | `1275 passed, 1 skipped in 657.40s (0:10:57)`，exit 0。与批次账目闭合：021BW t2 后 fast 1256 → 021BX t7 +7=1263 → t4 +6=**1269** → 加 slow 6 = **1275**；1 skipped 为已知 F-3（pytest 子进程 PATH 无 node）环境项，非缺陷 |
| `ruff check .` | `All checks passed!`，exit 0 |
| `mypy app.py config.py modules` | `Success: no issues found in 57 source files`，exit 0 |
| `python scripts/check_redlines.py` | `红线核验结果: 28/28 通过`，exit 0 |

## 2. 审计规则独立重跑（终验档案）

| 重跑 | selftest | 实跑结果 | 存档 |
|---|---|---|---|
| R1（`audit_consistency_021bs.py`） | ✅ 全部通过（含 t4 新增用例20：F1 表缝分数 49.7/64.9/29.5 标注回归锁） | **P0=0 / P1=0** / P2=95（OK 341，异常 0）；与 t4 报告口径逐项一致 | `docs/reports/021bw_verify_r1_rerun_20260924.md` |
| R2（`audit_consistency_021bs_r2.py`） | ✅ 6/6（含 N09 分级＋N10 情绪行常量结构） | **P0=0 / P1=0** / P2=11 / INFO=2（N01 兜底在场 10 股＝存量 P2；N02 命中 0）；**N10 双市场 live==常量模板 OK** | `docs/reports/021bw_verify_r2_rerun_20260924.md` |
| 完整度（`audit_data_completeness_021bx.py`） | —（K1/K2/K6 动态化＋K2 新鲜度断言内置） | EXIT=0；**K2「✅ 回测样本与评级对齐（差 0 天，阈值 2 天内）」**；矩阵 13 维度缺口与 t7 后快照一致 | `docs/reports/021bx_verify_data_20260924.md` |

> 存档卫生注意：R1/R2 脚本的**默认 `--out` 路径即 021BS 历史档案路径**（`021bs_audit_r1/r2_20260922.md`）——t4 期间一次未带 `--out` 的重跑已将两份历史档案覆盖为 09-24 时点内容（运行时刻 13:10:09，结果同为 P0=0/P1=0/P2=95）。旧版在 git 历史中可恢复（见 §8 F-V2）。本次终验所有重跑均显式指定新存档路径，未再覆盖。

## 3. 情绪分桶独立复算对账（不采信 builder，④ 核心）

**方法**：验证者独立手写纯 SQL（临时表去重样本基＋CTE＋`ROW_NUMBER` 窗口函数，逐条复刻 t1 冻结口径：S1 近7日最新非空 avg_sentiment 分桶 / S2a 最近≤5 个真实资金流行 5 日累计净流入分桶 / S4 最近≤6 个融资余额行首尾变化率分桶；全部 as-of 评级日、`is_estimated` 过滤与平局 rowid 决序均与冻结口径对齐），对官方查询脚本当前库 JSON 快照逐格比对；另对 3 股抽样打印 as-of 原始值人工复核桶归属。全程 `mode=ro`。

**结果：ALL PASS（exit 0）**

| 代理 | 独立 SQL（n_t5/up_t5） | 官方脚本快照 | 对账 |
|---|---|---|---|
| S1 新闻 | 正面 553/217 · 中性 156/72 · 负面 107/49 | 同 | **逐格一致** |
| S2a 主力5日 | 大幅流出 627/257 · 大幅流入 159/74 · 小幅流出 42/19 · 小幅流入 27/9 · 均衡 11/3 | 同 | **逐格一致** |
| S4 杠杆 | 降 478/208 · 升 258/96 · 平 130/58 | 同 | **逐格一致** |
| 样本基（去重 A股真实回测行） | 1240 | 1240 | **一致** |

3 股抽样（各股最新评级日 as-of 原始值，桶归属人工复核全部正确）：000858（新闻 +0.1998→正面；flow5 −8,713.5 万→大幅流出；融资余额 589,012→574,415 即 −2.48%→降）｜600276（+0.5184→正面；+45,406 万→大幅流入；−0.92%→降）｜601888（+0.1552→正面；+1,727 万→小幅流入；−0.31%→平）。

### 3.1 冻结数字 vs 当前样本漂移（重要观察，F-V3）

t4 F2 补跑后样本基 582→**1240**（冻结时常量标注 670 条）。独立复算显示冻结值已随样本滚动漂移，但**结论句在门槛体系下仍成立**：

| 指标 | 冻结值（670 样本，t1） | 当前复算（1240 样本） | 评估 |
|---|---|---|---|
| 新闻总体反向 | 负面 58% vs 正面 42%（+17pp） | 负面 45.8% vs 正面 39.2%（**+6.6pp**） | 方向未翻转，幅度已低于 15pp 门槛 |
| 新闻档内最大增量 | 7.7pp（「已被 021BC 消化」） | **18.7pp（持有观望档内：中性 46.8% vs 负面 28.1%，两侧 n=79/32）** | 单档超 15pp，但建议减仓档方向翻转（负面 50.0% vs 正面 42.7%）→「跨档方向稳定」不成立，**仍不入模型门** |
| 主力减仓档内 | 33pp（单档不稳观察项） | 26.4pp（流入 68.3% vs 流出 41.8%），持有观望档内仅 ~0.7pp | 定性不变（单档不稳→观察项） |
| 杠杆 | 升 34% vs 降 49%（14.5pp，三档方向一致） | 升 37.2% vs 降 43.5%（**6.3pp**），**三档方向仍一致**（升更差） | 定性不变（观察项之首），幅度收窄 |

**判定**：情绪行有明确 as-of 标注＋月度复核指针，作为冻结快照**诚实性成立**；但「档内增量仅 7.7pp 已被消化」的具体表述在当前样本下已不精确（F-V3）。建议队长**将 O2 月度复核提前触发**（`scripts/query_sentiment_evidence_021bw.py` 重跑＋按需更新 `SENTIMENT_EVIDENCE_NOTE` 常量一处，机制/权重零涉）。

## 4. 数据完整度轨核验（t6 缺口清单逐项＋修复项抽样）

| t6/t7 项 | 终验实测 | 判定 |
|---|---|---|
| K1 换手率修复＋回补 | 全库 25,546 行中非零 15,177（59.4%）；19 只回补股每交易日 19 行非零（07-16~09-23 窗口抽查）、值域 0.08~22.52%/均 1.53% 无异常；**唯一异常=600487 回归（§5）** | ✅（附 1 项回归） |
| K2 港股回测补跑 | hk 回测行 **322**（与 t7 报告完全一致）；最新评级日对齐 2026-09-23，**新鲜度差 0 天**；覆盖 10/10 只；审计断言「差 0 天 ✅」 | ✅ |
| 资金面缺口回补 | 10 只港股 09-10 起每只 **10/10** 天真实主力净流入在库；09-11 系统性缺口日 10/10 已回补（≥t7 报告的 38/38 口径） | ✅ |
| t4 F2 A股回测补跑 | a 回测行 **1492**（与 t4 报告完全一致）；去重样本基 1240、覆盖 46/46 只 | ✅ |
| K6 盘口停更 | stock_orderbook 仍止于 09-09（服务端断供，登记不修正确）；健康度面板 orderbook 说明列 **live 在场降级声明**（021BX 定案文案） | ✅ |
| 登记项现状 | 中免股东户数最新 2022-06-30（1547d，§6 域登记）；中芯融资余额最新 09-16（披露节奏，登记）；K3/K4/K5 维持 t6 判定 | ✅ |
| 回测待补滚动 | 近 30 天待补=4（fill_pending 未接线前的预期滚动缺口，新鲜度门槛内） | ✅（info） |

## 5. 新发现：600487 换手率回补被旧进程回采清零（F-V1，P2·可自愈）

- **现象**：全库非零换手率 15,177 行，较 t7 后快照（15,427，12:45）**净减 250 行，全部落在 600487 亨通光电**（t2 续跑 +268 → 现存 18）。
- **精确闭合**：600487 全表 269 行 = 被重写的 251 行（清零）＋窗口外幸存 18 行（2025-08-19~09-11，非本次回补窗）。
- **根因（代码/git/日志三证）**：12:44:29 补采调度器检测 5 只 kline 缺口 → 12:47:09 `[A股 600487] K线数据采集成功，共251条`。该次采集运行于 **08:58:28 启动的旧进程**（PID 17392）——t2 的换手率旁路/只升不降代码 11:26:49 才落盘、13:10:18 队长才重启服务；旧版代码实测硬编码 `0,  # 换手率（同上）`（`git show 111393f^` 实证），故窗口行被 INSERT OR REPLACE 清零。**非 t2 代码缺陷**（HEAD 与现行进程均含 merge＋只升不降，本次终验实测 live 生效）；属**部署/重启时序窗口**的一次性数据回归。同轮其余 4 只（002364/605358/300456/600884）均非回补股，零损失；12:47:15 调度器退避 120 分钟，旧进程再无伤害面。
- **自愈路径**：`scripts/backfill_kline_turnover_021bw.py` 幂等可续跑（WAF 解禁后与 36 只余量一并执行，**600487 需加回回补清单**——它此前被记为「已全量回补」）；或等该股下一次成功的东财旁路落库。
- **建议**：将「批次内改采集代码后尽快重启服务」纳入批次收尾纪律（本次重启发生在收尾之后、遗留约 2 小时时序窗）。

## 6. 运行实例只读冒烟（全部 GET，服务 13:10:18 重启后）

| 检查 | 结果 |
|---|---|
| `GET /api/health` | 200，status=running |
| `GET /api/backtest/market-report?market=a_stock` | 200，「情绪检验现状」行恰 1 条且**与 `SENTIMENT_EVIDENCE_NOTE` 常量逐字同源同值=True** |
| `GET /api/backtest/market-report?market=hk_stock` | 200，同上 True（R20 分行） |
| `GET /api/portfolio/watchlist-scores` 600519 行 | **score_tier_note 在场**：「评级口径说明：总分 49.7 位于「建议减仓」档分数区间（30–50 分）……」（t4 F1 连续边界文案正确，禁裸 '<' 合规；评级仍「持有观望」不变，B24 合规） |
| `GET /api/health/sources` | 200，orderbook 说明列含 021BX 降级声明（服务端断供定案＋「展示维度、评分链不消费」） |
| 诚实边界（ro SQL 扫描） | 当日 9 份报告 markdown/key_factors 零「情绪检验现状」泄漏；key_factors/markdown 全历史零出现；alert_history 零泄漏——**情绪行只在回测域市场报告（统计面），分域契约完好** |
| 情绪行文案 | 双市场情绪行零指令动词（建议/应当/买入/卖出/止损/止盈/加仓/清仓全无） |

## 7. 零触碰核验（⑦）

- `git diff 2db1c88 -- modules/advisor.py modules/scoring_engine.py modules/trader_advisor.py config.py config_weights.json RED_LINES.md` → **0 行差异**（覆盖批次全部提交 111393f/a9e5791/8eb5280＋工作区 t4 未提交改动）：B24 `generate_advice`、R7 评分引擎、classify_stage（`modules/trader_advisor.py`，021BQ 锁）、R16 风控阈值、021BC 权重裁定（`config_weights.json`）、RED_LINES §6 登记域全部零触碰。
- 函数签名在 HEAD 实存：`generate_advice(stock_id, report_date=None)` / `classify_stage(inputs)`。
- 工作区改动面（t4 全部）：`modules/rating_hysteresis.py`（F1，展示层）＋`scripts/audit_consistency_021bs.py`（selftest 用例20）＋`tests/test_rating_hysteresis.py`（6 例）＋`scripts/backfill_astock_backtest_021bw_t4.py`（F2 运行器）＋报告/存档＋CHANGELOG——与批次裁定一致。
- 服务重启核验：监听进程 PID 32688 启动 13:10:18 **晚于全部运行时代码最后改动**（rating_hysteresis.py 13:00:42），且启动后无运行时文件再改动——live 冒烟所证即终验对象代码。

## 8. Findings 汇总（无阻塞）

| # | 级别 | 内容 | 建议 |
|---|---|---|---|
| F-V1 | **P2** | 600487 换手率回补被旧进程回采清零（268→18；-250 行；根因=部署/重启时序窗，非 t2 代码缺陷，见 §5） | WAF 解禁后随幂等脚本补回（600487 加回清单）；批次收尾纪律补「改采集代码即重启」 |
| F-V2 | **P2** | t4 期一次未带 `--out` 的审计重跑覆盖了 021BS 历史档案 `docs/reports/021bs_audit_r1/r2_20260922.md`（运行时刻 13:10:09；旧版在 git 历史可恢复） | `git checkout -- docs/reports/021bs_audit_r1_20260922.md docs/reports/021bs_audit_r2_20260922.md` 恢复历史版；或在两脚本默认 `--out` 处加档位注释防复发（文档卫生，不改行为） |
| F-V3 | **P2** | 情绪行冻结数字随样本翻倍已漂移（新闻总体 +17pp→+6.6pp；档内 7.7pp→18.7pp 单档、跨档方向仍不稳；杠杆 14.5pp→6.3pp 三档方向一致）——结论句「均不满足入模型门槛」在门槛体系下**仍成立**，快照诚实性成立（as-of 标注＋复核指针在场） | 将 O2 月度复核提前触发：重跑 `query_sentiment_evidence_021bw.py`，按需只改 `SENTIMENT_EVIDENCE_NOTE` 一处（机制/权重零涉） |
| F-V4 | info | 近 30 天回测待补=4（fill_pending 未接线前的预期滚动缺口；K2 新鲜度断言「差 0 天 ✅」） | 维持 t7 §1.2 接线待批流程 |
| F-V5 | info | 全量唯一 skip 为已知 F-3 环境项（pytest 子进程 PATH 无 node） | 沿用 021BV F-3 处置建议 |

## 9. 可复现性与影响面

- 复现命令：§1 门禁表＋`python scripts/audit_consistency_021bs.py --selftest / --out …`＋`…_r2.py --selftest / --out …`＋`python scripts/audit_data_completeness_021bx.py --out …`＋`python %TEMP%\021bw_verify_recon\sentiment_recon.py`（独立复算对账，脚本与官方实现独立）＋`…\live_smoke.py`（GET 冒烟）＋§7 git 命令。
- 本终验对生产库零写入、零网络采集、零个股报告再生（全程绕开 report-latest 写库路径）；代码零改动。
- 本次新增文件：本报告＋`021bw_verify_r1_rerun_20260924.md`＋`021bw_verify_r2_rerun_20260924.md`＋`021bx_verify_data_20260924.md`（另有 `%TEMP%` 对账脚本，不入库）。

---

*报告完 · 021BW/021BX t5 终验 · **通过**（全量 1275 passed＋1 已知环境 skip；审计重跑零 P0/P1；独立复算 11/11 桶＋样本基全等；数据完整度轨实测吻合＋1 项可自愈回归（600487）；live 冒烟与诚实边界全过；零触碰核验 0 行差异；无阻塞 findings）*
