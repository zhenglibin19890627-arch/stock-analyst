# 021BZ 共振类型与强弱显性化：终验报告（t3）

> 日期：2026-09-25 ｜ 执行：checker（任务 t3，终验）｜ 批次：021BZ
> 验证立场：**独立对账，不采信 builder**——映射期望值由本验自有死表/自有正则/自有指标实现推导，
> 与 builder 代码输出逐字段对照；指标级触发日期用自实现 MACD/KDJ/MA20 复算交叉复核。
> 输入：AGENTS.md §5、docs/reports/021bz_resonance_plan_20260925.md（t1）、
> docs/reports/021bz_impl_20260925.md（t2）、commit 87c86ca（t2 实施）+ f5469db（审计留档）。

---

## 0. 结论速览

| # | 终验项 | 结果 |
|---|---|---|
| ① | pytest 全量含 slow 层 | ✅ **1322 passed, 1 skipped**（12:18），exit 0＝fast 基线 1316 + 6 个 slow 用例，零回归 |
| ② | ruff + mypy + 红线 | ✅ ruff 全仓绿；mypy 57 文件 0 错；红线 **28/28**（R17 零新增依赖含） |
| ③ | 一致性审计全套独立重跑 | ✅ R1：P0=0 / P1=0（P2=75，OK 344，异常 0）；R2：P0=0 / P1=0（N02 命中 0）——**零新增，与 021BW t4 基线持平** |
| ④ | 共振展示独立对账 | ✅ **ALL PASS**：12 条真实共振样本（买 6/卖 6）星级规则/分级/方向/触发日/时效逐字段手算全对；指标级交叉复核全命中（距离 0 根）；分级映射边界 12/12；resonance_view additive 不变量成立 |
| ⑤ | 运行实例只读冒烟 | ✅ **14/14 PASS**：报告页快照与选股两端点徽标 live 逐字段与手算一致；在线扫描诚实空态；运行实例静态资源为新代码 |
| ⑥ | B24/R7/扫描器边界零触碰 | ✅ git 核验通过（见 §6） |
| ⑦ | findings | **无阻塞性未达成项**（过程发现 1 项服务未重启，已由队长当场处置并复验通过，见 §7） |

**终验结论：021BZ 批次通过。** 共振类型+方向+强弱徽标+触发日期在选股结果与个股分析报告两处统一呈现；
强弱分级确为星级重标的纯标注映射（诚实声明随响应随渲染），不构成回测验证；全部门禁绿。

---

## 1. ① pytest 全量（含 slow 层）

```
python -m pytest tests/ -m "slow or not slow" -q
→ 1322 passed, 1 skipped in 738.39s (0:12:18)，exit 0
```

- 与 t2 fast 门禁 1316 passed 的差值恰为 6 个 slow 标记用例（OPT-5 分层默认跳过者）本次全数执行，**零失败零回归**。
- 含本批新增 tests/test_resonance_display_021bz.py（30 例）与全部既有回归网。

## 2. ② 静态检查三件套

| 门 | 命令 | 结果 |
|---|---|---|
| ruff | `ruff check .` | All checks passed!（exit 0） |
| mypy | `mypy app.py config.py modules` | Success: no issues found in 57 source files（exit 0） |
| 红线 | `python scripts/check_redlines.py` | **28/28 通过**（exit 0；R17 依赖白名单含零新增依赖核验） |

## 3. ③ 一致性审计独立重跑（t3 时点快照）

| 审计 | 命令 | 结果 |
|---|---|---|
| R1 | `python scripts/audit_consistency_021bs.py`（56 只自选全量拉取看板面） | 发现 98 条：**P0=0 / P1=0**，P2=75，OK=23，OK 明细 344，异常 0 |
| R2 | `python scripts/audit_consistency_021bs_r2.py` | **P0=0 / P1=0**（N01 落库缺失兜底在场均 P2；N02 旧副词命中 **0**），新维度 P2=6/INFO=2 |

两份报告已由审计脚本原位刷新（docs/reports/021bs_audit_r1|r2_20260922.md）并与 t2 存档逐行一致（仅运行时刻差异）。**P0/P1 维持双零，零新增。**

## 4. ④ 共振展示独立对账（核心项，不采信 builder）

### 4.1 方法（独立性声明）

- **星级规则手算**：双金叉/双死叉=「signals 字串内 @日期集合唯一→4★，否则 3★」；其余组=库固定 5★——期望值由本验自有正则从 signals 字串推导，不调用 builder 任何映射函数；
- **映射死表**：{5:'强',4:'中',3:'弱'}、bull→'多'/bear→'空'、时效=trigger_date vs kline_upto 字符串比较——全部本验自写；
- **指标级复核**：自实现 EMA(12,26,9) MACD、KDJ(9,3,3)、SMA20，从 raw_kline 原始数据直连 SQL 读取（不经 builder 读函数），验证每个声称触发日期存在真实交叉/事件（±1 根容差防 EMA 数值实现差异）；
- **对照对象**：`resonance_view` 输出（两处展示共同消费的同一份数据单元）。

### 4.2 样本对账（56 只 active 自选全扫描，买侧命中 7 只/卖侧 10 只，抽样 12 条逐字段核对）

| 侧 | 股票 | key | signals @日期 | 手算→视图 stars/grade/direction/trigger/timeliness | 指标复核 |
|---|---|---|---|---|---|
| 买 | 601888 中国中免 | res_week_daily | 09-23（单） | 5★/强/多/09-23/窗口内历史 ✅ | MACD 距离 0 ✅ |
| 买 | 002230 科大讯飞 | res_double_golden | 09-22+09-22（**同日**） | **4★**/中/多/09-22/窗口内历史 ✅ | MACD 0、KDJ 0 ✅ |
| 买 | 300015 爱尔眼科 | res_week_daily | 09-23（单） | 5★/强/多/09-23/窗口内历史 ✅ | MACD 0 ✅ |
| 买 | HK1810 小米集团-W | res_week_daily | 09-22（单） | 5★/强/多/09-22/窗口内历史 ✅ | MACD 0 ✅ |
| 买 | 300456 赛微电子 | res_week_daily | 09-23（单） | 5★/强/多/09-23/窗口内历史 ✅ | MACD 0 ✅ |
| 买 | 300229 拓尔思 | res_week_daily | 09-22（单） | 5★/强/多/09-22/窗口内历史 ✅ | MACD 0 ✅ |
| 卖 | 300750 宁德时代 | res_week_bear | 09-24（单） | 5★/强/空/09-24/今日 ✅ | KDJ 0 ✅ |
| 卖 | 688017 绿的谐波 | res_week_bear | 09-24（单） | 5★/强/空/09-24/今日 ✅ | KDJ 0 ✅ |
| 卖 | 002230 科大讯飞 | res_week_bear | 09-24（单） | 5★/强/空/09-24/今日 ✅ | KDJ 0 ✅ |
| 卖 | 300015 爱尔眼科 | res_double_dead | 09-24+09-24（**同日**） | **4★**/中/空/09-24/今日 ✅ | MACD 0、KDJ 0 ✅ |
| 卖 | HK1810 小米集团-W | res_double_dead | 09-23+09-24（**跨日**） | **3★**/弱/空/09-24/今日 ✅ | MACD 0、KDJ 0、MA20 0 ✅ |
| 卖 | HK2513 信达 | res_week_bear | 09-23（单） | 5★/强/空/09-23/窗口内历史 ✅ | KDJ 0 ✅ |

**12/12 逐字段一致；动态星两分支（同日 4★/跨日 3★）均由真实数据覆盖并验证；指标级复核 16/16 事件距离全部为 0 根。**
（过程注：首轮复核脚本曾将 HK1810 的「破位MA20@09-22」误配 KDJ 交叉判 FAIL——系本验脚本自身的标签归类缺陷，修正为 MA20 事件口径后通过；builder 数据无误。）

### 4.3 分级映射边界（手写期望 vs `strength_grade_for`）

| 入参 | 5 | 4 | 3 | 2 | 1 | 6 | 0 | 4.5 | '4' | None | True | '' |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 期望 | 强 | 中 | 弱 | None | None | None | None | None | None | None | None | None |
| 实测 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

**3★弱/4★中/5★强 三档边界正确；一切未知/非法值 None 不硬造（12/12）。**

### 4.4 additive 不变量

`resonance_view` 深拷贝冻结对照：原对象零改动 ✅ / 追加 grade+direction+trigger_date+timeliness 四键 ✅ / 原六键值保留 ✅。
检测器输出契约（detect_*/compute_* 键集）由契约锁测试锁定（见 ① 全量绿）。

## 5. ⑤ 运行实例只读冒烟（队长重启后，PID 39628，启动 10:33:03 > t2 落盘 10:19）

**14/14 PASS**（保持只读：report-latest/watchlist 端点均为 GET；POST /advise 会经 generate_advice 落库，为守住「只读」不实际调用，实时路径由 `_enrich_advice_result` 共享附着点 + 契约锁测试覆盖）：

| 核验点 | 结果 |
|---|---|
| report-latest(21) `resonance_snapshot` 在场，scope=watchlist_offline/window=3/kline_upto=2026-09-24/kline_count=250 | ✅ |
| snapshot.buy[0]=周线共振波段 5★/强/多/@2026-09-23/窗口内历史——与手算逐字段一致 | ✅ |
| snapshot.note 分级声明全文在场且无裸 '<' | ✅ |
| report-latest(36 HK1810) sell[0]=双死叉共振 **3★/弱/空/@2026-09-24/今日**（跨日降档实例） | ✅ |
| watchlist-signals 买侧 7 只命中，徽标四键（grade/direction/trigger_date/timeliness）全在场 | ✅ |
| watchlist-sell-signals 卖侧 10 只命中且 direction 全为「空」 | ✅ |
| POST scan-signals（腾讯K线现拉，零写库）在线面：kline_upto 附着、本时刻无共振→诚实空态不硬造 | ✅ |
| 运行实例 /static/js/analysis.js 含 `_renderResonanceStrip`（报告页条带代码已上线） | ✅ |
| 运行实例 /static/js/market.js 含 `_msGradeBadge/_msDirBadge/_msTriggerCell`（选股表徽标代码已上线） | ✅ |
| 首页 HTTP 200 | ✅ |

**两处展示同源同值**：报告页快照（report-latest）与选股端点（watchlist-signals）对同一股票（21/36）的共振视图逐字段相等，且等于本验手算值——实时/快照/选股三面共用 market_screener 展示层单一事实源得到 live 实证。

## 6. ⑥ B24 / R7 / 扫描器边界零触碰（git 核验）

| 边界 | 核验方法 | 结果 |
|---|---|---|
| B24 `advisor.generate_advice` | commit 87c86ca 文件清单**不含 modules/advisor.py**；`git diff --name-only` 双重筛查 | ✅ 零触碰 |
| R7 scoring_engine | commit 文件清单不含 modules/scoring_engine.py | ✅ 零触碰 |
| classify_stage（trader_advisor.py:339） | 逐行审阅该文件 diff：仅 `_res_date_desc` 改复用 `latest_trigger_date_of`（行为等价）+ 删 `import re`，classify_stage 函数体零改动 | ✅ 零触碰 |
| 扫描器「只产买点」边界 | market_screener.py diff 审阅：RESONANCE_LIBRARY/SELL_RESONANCE_LIBRARY/检测器/tier order 逐行未动（diff 纯新增展示层 85 行 + run_signal_chunk 附 kline_upto 1 键）；scan-signals 仅映射买侧 `resonances`，卖侧仅在平行库端点与报告快照出现 | ✅ 边界原样 |
| 报告侧落点（B24 合规） | analysis.py diff：新增 `_attach_resonance_snapshot`（组装层）+ 两处 attach 调用，generate_advice 调用点无改动 | ✅ 合规 |

## 7. 过程发现与处置（非阻塞 findings）

| # | 发现 | 处置 | 状态 |
|---|---|---|---|
| F-V1 | 终验开始时运行实例未重启（旧进程 00:31:57 启动早于 t2 落盘 10:12–10:19；live 探测 report-latest 无 resonance_snapshot、watchlist-signals 无 grade 键） | 已按任务约定即时报告队长；队长重启（PID 39628，10:33:03）后行为探针复验 additive 键全在场 | ✅ 已闭环 |
| F-V2 | 本验复核脚本首轮将「破位MA20」事件误配 KDJ 交叉（脚本自身缺陷） | 修正本验脚本口径后通过；builder 数据无误 | ✅ 已闭环 |
| N-1 | 队长在终验中途提交 87c86ca/f5469db，本验 ③ 的审计重跑产物随后被原位刷新留档 | 审计重跑本就要求产出 t3 时点快照，属预期产物行为 | 备注 |

**遗留（维持 t2 登记，不阻塞）：** 行动清单/预警消息/操盘手 prose 接入 grade 徽标口径（后续批次可选）；021BY 遗留观察（离线巡检无 pos_pctile/pos_band）维持原登记。

## 8. 诚实性声明

- 强弱分级（强/中/弱）**仅为共振星级（5★/4★/3★）的直接重标**，用于统一表述口径，**不构成对信号胜率的回测验证**——该声明经 GRADE_NOTE_KEY 随响应、随渲染两处实证在场（§5）；
- 本验 ④ 的「独立对账」是对**展示映射链与触发日期事实性**的独立复核，不构成对共振信号有效性的回测验证；
- 快照口径随行：scope=watchlist_offline / window=3 / kline_upto=2026-09-24 / kline_count=250；在线扫描为腾讯K线现拉口径；历史触发均带「窗口内历史，非今日」时效标注，不读起来像新信号。

---

**终验人：checker（t3）｜ 门禁：①②③④⑤⑥ 全绿 ｜ 结论：021BZ 批次通过，可合入交付。**
