# 021BS 终验报告（t5）— 独立重跑审计清零 + 全量测试 + 两案例运行实例冒烟

> 日期：2026-09-23 ｜ 执行：checker（任务 t5，终验）｜ 批次：021BS
> 依赖：t4（第二轮修复）完成 ✅；运行实例已随 t4 代码重启 ✅（核验过程见 §1）
> 方法：**全程独立**——不采信 builder 数据，六步全部由终验者自行执行；运行实例冒烟仅 GET
> 只读端点 + SQLite `mode=ro`，零写库、零网络采集、零触碰 advisor.generate_advice（B24）。

---

## 0. 结论速览

| # | 终验步骤 | 结果 |
|---|---|---|
| ① | pytest 全量（含 slow 层）完整跑完 | ✅ **1094 passed, 1 skipped, 0 failed**（594s） |
| ② | ruff + mypy + 红线核验 | ✅ ruff 全过；mypy 55 文件无问题；红线 **28/28** |
| ③ | 独立重跑一致性审计脚本 | ✅ R1：**P0=0 / P1=0** / P2=81 / OK=230 / 异常 0；R2：**P1=0**；与 t4 存档逐行一致（仅运行时刻行）；P2 均带标注/自愈分级 |
| ④ | 运行实例只读冒烟（中免/五粮液） | ✅ **17/17 PASS, 0 FAIL**（+2 项 SKIP=今日批次未到的正常跨日状态）；R1/R2 矛盾逐条核对不再出现 |
| ⑤ | B24 / R7 / classify_stage 零触碰（git diff + AST 语义比较） | ✅ **全部 SAME**（021BS 批次起点 f73b6d3 以来语义零改动） |
| ⑥ | 未达成 / 新回归 | ✅ **无**（存量 P2 均在既定自愈窗口内，见 §6） |

## **终验结论：通过（无保留）。** 021BS 批次可以收口。

---

## 1. 前置核验：t4 完成 + 服务已随 t4 代码重启

- **进程核验**（终验者自行执行，10:52）：`127.0.0.1:5000` 监听进程 **PID 8832（pythonw），
  启动于 2026-09-23 10:47:27**，晚于 t4 全部修改文件 mtime（trader_advisor.py 10:15:32 /
  action_list.py 10:17:50 / watchlist_scores.py 10:21:16 / analysis.js 10:23:41）——
  FLASK_DEBUG=False 单进程无热重载，重启后运行实例必为 t4 代码。
  > 过程说明：终验者 10:46:37 首查时监听进程为旧 PID 42036（09-22 20:37 启动），当即报告
  > 队长；10:47:27 服务被重启，本报告以复测结果为准。
- **t4 行为实测在线**：watchlist-scores 对中免返回 `trader_signal`（live 兜底 + 
  stage_leads_rating 分歧标注），为 t4 之前代码不可能产生的响应（详见 §5）。

## 2. ① pytest 全量（含 slow 层）

```
python -m pytest tests/ -m "slow or not slow" --durations=5 -q
→ 1094 passed, 1 skipped in 594.36s (0:09:54)   [exit 0]
```

- 与 t4 实施报告的全量基线（1094 passed, 1 skipped）**数量一致**：021BS 两轮新增测试
  （t2 四文件 + t4 test_trader_fallback_021bs.py 16 例）全部在场且通过，无回归、无失败。
- 唯一 skip 为既有 slow 层预期跳过项；slowest durations 均为 TestTickBackoff 真实时钟用例（符合预期）。

## 3. ② 静态门

| 门 | 命令 | 结果 |
|---|---|---|
| ruff | `python -m ruff check .` | **All checks passed!**（exit 0） |
| mypy | `python -m mypy app.py config.py modules` | **Success: no issues found in 55 source files**（exit 0；untyped-defs 提示为既有信息级 note） |
| 红线 | `python scripts/check_redlines.py` | **28/28 通过**（exit 0；R6/R7/R13/R14/R15/R1-R5/R8-R12/R16-R19 全 PASS） |

## 4. ③ 一致性审计脚本独立重跑（终验者自行执行，不采信 builder 数据）

| 脚本 | 结果 | 与 t4 存档比对 |
|---|---|---|
| `audit_consistency_021bs.py --selftest` | **14 用例全部通过**（含 R03 见证分级 2 例）——审计规则可信 | 与 t4 声明一致 |
| `audit_consistency_021bs.py --out …r1_rerun…` | **P0=0 / P1=0 / P2=81 / OK=230 / 异常 0** | 与 `021bs_audit_rerun_t4_20260923.md` **逐行一致**（Compare-Object 仅 2 行时刻行差异：10:36:14 → 10:48:11） |
| `audit_consistency_021bs_r2.py --out …r2_rerun…` | **P0=0 / P1=0** / P2=12 / INFO=4 / OK=11；N01 落库缺失 5 股（**兜底在场 P2 5 / 兜底亦缺 P1 0**）；N02 命中 7 股 P2 | 与 `021bs_audit_r2_t4_20260923.md` **逐行一致**（仅时刻行 10:36:25 → 10:48:13） |

- **P0/P1 双脚本均为 0**，清零结论独立可复现（存档：`021bs_verify_r1_rerun_t5_20260923.md` /
  `021bs_verify_r2_rerun_t5_20260923.md`）。
- **P2 均有标注/分级**：81 条 P2 逐条携带「已标注 / 时点差自愈 / 特性非缺陷」归类
  （R04 迟滞类＝读取面 score_tier_note 在场；F02/F03＝双口径与存量时点差，页面有口径标注；
  F04 漏报观察＝批次时点差，扫描后对齐；R03 5 条＝K 线事后修订型，生成时点见证一致），
  无一条裸 P0/P1 升级风险项。

## 5. ④ 运行实例只读冒烟：中国中免（601888）/ 五粮液（000858）

**方法与只读边界**：仅 GET 只读端点（`/api/portfolio/watchlist-scores`、
`/api/dashboard/action-list`、`/api/stocks/<id>/trader-advice`、`/api/stocks/<id>/trend`、
`/static/js/analysis.js`）+ SQLite `mode=ro` 读存量报告行。**刻意未调 report-latest**：
该端点在当日快照 >15 分钟或当日无报告时会实时重评并写库（021K/B11 路径，
blueprints/analysis.py L498-L560），与审计基准面的只读原则一致；存量报告面改由 ro 直读覆盖。

### 5.1 逐条核对表（17 PASS / 0 FAIL）

| 检查 | 中免 601888 | 五粮液 000858 |
|---|---|---|
| F05 看板评分卡=报告评级/总分 | ✅ 持有观望/56.4 = 存量报告 | ✅ 持有观望/54.7 = 存量报告 |
| chip 在场（stage_name+top_action） | ✅ 下跌期 / 止损·56.16（**已触发**） | ✅ 下跌期 / 止损·68.09（未触发，无后缀正确） |
| chip 基准面（N01 兜底 / F02 同源） | ✅ stored trader 缺失 → chip=**live 现算**（与 trader-advice `operations.top_action` 逐字一致） | ✅ stored 在场 → chip=stored 摘要（逐字一致） |
| chip ⚡分歧=live 分歧（N01 调和） | ✅ has_disagreement=True=live disagreement 在场（stage_leads_rating） | —（stored 基准面） |
| score_tier_note 与档位状态一致 | ✅ 56.4→持有观望档，无失配，note=空（无误报横幅） | ✅ 54.7→持有观望档，note=空 |
| chip 止损数字=报告 pa.stop_loss | ✅ 56.16 = 56.16（52.93 旧口径已随幽灵持仓删除收敛） | ✅ 68.09 = 68.09 |
| trend 只读端点 | ✅ 三周期共振下跌（强） | ✅ 综合下跌（强） |

### 5.2 两案例结论：R1/R2 矛盾不再出现

- **中免（R2 N01 实锤股）——已修复**：R2 时「key_factors.trader 被刷新覆盖丢失 → 看板 chip/⚡
  沉默而个股页 live 报警」的两面不一，现已由读取面 live 兜底调和：看板 chip 与个股页操盘手卡
  **同一 live 源**（止损·56.16（已触发）+ ⚡分歧标注逐字一致），「一面沉默一面报警」消除。
  stored 落库面缺失仍按 P2 存档（今日 15:54 批次重生成后落库合流）。
- **中免（用户原始矛盾「止损触发 vs 持有」）——分域调和生效**：报告评级「持有观望」与止损已触发
  并存时，行动清单以 **持仓纪律 stop_discipline 置顶项**（priority 1）显式给出
  「现价 52.27（09-22 日K收盘）< 有效止损 56.16（成本线/建议止损取高者），纪律无条件执行，
  不等评级、不等反抽」，个股页 ⚡ 分歧标注同口径（「减仓/离场听操盘手纪律，加仓继续看评级」）
  ——不再是矛盾，而是 021BR 分域契约的显式分层。
- **五粮液（用户原始矛盾「买点信号 vs 减仓」延续面）——无矛盾复现**：chip 止损·68.09 未触发与
  报告 pa.stop_loss 68.09 一致；评分/评级/操盘手三面同档（54.7·持有观望·下跌期）。
- **N02 stored×live 分歧漂移（五粮液）**：stored has_disagreement=False（09-22 15:57
  021BR t2 部署前批次）vs live=True——**仍漂移但属 R2 既判 P2 存量自愈**（今日批次未到，
  自愈窗口未到），非新回归；批次后自然对齐。
- **SKIP 2 项（非失败）**：行动清单 overview 为空——overview 行仅在当日有 ok 日报时生成
  （action_list 路1 `report_date==today`），今日 15:54 批次未到（stats.missing_today=56，
  R2 INFO-4 正常跨日状态）；F01 兜底断言已由本轮审计重跑（F01=0）+
  `tests/test_trader_fallback_021bs.py` 16 例（含中免形态回归）锁定。
- **N03 操盘手卡头副词**：运行实例服务的前端 `analysis.js` 含新分域副词
  「纪律无条件执行 · 减仓听操盘手 · 加仓看评级」，旧「仓位动作以评级为准」**零命中**
  ——同屏互斥 P1 已消除。

## 6. ⑤ B24 / R7 / classify_stage 零触碰核验（git + AST 双重）

**基线**：021BS 批次起点前最后提交 `f73b6d3`（t2 提交 3c339e8 为批次首提交；t4 变更在工作区未提交，
`git diff f73b6d3`（含工作树）即覆盖全批次）。

| 对象 | 核验 | 结果 |
|---|---|---|
| `advisor.generate_advice`（B24/R13） | AST unparse 语义比较 f73b6d3 vs 工作树 | **SAME**（t2 仅改 `_build_markdown_single`/`_build_key_factors` 构建器；t4 未触碰 advisor.py——工作区 git status 无此文件） |
| `advisor._build_capital_factors`（R14） | 同上 | **SAME** |
| `advisor._determine_action`（021BH 文案面） | 同上 | **SAME** |
| `scoring_engine.py`（R7） | **整文件**逐字节比较 | **SAME**（normalize_rating / score_main_capital 亦 SAME） |
| `data_contract.py`（R8） | 整文件 | **SAME** |
| `trader_advisor.classify_stage`（021BQ 锁） | AST 语义比较 | **SAME**（t4 对 trader_advisor.py 的变更为新增 `derive_trader_signal_summary` 读取面兜底函数，判定函数本体未动；`generate_trader_advice` 亦 SAME） |

→ **判定：`ALL_ZERO_TOUCH`**，两条红线冻结面全批次零触碰，无需 §6 新登记（019A 写库点未触碰）。

## 7. ⑥ findings 与观察（无未达成、无新回归）

1. **未达成项：无。** 六步验收全部通过；P0/P1 全批次双脚本独立清零。
2. **存量 P2 自愈窗口（已知、已标注，非回归）**：
   - 今日 15:54 批次重生成后：F02 3 条（t3 前存量 chip 止损）、N01 5 股落库面、
     N02 7 股分歧漂移、R03 5 条 K 线事后修订自然合流；
   - F04 预警漏报观察类 P2 随今日扫描对齐（t4 起 score_below 消息已带数据时点注记）。
3. **观测建议（不构成本批验收条件，供后续批次参考）**：① 行动清单 overview 仅在当日有
   ok 日报时存在，批次前（如上午）看板副表为空——属既定口径，若需「跨日可见」可另立
   方案（显示最近一期报告日）；② report-latest 的 15 分钟实时重评在盘中会持续覆盖
   stored 报告（021K 设计），系 N01 落库面反复丢失的间接推手，t4 读取面兜底已化解其
   用户可见影响；长期可评估「报告生成改在 K 线定稿后/批次时点后移」（t4 §3.3 移交备注同源）。
4. **测试面**：全量 1094 passed / 1 skipped，021BS 新增测试（t2 四文件 + t4 16 例）全部在场。

## 8. 复现命令

```bash
python -m pytest tests/ -m "slow or not slow"          # 全量（实测 594s，1094P/1S）
python -m ruff check .                                  # All checks passed
python -m mypy app.py config.py modules                 # 55 files, no issues
python scripts/check_redlines.py                        # 28/28
python scripts/audit_consistency_021bs.py --selftest    # 14 用例
python scripts/audit_consistency_021bs.py --out docs/reports/<归档名>.md      # R1 独立重跑
python scripts/audit_consistency_021bs_r2.py --out docs/reports/<归档名>.md   # R2 独立重跑
```

> 本轮证据存档：`docs/reports/021bs_verify_r1_rerun_t5_20260923.md`（R1 重跑，与 t4 存档仅时刻行不同）、
> `docs/reports/021bs_verify_r2_rerun_t5_20260923.md`（R2 重跑，同上）。冒烟明细由
> 只读脚本执行（HTTP GET + SQLite mode=ro），响应快照存于执行期临时目录。

---

## 9. 终验结论

**通过（无保留）。** 021BS 批次目标达成：报告自身七要素两两核对与报告↔看板五数据流
P0/P1 矛盾清零且独立可复现；两轮修复（迟滞三层标注 + 三层口径脚注 + 预警时点注记 +
trader 读取面兜底 + N03 副词统一）在运行实例实测生效；B24/R7/classify_stage 冻结面
零触碰；全量测试、静态门、红线全绿。
