# 021BY 终验报告：全量测试 + 独立复算对账 + 市场行情页全链核验（t5）

> 日期：2026-09-25 ｜ 执行：checker（任务 t5，终验）｜ 批次：021BY
> 终验对象：t2 实施（commit `ea7ced3`，C1-C7）+ t4 修复（工作区：`blueprints/market.py`
> F-V2 / `static/js/market.js` F-V3+F-V4 / tests +3 / `scripts/check_market_js_021by.js` 入库）。
> 方法：全量测试独立复跑（含 slow 层）+ 静态门 + 四一致性审计独立重跑 + 位置分位
> **双通道**独立复算（t3 可复用脚本 + checker 全新独立实现随机抽样，不采信 builder）+
> 运行实例只读冒烟 + UX 走查 + git 红线边界核验。
> 只读边界：审计面全部 mode=ro 零写库；运行面仅 GET 探针（未触发扫描/采集/加自选写路径，
> **零新增**新浪/腾讯/东财请求）；零触碰 B24/R7/R8/R16/classify_stage。

---

## 0. 结论速览

1. **全量测试（含 slow 层）通过**：`1292 passed, 1 skipped`（详见 §1）——与 t4 基线
   1292 passed 完全一致，零回归。
2. **静态门全绿**：ruff All checks passed / mypy no issues in 57 source files /
   红线 **28/28**（EXIT=0）/ `node --check` SYNTAX OK / 前端逻辑冒烟 **16/16 PASS**。
3. **四一致性审计独立重跑全部 EXIT=0、零 P0/P1**：r1 异常 0（P2×67 全部既有类，构成
   F04×53/R04×8/F03×2/R11×2/R06×2 与 t4 记录逐类一致）；r2 P1=0；021BX 核心评分链全绿；
   位置分位同源 10/10。r1 内 021BU 证据规则 R16/R17/R18/F06 全 OK。
4. **位置分位独立复算对账 25/25 OK（不采信 builder）**：通道一 t3 可复用脚本
   （脚本内独立公式）10/10；通道二 checker 全新独立实现（随机抽样 15/56、seed=20260925、
   独立 SQL+独立公式）15/15，样本覆盖 low×11/mid×3/high×1，四路一致
   （独立基准 = `position_pctile_of_klines` = `_current_pos_pctile` = `_volume_structure`±0.005）。
5. **运行实例只读冒烟通过**：当前监听进程 PID 25544（启动 2026-09-25 00:32:03）晚于
   全部 021BY 文件 mtime（最晚 00:10:35），health×3 均 200；模板 8 个新控件标记全在位；
   服务端 market.js 与磁盘逐字符一致（110,357 字符 tail-equal，新函数标记全在）；
   6 端点 GET 探针全部正常（§5.2）。
6. **红线边界 git 核验零触碰**（§6）：变更集 13 文件全部在 t2/t4 声明清单内；
   advisor.py（B24）/scoring_engine.py（R7）/config.py 与 config_weights.json（R16 默认值）/
   data_contract.py（R8）/trader_advisor.py（classify_stage 实体）diff 均为空；
   `SIGNAL_LIBRARY`/`RESONANCE_LIBRARY`/`detect_*` 零改动（唯一匹配为 docstring 引用）；
   只产买点库实测 4 键全为金叉买点类；新增文案零裸 '<'。
7. **唯一过程发现已闭环（F-T1，INFO）**：终验开始时运行实例为 00:04:50 旧进程（早于
   t4 mtime，F-V1 型复发）→ 已按任务约定立即上报队长 → 队长重启后复验通过。
   **无未决 P0/P1，7 项验收全达成，终验结论：通过。**

---

## 1. 全量测试（含 slow 层）

```
python -m pytest tests/ -m "slow or not slow" -q
→ 1292 passed, 1 skipped
```

- 与 t4 报告基线（1292 passed, 1 skipped）**完全一致，零回归**；
- skip 1 例为既有条件跳过项（021BY t2 起各轮一致，与本批无关）；
- 套件内含本批新增 17 例（t2 14 例 + t4 `TestIndustryBgCache` 3 例）随全量通过。

## 2. 静态门

| 验证项 | 命令 | 结果 |
|---|---|---|
| ruff | `ruff check .` | **All checks passed!**（EXIT=0） |
| mypy | `mypy app.py config.py modules` | **Success: no issues found in 57 source files** |
| 红线 | `python scripts/check_redlines.py` | **28/28 通过（EXIT=0）**（含 R16 风控阈值默认值、R17 依赖白名单、R18/R19） |
| JS 语法 | `node --check static/js/market.js` | **SYNTAX OK** |
| 前端逻辑 | `node scripts/check_market_js_021by.js` | **ALL FRONTEND CHECKS PASSED（16/16）**（F-V3 挂起/重应用/清挂起、F-V4 提示两态、t2 回归：F1 载荷/三态排序/位置筛选/CSV 转义/预设闭环） |

## 3. 一致性审计独立重跑（全部 mode=ro，可复现）

| 审计 | 命令 | 结果 | P0/P1 |
|---|---|---|---|
| 021BS r1 | `python scripts/audit_consistency_021bs.py --out docs/reports/021by_verify_rerun_r1_20260925.md` | EXIT=0；**异常 0**；一致 344 条；发现 90 条 = OK 23 + P2 67 | **0/0** |
| 021BS r2 | `python scripts/audit_consistency_021bs_r2.py --out docs/reports/021by_verify_rerun_r2_20260925.md` | EXIT=0；新维度 INFO 3 + P2 5；N01 兜底在场 P2×5、**兜底亦缺 P1=0**；N02 命中 0；五数据流重跑无新增不一致 | **0/0** |
| 021BX 完整度 | `python scripts/audit_data_completeness_021bx.py --out docs/reports/021by_verify_rerun_021bx_20260925.md` | EXIT=0；**核心评分链全绿**（13 维度中 9 个零缺口；缺口集中盘口/股东户数=既有登记项） | **0/0** |
| 位置分位同源 | `python scripts/audit_pos_pctile_021by.py --out docs/reports/021by_verify_pos_pctile_20260925.md` | EXIT=0；抽样 **10/10 OK** | **0/0** |

**P2 类构成对账（r1，67 条全部既有类）**：F04（预警铃铛×报告时点差）×53 ——与 t4 记录
「随交易日自然累积」一致；R04（迟滞标注面）×8；F03（盘中vs收盘双口径）×2；
R11（罗盘×操盘手阶段口径差）×2；R06×2。**与 t3（P2×15 期）相比仅 F04 数量随时间自然
增长，无新增异常类**。r1 内 021BU 证据规则面：R16 评级徽章×回测基准、R17 诚实门、
R18 价格基准×现算、F06 看板证据流全 OK——021BU 同源契约未被 021BY 破坏。

> 复核备注：r2 归档 §5 表头「修复建议汇总（P1 两条…）」为脚本存量模板文案（继承自更早
> 批次），本次已逐节复核——**实际表格 0 行，摘要明确 P1 findings：0 项**，非新发现。

## 4. 位置分位独立复算对账（不采信 builder）

**双通道独立复算，合计 25/25 OK**：

| 通道 | 实现 | 抽样 | 结果 |
|---|---|---|---|
| ① 复用 t3 审计脚本 | 脚本内独立公式（近 60 根收盘 (now−lo)/(hi−lo) round3 + 分带 0.4/0.7），mode=ro SQL 抽样 | raw_kline≥40 根前 10 只 | **10/10 OK** → 存档 `021by_verify_pos_pctile_20260925.md` |
| ② checker 全新独立实现 | 本次终验另写的独立脚本（`%TEMP%`，不 import scout 脚本）：独立 SQL（`ORDER BY RANDOM()` 抽 15/56，seed=20260925）+ 独立公式实现，比对生产三处（`position_pctile_of_klines` / `_current_pos_pctile` / `_volume_structure`，round2 容差 0.005） | 随机 15 只 | **15/15 OK**，分带覆盖 low×11 / mid×3（0.450/0.464/0.472）/ high×1（0.817） |

两通道均证明：独立基准 = 扫描侧纯函数 = 回测证据面 = 操盘手量价结构（舍入差容差内），
**同源唯一真相成立**；比对对象是生产实现而非 builder 的任何声明或中间产物。

## 5. 运行实例只读冒烟 + UX 走查

### 5.1 进程新鲜度（F-T1：F-V1 型复发 → 已闭环）

| 时点 | 事件 |
|---|---|
| 09-25 00:04:50 | t3 复审后队长重启的实例（处置 t2/t3 的 F-V1） |
| 00:07:08 / 00:10:35 | t4 代码落盘（`blueprints/market.py` / `static/js/market.js` mtime）——**该实例随即过时** |
| 终验开始 | checker 实测监听进程 start=00:04:50 **早于 t4 mtime** → 按任务约定**立即上报队长**（影响面：仅 t4 增量 F-V2 未加载；C1/模板已在线） |
| 00:32 左右 | 队长重启完成 |
| 复验 | 监听进程 **PID 25544，start=00:32:03**（晚于全部 021BY mtime），`/api/health` 连续 3 次 200 running v5.0 ✅ |

### 5.2 端点探针（全部 GET 只读，零扫描/零写库/零外部请求）

| 探针 | 结果 |
|---|---|
| `GET /` | 模板新控件标记 8/8 在位：`msPresetSel`/`msPosFilter`/`msPosSort`/`msGroupSel`/`msCoarseCsvBtn`/`msSignalCsvBtn`/「存为方案」/「低位优先」 |
| `GET /static/js/market.js` | 与磁盘**逐字符一致**（served chars = disk chars = 110,357，尾部 300 字符相等；字节差为 UTF-8 多字节计数差）；新函数标记全在：`_msFiltersPayload`/`boards`/`industries`/`_msPendingIndustry`/`_msApplyPendingIndustry`/`group_id`/`﻿` BOM |
| `GET /api/market/scan/status` | success=true；快照 2026-09-21T22:28:45（stale=true 属快照语义正常）；universe=5215 |
| `GET /api/groups?type=watchlist` | 8 组；字段 `id/name/stock_count/...` 与 C6 契约逐项匹配（分组直达数据源就绪） |
| `GET /api/market/industry-fund-flow` | trade_date=2026-09-24，496 行，summary 在；字段含 `main_net/streak_days/main_net_5d`（C2 行业背景数据源就绪） |
| `GET /api/market/scan/library` | 信号库 **4 键全为金叉买点类**：`kdj_golden`/`kdj_golden_low`/`macd_golden_above`/`macd_golden_below`——「只产买点候选」边界运行面实证 |
| `GET /api/market/scan/watchlist-signals` | scope=watchlist_offline，success=true；results 含 `matches/resonances/kline_count/kline_upto`（离线巡检零网络正常）；**无 pos 字段 = t2 遗留观察 3（既有登记项，C1 范围仅在线第②段），维持原移交** |

### 5.3 F-V2/F-V3/F-V4 核验方式说明（诚实标注）

- **F-V2（行业背景缓存）**：只读边界内不能 POST `scan-signals`（会触发真实扫描=腾讯 K 线
  网络+快照整表替换），故缓存命中行为不做 live 触发，核验=①静态代码（`_scan_industry_bg_map`
  轻查询 `MAX(trade_date)` 先行于缓存键检查，未命中才全量装配，blueprints/market.py L196-233）
  + ②pytest `TestIndustryBgCache` 3 例（命中 1 次装配/空表 0 次/新交易日重装配，随全量套件通过）。
- **F-V3/F-V4**：前端逻辑冒烟 16/16 PASS + 服务端 JS 与磁盘一致（5.2）→ 行为面已覆盖。
- **UX 走查**：t3 §3.1 代码级走查结论维持（t4 仅 F-V3/F-V4 增量，未动 UI 结构）；
  本次运行面复核：模板控件在位（5.2）、served JS 含位置列渲染/F-V4 行业筛选提示/
  预设三件套/CSV 导出/分组直达全链标记、文案合规（「低位（40%以下）」等无裸 '<'）、
  空态降级（pos null→「—」带 title）在冒烟断言内。浏览器端到端体验项（扫描后位置列
  实际渲染等）仍属 t3 §3.2 用户复验清单范围，非只读探针可替代，如实标注。

## 6. 红线边界 git 核验（005258a → 工作区）

| 项 | 方法 | 结论 |
|---|---|---|
| 变更集范围 | `git diff 005258a --stat` | 13 文件（market_screener.py / blueprints/market.py / market.js / index.html / tests / 新增审计与冒烟脚本 / 报告）——全部在 t2 §4 + t4 §8.4 声明清单内 |
| **B24** | `git diff 005258a --name-only -- modules/advisor.py modules/price_advisor.py …` | **空**——advisor.py / price_advisor.py / price_backtest.py 零触碰 |
| **R7** | 同上（modules/scoring_engine.py modules/rating_config.py） | **空**——v5 引擎与评级档位零触碰 |
| **R8** | 同上（modules/data_contract.py modules/backtest_engine.py） | **空**（backtest_engine 亦零触碰，位置分位证据面原样） |
| **R16** | config.py diff 空 + `BATCH_OPERATION_LIMIT = 20` 在位 + market.js L498 `checked.length > 20` 拦截在位 | **≤20 上限原样** |
| **classify_stage** | 变更集 Select-String 'classify' 为空；实体在 `modules/trader_advisor.py`（不在变更集） | **零触碰** |
| **只产买点边界** | market_screener.py diff 全览（50 行）：纯新增（pos 纯函数块 + docstring 更新 + run_signal_chunk 3 行透传）；`SIGNAL_LIBRARY/RESONANCE_LIBRARY/def detect_` 在变更行中唯一匹配为 **docstring 引用**（「不参与…判定」）；运行面 scan/library 4 键全为买点类（5.2） | **边界未动** |
| **文案禁裸 '<'** | 新增行 `<数字` 模式扫描：market.js 0 处 / index.html 0 处；存量 L1909「<30偏冷」diff 0 行未触碰 | **合规**（存量豁免项维持 t3 登记） |
| **零新表/快照语义** | 无 schema 变更文件；scan/status 快照面正常（5.2）；预设走 localStorage、🆕 走内存 | **market_snapshot 语义不动** |
| **R17 零新依赖** | requirements.txt 不在变更集；红线 R17 白名单项 PASS | **合规** |

## 7. 发现清单（findings）

**无 P0/P1 未决项。** 过程观察 2 条（INFO 级，不阻塞）：

| # | 级别 | 描述 | 处置 |
|---|---|---|---|
| F-T1 | INFO | 终验开始时运行实例（00:04:50）早于 t4 代码落盘（00:07:08/00:10:35）——「改代码即重启」纪律的窗口期再次出现（同 t3 F-V1 型）。本次由 checker 上报、队长重启、复验闭环 | **已闭环**。建议后续：t4 类收尾批次代码落盘后立即重启；可考虑收尾清单加「进程 start > 最新 mtime」自检探针 |
| F-T2 | INFO | F-V2 缓存命中行为未在真实扫描中 live 观测（只读边界禁 POST 扫描） | 已由静态代码 + 3 例 pytest 覆盖（§5.3）；建议下次用户真实扫描时顺带观察批统计（非阻塞） |

维持 t2 遗留观察 1-3 原移交（C2 匹配率待实测；「低位优先」默认关闭待真实使用反馈；
离线巡检路径无 pos 字段——本次运行面探针 H 项复证第 3 条）。

## 8. 验证记录（全部可复现）

| # | 验证项 | 命令 | 结果 |
|---|---|---|---|
| 1 | 全量测试含 slow | `python -m pytest tests/ -m "slow or not slow" -q` | **1292 passed, 1 skipped** |
| 2 | ruff | `ruff check .` | All checks passed! |
| 3 | mypy | `mypy app.py config.py modules` | no issues in 57 source files |
| 4 | 红线 | `python scripts/check_redlines.py` | 28/28（EXIT=0） |
| 5 | JS 语法 | `node --check static/js/market.js` | SYNTAX OK |
| 6 | 前端逻辑 | `node scripts/check_market_js_021by.js` | 16/16 PASS |
| 7 | 审计 r1 | `python scripts/audit_consistency_021bs.py --out docs/reports/021by_verify_rerun_r1_20260925.md` | EXIT=0，异常 0（P2×67 既有类） |
| 8 | 审计 r2 | `python scripts/audit_consistency_021bs_r2.py --out docs/reports/021by_verify_rerun_r2_20260925.md` | EXIT=0，P1=0 |
| 9 | 审计 021BX | `python scripts/audit_data_completeness_021bx.py --out docs/reports/021by_verify_rerun_021bx_20260925.md` | EXIT=0，核心链全绿 |
| 10 | 位置分位① | `python scripts/audit_pos_pctile_021by.py --out docs/reports/021by_verify_pos_pctile_20260925.md` | EXIT=0，10/10 OK |
| 11 | 位置分位②（checker 独立实现） | `python %TEMP%\verify_pos_021by_checker.py`（随机抽样 seed=20260925） | **15/15 OK** |
| 12 | 进程新鲜度 | `Get-NetTCPConnection :5000` + StartTime 对比 mtime | PID 25544 start 00:32:03 > 全部 mtime |
| 13 | 只读端点探针 ×7 | GET /、market.js、scan/status、groups、industry-fund-flow、scan/library、watchlist-signals | 全部正常（§5.2） |
| 14 | 边界 git 核验 | `git diff 005258a`（--stat / --name-only 保护集 / 全览 market_screener.py） | §6 全部通过 |

## 9. 产物

- 本终验报告：`docs/reports/021by_verify_20260924.md`
- 审计独立重跑存档：`021by_verify_rerun_r1_20260925.md` / `..._r2_...` / `..._021bx_...` / `021by_verify_pos_pctile_20260925.md`
- （通道②独立复算脚本为临时产物置于 %TEMP%，不入库；方法与抽样参数已完整记录于 §4）

## 10. 结论

**021BY 终验通过。** 任务七项验收逐项达成：①全量测试（含 slow）1292 passed 零回归；
②ruff/mypy/红线 28/28 全绿；③四一致性审计独立重跑零 P0/P1（P2 全部既有类且构成与
t4 记录一致）；④位置分位独立复算双通道 25/25 对账一致（不采信 builder）；⑤运行实例
只读冒烟 + UX 走查通过（服务已运行最新代码，全链端点正常）；⑥扫描器只产买点边界/
R16 ≤20/B24/R7/R8/classify_stage/文案红线 git 核验零触碰；⑦发现项仅 2 条 INFO 观察
（F-T1 已闭环、F-T2 有测试面覆盖），无阻塞。021BY 批次具备收尾条件。
