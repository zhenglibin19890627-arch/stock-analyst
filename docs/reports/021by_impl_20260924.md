# 021BY 实施报告：位置分位证据选股 + 筛选增强 + 便捷性高价值项（t2）

> 日期：2026-09-24 ｜ 执行：builder（任务 t2，实施）｜ 批次：021BY
> 依据：t1 方案 `docs/reports/021by_market_plan_20260924.md`（C1-C7 全量实施，
> C8 不做清单锁定不碰）；captain 指令按 C1→C7→C4/C5/C6→C2→C3 顺序执行。
> 硬约束遵守：扫描器「只产买点候选」边界不动；全案**零新增**新浪/腾讯/东财请求；
> 零新表零新列（market_snapshot 快照语义不动）；B24/R7/R8/classify_stage 零触碰；
> 文案禁裸 '<'；加自选批量 ≤20 上限原样保留。

---

## 1. 实施清单（C1-C7 全做）

### C1【证据驱动核心】位置分位接入选股流 ✅

**后端**（`modules/market_screener.py`）：
- 新增纯函数 `position_pctile_of_klines(kline_rows, lookback=60, min_bars=40)`
  → `{'pos_pctile': float|None, 'pos_band': 'low'|'mid'|'high'|None}`。
  口径与 `backtest_engine._calc_pos_and_dd20` / `trader_advisor._volume_structure`
  完全同源：近 60 根**收盘**、≥40 根才算、round 3、分带 low<0.4 / mid 0.4~0.7 / high≥0.7。
- `run_signal_chunk` 对每只**命中股**用已拉取的同一份日 K 现算，results 项新增
  `pos_pctile` / `pos_band`——**零新增网络请求**；K 线不足 40 根为 None（不硬造）。

**前端**（`static/js/market.js` + `templates/index.html`）：
- 信号结果表（共振组 + 单一信号组）新增「位置」列：`低位 12%` / `中位 55%` / `高位 85%` / `—`；
  低位绿 / 高位红 / 中位灰（仅视觉分组）。
- 「位置」筛选下拉：全部 / 低位（40%以下）/ 中低位（70%以下）——纯前端过滤，与「只看共振」可叠加。
- 「低位优先」开关（默认关闭）：开启时各组内按 `pos_pctile` 升序、None 殿后；关闭维持现状。

**不做**（与方案一致）：不在第①段/`apply_filters`/快照层做（否决全市场逐票拉 K 线）；
不入库；不进 `SIGNAL_LIBRARY`/`RESONANCE_LIBRARY` 判定；不做打分/仓位映射。
测试 `test_hit_judgment_untouched_by_position` 锁定 matches 与 `detect_signals` 直接输出一致
（位置只做展示/筛选/排序标注，命中语义零改动）。

### C7【审计修复+交互补齐】F1 筛选键对称化 + 粗筛表排序 ✅

- **F1 修复**：新增 `_msFiltersPayload()`——服务端载荷改传 `boards: [v]` / `industries: [v]`
  复数数组键（非空时）；`_msFilters()` 单数键保留给本地过滤与预设存取。服务端零改动。
- **500 截断静默丢弃点核查**（captain 指定确认项）：读 `run_coarse_scan` 代码路径——
  `pre_filters`（除量比外全部条件，**含 boards/industries**）在 `apply_filters(snap['rows'], ...)`
  对全量宇宙（~5553 只）先行过滤，`rows[:500]` 截断发生在其后。**修复后服务端行业筛选
  先于截断生效，「小行业候选被静默丢弃」路径不可达**（修复前该路径可达：单数键服务端
  不识别 → 行业/板块过滤缺位于截断前）。
- **粗筛表三态排序**：现价/涨跌%/换手%/量比/市值 五列表头可点击（↓降序→↑升序→↕默认市值序，
  与 021T 行业资金流表同型交互）；None 值殿后；说明行动态显示当前排序。

### C4【便捷】筛选预设方案保存（localStorage）✅

- 「💾 存为方案」/「方案」下拉 /「🗑 删除」三件套；保存内容 = `_msFilters()` 全部 12 键
  + 信号勾选集 + 触发窗口；localStorage 键 `ms_filter_presets_v1`
  （结构 `{name: {filters, signals, window, saved_at}}`），上限 10 个（超限提示先删），
  同名覆盖需确认；`ms_filter_presets_last` 仅记忆上次使用的方案名（不自动改值）。
- 应用预设：恢复全部筛选值 + 信号勾选 + 窗口；行业下拉在快照已拉取时同步重建并恢复选中。
- **不复用 `strategy_params` 表**（F5 裁定：语义是评分优化审计日志，零新表约束下
  localStorage 是唯一干净落点）。

### C5【便捷】扫描结果导出 CSV ✅

- 粗筛表「⬇ 导出CSV」：导出**当前过滤后全集**（含当前排序，非仅展示的前 100 行），
  列与页面一致。
- 信号区「⬇ 导出CSV」：导出位置筛选后全集（不受「只看共振」视图开关影响），
  列含：代码/名称/命中信号/买点共振(星)/位置/位置分位%/现价/涨跌%/量比/换手%/市值/行业/
  行业资金背景/新命中。
- Blob 生成，UTF-8 带 `\ufeff` BOM（防 Excel 中文乱码），文件名带时间戳。

### C6【便捷】加自选分组直达 ✅

- 「加入自选」条新增分组下拉（数据源 `GET /api/groups?type=watchlist`，含各组自选数，
  懒加载一次，本地库零外部请求）；`msAddSelected` 的 `POST /api/stocks` 请求体附
  `group_id`（端点既有支持，`blueprints/watchlist.py` L323/L351——后端零改动）；
  默认「未分组」。≤20 批量上限与前端拦截原样保留（R16）。

### C2【能力】行业资金流 × 个股候选软联动 ✅

- **后端**（`blueprints/market.py`）：`api_market_scan_signals` 响应为每只命中股附
  `industry_flow_bg`（读库 `industry_fund_flow` 最新交易日，经 `get_industry_flow_bg_map`
  + `match_board_name` 匹配 entries 透传的行业名——与看板行业背景同型同源，不新造匹配器）；
  批统计 `industry_match_hit` / `industry_match_total` 供观察匹配率。
  模块级缓存 `_scan_industry_bg_map()`：key=`交易日|库路径`，同一次扫描 12 批共享一次读库，
  跨库（测试隔离）不串；读库失败降级为无行业列，不阻塞扫描。**零网络**。
- **前端**：entries 透传 `industry`；信号表行业列附 chip
  「行业净流入 +2.3亿 · 连流3日」（匹配成功才显示；streak 为 0 时省略方向段）；
  匹配不上的候选如实不显示（不硬造）。
- 不动行业资金流采集与刷新冷却；不做行业资金流强过滤。

### C3【轻量】重扫「新命中」标记（会话内内存对比）✅

- 本轮正常完成时把命中 symbol 集存入页面变量 `_msPrevHits`（中途停止不改基线）；
  同会话再次扫描后新出现的命中在名称旁标 🆕，结果区顶部注明
  「与上一轮扫描对比：N 只；仅内存对比，刷新页面即清除」。纯前端内存，零存储零网络。

---

## 2. 数据源风控核验（扫描请求频率不变量）

| 通道 | 本轮变化 |
|---|---|
| 新浪 hs_a 列表（56 页/轮） | 不变（零新增） |
| 新浪行业映射（7 天缓存） | 不变 |
| 腾讯批量行情增强 | 不变 |
| 腾讯日K/周K（第②段） | **不变**——位置分位复用同一份已拉取 K 线，零新增请求 |
| 东财（行业资金流） | 不变——C2 只读已落库表（库内毫秒级）+ 批间缓存 |
| `GET /api/groups` | 新增 1 次本地库读（懒加载一次，加自选条首次展示时） |

## 3. 红线合规核验

| 红线/约束 | 结论 |
|---|---|
| 扫描器只产买点边界 | ✅ 位置分位仅展示/筛选/排序；`SIGNAL_LIBRARY`/`RESONANCE_LIBRARY`/`detect_*` 零改动（`tests/test_sell_signal_alert.py` 平行库边界测试全绿） |
| R16 批量 ≤20 | ✅ 上限与前端拦截原样保留 |
| 零新表 / market_snapshot 语义不动 | ✅ 位置分位现算零落库；预设走 localStorage；🆕 会话内存 |
| B24 / R7 / R8 / classify_stage | ✅ 零触碰（改动面仅 market_screener.py / blueprints/market.py / market.js / index.html / tests） |
| 文案禁裸 '<' | ✅ 新 UI 文案用「40%以下」「70%以下」表述 |
| R17 零新依赖 | ✅ 零新 pip 依赖 |
| 诚实原则 | ✅ K 线不足显示「—」；行业匹配不上不显示；🆕 标注时效 |

## 4. 变更文件清单

| 文件 | 变更 |
|---|---|
| `modules/market_screener.py` | +`position_pctile_of_klines` 纯函数与分带常量；`run_signal_chunk` 透出 pos_pctile/pos_band（docstring 同步） |
| `blueprints/market.py` | +`_scan_industry_bg_map` 读库缓存；scan-signals 附 industry_flow_bg 与匹配统计 |
| `static/js/market.js` | C1 位置列/筛选/排序；C7 F1 载荷修复+粗筛三态排序；C4 预设五函数；C5 CSV 四函数；C6 分组懒加载与 group_id；C3 🆕 基线；entries 透传行业 |
| `templates/index.html` | 方案三件套、两个导出按钮、位置筛选下拉、低位优先开关、分组下拉 |
| `tests/test_market_screener_021by.py` | 新增 14 例（见 §5） |
| `docs/reports/021by_impl_20260924.md` | 本报告 |

## 5. 测试

**新增 `tests/test_market_screener_021by.py`（14 例，离线不触网）**：
- `TestPositionPctile`（7 例）：与 `_calc_pos_and_dd20` **公式对拍**（临时库种 raw_kline，
  同输入两路计算必同值——同源唯一真相守卫）、不足 40 根 None、hi==lo None、
  分带边界 0.39/0.40/0.699/0.70、60 根回看窗（首根极端值剔除）、close 缺失行跳过、空输入。
- `TestRunSignalChunkPos`（2 例）：mock K 线透传字段与纯函数同值；matches 与
  `detect_signals` 直接输出一致（命中判定不被位置改动的回归位）。
- `TestScanSignalsBlueprint`（5 例）：临时库 seed `industry_fund_flow` + mock
  run_signal_chunk——匹配成功附 bg（main_net/streak_days 断言）、匹配不上 None 不报错、
  旧前端无 industry 兼容、无资金流数据无字段不报错、别名/空行业名降级。

**前端逻辑冒烟台架**（临时验证脚本，不入库；stub DOM/localStorage 后 vm 加载 market.js，
26 断言全过）：F1 载荷数组、三态排序（含 None 殿后）、位置筛选/低位优先排序、
🆕 判定三态、CSV 转义（逗号/引号/null）、预设保存→应用恢复（筛选值/窗口/信号勾选）→
删除→最后使用名清理。

## 6. 验证记录

| 验证项 | 命令 | 结果 |
|---|---|---|
| 快层测试 | `python -m pytest tests/` | **1283 passed, 1 skipped**（111.8s） |
| 全量测试 | `python -m pytest tests/ -m "slow or not slow"` | 见下补记 |
| ruff | `ruff check .` | **All checks passed!** |
| mypy | `mypy app.py config.py modules` | **no issues in 57 source files**（修复 1 处 var-annotated 后） |
| 红线 | `python scripts/check_redlines.py` | **28/28 通过（EXIT=0）** |
| JS 语法 | `node --check static/js/market.js` | **SYNTAX OK** |

> 全量（slow 层）结果补记：`1289 passed, 1 skipped`（817s；fast 基线 1283 + slow 层
> 6 例全部通过，零回归；skip 1 例为既有条件跳过项，与本批无关）。

## 7. 遗留观察项（不阻塞，移交后续）

1. **C2 匹配率待实测**：新浪行业名↔东财板块名匹配率需真实扫描观察
   （`industry_match_hit/total` 统计已透出）；若长期低于五成，再评审补别名映射表
   （新表需豁免，本批次不做）。
2. **「低位优先」默认关闭**：按方案开放问题 2，建议真实使用一周后再定是否默认开启
   （UI/UX 反向校验契约层——AGENTS.md §9.6 教训）。
3. **离线巡检路径无 pos 字段**：`scan_watchlist_signals`（021BP 离线复算）未附
   pos_pctile（方案 C1 范围仅在线第②段）；如需对齐可按同型低成本追加。
4. 手工验收清单（方案 §6.3：C4 保存/加载/删除、C5 导出 BOM 中文、C6 分组直达、
   C7 排序与 boards 数组、C1 筛选/排序/列渲染、C3 🆕 标记）已由逻辑冒烟台架覆盖
   函数级断言，浏览器端到端体验验收留给用户实际扫描时确认。

---

## 8. t4 补充节：第二轮修复（复审发现全清）

> 日期：2026-09-25 ｜ 执行：builder（任务 t4）｜ 依据：t3 复审报告
> `docs/reports/021by_review_20260924.md` §4 发现清单。
> F-V1（P1，服务进程早于新代码）已由队长处置（重启 + 首页新控件标记实测在位），
> 不在本任务范围；本节覆盖队长确认的三项轻量修复。

### 8.1 修复内容

| # | 级别 | 修复 | 落点 |
|---|---|---|---|
| **F-V2** | P2 | 行业背景缓存键探测提前：`_scan_industry_bg_map` 改为先用轻查询（`SELECT MAX(trade_date)`，毫秒级）做缓存键，命中即返回缓存 bg_map，未命中才调 `get_industry_flow_bg_map()` 全量装配（含 compute_streaks 全表扫描）。修复前全量读库在键检查前无条件执行，「12 批共享一次读库」声明不成立 | `blueprints/market.py` |
| **F-V3** | P2 | 预设行业条件 fresh 会话恢复：`msApplyPreset` 检测行业下拉尚无该 option 时挂起至 `_msPendingIndustry`；扫描成功回调（下拉已重建）经 `_msApplyPendingIndustry()` 重应用一次（一次性，无论命中与否清挂起）；用户手动改行业立即清挂起（手动优先） | `static/js/market.js` |
| **F-V4** | P2·低 | 行业筛选生效时粗筛表说明行补提示「已按行业「X」筛选（本表仅含本轮扫描的该行业结果）；切换行业请重新扫描」，标注下拉收缩语义（F1 修复后的行为一致化副作用） | `static/js/market.js` |

不做（维持 t3 登记）：F-V5/F-V6/F-V7 为 INFO 级，不在本批范围（F-V7 前端冒烟台架
已随 F-V3/F-V4 测试需求持久化为 `scripts/check_market_js_021by.js`，事实上闭环）。

### 8.2 测试（每修一条附测试）

- **F-V2**（pytest，`tests/test_market_screener_021by.py::TestIndustryBgCache`，3 例）：
  `test_cache_hit_skips_full_assembly`（同交易日第二次调用命中缓存，全量装配仅 1 次——
  修复前为 2 次）、`test_empty_table_zero_full_assembly`（空表键探测即返回，全量装配 0 次）、
  `test_new_trade_date_reassembles`（新交易日落库 → 键变化 → 重新装配，新数据可见）。
- **F-V3/F-V4**（前端逻辑检查脚本 `scripts/check_market_js_021by.js`，16 断言全过，
  dev-only 可复跑：`node scripts/check_market_js_021by.js`）：
  F-V3 挂起生效（select 未被静默赋值）→ 扫描后重应用 → 一次性清除 → 幂等 →
  手动改行业清挂起 → option 已存在时立即生效不挂起；F-V4 提示有/无两态；
  另含 t2 回归（F1 载荷数组、三态排序 None 殿后、位置筛选/低位优先、CSV 转义、
  预设保存-应用-删除闭环）。该脚本同时落地 t3 F-V7 建议（最小前端冒烟持久化）。

### 8.3 验证记录（t4）

| 验证项 | 命令 | 结果 |
|---|---|---|
| 快层测试 | `python -m pytest tests/ -q` | **1286 passed, 1 skipped**（118.6s；= t2 基线 1283 + 新增 3 例） |
| 全量测试 | `python -m pytest tests/ -m "slow or not slow" -q` | **1292 passed, 1 skipped**（718s，零回归） |
| ruff | `ruff check .` | **All checks passed!** |
| mypy | `mypy app.py config.py modules` | **no issues in 57 source files** |
| 红线 | `python scripts/check_redlines.py` | **28/28 通过（EXIT=0）** |
| JS 语法 | `node --check static/js/market.js` | **SYNTAX OK** |
| 前端逻辑 | `node scripts/check_market_js_021by.js` | **16/16 PASS** |
| 审计重跑 r1 | `python scripts/audit_consistency_021bs.py --out docs/reports/021by_t4_rerun_r1_20260925.md` | **EXIT=0，异常 0**（P2×67 全部既有类：F04 预警时点差×53 随交易日自然累积、R04×8、F03/R11/R06×6——与 t3 分级一致，无新增类） |
| 审计重跑 r2 | `python scripts/audit_consistency_021bs_r2.py --out docs/reports/021by_t4_rerun_r2_20260925.md` | **EXIT=0，P1=0**（N01 兜底在场 P2×5、N02 命中 0，与 t3 一致） |
| 审计重跑 021BX | `python scripts/audit_data_completeness_021bx.py --out docs/reports/021by_t4_rerun_021bx_20260925.md` | **EXIT=0**，核心评分链全绿（9/13 维度零缺口，缺口为既有登记项） |
| 位置分位同源 | `python scripts/audit_pos_pctile_021by.py --out docs/reports/021by_t4_pos_pctile_20260925.md` | **EXIT=0**，抽样 10/10 OK（独立基准=扫描=回测=操盘手） |

### 8.4 t4 变更文件

| 文件 | 变更 |
|---|---|
| `blueprints/market.py` | F-V2：缓存键轻查询先行，未命中才全量装配 |
| `static/js/market.js` | F-V3：`_msPendingIndustry` 挂起/`_msApplyPendingIndustry` 重应用/手动改清挂起；F-V4：行业筛选提示行 |
| `tests/test_market_screener_021by.py` | +`TestIndustryBgCache` 3 例 |
| `scripts/check_market_js_021by.js` | 新增：前端逻辑持久冒烟检查（16 断言，dev-only） |
| `docs/reports/021by_t4_rerun_*.md` ×3 / `021by_t4_pos_pctile_20260925.md` | 审计重跑存档 |
| 本报告 §8 | t4 补充节 |

**审计断言结论**：四审计重跑 **零 P0/P1**（r1 异常 0、r2 P1=0、021BX 核心链全绿、
位置分位同源 10/10）——复审发现全清，具备 t5 合并终验条件。
