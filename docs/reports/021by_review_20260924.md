# 021BY 复审报告：同源核验 + 加自选链路回归 + UX 走查（t3）

> 日期：2026-09-24 ｜ 执行：scout（任务 t3，复审）｜ 批次：021BY
> 复审对象：t2 实施（commit `ea7ced3`，2026-09-24 23:52:30；变更 5 文件 +759/−25）
> 方法：三只读审计脚本重跑 + 独立 SQL 抽样复算（新增可复现脚本
> `scripts/audit_pos_pctile_021by.py`）+ 代码级逐项对账 + git 变更集核验 + 运行面探针。
> 全程零写库（审计面 mode=ro）、零网络采集、零触碰 B24/R7/R8/classify_stage。

---

## 0. 结论速览

1. **一致性审计重跑零 P0/P1 达成**：021BS r1（异常 0，OK 344 + P2 15）、
   r2（P1 0，N01 兜底在场 P2×5、N02 命中 0）、021BX 完整度（核心评分链 9/13 维度零缺口，
   已知项 K1-K6 与 t7 后状态一致，无新增异常）。
2. **位置分位同源成立（独立 SQL 抽样 10/10 OK）**：审计脚本内独立实现公式，对真实库
   raw_kline 近 60 根收盘复算，与 ①`market_screener.position_pctile_of_klines`（021BY C1）
   ②`backtest_engine._current_pos_pctile`（021BU 证据面）③`trader_advisor._volume_structure`
   （round 2 容差 0.005）四路逐一一致。
3. **⚠ 交付门 P1（F-V1）：运行中服务进程早于 021BY 代码**——python/pythonw 进程启动于
   22:13~22:40，t2 提交于 23:52:30（蓝图 mtime 23:34:59）。**在线服务仍是 021BW 时代代码**：
   扫描响应无 `pos_pctile`/`industry_flow_bg`，模板/蓝图零新功能。前端 market.js 由 Flask
   静态路由直接读盘（新 JS 已在磁盘）——新旧混合态经核查**不崩溃**（pos 缺失→「—」、
   bg 缺失→无 chip，均为优雅降级），但 **021BY 全部服务端功能在用户浏览器中静默缺席**，
   直至重启服务。处置：重启（start.bat/托盘）→ 浏览器硬刷新（Ctrl+F5）→ 按 §3.2 清单复验。
4. **P2 两项代码发现**：F-V2 行业背景"批间缓存"无效（`get_industry_flow_bg_map()` 在缓存键
   检查前每次全量执行，含 `compute_streaks` 全表扫描——t2「12 批共享一次读库」声明不成立，
   实际影响毫秒级）；F-V3 预设中行业条件在**未扫描过的会话**恢复时静默丢失
   （行业下拉尚无该 option，`el.value` 赋值静默失败，扫描载荷即缺行业条件）。
5. 代码面其余全部确认：F1 修复路径复核成立（服务端行业筛选先于 500 截断，静默丢弃不可达）、
   ≤20/R16 原样、分组契约匹配、命中判定未被位置分位改动（测试锁定）、文案禁裸 '<' 合规、
   changedPaths 与 t2 声明逐文件一致。
6. 验证全部可复现：三审计 + 位置分位脚本 + `pytest tests/`（**1283 passed, 1 skipped**，
   与 t2 报告一致）+ 红线 **28/28** + ruff 全绿 + `node --check` OK，均附于 §6。

---

## 1. 一致性审计重跑（①；全部 mode=ro 可重复）

| 审计 | 命令 | 结果 | P0/P1 | 与上轮（021BX t7 后基线）对比 |
|---|---|---|---|---|
| 021BS r1 | `python scripts/audit_consistency_021bs.py --out docs/reports/021by_review_rerun_r1_20260924.md` | EXIT=0；发现 38 条（OK 23 + P2 15），一致 344 条，**异常 0** | **0/0** | P2 构成不变（R04×8 迟滞标注面、R06×2、R11×2 罗盘口径差、F03×2 盘中vs收盘双口径、F04×1 预警时点差——全部既有"已标注/时点差"类） |
| 021BS r2 | `python scripts/audit_consistency_021bs_r2.py --out docs/reports/021by_review_rerun_r2_20260924.md` | EXIT=0；新维度 7 条（INFO 2 + P2 5）；N01 兜底在场 P2×5、**兜底亦缺 P1=0**；N02 命中 0 | **0/0** | 与 021BW 后基线一致（存量报告 stored 摘要缺失由 live 兜底承接，设计内） |
| 021BX 完整度 | `python scripts/audit_data_completeness_021bx.py --out docs/reports/021by_review_rerun_021bx_20260924.md` | EXIT=0；K线日/周/月、财报、资金面、新闻、估值、行业、日报全绿；盘口 K6（源侧断供）与股东户数披露滞后为既有登记项 | **0/0** | K1 换手率已修复回补中（非零 16,932 行）、K2 港股回测已对齐（差 1 天≤阈值）——较 021BX 首轮**改善** |

021BU 证据规则面（r1 内）：R16 评级徽章×回测基准 57 股全 OK、R17 诚实门 56 股全 OK、
R18 价格基准×现算 2 处全 OK、F06 看板证据流 0 异常——**021BU 同源同值契约未被 021BY 破坏**。

---

## 2. 新增功能逐项对账（②）

| 项 | 核验方法 | 结论 |
|---|---|---|
| **C1 位置分位口径同源** | 独立 SQL 抽样复算：`scripts/audit_pos_pctile_021by.py`（mode=ro；审计脚本内**独立实现**公式 (now−lo)/(hi−lo) round3 + 分带 0.4/0.7；抽样 raw_kline≥40 根前 10 只）→ 对比三处生产实现 | **10/10 OK**（如 stock_id=4 基准 0.148/low = 扫描 0.148/low = 回测 0.148 = 操盘手 0.15）。同时套件内 `test_matches_backtest_engine_formula` 公式对拍测试在位（同源唯一真相守卫）。产物：`docs/reports/021by_review_pos_pctile_20260924.md` |
| C1 命中判定不动 | `test_hit_judgment_untouched_by_position`（chunk matches ≡ detect_signals 直接输出）+ git 确认 `SIGNAL_LIBRARY`/`RESONANCE_LIBRARY`/`detect_*` 零改动 | ✅「只产买点」边界未被位置分位触碰 |
| C1 零新增请求 | 代码审读：`run_signal_chunk` 内 pos 现算用已拉取 `klines`（L1263），无新 fetch | ✅ |
| **C7/F1 筛选键** | 前端 `_msFiltersPayload()` board→`boards:[v]`、industry→`industries:[v]`（空则省略）；服务端路径独立复核：`run_coarse_scan` 的 `pre_filters`（含 boards/industries）对全量宇宙先过滤，`rows[:500]` 截断在后 | ✅ t2「静默丢弃路径不可达」结论**复核成立**；`_msFilters()` 单数键保留给本地过滤/预设，双轨清晰 |
| C7 粗筛表排序 | 代码走查：三态（desc→asc→none）+None 殿后 + 说明行动态化；与 021T `flowSort` 同型 | ✅ |
| **C4 预设保存恢复** | 代码走查 + 边界分析：保存（12 键+信号勾选+窗口、上限 10、覆盖确认、last 名记忆）→ 应用恢复 → 删除（连带清 last）链路成立；**发现 F-V3**（fresh 会话行业恢复静默丢失，见 §4） | ⚠ 功能成立，1 项 P2 |
| **C5 CSV 导出** | 代码走查：`\ufeff` BOM、`_msCsvCell` 转义（逗号/引号/换行）、粗筛导出=过滤+排序后全集、信号导出=位置筛选后全集（不受「只看共振」影响）、文件名时间戳 | ✅ |
| **C6 加自选分组直达** | 端点契约核对：`GET /api/groups?type=watchlist` 返回 `groups[].id/name/stock_count` 与 JS 读取字段逐一匹配；`group_id` 仅在 >0 时附加；`POST /api/stocks` 端点既有支持（watchlist.py L323/L351） | ✅；**≤20 拦截原样**（market.js L462 alert），R16 红线核验 `risk_thresholds_default` PASS |
| **C2 行业资金流软联动** | 蓝图代码走查 + 蓝图 seed 测试 5 例（匹配附 bg/匹配不上 None/旧前端兼容/无数据不报错/别名降级）；`match_board_name` 与看板同源未复制 | ✅ 功能正确；**发现 F-V2**（缓存无效，见 §4） |
| C3 会话内🆕 | 代码走查：基线仅在**正常完成**时更新（中途停止不改基线 ✅）、首轮无基线不误标、结果区时效标注 | ✅ |
| 变更集边界 | `git show --stat HEAD`：5 文件与 t2 报告声明逐一致；工作区仅余复审产物（未跟踪） | ✅ B24/R7/R8/classify_stage/卖侧平行库零触碰 |

---

## 3. UX 核对（③真实操作路径走查）

### 3.1 代码级走查（扫描→筛选→排序→加自选→分析）

| 路径节点 | 走查结果 |
|---|---|
| 控件接线 | `msPresetSel`（onchange=msApplyPreset）/💾存为方案/🗑删除/`msCoarseCsvBtn`/`msSignalCsvBtn`/`msPosFilter`/`msPosSort`/`msGroupSel` 全部在模板就位，ID 与 JS 一一对应；`msPosFilter`/`msPosSort` change 事件已绑定重渲 |
| 表格结构 | 共振组与单一信号组表头均 11 列，行单元格数对齐（加入/代码/名称/构成或触发日/**位置**/现价/涨跌/量比/换手/市值/行业）；无错列 |
| 空态与降级 | pos 为 null →「—」带 title 说明（K线不足40根）；位置筛选后为空 → 提示可重置为「全部」；行业匹配不上 → 无 chip 不硬造；CSV 无数据 → 提示先扫描 |
| 文案禁裸 '<' | 新增文案全部合规（「低位（40%以下）」「中低位（70%以下）」）；全文件扫描命中 2 处存量 '<' 均非本批引入（L1122 代码注释、L1857 存量 RSI tooltip「`<30`偏冷」为 020R 期文案，git 确认未在本批变更中） |
| 诚实原则 | 🆕 标注「仅内存对比，刷新页面即清除」；行业 chip 带 board+trade_date title；位置列分带着色仅视觉分组 |

### 3.2 运行面走查（受阻 → 重启后复验清单）

**探针结果**：`GET /api/health` 200（v5.0 running）；`GET /api/market/scan/status` 正常
（快照 2026-09-21，stale）；但 python/pythonw 进程启动时间（22:13~22:40）**早于** t2 提交
（23:52:30）→ **F-V1（P1）：021BY 服务端功能未在运行面生效**。静态 JS 虽已从磁盘读取新文件，
但 scan-signals 响应（旧服务端）不含 `pos_pctile`/`industry_flow_bg`，位置列恒「—」、行业 chip
恒缺、F1 服务端筛选（boards/industries 数组键旧服务端本就支持，此项恰好不受影响）与模板新控件
（Jinja 进程内缓存）均不可依赖。混合态经核查优雅降级、不报错（缺失字段 → 「—」/无 chip）。

**重启后复验清单**（用户浏览器端到端，约 5 分钟）：
1. 硬刷新（Ctrl+F5）市场行情页 → 出现「方案/💾存为方案/位置/低位优先/⬇导出CSV/加入分组」控件；
2. 扫描（可用现有快照条件）→ 第②步命中表出现「位置」列（低位/中位/高位 + %）；
3. 位置筛选选「低位（40%以下）」→ 表内全部为低位带；开「低位优先」→ 组内升序；
4. 「⬇ 导出CSV」→ Excel 打开中文正常、行数=过滤后全集；
5. 存方案 → 刷新页面 → 应用方案 → 12 项条件+信号勾选+窗口恢复；
6. 加自选选分组 → 自选页对应分组可见（≤20 拦截抽查）；
7. 有行业资金流数据时命中表行业列出现「行业净流入 X 亿」chip（匹配率观察
   `industry_match_hit/total`）。

---

## 4. 发现清单（分级）

| # | 级别 | 问题 | 依据 | requiredFix |
|---|---|---|---|---|
| **F-V1** | **P1** | 运行中服务进程早于 021BY 代码：python/pythonw 启动 22:13~22:40 < t2 提交 23:52:30（蓝图 mtime 23:34:59）——021BY 服务端功能（pos_pctile/industry_flow_bg/新模板控件）在在线服务中静默缺席；混合态（新 JS×旧服务端）经核查优雅降级不报错，更易让用户误以为功能已上线 | 进程 StartTime 探针 vs git/文件 mtime；GET /api/health + scan/status 正常 | 重启服务（start.bat 或托盘退出重启；watchdog 1 分钟内自愈兜底）→ 浏览器 Ctrl+F5 → 按 §3.2 清单复验。收尾纪律同 021BW F-V1 教训：改代码即重启 |
| **F-V2** | **P2** | `_scan_industry_bg_map`（blueprints/market.py L196-219）缓存无效：`get_industry_flow_bg_map()`（含 `compute_streaks` 全表扫描，industry_fund_flow 8,649 行）在缓存键检查**之前**无条件执行——每次调用都完成全部读库，缓存只省了字典重建。t2 报告「同一次扫描 12 批共享一次读库」声明与实现不符 | 代码审读 + `get_industry_flow_bg_map`（market_overview.py L753-797）无内部缓存确认 | 把键探测提前：先用轻查询（`SELECT MAX(trade_date) FROM industry_fund_flow`）做缓存键，命中即返回缓存 bg_map，未命中才调 `get_industry_flow_bg_map()`。实际性能影响毫秒级×12 批，不阻塞交付 |
| **F-V3** | **P2** | 预设行业条件在**未扫描过的会话**恢复时静默丢失：`msApplyPreset` 先 `setVal('msIndustry', f.industry)`——fresh 页面行业下拉仅「全部」一个 option，赋值静默失败；仅当 `_msRows.length` 时才重建下拉并重应用（L600-601）。用户保存了含行业的方案、新会话应用后直接扫描 → 载荷缺 `industries`，结果与保存意图不符且无任何提示 | 代码走查（market.js L579-602）+ select 赋值语义 | 扫描完成后（msRunCoarse 成功回调）若当前方案含行业值则重应用一次；或应用预设时对动态下拉缺失值给出一次性提示（「行业条件 X 将在扫描后生效」） |
| F-V4 | P2·低 | 行业下拉在服务端行业筛选生效后只剩当前行业（`msPopulateIndustries` 从筛选后的 `_msRows` 构建）：切换行业必须重扫且 UI 无提示——F1 修复使行为一致化（修复前"本地切换碰运气"正是静默丢弃的另一面），但体验退化未标注 | 代码走查（msPopulateIndustries 输入源） | 说明行补一句「已按行业筛选，切换行业请重新扫描」；或后续批次从快照全量构建行业列表 |
| F-V5 | INFO | 预设名未转义即拼进 `<option>` markup（msRenderPresets）——本地单用户自伤面（名字含引号/尖括号仅破坏显示），与站内其他下拉同类 | 代码走查 | 低优加固：选项文本统一 escapeHtml |
| F-V6 | INFO | 存量 tooltip「RSI…<30偏冷」（market.js L1857）含裸 '<'——020R 期既有文案，非本批引入（git 确认不在变更集），按"存量豁免"登记 | Select-String 扫描 + git 变更集 | 随下轮文案治理顺手改为「30以下」 |
| F-V7 | INFO | t2 的 26 断言 JS 冒烟台架为临时脚本未入库——JS 行为回归防护只剩 `node --check`（语法级）；与 t1 方案 §6.3"前端手工验收"一致，非违规 | t2 报告 §5 + 仓库无该脚本 | 建议后续把最小冒烟台架持久化（tests/ 或 scripts/），便于复验 F-V3 类边界 |
| — | INFO | 存量登记项维持：离线巡检路径无 pos 字段（t2 遗留观察 3）、C2 匹配率待实测（t2 遗留观察 1）——本次复审不改变其处置 | t2 报告 §7 | 维持原移交 |

---

## 5. 建议（不阻塞）

1. **立即**：执行 F-V1 重启 + 复验清单（一分钟动作，解锁全部 021BY 功能的可见性）。
2. F-V2/F-V3 可并入下一小批修复（均为局部小改，测试面现成：蓝图 seed 测试可加
   「缓存命中不重复读库」断言；F-V3 加 msRunCoarse 后重应用断言）。
3. C2 匹配率建议在 F-V1 重启后首次真实扫描时记录 `industry_match_hit/total`，
   作为是否补别名映射表的决策输入（t1 方案开放问题 1 的闭环数据）。

## 6. 验证记录（全部可复现）

| 验证项 | 命令 | 结果 |
|---|---|---|
| 一致性审计 r1 | `python scripts/audit_consistency_021bs.py --out docs/reports/021by_review_rerun_r1_20260924.md` | **EXIT=0**，异常 0（P2×15 均既有类） |
| 一致性审计 r2 | `python scripts/audit_consistency_021bs_r2.py --out docs/reports/021by_review_rerun_r2_20260924.md` | **EXIT=0**，P1=0 |
| 数据完整度 | `python scripts/audit_data_completeness_021bx.py --out docs/reports/021by_review_rerun_021bx_20260924.md` | **EXIT=0**，核心评分链全绿 |
| 位置分位同源 | `python scripts/audit_pos_pctile_021by.py --out docs/reports/021by_review_pos_pctile_20260924.md` | **EXIT=0**，抽样 10/10 OK（独立基准=扫描=回测=操盘手） |
| 快层测试 | `python -m pytest tests/ -q` | **1283 passed, 1 skipped**（70.3s；与 t2 报告 1283 passed 一致，复现成立） |
| 红线 | `python scripts/check_redlines.py` | **28/28 通过（EXIT=0）** |
| 静态门 | `ruff check .`（含新增审计脚本）/ `node --check static/js/market.js` | **All checks passed!** / **SYNTAX OK** |
| 变更集边界 | `git show --stat HEAD` | 5 文件与 t2 声明一致；工作区仅余本复审产物 |
| 运行面探针 | `GET /api/health`、`GET /api/market/scan/status`、进程 StartTime vs 文件 mtime | 服务在跑但**早于 021BY 代码**（F-V1） |

> 只读性：三个存量审计脚本与新增 `audit_pos_pctile_021by.py` 均为 mode=ro 连接；
> 运行面探针仅 GET 只读端点；未触发任何扫描/采集/写库。

## 7. 产物

- 本复审报告：`docs/reports/021by_review_20260924.md`
- 审计重跑存档：`021by_review_rerun_r1_20260924.md` / `..._r2_...` / `..._021bx_...`
- 位置分位同源核验存档：`docs/reports/021by_review_pos_pctile_20260924.md`
- 可复用只读审计脚本：`scripts/audit_pos_pctile_021by.py`（`--out`/`--sample`；ruff 通过）
