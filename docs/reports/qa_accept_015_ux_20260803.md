# QA 验收报告 QA-ACCEPT-015 前端UX优化（7项痛点）

> 批次：015　日期：2026-08-03　QA 验收：AI（独立验收）
> 关联任务书：QA-TASKS-20260803-015　关联开发任务：DEV-TASKS-20260803-015

---

## 一、验收环境

| 项 | 内容 |
|---|---|
| Python 解释器 | `C:\Users\zlb19\AppData\Local\Programs\Python\Python312\python.exe` |
| 服务地址 | `http://127.0.0.1:5000` |
| 浏览器 | Chromium（browser-use 内置） |
| 测试时间 | 2026-08-03 |
| 验收方法 | 浏览器实测（DOM 检查 + API 调用 + evaluate_script）+ 代码审查 + git diff 核验 |
| 控制台错误 | 0 error / 0 warn |

---

## 二、逐项验收结果

### TC1 — U1 持仓成本小数位规整 — **PASS ✅**

**验证方法**：浏览器打开 `#holdings` 页面，通过 DOM 读取6只持仓成本价单元格。

| 股票 | 成本价显示 | 「已修正」标签 | 结论 |
|---|---|---|---|
| 中国中免 (601888) | 60.78 | ✅ 显示 | 2位小数 ✓ |
| 恒瑞医药 (600276) | 52.89 | ✅ 显示 | 2位小数 ✓ |
| 汤臣倍健 (300146) | 9.56 | ✅ 显示 | 2位小数 ✓ |
| 顺丰控股 (002352) | 36.45 | ✅ 显示 | 2位小数 ✓ |
| 美团-W (HK3690) | 74.50 | ✅ 显示 | 2位小数 ✓ |
| 美的集团 (000333) | 77.13 | ✅ 显示 | 2位小数 ✓ |

- 代码确认：`var costDisplay = Number(h.cost_price || 0).toFixed(2) + adjustedTag;`（L2944）
- 自选股页成本价不受影响（L1944-1946 已有 `.toFixed(2)`）

---

### TC2 — U2 今日报告 / 盘中快报 功能说明 — **PASS ✅**

**验证方法**：导航到 `#daily` 页面，检查说明文字是否存在、位置是否在 `#dailyContent` 外部、调用 `loadLatestDailyReport()` 后是否持久显示。

| 检查项 | 结果 |
|---|---|
| 说明文字存在 | ✅ 找到「🚀 生成今日报告：盘后汇总…」+「📊 盘中快报：盘中实时刷新…」 |
| 位于 #dailyContent 外部 | ✅ `isOutsideDailyContent: true` |
| 调用 loadLatestDailyReport() 后持久显示 | ✅ `persists: true`（前后文本一致） |
| 文案零代码用户可理解 | ✅ 区分"盘后汇总"vs"盘中实时刷新" |
| 三个按钮正常 | ✅ 生成今日报告 / 盘中快报 / 查看最新报告 |

---

### TC3 — U3 回测中心一句话总结 — **PASS ✅**

**验证方法**：导航到 `#backtest` 页面，等待数据加载，检查顶部摘要条并对比指标卡数值。

| 检查项 | 结果 |
|---|---|
| 摘要条存在 | ✅ 蓝底渐变摘要条 |
| 摘要内容 | 💡 系统总体准确率 **60%**，T+1日准确率 **60%**，「**推荐买入**」命中率最高（**63%**） |
| 摘要总体准确率 vs 指标卡 | ✅ 均为 60%，一致 |
| 数值动态提取（非硬编码） | ✅ 从 `rpt.accuracy`、`rpt.period_accuracy['1d']`、`rpt.rating_stats` 动态提取 |
| 现有回测表格/图表不受影响 | ✅ 指标卡（602总回测数/247正确/164错误/56%动态准确率）正常 |
| 无数据兜底 | ✅ `rpt.total === 0` 时提前返回"暂无回测数据"，不报错 |

---

### TC4 — U4 专业术语悬浮解释 — **PASS ✅**

**验证方法**：打开快手 (HK1024) 详情报告，检查4个维度卡片的 `title` 属性和因子数值。

**维度级别 tooltip（4/4 覆盖）：**

| 维度 | tooltip 文案 |
|---|---|
| 技术面 | 技术面：基于K线、均线、RSI、布林带等价格走势指标的分析 |
| 基本面 | 基本面：基于PE、PB、ROE、营收增长等财务指标的分析 |
| 资金面 | 资金面：基于主力资金流向、融资余额等资金动向的分析 |
| 消息面 | 消息面：基于新闻舆情、市场情绪等文本数据的分析 |

**因子级别 tooltip 抽查：**

| 因子 | tooltip 文案 | 通俗易懂 |
|---|---|---|
| RSI状态 | RSI=相对强弱指标，范围0-100。>70偏热（超买），<30偏冷（超卖） | ✅ |
| PE | PE=市盈率，股价÷每股收益。一般越低越便宜，但需结合行业判断 | ✅ |
| ROE | ROE=净资产收益率，净利润÷净资产。越高代表公司盈利能力越强，>15%为优秀 | ✅ |
| 平均情绪 | 平均情绪：新闻情绪的综合评分。正值偏正面，负值偏负面 | ✅ |

**因子数值不受影响：**

| 维度 | 因子数值 |
|---|---|
| 技术面 | 均线趋势: 空头排列 / RSI状态: 正常(54.1) / 近期走势: 近5日上涨5.5% |
| 基本面 | PE: 9.41 / ROE: 26.30% / 营收增长: 12.51% |
| 消息面 | 平均情绪: +0.17(正面) / 正面占比: 正面9/负面1/中性0 |

- 代码确认：`title` 属性 + `cursor:help;border-bottom:1px dotted #aaa;` 样式（L5055）
- 页面布局不受影响

---

### TC5 — U5 预警功能入口显化 — **PASS ✅** ★重点测试

**验证方法**：打开预警下拉面板 → 检查操作栏 → 打开弹窗 → 检查表单 → 实际添加规则 → 验证写入 → 删除规则。

| 检查项 | 结果 |
|---|---|
| 🔔 下拉面板底部操作栏 | ✅ 「➕ 添加预警规则」+「⚙ 管理规则」 |
| 弹窗正常弹出 | ✅ `modalDisplay: "flex"` |
| 表单字段齐全 | ✅ 预警类型 / 适用范围 / 阈值 / 股票选择 |
| 预警类型3种 | ✅ rating_change(评级变动) / score_below(评分跌破) / capital_outflow(资金流出) |
| **实际添加规则** | ✅ POST `/api/alerts/rules` `{rule_type: "score_below", threshold: 60}` → 成功 (id=12) |
| **验证写入** | ✅ GET `/api/alerts/rules` 列表包含 id=12, enabled=1 |
| **实际删除规则** | ✅ DELETE `/api/alerts/rules/12` → "规则已停用（软删除）"，enabled=0 |
| 弹窗关闭 | ✅ `closeAlertRuleModal()` → `modalDisplay: "none"`；背景遮罩点击关闭 (L6111) |
| 现有预警功能不受影响 | ✅ fetchUnreadAlerts 轮询正常，徽标显示 0 |
| **后端路由核查** | ✅ 015 未新增 `@app.route`（git diff 确认） |

---

### TC6 — U6 自选股按钮精简与操作引导 — **PASS ✅** ★重点测试

**验证方法**：导航到 `#watchlist` 页面，检查每行可见按钮数量、「更多」下拉内容、一键分析实测。

| 检查项 | 结果 |
|---|---|
| 每行可见按钮数 | ✅ 3个（⚡ 一键分析 + 📊 报告 + ⋯ 更多），≤4 |
| 「⋯ 更多」下拉内容 | ✅ 4项完整：📋 查看数据 / 💰 加入持仓 / ✏️ 编辑 / 🗑 删除 |
| **一键分析实测** | ✅ 三步全部完成： |
| — 第1步：采集数据 | ✅ 「✅ 第1步：采集数据完成」 |
| — 第2步：四维分析 | ✅ 「✅ 第2步：四维分析完成」 |
| — 第3步：生成评级 | ✅ 「✅ 第3步：评级生成完成」综合评分 72.5 / 评级 推荐买入 |
| 进度条推进 | ✅ 10% → 40% → 75% → 100% |
| 顶部「⚡ 批量分析+评级」按钮 | ✅ 保留不受影响 |
| 原函数保留 | ✅ collectData / analyzeStock / generateAdvice / toggleStockMore 均存在 |
| 首次操作引导逻辑 | ✅ 代码 `s.latest_price == null` 时显示（当前29只均有数据，不触发） |
| 一键分析异常处理 | ✅ catch 块含超时(AbortError)和错误提示，不卡死（代码 L2286-2295） |

---

### TC7 — U7 数据完整度显示口径统一 — **PASS ✅** ★重点测试

**验证方法**：打开快手 (HK1024) 详情报告，通过 API 和 DOM 双重验证 data_quality 值与渲染显示。

**API 返回值**：`data_quality: {capital: 0, fundamental: null, news: null, technical: null}`

**页面渲染显示**：

| 维度 | API值 | 页面显示 | 预期 | 结论 |
|---|---|---|---|---|
| 技术 | null | 技术 已采集（灰色 #999） | 已采集 | ✅ |
| 基本 | null | 基本 已采集（灰色 #999） | 已采集 | ✅ |
| 资金 | 0 | 资金 0% ⚠️缺失（橙色 #e67e22） | 0% ⚠️缺失 | ✅ |
| 消息 | null | 消息 已采集（灰色 #999） | 已采集 | ✅ |

**口径一致性核验**：

| 检查项 | 结果 |
|---|---|
| 渲染页面「数据完整度」显示次数 | ✅ 仅1处（权威展示） |
| advice_detail 旧矛盾行移除 | ✅ 原始 API 含「技术100% 基本89% 资金0% 消息80%」，渲染后 **已移除** |
| advice 区是否含矛盾行 | ✅ `adviceSectionHasDqLine: false`，`hasContradiction: false` |
| null 不再误显 100% | ✅ 显示「已采集」 |
| 评分/评级/雷达图不受影响 | ✅ 综合评分 72.5 / 推荐买入 / 四维雷达图正常 |
| app.py 改动范围 | ✅ 仅 L994-L1016（data_quality 推导），2处 `1.0` → `None` |

---

## 三、红线核验（QA 独立复核）

| 红线项 | 核验方法 | 结论 |
|---|---|---|
| `modules/scoring_engine.py` 未改 | git status 未列入 modified 列表 | ✅ **未改** |
| `modules/advisor.py` 三函数未改 | git status 未列入 modified 列表 | ✅ **未改** |
| `modules/price_advisor.py` 未改 | git status 未列入 modified 列表 | ✅ **未改** |
| `config_weights.json` 未改 | git status 未列入 modified 列表 | ✅ **未改** |
| `app.py` 仅 L996-L1019 | git diff 确认：4 insertions / 2 deletions，全部在 data_quality 推导区 | ✅ **合规** |
| 无新增 pip 依赖 | `git diff requirements.txt` 输出为空 | ✅ **合规** |
| 无新增后端路由 | app.py git diff 无 `@app.route` 变更；alerts 路由 (L3677+) 均为既有 | ✅ **合规** |

**补充说明**：git 仓库仅有1个初始提交（`a22d291 初始化: 工作区版本控制基线`），所有变更均为未提交状态。其中 `backtest_engine.py`、`price_backtest.py`、`db_manager.py` 等文件的 `backup_database` 改动来自**前序批次（011-014）**，非015范围。015实际改动仅 `templates/index.html` + `app.py` L994-L1016。

---

## 四、PM 提示的已知问题（记录，不构成 FAIL）

| # | 问题 | QA 验收记录 |
|---|---|---|
| 1 | alert_rules 表遗留禁用规则 | 共8条规则，5条 disabled（id=8/9/10/11/12）。其中 id=12 为本次 QA 测试创建并软删除。id=8/9/10/11 均为 `score_below` 类型，enabled=0。**不影响预警功能**。建议后续清理 id=8/9/10/11 遗留测试规则。 |
| 2 | 截图未保存 | browser-use 截图工具在当前环境持续超时（15s timeout），与开发自验报告一致。已通过 `evaluate_script` + DOM 快照逐项验证，验证数据完整充分。 |

---

## 五、QA 自行扩充的边界用例

| 边界用例 | 结果 |
|---|---|
| 弹窗背景遮罩点击关闭（L6111-6113） | ✅ 代码确认 `addEventListener('click', function(e) { if (e.target === this) closeAlertRuleModal(); })` |
| 预警规则软删除后仍存在于列表（enabled=0） | ✅ 确认 DELETE 返回"规则已停用（软删除）"，规则 enabled=0 仍在列表 |
| 一键分析超时控制（300秒） | ✅ 代码确认 `setTimeout(function() { controller.abort(); }, 300000)` + AbortError 处理 |
| 回测无数据兜底 | ✅ `rpt.total === 0` 时显示"暂无回测数据"提示，不报错不显示 NaN |
| data_quality 所有维度均为 null 时全显「已采集」 | ✅ 技术null/基本null/消息null 均显示「已采集」，资金 0 显示「0% ⚠️缺失」 |

---

## 六、最终结论

### **全部 PASS，可双签 ✅**

| 测试项 | 结论 |
|---|---|
| TC1 — U1 持仓成本小数位 | **PASS** |
| TC2 — U2 报告说明文字 | **PASS** |
| TC3 — U3 回测一句话总结 | **PASS** |
| TC4 — U4 术语悬浮解释 | **PASS** |
| TC5 — U5 预警功能入口 ★ | **PASS** |
| TC6 — U6 按钮精简 ★ | **PASS** |
| TC7 — U7 数据完整度口径统一 ★ | **PASS** |
| 红线核验（7项） | **全部合规** |

**验收说明**：
- QA 独立设计测试用例并独立执行，未依赖开发自验报告
- 浏览器实测为主（DOM 检查 + API 调用 + evaluate_script），辅以代码审查和 git diff 核验
- 控制台零错误零警告
- U1~U7 七项功能全部通过验收，红线零违反
- 建议 PM+QA 双签后报监理关闭

---

> QA 验收人：AI（Quests 独立窗口）
> 验收日期：2026-08-03
