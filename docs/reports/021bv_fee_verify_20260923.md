# 021BV · t3 终验报告（费用轨）：修复独立对账 + 重估与重算核验

> 日期：2026-09-24 ｜ 执行：checker（AgentTeams sa-fee-accuracy）｜ 结论：**通过（无阻塞 findings，2 项登记）**
> 依赖：t1 诊断（docs/reports/021bv_fee_diag_20260923.md）+ t2 修复与重估（config/trade_fees/reevaluate 脚本，真实库 apply 2026-09-24 01:00）+ 队长服务重启（01:10:32，验证中途确认）
> 独立性声明：本报告的数值核验**不采信 builder 产物**——对账脚本（%TEMP%\fee_verify_t3.py，未入仓库）不 import 任何项目模块，费用公式与持仓重算语义按 t1 诊断口径在脚本内独立重实现；两个库（现库 + R11 备份）均以 `file:...?mode=ro` 只读 URI 打开，终验全程零写库。

---

## 0. 结论摘要（TL;DR）

1. **六项终验全部通过**：①pytest 全量含 slow 层完整跑完（1207 passed / 1 skipped / 14:02）；②ruff 绿 + mypy 57 文件 0 错 + 红线 28/28；③独立对账 15/15 estimated 行 stored 值与独立重实现公式逐分一致（mismatch=0），备份库 7/7 证明修复前确为旧口径（min=5 地板）值；④R11 备份在位，44 笔 est=0 行（含 33 笔银河导入旧行 + 8 笔东财实填真实值）字段级零改动；⑤持仓重算 11/11 组独立重算一致，变更面恰为 7 项（3 银河成本 + 2 acc2 自动建行 + 2 处 is_cost_adjusted 清零）；⑥无阻塞 findings，2 项登记（§7）。
2. **修复效果量化（独立口径）**：银河 7 笔 estimated 合计 56.92 → 36.94 元（−19.98，占修复前费用额 35.1%；对裁定口径的高估率由 54% 收敛至分项舍入级 ≤0.01 元/笔）。东财对照组残差不变（+0.05~+0.21 元/笔，纯过户费记账口径差）——修复未恶化任何已实证口径。
3. **活体冒烟通过**：服务 PID 9964（01:10:32 启动，晚于 config.py 修改 00:47:51）已加载免 5 新配置；独立进程 `estimate_trade_fee` 冒烟 银河买 1.95/卖 6.95、东财地板 5.10；live API 只读抽验返回重估后值（id=58 comm=3.06、id=54 comm=1.01、恒瑞 cost=51.275）。
4. **登记项（不修数据）**：acc2 中免 1000@53.6686 系重估按流水重放自动重建（用户此前删除过该持仓行、称真实持仓不含此笔）——用户数据待处置项，非本轮缺陷；4 组有流水无持仓行的对（000333/603501 acc1、300034/301269 acc2）为备份期既有现象（全部 est=0 旧行），非本轮回归。

---

## 1. ① pytest 全量（含 slow 层）

```
python -m pytest tests/ -m "slow or not slow"
→ 1207 passed, 1 skipped in 842.13s (0:14:02)   [exit 0]
```

- 完整跑完，无 fail 无 error；slow 层（真实时钟退避 6 例）全部执行。
- 唯一 skip 定位：`tests/test_intraday_v2.py:374`「node 不可用，跳过前端排序逻辑契约测试」——运行时环境条件跳过（t4 盘中轨用例，node 可用时该契约已另行验证过），**与费用轨无关**。测试目录无静态 skip 标记（grep 证实）。
- 费用轨相关测试均在其中：`tests/test_trade_fees_021bk.py`（更新后口径）+ `tests/test_reevaluate_fees_021bv.py`（新增 5 例）全绿。

## 2. ② ruff + mypy + 红线

| 门 | 命令 | 结果 |
|---|---|---|
| ruff | `ruff check .` | All checks passed!（exit 0） |
| mypy | `mypy app.py config.py modules` | Success: no issues found in 57 source files（exit 0） |
| 红线 | `python scripts/check_redlines.py` | **28/28 通过**（exit 0；R11 备份机制/失败即中止、B24 锚、R7、R16 风控默认值等全 PASS） |

## 3. ③ 独立对账（不采信 builder：自写公式 + 自写 SQL 重抽）

**方法**：独立脚本自建券商档参数（银河 万1.853/min=0；东财 万1.853→实为 万1.5/min=5；默认 万1.5/min=5）、自建新口径（佣金/过户费/印花税**分项各自 round 到分再求和**）与旧口径（min=5 + 总和先加后舍）两套实现；SQL 重抽全部 59 笔 buy/sell 流水（JOIN accounts/stocks 取账户名与市场）；金额回退口径（amount>0 用 amount，否则 price×quantity）独立复刻。全程只读。

### 3.1 修复后：现库 stored vs 独立新公式 —— 15/15 逐分一致（mismatch=0）

银河 7 笔 estimated（id=50,51,52,54,55,58,59）：

| id | 品种 | 类型 | 金额 | 旧公式(min=5) | 独立新公式(免5) | DB stored | 一致 |
|---|---|---|---|---|---|---|---|
| 50 | 601888 | sell | 21,520 | 15.98 | 14.97 | 14.97 | ✅ |
| 51 | 600276 | buy | 5,100 | 5.05 | 1.00 | 1.00 | ✅ |
| 52 | 600276 | buy | 12,864 | 5.13 | 2.51 | 2.51 | ✅ |
| 54 | 601888 | buy | 5,160 | 5.05 | 1.01 | 1.01 | ✅ |
| 55 | 000858 | sell | 7,018 | 8.58 | 4.88 | 4.88 | ✅ |
| 58 | 601888 | buy | 15,660 | 5.16 | 3.06 | 3.06 | ✅ |
| 59 | 600276 | sell | 13,674 | 11.97 | 9.51 | 9.51 | ✅ |

东财 8 笔 estimated 亦全部与独立新公式一致（其中 3 笔因分项舍入 ±0.01：id=47 21.80→21.79、id=56 82.57→82.56、id=57 16.11→16.12，id=45 等 5 笔等值幂等）。

### 3.2 修复前证明：备份库 stored vs 独立旧公式 —— 7/7 一致

R11 备份（apply 前状态）中银河 7 笔 stored 值与「min=5 + 总舍」独立旧公式 **7/7 逐分一致**，证明：修复前库存值确为旧口径地板估算，t2 的变更面就是这 7 笔（+东财 3 笔舍入），无意外外溢。

### 3.3 误差量化（对裁定口径 免5）

- 修复前高估：56.92 − 36.94 = **+19.98 元（占修复前费用额 35.1%；对重估后基数的比率为 54.1%）**，形态=固定地板高估（每笔 +2.10~+4.05 元），与用户「偏高 2~4 元/笔」反馈吻合；
- 修复后残差：0.00（仅 id=50 存在分项舍入对齐 +0.01，为与交割单分项粒度一致的预期行为）；
- **误差显著小于修复前**：达成。
- 东财实填 8 笔对照（ground truth）：新公式−实填 残差 +0.05~+0.21 元/笔（纯过户费记账口径差，t1 已裁定不改模型），与旧公式残差差异 ≤0.01 —— **修复未恶化任何已实证口径**。

## 4. ④ 备份在位 + 用户实填行零改动核验

| 核验项 | 结果 |
|---|---|
| R11 备份文件在位 | ✅ `backups/db_backup_20260924_010021_021bv_fee_reeval.db`（20,475,904 B，2026-09-24 01:00:22，apply 时由 backup_database 生成） |
| est=0 行零改动 | ✅ 备份 44 笔 vs 现库 44 笔，逐行逐字段（stock/account/type/price/qty/amount/commission/date）**差异=0、新增=0、丢失=0** |
| 银河 33 笔 comm=0 导入旧行 | ✅ 全部原样（021BK 既定决策：历史流水不补估，未被触碰） |
| 东财 8 笔实填真实值 | ✅ 全部原样（用户交割单口径数据零改动） |
| 全表行数 | ✅ trade_records 备份=现库=59，无增删 |
| est 标记 | ✅ 10 笔回写行 commission_estimated 保持 1（仍为估算口径，交割单到达后可改实际值） |
| commission 变更面 | ✅ 恰好 10 笔（银河 7 + 东财 3），与 CHANGELOG 自述完全一致 |
| 审计记录 | ✅ position_cost_adjustments 备份=现库=15 条，未删改 |

## 5. ⑤ 持仓重算正确性抽验（独立重实现 `_recalculate_holding` 语义）

按文档口径（买入费用入成本、卖出摊薄法、清仓转已实现、(股票,账户) 维度隔离）独立重实现，对全部 15 组有 buy/sell 流水的（股票,账户）对重放：

- **有持仓行的 11 组：独立重算 vs holdings 表 mismatch=0**（quantity/cost_price/realized_pnl/status 全一致）。
- 备份→现库 holdings 变更面**恰好 7 项**，与 t2 声明一致：
  - acc1 恒瑞 600276：cost 51.2903→**51.2750**（+is_cost_adjusted 1→0）
  - acc1 中免 601888：cost 59.3894→**59.3843**（+is_cost_adjusted 1→0）
  - acc1 五粮液 000858：cost 74.0058→**73.9688**
  - acc2 中免 (21,2)：自动重建 q=1000 @53.6686 active（=(53660+8.59)/1000，独立复算一致）
  - acc2 富祥 (100020,2)：自动重建后按全流水收敛 q=0 cleared
- 东财拓尔思/无线传媒幂等重算数值不变（无 diff，符合预期）。
- 4 组「有流水无持仓行」对——000333/603501（acc1）、300034/301269（acc2）：**备份期即无持仓行**且流水全部为 est=0 旧行 → 重估脚本未触及（其 affected 集合只含 estimated 行），属历史既有现象（成因同 t1 §8.3 登记的持仓行缺失类问题），**非本轮回归**。
- 队长指定登记事实核验：**acc1 中免现 1400 股自洽**——流水重放 买入 100+100+100+200+200+200+200+200+200+100+300=1900 股，卖出 100（2025-12-31）+400（09-09）=500 股，净 1400 ✅；09-23 自录买入前净持仓 1100 ✅（1100+300=1400，与用户口径一致）。

## 6. 活体冒烟（服务重启 + 新配置生效）

- **重启确认**：PID 9964 = `pythonw.exe app.py`（本项目），启动 2026-09-24 01:10:32 **晚于** config.py 修改 00:47:51 → 新银河档已在运行实例加载。（终验开始时曾实测到旧进程 38320@23:37 未重启，已即时通报队长并获解除。）
- **独立进程公式冒烟**：`estimate_trade_fee` 银河 10000 买 = **1.95**（免5：1.85+0.10）、卖 = **6.95**（1.85+5.00+0.10）；东财 10000 买 = **5.10**（地板 5+0.10 不变）——与更新后单测断言逐分一致。
- **live API 只读抽验**：`GET /api/portfolio/holdings/21/trades?account_id=1` 返回 count=13、id=58 comm=3.06 / id=54 comm=1.01（est=1）；`GET /api/portfolio/holdings?account_id=1` 返回恒瑞 cost_price=51.275 —— 运行实例与重估后 DB 同源一致。
- 未做 live POST 冒烟的原因：录入端会写真实用户库且当日流水受 T+1 锁保护不可回退；POST 路径已由隔离临时库的 API 单测覆盖（`test_add_trade_empty_commission_auto_estimates` 等）。

## 7. ⑥ Findings（无阻塞；按处置类型分级）

**登记项（不在本轮修，用户数据待处置）**

- **F-1（medium，用户数据）** acc2 中免 1000 股 @53.6686 active 系重估按流水重放自动重建（id=45，2026-09-03 买入 53,660）。机制上完全符合 t1 §6 预告的「自动建行收敛」预期路径与脚本契约；但用户此前删除过该持仓行并称真实持仓不含此笔（真实持仓仅 acc1 的 1100 口径）。**属用户数据待处置项**：请用户确认后在 UI 删除该 acc2 持仓/流水或更正，本轮按队长指示不修数据。
- **F-2（low，观测）** 4 组有流水无持仓行对（000333/603501/300034/301269，全 est=0 旧行）：历史持仓行缺失现象面比 t1 登记的 2 只再多 2 只，同成因类（疑似历史删除/迁移），不属本轮回归，维持 021BK「历史流水不补估」决策不动。

**移交后续批次的遗留（非缺陷）**

- **F-3（low，取证依赖）** 银河免 5 为队长按用户预授权作出的裁定（t1 判定树情景 A），**尚无交割单分项直接实证**。回退路径就绪：交割单若显示最低 5 元，config `commission_min` 改回 5.0 后重跑 `scripts/reevaluate_fees_021bv.py --apply` 即完全回退（R11 备份在）。建议用户下次银河交割单到手后顺手核对一笔佣金分项闭环。
- **F-4（info，范围）** 团队目标②「盘中费用预估实时展示」未实施：t4 实际交付为盘中速览卡 v2（当日盈亏/排序/指引/自动刷新）；录流水费用预估目前仍为提交后返回值展示，无实时预估端点。属低成本可选顺手项，留待后续批次。

## 8. 验证证据链与可复现性

- 独立对账脚本：`%TEMP%\fee_verify_t3.py`（未入仓库；七段核验 A~G 一次全 PASS，exit 0）；两库均 `file:...?mode=ro` 只读打开，终验零写库。
- 全部命令在项目根目录执行：`python -m pytest tests/ -m "slow or not slow"`、`ruff check .`、`mypy app.py config.py modules`、`python scripts/check_redlines.py`。
- 红线零触碰声明：B24（generate_advice）、R7、classify_stage、风控阈值全程未动；本轮自身无任何代码/数据写入（t2 产物按原样核验）。

---

*报告完 · 021BV t3 费用轨终验 · 通过（无阻塞 findings；F-1 用户数据待处置、F-3 交割单闭环建议移交后续）*
