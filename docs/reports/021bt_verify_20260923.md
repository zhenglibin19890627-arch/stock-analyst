# 021BT 盘中操作便利化 — 终验报告

> 日期：2026-09-23 ｜ 执行：checker（终验任务 t4）｜ 依据：AGENTS.md §5 / docs/RED_LINES.md /
> docs/reports/021bt_intraday_plan_20260923.md（t1 方案）/ t2 提交 6f6eaf9 / t3 提交 3b578a1
> 验证环境：Windows 本机运行实例（PID 36388，2026-09-23 20:42:40 启动，晚于 t2/t3 全部代码改动）；
> 验证时段为**非交易时段**（20:41–21:00，收盘后），按方案 §五 采用「运行实例只读冒烟 + 合成/mock 触线核验」组合。

---

## 0. 结论速览

| # | 终验项 | 结果 |
|---|---|---|
| ① | pytest 全量（含 slow 层）完整跑完 | ✅ **1143 passed / 1 skipped / 0 failed**（10:48）；fast 层复跑 1137 passed / 1 skipped / 6 deselected |
| ② | ruff + mypy + 红线核验 | ✅ ruff 0 违规；mypy 57 文件 0 问题；**红线 28/28** |
| ③ | audit_consistency_021bs 独立重跑 | ✅ **P0=0**；P1=1（经证据裁定为**数据时点差，非 021BT 回归**，§4）；P2=26；OK 229；异常 0 |
| ④ | 运行实例只读冒烟（非交易时段） | ✅ 新端点全部在线；非时段零网络零写库；口径标注正确；触线逻辑合成用例 6/6 |
| ⑤ | t2/t3 验收点逐项核对 | ✅ 方案 6 实施项全部落地，真实操作路径核验通过 |
| ⑥ | 频率风控专项（代码审读） | ✅ 与方案 §四 频率设计一致；四层克制全部在场 |
| ⑦ | Findings | 低×2、信息×4（§9），无阻断项 |

**总评：021BT 批次（t2 后端巡检 + t3 看板速览卡）终验通过，收盘确认口径零改动红线保持，可交付。**
遗留仅低危标注类/口径文档类建议（不阻断），真实交易时段的实盘观察（速览卡对账/11:31、15:01 边界/断网演练）留待下一交易日用方案 §五"实盘验收"清单补做（非本终验可替代项）。

---

## 1. 阻塞事件与解除（如实记录）

- 20:41 首测：端口 5000 属主为 **旧实例**（pythonw PID 12972，19:55:40 启动），早于 t2 提交（20:26:41）与 t3 文件改动（20:31–20:33）；实测 `GET /api/dashboard/intraday` → **404**。已按任务约定即时上报队长。
- 20:42 队长完成重启：新实例 PID 36388（20:42:40 启动）。本报告全部运行实例数据以**新实例**为准。
- 佐证（logs/app.log）：`20:42:07 GET /api/dashboard/intraday 404`（旧实例最后一响）→ `20:42:43 [modules.intraday_patrol] INFO [盘中巡检] 调度器已启动（间隔 5 分钟，仅交易时段活动；总开关 True）`。20:42:43 至 21:00 **无任何巡检轮次日志** —— 非交易时段 tick 静默空转、零请求、零写库，与设计一致。

---

## 2. 全量测试（①）

| 命令 | 结果 |
|---|---|
| `python -m pytest tests/ -m "slow or not slow" --timeout=900` | **1143 passed, 1 skipped**（648s，含 slow 层真实时钟退避 6 例） |
| `python -m pytest tests/ -q -rs`（fast 基线层复跑，定位 skip） | 1137 passed, 1 skipped, 6 deselected（114s） |

- 唯一 skip：`tests/test_cascade_integrity.py:153 — got empty parameter set for (table)`（参数集为空的 pytest 标准行为，**先于 021BT 存在**，非本批引入）。
- 021BT 新增测试全部通过：`tests/test_intraday_patrol.py` 55 例（t2 49 + t3 6：MA20×2 / 信号标记×3 / 端点行键）+ `tests/test_routes.py` 端点冒烟 2 例（intraday 端点 / refresh 冷却）+ `_scan_stop_discipline` 参数化回归组（默认路径收盘口径逐字保持断言）。
- 020R-59 收盘回填闭环 17 例（test_intraday_020r59.py）全绿 —— 收盘口径行为零变化的最直接回归证据。

## 3. 静态检查与红线（②）

| 命令 | 结果 |
|---|---|
| `ruff check .` | **All checks passed**（exit 0） |
| `python -m mypy app.py config.py modules` | **Success: no issues found in 57 source files** |
| `python scripts/check_redlines.py` | **28/28 通过**（R1–R19 全锚点；含 R7/R13–R15 行为锁签名） |
| `node --check static/js/portfolio.js` | OK（语法合法） |

B24（generate_advice）/ R7（scoring_engine）/ classify_stage：021BT 两提交 diff 未触碰上述对象（t2/t3 提交清单核对 + 红线 28/28 + test_redlines 签名锁全绿三重印证）。

## 4. 一致性审计独立重跑（③）

`python scripts/audit_consistency_021bs.py`（只读：ro 连接 + Flask test_client 纯 GET）重跑结果：
**发现 27 条：P0=0 / P1=1 / P2=26；OK 229 条；异常 0**。报告已按脚本设计覆写至 `docs/reports/021bs_audit_r1_20260922.md`（git 可见 diff，属审计工具可重复执行语义）。

**P1-1（R13 操盘手摘要×现算，600276 恒瑞医药）裁定：非 021BT 回归，系数据时点差。** 只读取证：

| 证据 | 时间/值 |
|---|---|
| 最新 ok 日报生成（成本基数 49.37、stop_loss 45.42） | 2026-09-23 **20:05:59** |
| 用户新记流水：卖出 300 股 @45.58（trade_records id=59） | 2026-09-23 **20:39:13** |
| 持仓成本随即重算 51.2903（holdings.updated_at） | 20:39:13 |

即：报告生成在先（20:06），流水录入在后（20:39）→ 现算止损按新成本上移至 47.19（51.29×0.92），与存量报告摘要 45.42 形成同日双数字，触发审计 R13 升级规则。**重生成当日报告即自愈**；021BT 未触碰 daily_report / trader_advisor / price_advisor / holdings 重算任一环节（t2/t3 提交清单核对），与代码回归无关。建议：用户下次生成日报时自然收敛；若同日重生成后仍复现，再按审计建议排查生成链时序（届时与本批无关）。

## 5. 运行实例只读冒烟（④，非交易时段）

实测（20:43–20:45，全部只读；两次 POST refresh 在非时段语义下为零写零网空跳，用于验证冷却）：

| 检查 | 实测 |
|---|---|
| `GET /api/dashboard/intraday` | **200**，`success=true`、`source=fallback`（无今日快照→零网络兜底现算，正确）、`session.in_session=false`、`markets=[a_stock,hk_stock]`、8 只持仓全量、`disclaimer=盘中口径，以收盘确认为准`、`patrol={enabled:true, interval_min:5, consecutive_failures:0, paused:false}` |
| 非时段零开销 | 兜底路径零网络（price_cache 显示价）；每行 `note='休市'`、`quote_ok=false`、价旁 `缓存` 标注、`as_of=20:06`（缓存时点）——**"最近收盘口径标注正确"达成**；调度器侧：启动即检 tick 判定非时段→不跑轮（app.log 无轮次日志） |
| `POST /api/dashboard/intraday/refresh` ×2 | 第 1 次：`{"ok":false,"reason":"非交易时段","skipped":true,"written":0}`（明确 reason，零写库）；第 2 次（58s 后）：`{"reason":"冷却中（59s 后可再次刷新）","cooldown":59}`（**60s 冷却生效**） |
| `GET /api/dashboard/action-list` | 200，结构完好；`stats.intraday_alerts=4` 与 `kind=intraday_alert` 项 4 条一致（快照含 2 触线 + 2 异动）；项 reason/detail 均带"盘中口径，以收盘确认为准"（另见 §9-F1 时段外语义讨论） |
| 静态资源 | 运行实例所服务 `static/js/portfolio.js` 含 `renderIntradayCard`/`intradayManualRefresh`/脚注 disclaimer（浏览器刷新即可见新卡，无需再重启） |
| 合成触线核验（`evaluate_stock_state` 直调） | 6/6 ✅：9.0≤9.2→below_stop；**价=线（9.2=9.2）→below_stop（触及即警）**；9.28（+0.87%）→near_stop；9.384（+2%）→normal；止损缺失→unknown；\|pct\|=4%→swing 标记（pytest 另有边界值/双源取高/量能异动用例锁定） |

## 6. t2/t3 验收点逐项核对（⑤，真实操作路径视角）

**t2 盘中巡检后端（6f6eaf9）**

| 验收点 | 核对结果 |
|---|---|
| 调度器 Timer 串联/daemon/幂等/atexit，app.py 注册且失败不阻断 | ✅ intraday_patrol.py L629–686 + app.py L202–209（try/except） |
| 时段门控复用 `_is_intraday_session`，A/H 分市场判定 | ✅（run_patrol_round/tick 双层门控；测试锁定 out_of_session 零请求） |
| 批量 1 请求/轮（≤40 只/批）、零 mootdx 降级、零 @retry | ✅ realtime_quotes._fetch_quote_snapshot_batch；全批次失败才抛错→调用方降级 |
| 节假日/停牌守卫：quote_ts 非今日跳过不写；连续 2 轮全 stale 暂停 | ✅（测试 test_stale_ts_skipped_no_write / test_two_all_stale_rounds_pause） |
| 触线/逼近/异动/量能四态 + 边界=触及即警 + 双源取高 | ✅（测试 7 例组 + §5 合成 6/6） |
| 写库面仅 price_cache；失败轮零写入；旧值不归零 | ✅（测试 test_failure_zero_price_cache_writes / test_write_only_for_fetched_keeps_old） |
| 降级链：保旧快照+连败 3 暂停+force 绕过+时段边界重置 | ✅（测试 4 例组） |
| 行动清单路0b：kind=intraday_alert P0 置顶 + 免责标注 + 异常隔离 | ✅（action_list.py L326–383；测试 P0 置顶/异动/无快照零项） |
| `_scan_stop_discipline` 参数化：默认路径逐字保持 | ✅（price_map=None 走 raw_kline 收盘，021BR 既有断言全绿 + 新增 3 例） |
| config 常量与零触碰清单 | ✅ config.py 7 常量；B24/R7/classify_stage 零 diff |

**t3 看板速览卡（3b578a1）**

| 验收点 | 核对结果 |
|---|---|
| loadDashboard 第 4 路并行（no-store）+ 失败 null 降级不阻塞看板 | ✅ portfolio.js diff 审读 |
| 速览卡 1.25 卡位 + 状态徽标（触线/逼近/正常/无参考）+ 距止损/距MA20/今日信号 | ✅（served JS 验证；信号 overlay 走 `scan_watchlist_*` **离线复算零网络**——market_screener L894/L962 `scope='watchlist_offline'` 核实） |
| 一键刷新：前端 60s 记忆 + 后端冷却权威 + 原位局部重渲染 | ✅（实测冷却 59s 拒绝；前端双层节流防连点） |
| 非时段呈现 + 口径标注 | ✅ ⚪ 休市徽标 + "当前非交易时段，以下为最近快照…"提示行 + 行级 `缓存`/as_of + 卡级脚注（实现取"提示行+完整表"而非方案字面"收缩为一行"——标注更完备，判定可接受，见 §9-F5） |
| 快照契约（字段/disclaimer/patrol 状态） | ✅ 与端点 docstring 契约逐字段一致（实测响应核对） |
| `node --check` | ✅ |

## 7. 频率风控专项（⑥，代码审读结论）

| 风控层 | 设计（方案 §四） | 实现 | 判定 |
|---|---|---|---|
| 数据源 | 腾讯 qt.gtimg.cn 批量；零东财/零新浪/零 mootdx | `_fetch_quote_snapshot_batch` 单一腾讯源；巡检路径无 mootdx 降级、无 @retry | ✅ |
| 频率 | 默认 5 分钟；8 只=1 批量请求/轮；≤48 请求/日 | `INTRADAY_PATROL_INTERVAL_MIN=5`；batch_size=40（8 只单批 1 请求）；平均 ≤0.0035 req/s，对 0.25s 通道约束余量 ~70 倍 | ✅ |
| 通道节拍 | 防手动连点叠加巡检 | `realtime_quotes._pace()` 模块级 0.25s 最小间隔（手动刷新与巡检同源共享节拍） | ✅ |
| 时段门控 | 仅交易时段；非时段零开销 | 双层门控（tick 粗判 + 轮内分市场精判）；实测 app.log 非时段零轮次；**注**：非时段 tick 有一次本地 DB 读（§9-F4），零网络零写库 | ✅ |
| 失败静默降级 | 保旧快照+连败 3 轮暂停至时段边界+手动 force 绕过 | 全部在场并有测试锁定；失败不写 price_cache（保旧值禁归零） | ✅ |
| 手动刷新 | 独立计数 60s 冷却，最坏叠加 ≤1 请求/分钟 | `INTRADAY_MANUAL_REFRESH_COOLDOWN_SEC=60` 后端权威 + 前端 60s 记忆双层 | ✅ |
| 节假日盲区 | 快照时间戳≠今日跳过；连续 2 轮全 stale 暂停 | `_stale_rounds` 机制 + quote_ts 守卫（A股/港股两种时间戳格式归一） | ✅ |
| 写库面 | 仅 price_cache，INSERT OR REPLACE 幂等 | run_patrol_round 唯一写点核对；raw_kline/alert_history/新表零写入 | ✅ |

## 8. 与方案的已登记偏差（实现取舍，非缺陷）

1. `INTRADAY_NEAR_STOP_PCT` 默认 **1.0**（方案 §三.项2 示例 3.0）——逼近提示更克制；行动清单文案与前端 near 判定同按 1% 硬编码，内部自洽（见 §9-F2 文案同步建议）。
2. 速览卡非时段呈现为"休市提示行 + 完整表（缓存价标注）"，非方案字面"收缩为一行"——信息量与标注完备性更优。
3. 量能/涨跌异动（swing）为方案"触线/逼近"之外的增强态，独立标记可叠加，不改变触线优先级。

## 9. Findings（⑦，报队长裁定；本任务不自行修复）

| # | 级别 | 发现 | 建议 |
|---|---|---|---|
| F1 | 低 | **非交易时段 fallback 快照可使行动清单产生 P0「盘中触线」项**：`get_snapshot_for_dashboard` 将兜底快照持久化为 `_LAST_SNAPSHOT`（date=今日），此后 `get_intraday_alerts` 的"快照非今日不提醒"防线即失效——巡检今日未运行亦产提醒（实测 20:43 休市状态下 action-list 出现 4 条，价格源为 20:06 缓存）。标注完备（as_of+免责+detail.intraday），无数据污染，但与 patrol 模块自述"巡检未运行时不虚构"意图不符；夜间展示"盘中触及止损线"措辞易生误解 | 路0b 或 `get_intraday_alerts` 增加门控：仅 `source in ('auto','manual')` 或 `session.in_session=true` 的快照产提醒；fallback 快照仅供速览卡展示 |
| F2 | 低 | 逼近阈值 config 默认 1.0，但行动清单 near_stop 文案硬编码"不足 1%"、前端 near 高亮同样硬编码 1——若日后调 config，文案与高亮不同步 | 文案/前端改读 config 值，或在 config 注释登记"三处联动" |
| F3 | 信息 | 审计 P1-1（600276 摘要 vs 现算止损双数字）实为报告(20:06)后录流水(20:39)的时点差，重生成日报自愈；非本批回归（§4 取证） | 无需代码动作；下次日报生成自然收敛 |
| F4 | 信息 | 非时段 tick 先查一次 `_held_positions()`（本地 SQLite 读）再做时段判定，非方案所述"纯布尔空转" | 毫秒级本地读，可忽略；如求极致可先判时段再查持仓 |
| F5 | 信息 | 速览卡非时段未按方案字面收缩（见 §8.2），判定为可接受实现取舍 | 如需严格遵循方案可后续调整 |
| F6 | 信息 | 方案 §五"实盘验收"（交易时段速览卡对账/11:31、15:01 边界自动停/断网连败演练）无法在终验时段替代 | 下一交易时段按清单人工观察一次 |

## 10. 终验命令与产物

```bash
python -m pytest tests/ -m "slow or not slow" --timeout=900   # 1143 passed, 1 skipped (10:48)
python -m pytest tests/ -q -rs                                # 1137 passed, 1 skipped, 6 deselected
ruff check .                       # All checks passed
python -m mypy app.py config.py modules   # no issues in 57 files
python scripts/check_redlines.py   # 28/28 通过
python scripts/audit_consistency_021bs.py  # P0=0/P1=1(时点差)/P2=26/OK 229
node --check static/js/portfolio.js        # OK
```

运行实例证据：logs/app.log（20:42:43 调度器启动行、非时段零轮次、404→200 切换）；本报告 §5 实测响应。

---

## 11. 复验（t6，2026-09-23 21:14–21:16）——F1/F2 收尾修复确认（t5 提交 0c1010b）

> 复验环境：队长重启后实例 PID 36356（21:14:12 启动，晚于 t5 全部落盘 21:04–21:08；t5 已提交 0c1010b，
> 工作树 clean）。过程记录：21:11 首测发现端口属主仍为 t4 版实例（PID 36388，早于 t5 改动），按约定上报后
> 队长重启，21:14 复测。非交易时段（21:14，收盘后）。

| 复验项 | 结果 |
|---|---|
| **F1 非时段静默（live）** | ✅ `GET /api/dashboard/action-list` → `intraday_alert` 项 **4 → 0**、`stats.intraday_alerts=0`，置顶回到 `stop_discipline` 收盘口径纪律行；同刻 `GET /api/dashboard/intraday` fallback 快照 8 只照常展示（2 只 below_stop 如实标注）——**展示层不变、提醒层门控**，正是终验 §9-F1 建议的形态 |
| **F1 门控语义（合成，不经 pytest 独立直调）** | ✅ 3/3：source='fallback' → 零提醒；source='manual' 但行 quote_ok=False（休市侧缓存兜底行）→ 零提醒；source='auto' + quote_ok=True → 提醒照常（仅收紧非巡检来源，真实轮语义不受影响） |
| **F2 阈值单源（代码审读）** | ✅ 三处硬编码全部收敛 config 单点：①行动清单 near_stop 文案读 `config.INTRADAY_NEAR_STOP_PCT`（`%g` 去尾零）；②快照根透出 `thresholds:{near_stop_pct,swing_pct}`（live 响应实测在场 `{"near_stop_pct":1.0,"swing_pct":3.0}`）；③前端逼近高亮改消费端点 `state==='near_stop'` 字段（后端按 config 判定），不再自行算 `dv<1` |
| **F2 单点生效（合成）** | ✅ 改 `config.INTRADAY_NEAR_STOP_PCT` 0.5↔1.0，行动清单文案随之翻转（"不足 0.5%"↔"不足 1%"），阈值调整单点生效达成 |
| **快速门** | ✅ pytest fast **1143 passed / 1 skipped**（同款 cascade 空参数集，先于本批）/ 6 deselected（= t4 基线 1137 + t5 新增 6，与 CHANGELOG 一致）；ruff 绿；红线 **28/28**；`node --check` OK |
| **回归面** | ✅ 无新回归：t5 触碰面 6 文件（patrol/action_list/dashboard/portfolio.js/tests/CHANGELOG），fast 层 + 红线 + 双端点 live 复测全绿；全量含 slow 已在 t4 完整跑过（§2），t5 未触碰测试基线外域 |

**t5 测试增量核对**：tests/test_intraday_patrol.py +6（F1：fallback 快照零盘中项回归锁（复现终验实测缺陷场景：缓存价 9.0 破 9.2 线 → 展示层如实标注 below_stop 且提醒层为空）/ auto 轮 quote_ok 行级门 / auto 轮提醒不受影响；F2：文案随 config / 默认 1% 基线 / thresholds 透出+判定跟随）——与本复验独立合成结论互相印证。

**复验结论**：F1/F2 均按终验建议修复到位且实测通过，**021BT 批次维持通过结论**。终验 §9 findings 状态更新：F1 ✅ 已修复（0c1010b）、F2 ✅ 已修复（0c1010b）；F3（P1-1 时点差，重生成日报自愈）/F4（tick 非时段一次本地 DB 读）/F5（速览卡非时段呈现取舍）/F6（实盘验收留待交易时段）维持原状，均非阻断。
