# 开发自测报告 019R：总览看板非交易日显示全部自选股最新评分（dev_selftest_019R）

- 开发自测人：开发（019R 批次）
- 自测日期：2026-08-09
- 依据：任务书 `docs/tasks/dev_tasks_20260809_019R_watchlist_latest_scores.md`（已批准）+ 架构评审意见 `docs/reports/review_019R_watchlist_latest_scores_20260809.md`
- 自测方式：重启应用（python app.py）→ 接口实测 + 生产库**只读**（`file:...?mode=ro`）SQL 比对 + 内存库构造兜底断言 + 前端静态核验
- 红线核查：全程对 `stock_analyst.db` 零写入（所有自测脚本仅 mode=ro 连接；兜底场景用 `:memory:` 内存库构造）；未改动评分引擎（modules/daily_report.py 等）与采集/019Q 补采链路；仅改动 `app.py` 两个读接口 + `templates/index.html` 看板渲染

---

## 一、结论（大白话）

**开发完成，自测全部通过（36/36 项断言 PASS，0 失败），可以交 QA 验收。**

一句话：看板现在对**每只自选股**取"它自己最近一份有效报告"（同一天同时有日盘/盘中报告时优先取日盘），不再用"全库最新那一天"一刀切；每行都标注这份报告是哪天的，头部日期区间按 min ~ max 显示，"较昨日"标签按监理裁定改为"较上期"；汇总卡片的平均分/评级分布和看板同口径，不会出现"看板 29 只、均分只按 3 只算"的自相矛盾。自测中发现并修复了一个我自己的 SQL 笔误（漏写关联条件导致行数翻倍为 899 行），修复后复测全绿，如实登记在第四节。

---

## 二、改动清单（文件 + 位置）

### 后端 `app.py`（2 个接口 + 1 个共享辅助）

| # | 位置（最终文件行号） | 改动 |
|---|---|---|
| 1 | L1422-1445：新增 `_latest_report_join_sql()` | 返回"每股最新一份有效报告"派生表 SQL 片段：`status='ok'` 前置过滤 → `ROW_NUMBER() OVER (PARTITION BY stock_id ORDER BY report_date DESC, CASE WHEN report_type='daily' THEN 0 ELSE 1 END)` → 取 `rn=1`。看板与汇总两接口共用此片段（含 MAX(generated_at) 查询），杜绝两处口径漂移。`_resolve_report_type` 本身保留不动，继续服务 `/api/ratings`、`report-latest` 等单日期入口 |
| 2 | L1881-1935：`api_portfolio_watchlist_scores` 主查询改造 | 移除"全局 MAX(report_date) 单日期 JOIN"与 `_resolve_report_type` 调用；改为 `stocks LEFT JOIN <共享片段> ON lr.stock_id = s.id`（无报告股票 LEFT JOIN 兜底，评分 NULL 排末尾）；顶层日期由入选行集现算（L1908-1913）；`report_generated_at` 改为"每股最新报告集合中的 MAX(generated_at)"（L1916-1922），ETag 排除时间戳机制不变 |
| 3 | L1955-1982：行级字典 | 新增行级 `report_date`（该股最新有效报告日期，无报告为 null） |
| 4 | L1984-1991：顶层返回 | **保留** `report_date`（语义延续为"入选报告的最新日期"，向后兼容）+ **新增** `report_date_min`（入选报告最早日期） |
| 5 | L1800-1848：`api_portfolio_summary` 评分维度段 | 均分/评级分布/引擎统计改为与看板同口径（每股最新一份，`status='ok'`、daily 优先），聚合范围限定非退市自选股（`stocks JOIN <共享片段>`），与看板行集自洽；顶层新增 `report_date_min`；`generated_at` 同步改为每股最新报告集合中的 MAX。**持仓汇总段（L1709-1778）与 group_id 参数原样未动** |

### 前端 `templates/index.html`（看板渲染 4 处）

| # | 位置（最终文件行号） | 改动 |
|---|---|---|
| 1 | L4748：数据组装 | 读取新字段 `report_date_min` 存入 `_dashData.reportDateMin`；行级 `report_date` 随 stocks 数组天然携带 |
| 2 | L4766-4771：看板头部日期 | `min == max` 时显示单一日期；否则显示 `min ~ max`（如 "2026-08-07 ~ 2026-08-09"） |
| 3 | L4832 / L4834 / L4882：表格 | 表头新增"报告日期"列（位于生成时间与行业之间）；每行展示该股报告日期，无报告显示灰色"暂无"；样式与现有列一致 |
| 4 | L4832：标签修正（监理裁定） | 看板"较昨日 ↕"改为"**较上期 ↕**"（多日期混排下 08-07 行的 score_change 实为相对 08-06 的变化）。每日报告页（L4686）为单日期页面，不在混排场景，未改 |

---

## 三、自测证据（按任务书第五节 1-8 逐项）

> 查询时刻快照（数据动态性留痕，验收标准 1 依据）：`daily_reports` 共 481 条、status='ok' 453 条；08-09 现存 **7** 份（全 daily：000333/002352/300146/300750/600276/601888/HK3690）；08-07 全 29 份同日全 daily；08-06、07-31 各 58 份（29 daily + 29 intraday 并存）；另有 07-18 至 07-30 各历史日期。自测断言全部按"查询时刻现算"，不写死股数/日期分布。
> 数据发现（已核查非缺陷）：全库有报告的股票数 = **31**（含 2 只非当前非退市自选股，其报告为历史遗留）；看板/汇总口径按 29 只非退市自选股统计，与 stocks 表一致。

| # | 验收点 | 结果 | 证据 |
|---|---|---|---|
| 1 | 全部非退市自选股可见，每行 report_date = 该股 daily_reports 最新 status='ok' 报告日期（抽查≥5 只） | **PASS** | 接口返回 **29 只**（不硬编码，等于 stocks 表非退市数 29，唯一 id 集合与库一致）；**全量 29 只逐行比对**（覆盖"抽查≥5 只"且强于抽查）：每行 `report_date`/`total_score`/`rating`/`engine_version` 与该股最新 ok 报告（daily 优先）完全一致，0 不一致；明确含宁德时代(300750)、中国中免(601888)及任意 08-07 股票。无报告股票兜底存在性检查：当前库全部有报告（无缺失场景） |
| 2 | 每行评分/评级与该股对应日期报告一致（抽查≥5 只） | **PASS** | 全量比对见上（29/29 一致）。行级 `report_date` 值域 = {2026-08-09(7), 2026-08-07(22)}，与库中每股最新 ok 报告日期一一对应 |
| 3 | 汇总卡片均分/评级分布与看板同口径自洽 + 数据日期范围 | **PASS** | summary `avg_score=62.1`（n=29）与看板 29 行均值完全一致；`rating_distribution={持有观望:17, 推荐买入:9, 强烈推荐买入:1, 建议减仓:2}` 与看板行集一致；`engine_stats={v5:29}` 一致；顶层 `report_date=2026-08-09`、`report_date_min=2026-08-07` 与看板一致；持仓汇总字段（市值/盈亏/数量）保留无回归 |
| 4 | 交易日等价性：08-07 全 29 份同日全 daily 与旧口径一致 + daily/intraday 并存取 daily | **PASS** | ① 前提成立：08-07 恰好 29 份、全 daily（SQL 验证 `rows=[('daily', 29)]`）。② 行集等价：新口径（每股最新）与旧单日期口径（08-07 daily ok）股票集合完全一致（29=29，差集为空）。③ 取值等价：该股最新日期为 08-07 的 **22 只**逐只与旧口径行比对（total_score/rating）完全一致；其余 **7 只**该股最新日期已是 08-09（存在更新的报告，属本需求要修复的正常行为，非回归，见第四节）。④ daily/intraday 并存：08-06、07-31 各 29 只确为 daily+intraday 双行（现成数据），全库逐股断言：每股最新日期上凡存在 daily 必取 daily，0 取错 |
| 5 | 无报告股票正确兜底（只读构造，零写入） | **PASS** | 用 `:memory:` 内存库构造 stocks(含 1 只无报告股、1 只退市股)+ daily_reports（含 failed 状态行）后执行同一 LEFT JOIN 片段：无报告股返回 NULL 评分/日期且排末尾；退市股被排除；`status='failed'` 行被前置过滤（同日期取到 intraday 85.0 而非 failed daily 90.0）。断言 `rows=[(1, 85.0, '2026-08-09'), (2, None, None)]` 完全命中 |
| 6 | 前端：29 行齐全、日期列正确、头部日期范围、"较上期"生效 | **PASS** | 接口层已证 29 行 + 行级日期正确（见 #1）；服务端 HTML 静态核验包含全部新逻辑：`reportDateMin: scores.report_date_min`（数据组装）、`reportDateMin && data.reportDateMin !== data.reportDate` 与 `min + ' ~ ' + max`（头部范围）、`较上期 ↕`（监理裁定标签）、新增"报告日期"表头、`st.report_date` 行级渲染、`暂无` 兜底文案。浏览器最终渲染建议 QA 复核（CLI 环境无法执行 JS） |
| 7 | 数据动态性处理 | **PASS** | 所有断言按查询时刻现算，未写死股数/日期分布；自测过程中未点击个股详情（不触发 B11 实时分析），报告数在自测前后保持 481/453 不变（快照留痕） |
| 8 | 全程零写入 | **PASS** | 生产库连接一律 `sqlite3.connect("file:...?mode=ro", uri=True)`；兜底断言在 `:memory:` 内存库完成；自测脚本不包含任何 INSERT/UPDATE/DELETE |

**ETag 机制专项**（验收标准 3 对应项）：`watchlist-scores` 首次请求 200 且返回 ETag，带 `If-None-Match` 再请求返回 **304**；`summary` 同样 304。payload 内容变化后 ETag 自动失效、304 语义正确（机制未动，仅 payload 随新字段自然变化）。

---

## 四、遗留 / 异常如实登记

| # | 类型 | 描述 | 处置 |
|---|---|---|---|
| 1 | **开发过程缺陷（已修复并复测）** | 初版 watchlist 主查询漏写 `ON lr.stock_id = s.id`，SQLite 将 LEFT JOIN 无 ON 按笛卡尔积执行，接口一度返回 **899 行**（29 股 × 31 份每股最新报告）、评分错乱。属本次开发真实失误，检出后立即补上关联条件并**重启复测 36/36 全绿**；自测脚本已加入"行数 == 非退市股数"防回归断言 | 已修复；本登记即诚实原则留痕 |
| 2 | 数据事实（非缺陷） | 全库有报告的股票 31 只 > 非退市自选股 29 只（2 只非自选/退市股票的历史报告存在）。新口径看板/汇总仅统计 29 只非退市自选股，与 stocks 表一致；旧代码单日期聚合会把这 2 只也计入均分 | 符合"全部非退市自选股"口径；QA 可比对 29 与 31 的差异 |
| 3 | 边界场景（极低概率） | `daily_reports` 全表为空时：旧代码按 `added_at DESC` 排序返回全部股票；新代码返回全部股票、评分 NULL 排末尾，但排序并列时无指定次序。验收标准未覆盖，行为语义（无分排末尾）一致 | 登记，不做特殊处理 |
| 4 | 既有行为（评审风险 #2 复述） | 非交易日点击个股详情（`viewReport` → `/report-latest` 无日期参数）会触发 B11 实时分析写入当日新报告，使看板日期动态前移——属既有行为、非本次引入，本期按评审裁定不动 | 登记，建议后续批次给 viewReport 传该股 report_date 复用 |
| 5 | 浏览器渲染未实测 | CLI 环境无法执行前端 JS，看板渲染以"接口数据自洽 + 服务端 HTML 静态核验"完成 | 建议 QA 浏览器复核（日期列/头部范围/较上期文案） |

---

## 五、交付物状态

1. ✅ 代码改动已拷回并核验：`app.py`（3980 行）/ `templates/index.html`（6239 行），SHA256 与工作区编辑版一致，行尾风格与原文件一致（全 CRLF，未引入混合行尾）；`py_compile` 通过。
2. ✅ 应用已重启（python app.py），运行正常，服务 127.0.0.1:5000。
3. ✅ 临时脚本已清理（工作区 `.dev_019R_work/` 整体删除，项目内无新增临时脚本；日志 `logs/019R_dev_start.*.log` 为应用启动日志，保留）。
4. 自测报告：本文（docs/reports/dev_selftest_019R_watchlist_latest_scores_20260809.md）。
