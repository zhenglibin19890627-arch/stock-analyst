# 021BU 报告回测证据注入 — 终验报告（t5）

> 日期：2026-09-23 ｜ 执行：captain（builder/checker 触发 5 小时用量上限 429，用户指示"继续任务"，终验由队长接管执行）
> 批次基线：c57bb5a（021BT 末提交）→ HEAD 0795a9b；服务重启 PID 38320（已加载 021BU 全部运行时改动）

## 结论：**通过（无保留）**

| 步骤 | 结果 |
|---|---|
| ① pytest 全量含 slow 层 | ✅ **1181 passed / 1 skipped / 0 failed**（864s 完整跑完；基线 1170+新增） |
| ② ruff / mypy / 红线 | ✅ 全过；mypy 57 文件 0 错；红线 **28/28** |
| ③ 一致性审计独立重跑 | ✅ R1：P0=0/P1=1（存量恒瑞 R13 时点差，逐字同值零新增）/P2=26/OK=341；R2：**P1=0**（N08 规则误报修正后）；selftest 全过 |
| ④ 独立 SQL 证据对账 | ✅ 三方交叉验证（见下） |
| ⑤ 运行实例只读冒烟 | ✅ 四处证据面全过（见下） |
| ⑥ B24/R7 零触碰 | ✅ `git diff c57bb5a..HEAD -- modules/advisor.py modules/scoring_engine.py modules/trader_advisor.py` 无输出 |

## ④ 独立证据对账（三方交叉）

| 验证方 | 持有观望档口径 | 结果 |
|---|---|---|
| t1 存档（scout 独立查询，实施前） | 69% (159/230) | 基准 |
| t2 实现（builder，live API 实测） | 69% (159/230)，acc=0.6913，grade=A | **逐字一致** |
| t5 队长独立 SQL（市场级×档位） | A股持有观望 n=362/m=242（含模拟+legacy 的粗口径 66.85%；生产口径过滤真实行+自然键去重后 230/159） | 方向/量级一致，差异=口径过滤（合理） |

补充：t3 已由 scout 用独立 SQL 完成 5 股徽章逐格对账全 PASS；本轮队长抽验与 t3 结论一致。

诚实门实测（live）：HK3690 primary=「样本不足（n=16）」acc=null——小样本百分数在数据层断流，无泄漏；行动清单零 evidence 消费（分域契约 ✓）。

## ⑤ 运行实例只读冒烟（PID 38320）

- watchlist-scores：茅台/HK3690 rating_evidence 内联 ✓（同上）
- report-latest（茅台）：rating_evidence / position_note / price_advice_evidence 三键在场 ✓
- action-list：evidence 泄漏行数 0（分域契约）✓

## 观察与修正记录（均非阻塞）

1. **N08 规则误报修正（本轮唯一代码改动，仅审计工具）**：口径说明行的解释文字自带「评级迟滞保持态（021AG）」字样，N08 全文子串匹配把单行误判为"迟滞行与说明行并存"→ P1 误报。已修正为行级互斥判定（说明行排除后无独立迟滞行即通过），selftest 全过，R2 重跑 P1=0。生产 markdown 实测仅单行标注，advisor else 门控设计工作正常。
2. **冒烟副作用说明**：终验冒烟触碰 report-latest 的 15 分钟重评路径（021K/B11 设计内写库），意外触发茅台报告以新代码重生成（23:38）——重生成后的报告恰为本轮修复的正确形态（单行口径说明），反向验证了修复。
3. **存量 P1（非本轮）**：恒瑞医药 R13 时点差（用户 09-22 20:39 录卖出流水致成本重算，chip 与存量报告分歧），重生成自愈，与 021BU 零交集（t3 实锤）。
4. **P2×2（t3 发现，处置记录）**：t1 方案 E-A 表 grade 勘误已当场修正；r1 §3 矩阵统计口径已由 t4 build_rule_stat 定稿消除误导行。

## 批次交付清单

- 报告页/看板/advise 三响应面：评级旁历史命中徽章（诚实门 n<20 断流）、位置分化注记、价格建议历史基准
- 行动清单零消费（分域契约：指令面≠统计面）；港股证据隐藏（无 A 级格）
- 审计常驻工具：R16/R17/R18/F06/N09 断言化规则 + selftest；统计聚合定稿 build_rule_stat
- 测试 +32（t2 27 + t4 5）；全量 1181 passed
