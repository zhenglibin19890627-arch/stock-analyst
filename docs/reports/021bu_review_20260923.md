# 021BU 复审报告（t3）— 证据对账（SQL 独立重算）+ 诚实性核验 + 一致性重跑

> 日期：2026-09-23 ｜ 执行：scout（任务 t3，复审）｜ 批次：021BU
> 依赖：t2 实施（`docs/reports/021bu_impl_20260923.md`）✅
> 方法：**全程独立**——不采信 builder 数据，五步核验全部自行执行；全程只读
> （SQLite `mode=ro` + Flask test_client 仅只读 GET），零写库、零网络、零触碰
> advisor.generate_advice（B24）/评分引擎（R7）/classify_stage。

---

## 0. 结论速览

| # | 复审步骤 | 结果 |
|---|---|---|
| ① | 一致性审计重跑（r1 + r2 + 新增规则 R16/R17/R18/F06/N09 + selftest） | ✅ r1 **P0=0 / P1=1（存量）/ P2=26 / OK=341**；r2 **P0=0 / P1=0** / P2=3 / INFO=2；新规则零发现；**新增 P0/P1 = 0** |
| ② | 证据正确性：独立 SQL 重算 × 看板响应对账（5 股 + 价格基准 + 位置注记） | ✅ **全部 PASS**（逐格 n/m/display 一致） |
| ③ | 样本门槛诚实性：边界用例（n=0/19/20/29/30）+ 线上实态抽查 | ✅ **零泄漏**（C 级 acc=None、无百分数；边界分级全对） |
| ④ | 抽样人工核读 3 份完整报告 × 看板对照 | ✅ 评级/总分/评级行/徽章/位置注记全部一致 |
| ⑤ | 证据查询脚本独立重跑对账 | ✅ 与 t1 存档**逐行一致**（仅运行时刻行） |
| — | 门禁复验 | ✅ pytest fast **1170 passed / 1 skipped**（与 t2 一致）；ruff 全过；红线 **28/28**；JS 语法 OK；B24/R7/classify_stage 冻结面 git 零触碰 |

## **复审结论：通过（新增 P0/P1 = 0）。** 021BU 可收口；存量事项与文档勘误建议见 §7。

---

## 1. ① 一致性审计重跑（独立复现）

| 脚本 | 结果 | 与 t2 存档比对 |
|---|---|---|
| `audit_consistency_021bs.py --selftest` | **全部通过**（含 R16 不一致 P1 / 缺失 P2 / 一致 OK、R17 C 级泄漏 P1、诚实门 n=19/20/29/30 边界用例） | 一致 |
| `audit_consistency_021bs.py --out …r1_rerun_t3…` | **P0=0 / P1=1（存量）/ P2=26 / OK=341 / 异常 0** | 与 `021bu_audit_r1_20260923.md` 同构（仅运行时刻与 OK 计数微增） |
| `audit_consistency_021bs_r2.py --selftest` | **4 用例全过**（N09 检测器与分级器） | 一致 |
| `audit_consistency_021bs_r2.py --out …r2_rerun_t3…` | **P0=0 / P1=0** / P2=3 / INFO=2；**N09：全部存量报告零落库命中率副本** | 一致 |

**021BU 新规则逐项确认（本轮重跑）**：
- **R16 评级徽章同源**：56/56 股 OK + 静态同源断言通过（六处消费点均指向共享函数），0 发现；
- **R17 诚实门**：56/56 股证据格自洽，0 发现；
- **R18 价格基准同源**：2 市场与 ro 现算基准一致，0 发现；
- **F06 看板证据流**：0/0/0/0，行动清单零证据键泄漏（分域契约保持）；
- **N09 反落库守卫**（r2）：零副本。

**唯一 P1 为存量（非本轮引入）**：P1-1｜R13｜600276 恒瑞医药（chip 止损 45.42 vs 现算 47.19）
——与 021BS 基线存档 `021bs_audit_r1_20260922.md` **逐字同值**。本轮复核确认：chip 摘要
45.42 == 存量 pa.stop_loss 45.42（两面自洽），分歧仅因持仓成本 49.37→51.29 抬高纪律线
（现算 47.19）——典型「持仓变更型时点差」，重生成报告自愈，与 021BU 证据链路零交集。

## 2. ② 证据正确性核验（独立 SQL 重算 × 响应对账）

**方法**：复审者自行编写全新 SQL（不复用生产函数与审计基准代码）：
排除模拟行与 rating_id=-1、自然键 (stock_id, rating_date) max(id) 去重、
is_correct 主口径 + dynamic_is_correct 次行 + 真实锚点价格口径；再经 test_client
拉取 `/api/portfolio/watchlist-scores`（只读 GET）逐格对账。

**独立基准（a_stock）**：持有观望 159/230（69%）｜建议减仓 95/156（61%）｜推荐买入 34/64
（53%）｜强烈建议卖出 2/8（C）｜强烈推荐买入 2/2（C）；推荐买入×位置带（动态）低 9/10、
高 5/12；价格基准 buy 97/106、target 31/106、stop 13/142（n_real=142）。

| 对账项 | 看板响应值 | 独立 SQL | 判定 |
|---|---|---|---|
| evidence_price.a_stock 买入区间/目标价/止损 n/m | (97,106)/(31,106)/(13,142) | 同 | ✅ PASS |
| 600519 茅台·持有观望 primary/dynamic | (159,230)/(94,159) | 同 | ✅ PASS |
| 002415 海康·推荐买入 primary/dynamic | (34,64)/(16,32) | 同 | ✅ PASS |
| 000858 五粮液·持有观望 primary/dynamic | (159,230)/(94,159) | 同 | ✅ PASS |
| 601888 中免·持有观望 primary/dynamic | (159,230)/(94,159) | 同 | ✅ PASS |
| 600276 恒瑞·持有观望 primary/dynamic | (159,230)/(94,159) | 同 | ✅ PASS |
| 002415 position_note（own band n / c） | n=10, acc=0.9 | 推荐买入×low 动态 9/10 | ✅ PASS |
| 港股证据形态 | HK3690「样本不足（n=16）」acc=None | hk 建议减仓判定数 16 | ✅ PASS（诚实+C级） |

**结论：证据函数与展示链路无算错，全部与独立重算一致。**

## 3. ③ 样本门槛诚实性核验（数据层断流）

直接调用生产 `backtest_engine.format_evidence_cell / grade_sample_class`（独立于审计 selftest 副本）：

| n | grade | acc | display | 泄漏 |
|---|---|---|---|---|
| 0 | C | None | 样本不足（n=0） | 无 |
| 19 | C | None | 样本不足（n=19） | 无 |
| 20 | B | 0.6 | 历史命中 60%（12/20） | 无（带 n/m） |
| 29 | B | 0.8621 | 历史命中 86%（25/29） | 无（带 n/m） |
| 30 | A | 0.7 | 历史命中 70%（21/30） | 无 |
| 230 | A | 0.6913 | 历史命中 69%（159/230） | 无 |

- 边界分级 n=19→C / n=20→B / n=29→B / n=30→A **全对**；`acc` 在 C 级为 None、display 无百分数
  ——**数据层断流成立**（任何消费面无法泄漏小样本命中率）。
- 线上实态抽查：C 级唯一在评档位（HK3690 建议减仓 n=16）如实显示「样本不足（n=16）」，
  LEAK=False；B 级格（港股持有观望动态 69%（18/26））均带 n/m，且港股证据由前端
  `_evMarketHidden` 整体隐藏（O8 形态正确）。A股当前无 B 级格（推荐买入主口径 34/64=A）。

## 4. ④ 抽样人工核读（3 份完整报告 × 看板对照）

| 股票 | 报告评级/总分（markdown 评级行） | 看板评级/总分 | 徽章 | 位置注记 | 判定 |
|---|---|---|---|---|---|
| 600519 贵州茅台 | 持有观望 / 65.9（含迟滞保持说明行） | 同 | 69%（159/230）A + 动态 59%（94/159） | 无（未达分化门槛，观望档不标注=正确） | ✅ |
| 002415 海康威视 | 推荐买入 / 62.7 | 同 | 53%（34/64）A + 动态 50%（16/32） | 「✓ 底部提示…低位 90%（9/10），高位仅 42%」含 n/m | ✅ |
| 600276 恒瑞医药 | 持有观望 / 56.3 | 同 | 69%（159/230）A + 动态 59%（94/159） | 无（正确） | ✅ |

三份报告的存量 pa.stop_loss 与看板 chip 抽样核对无新增矛盾（恒瑞 45.42==45.42，见 §1）。

## 5. ⑤ 证据查询脚本独立重跑对账

`python scripts/query_backtest_evidence_021bu.py --market a_stock --out …`（EXIT=0）：
与 t1 存档 `021bu_evidence_query_a_20260923.md` **Compare-Object 零差异**（仅运行时刻行）——
670 条真实样本、真实锚点 142 点、分档总体与矩阵数字完全稳定可复现。

## 6. 门禁复验

| 门 | 命令 | 结果 |
|---|---|---|
| pytest（fast 层） | `python -m pytest tests/ -q` | **1170 passed, 1 skipped, 6 deselected**（92s，与 t2 声明一致；含 t2 新增 27 例） |
| pytest（全量） | `python -m pytest tests/ -m "slow or not slow" -q` | **1176 passed, 1 skipped, 0 failed**（835s，含 slow 层；1170 fast + 6 slow 全绿） |
| ruff | `python -m ruff check .` | **All checks passed!** |
| 红线 | `python scripts/check_redlines.py` | **28/28 通过** |
| JS 语法 | `node --check analysis.js / portfolio.js` | 均 OK |
| B24/R7/classify_stage | `git status --porcelain modules/advisor.py modules/scoring_engine.py modules/trader_advisor.py` | **空输出——冻结面零触碰** |

## 7. 新发现与建议（R1 分级；无 P0/P1）

| # | 分级 | 发现 | 建议 |
|---|---|---|---|
| 1 | **P2（文档）→ 已勘误** | t1 方案 `021bu_backtest_evidence_plan_20260923.md` §E-A 表推荐买入行三处分级标注笔误：53%（34/64）、73%（24/33）、50%（16/32）的 m 均≥30，应为 **A 级**，原文误标「B⚠」（生产实现按真实 m 计算，未受影响——看板实测 grade=A 正确） | **本轮已勘误**（t1 文档该行已改为 A 级）；无需改代码 |
| 2 | **P2（展示统计）** | r1 审计报告 §3 规则矩阵中 R18 行 OK=0（其 2 条市场级 OK 计入 §0 总 OK=341，但未入每规则矩阵——R18 为全局规则、非每股上下文） | 统计表口径注记或后续轮次把全局规则 OK 单列；纯统计展示，无一致性影响 |
| 3 | INFO | 港股持有观望动态格为 B 级（18/26），数据层按诚实门带 n/m 返回、前端按 O8 整体隐藏——设计内形态，随样本≥30 或将来放开港股展示时自动生效 | 无需动作 |
| 4 | INFO（存量移交） | 恒瑞医药 R13 P1（存量时点差）与 r1 §4 建议一致：重生成当日报告自愈；同日复现才需查生成链时序 | 照旧，属 021BS 既有事项 |

## 8. 复现命令

```bash
python scripts/audit_consistency_021bs.py --selftest
python scripts/audit_consistency_021bs.py --out docs/reports/021bu_audit_r1_rerun_t3_20260923.md
python scripts/audit_consistency_021bs_r2.py --selftest
python scripts/audit_consistency_021bs_r2.py --out docs/reports/021bu_audit_r2_rerun_t3_20260923.md
python scripts/query_backtest_evidence_021bu.py --market a_stock --out <任意路径>   # 与 t1 存档逐行一致
python -m pytest tests/ -q                      # 1170 passed / 1 skipped
python -m ruff check .                          # All checks passed
python scripts/check_redlines.py                # 28/28
```

> 本轮证据存档：`docs/reports/021bu_audit_r1_rerun_t3_20260923.md`、
> `021bu_audit_r2_rerun_t3_20260923.md`。对账/核读脚本为复审期临时脚本（%TEMP%），
> 未入库——对账逻辑已由审计脚本 R16–R18/F06 常驻化，无需保留第二份脚本。
