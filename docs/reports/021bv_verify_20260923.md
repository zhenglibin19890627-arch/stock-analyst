# 021BV · t5 合并终验报告：费用轨独立对账 + 盘中轨逐项验收

> 日期：2026-09-24 ｜ 执行：checker（AgentTeams sa-fee-accuracy）｜ 结论：**通过（无阻塞 findings；登记/移交项 6 条，均非缺陷）**
> 范围：费用轨（t2 修复 `dc2f688` + t3 费用终验）与盘中轨（t4 速览卡 v2 `1f1c520`）两轨合并终验。
> 前置确认：**服务已重启且同时载有两轨代码**——PID 9964 启动 2026-09-24 01:10:32，晚于 config.py（00:47:51）与 intraday_patrol.py/dashboard.py（00:27:58）修改时间；live 实测费用与速览卡均为新行为。

---

## 0. 结论摘要（TL;DR）

1. **①pytest 全量含 slow 完整跑完**：`1207 passed, 1 skipped in 819.07s (0:13:39)`，exit 0。唯一 skip 为 `tests/test_intraday_v2.py:374` node 环境守卫（pytest 子进程 PATH 无 node；node CLI 实测可用，`tests/js/intraday_sort_test.js` 8 断言直接运行全过），与费用/盘中功能正确性无关。
2. **②静态门全绿**：ruff All checks passed；mypy 57 files 0 issues；红线 **28/28**（R11 备份机制、B24 锚、R7、R16 风控默认值等全 PASS）。
3. **③费用轨复验（独立口径）**：t3 的独立对账脚本（零项目 import、双库只读）复跑 **A~G 全 PASS**——现库 15/15 estimated 行与独立新公式逐分一致；备份库 7/7 证明修复前=旧 min=5 地板值；44 笔 est=0 行（33 银河导入旧行 + 8 东财实填真实值）字段级零改动；commission 变更面恰 10 笔；持仓独立重放 11/11 一致。银河 56.92→36.94（高估 +19.98 收敛至分项舍入级 ≤0.01/笔）。
4. **④盘中轨逐项验收全过**（live 只读 API + 源码 + node 契约三路证据，见 §4）：盈亏列数值与独立持仓算式逐分一致；排序三键+殿后+记忆；动作指引 stored 优先/警示行 live 兜底/普通行沉默实测吻合；自动刷新默认关、90s、GET-only、服务端 session 权威；当日流水概览口径正确。**非交易时段行为正确**：01:35 实测 `session.in_session=false`、快照 `source=fallback`（零网络 price_cache 兜底）、自动刷新休市暂停零请求。
5. **⑤findings**：无阻塞、无需修复项。登记/移交 6 条（§6）：2 条用户数据待处置（acc2 中免持仓行、速览卡 2400 股聚合显示口径）、1 条环境提示（pytest 子进程 node PATH）、1 条取证遗留（银河交割单闭环）、1 条范围移交（录流水费用实时预估未实施）、1 条设计确认（收盘/盘中双口径并列展示）。

---

## 1. ① pytest 全量（含 slow 层）

```
python -m pytest tests/ -m "slow or not slow"
→ 1207 passed, 1 skipped in 819.07s (0:13:39)   [exit 0]
```

- 无 fail/error；slow 层（真实时钟退避）全部执行；与 t3 费用终验轮（842.13s, 1207+1）结果一致——两轮之间无代码变更（t2 已于 01:00 后提交为 `dc2f688`，t4 已提交为 `1f1c520`），终验对象唯一确定。
- 费用轨：`test_trade_fees_021bk.py`（免5+分项舍入口径）+ `test_reevaluate_fees_021bv.py`（5 例全管线）全绿。
- 盘中轨：`test_intraday_v2.py` 16 例（盈亏 5/流水 4/指引 5/端点契约 1/node 排序包装 1）+ `test_intraday_patrol.py`（日期腐坏修复后）全绿。

## 2. ② ruff + mypy + 红线

| 门 | 结果 |
|---|---|
| `ruff check .` | All checks passed!（exit 0） |
| `mypy app.py config.py modules` | Success: no issues found in 57 source files（exit 0） |
| `python scripts/check_redlines.py` | **28/28 通过**（exit 0） |

## 3. ③ 费用轨复验（独立对账抽样 + 零改动核验）

复跑 t3 独立对账脚本（`%TEMP%\fee_verify_t3.py`，自建公式/自写 SQL、两库 `mode=ro`，不采信 builder）：

| 段 | 核验 | 结果 |
|---|---|---|
| A | 现库 15 笔 estimated stored vs 独立新公式（银河免5+分项舍入） | mismatch=**0**；银河 7 笔合计 36.94（14.97/1.00/2.51/1.01/4.88/3.06/9.51） |
| B | 备份库（apply 前）银河 stored vs 独立旧公式（min=5） | 7/7 一致 → 修复前=旧口径地板估算实锤；高估 +19.98（35.1%）→ 修复后残差 ≤0.01/笔 |
| C | est=0 行零改动 | 44/44 逐行逐字段差异=0（33 银河导入旧行 + 8 东财实填真实值）；无增无删 |
| D | 变更面 | trade_records 59=59；commission 变更恰 10 笔（银河 7 + 东财 3 笔 ±0.01 舍入）；est 标记保持 1 |
| E | 持仓独立重放 | 有持仓行 11 组 mismatch=0；变更面恰 7 项（恒瑞 51.2750/中免 acc1 59.3843/五粮液 73.9688/acc2 中免 1000@53.6686 重建/富祥 cleared/2 处清标记）；4 组无持仓行对为备份期既有非回归 |
| F | 审计面 | position_cost_adjustments 15=15 保留；is_cost_adjusted 重算对清零 |
| G | 东财 8 笔实填对照 | 新公式残差 +0.05~+0.21 不变（纯过户费记账口径差），修复未恶化实证口径 |

R11 备份在位：`backups/db_backup_20260924_010021_021bv_fee_reeval.db`。活体一致性：live API 返回恒瑞 cost=51.275、id=58 comm=3.06/ id=54 comm=1.01——运行实例与库内修复值同源。

## 4. ④ 盘中轨速览卡 v2 逐项验收（GET /api/dashboard/intraday，只读）

**验收时点 2026-09-24 01:35（非交易时段）**，8 行持仓全部返回，逐项：

### ① 当日浮动盈亏列 ✅
- 后端行增量键齐备：`total_qty/avg_cost/market_value/unrealized_pnl/unrealized_pnl_pct`（`build_snapshot` L320-343，价/量/成本任一缺失显式 None）。
- 数值独立抽验（现价×数量−加权成本，与持仓列表端点同源公式）：恒瑞 600×(45.58−51.275)=**−3417.00（−11.11%）** ✓；五粮液 100×(70.34−73.9688)=**−362.88（−4.91%）** ✓（两例均为费用修复后新成本基）；顺丰 −1490.34 ✓；无线传媒 +5379.11（+3.11%）✓。
- 前端 `_intradayPnlCell`：红盈绿亏、缺失显式"—"、tooltip 载「成本×数量」+ escapeHtml ✓（源码 L1757-1770）。

### ② 排序切换 chips ✅
- `_intradaySortRows` 纯函数（L1724-1748）：`stop` 距止损升序（默认）/`pnl` 亏损最大在前/`pct` 跌幅最大在前；null 殿后、同值按严重度（触线>逼近）再按代码——与 8 断言 node 契约测试一致。
- localStorage 记忆（`intradaySort`，非法值/隐私模式静默回默认 'stop'）✓（L1703-1716）。
- **实测**：`node tests/js/intraday_sort_test.js` → "intraday sort tests passed"（exit 0）；`node --check static/js/portfolio.js` exit 0。

### ③ 行动作指引列 ✅
- 读取策略实测吻合（`_scan_today_top_actions` L615-661）：stored `key_factors.trader.top_action` 优先 7 行（恒瑞/美团/汤臣/顺忠/五粮液/无线传媒等 source=stored）；**601888 触线行 stored 缺失 → source=live 兜底**（"止损·56.16（已触发）"）；拓尔思/无线传媒 normal 行 stored 缺失 → 沉默（无动作文本）——「一面沉默一面报警」021BS N01 形态无复发。
- 前端 `_intradayActionCell`：已触发 🎯 红色加粗；stored/live 双 tooltip 区分口径 ✓（L1772-1786）。

### ④ 交易时段自动刷新开关 ✅
- 默认关；开启后 `INTRADAY_AUTO_INTERVAL_SEC = 90`（60~120 区间取中）✓（L1700）。
- 会话双信号：服务端 `session.in_session` 权威 + 本地时钟（工作日 9:15-11:35/12:55-15:05）兜底 ✓（L1788-1805）。
- **GET-only 零写库**：`intradayAutoTick` 仅 fetch 两个 GET 端点（`/api/dashboard/intraday`、`/api/dashboard/action-list`），无任何 POST——021BN-c 数据源风控保持 ✓（L1834-1836）。
- 卡片不在 DOM 跳过、轮询中关闭即停、休市标注「休市 · 已暂停」零请求 ✓（L1826-1832）。

### ⑤ 当日已录流水概览行 ✅
- `_read_today_trades`（L664-696）：仅 buy/sell 聚合、amount 口径=成交金额不含费、dividend 排除。
- **实测**：今日（09-24）0 笔 → 返回 `buy=0/0.0 sell=0/0.0 total=0`，前端显示「🧾 今日暂无已录流水」+「金额不含费」注记逻辑 ✓（L1856-1866）。

### 非交易时段行为 ✅（专项）
- `session.in_session=false`（01:35 实测，markets=a_stock,hk_stock）✓。
- 快照 `source=fallback, degraded=false, date=2026-09-24`：跨日/未巡检时零网络现算、price_cache 兜底显示价（各行 note 含「休市」语义、quote_ok=false 路径）✓。
- 自动刷新在此时段必然走 `_intradayAutoSessionActive()=false` → 暂停零请求 ✓。

## 5. 两轨交叉一致性

- 速览卡盈亏列消费的 cost 基 = 费用轨重估后新成本（恒瑞 51.275/五粮液 73.9688 实测入卡）——费用修复已贯通展示层。
- 中免行 qty=2400 为**账户无关聚合**（acc1 1400 + acc2 1000 重建行），成本 57.0028 加权、upnl −11550.62 复算一致——聚合口径符合 021S/021W 文档语义（见 §6 F-5 显示口径提示）。

## 6. ⑤ Findings（无阻塞、不自行修复；按处置类型）

| # | 级别 | 类型 | 内容 | 建议 |
|---|---|---|---|---|
| F-1 | medium | 用户数据待处置 | acc2 中免 1000@53.6686 active 系重估按流水重放自动重建；用户称真实持仓不含此笔 | 请用户确认后 UI 处置（沿用 t3 登记，未修数据） |
| F-2 | low | 显示口径提示 | 速览卡盈亏列按账户无关聚合显示中免 2400 股——若用户仅认 acc1 1100+300 口径，卡面数字会与其实感持仓不一致（机制正确，感知待对齐） | 随 F-1 处置后自然收敛；如需可后续加账户筛选 |
| F-3 | low | 环境提示 | `test_intraday_v2` node 契约测试在 pytest 子进程内因 PATH 无 node 被 skip（本机 node 实测可用，契约测试手工运行通过） | 可忽略；或后续统一 conftest 注入 node 路径 |
| F-4 | low | 取证遗留 | 银河免5 为裁定口径，尚无交割单分项直接实证；回退路径就绪（config 改回 5.0 + 重跑 `scripts/reevaluate_fees_021bv.py --apply`） | 用户下次银河交割单到手后核对一笔佣金分项闭环 |
| F-5 | info | 范围移交 | 团队目标②之「录流水时费用预估实时展示」未实施（t4 实际交付为速览卡 v2）；费用预估目前仍为提交后返回值 | 留后续批次低成本实施 |
| F-6 | info | 设计确认 | 触线行「盘中 state=below_stop」与「收盘口径 top_action=止损·45.42」并存（恒瑞实测）——两口径并列展示属 021BV 设计（不混同，tooltip 已注明），非数据矛盾 | 无需动作；用户教育可在 tooltip 已覆盖 |

## 7. 验证证据链与可复现性

- 全部命令项目根目录执行：`python -m pytest tests/ -m "slow or not slow"`、`ruff check .`、`mypy app.py config.py modules`、`python scripts/check_redlines.py`、`python %TEMP%\fee_verify_t3.py`（独立对账）、`node --check static/js/portfolio.js`、`node tests/js/intraday_sort_test.js`。
- live 验收全部为 **GET 只读**（`/api/dashboard/intraday`、`/api/portfolio/holdings*`、`/api/health`）；未做 live POST（T+1 锁与用户数据保护；POST 路径由隔离临时库单测覆盖）。
- 终验自身零写库、零代码改动；本报告与 t3 报告为本轮仅有的新增文件。
- 红线零触碰：B24/R7/classify_stage/风控阈值全程未动（红线 28/28 为独立佐证）。

---

*报告完 · 021BV t5 合并终验 · 通过（无阻塞 findings；F-1/F-2 用户数据待处置、F-4 交割单闭环、F-5 范围移交后续）*
