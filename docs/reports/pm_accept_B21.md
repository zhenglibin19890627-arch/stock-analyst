# PM 验收报告 B21

**批次**：B21  
**验收日期**：2026-07-26  
**验收人**：AI产品经理  
**任务书**：`docs/tasks/dev_tasks_20260726_B21.md`

---

## 一、验收结果总览

| # | 验收项 | 预期 | 实际 | 结论 |
|---|---|---|---|---|
| V1 | adapter 聚合回退 PE/PB | 最新行 NULL 时回退次新行 | L234-247 三字段循环回退，受控测试 PASS | ✅ |
| V2 | PE/PB 填充率 | ≥89% | **92%（25/27）** | ✅ |
| V3 | holder_increase 回退读取 | NULL 时从其他行取 | 逻辑就绪，全库 0 行非空故当下无值可取 | ✅ 逻辑 |
| V4 | fundamental data_quality | ≥89% | 多数 A 股 89%（PE/PB 有值） | ✅ |
| V5 | news data_quality | holder 有值则 100% | holder 全库无值，news 硬顶 50% | ⚠️ 受阻采集端 |
| V6 | 红线守恒 | 三处 if False 不变 | L1645/L1684/L1717 守恒 | ✅ |
| V7 | 评分不变或合理变化 | PE/PB 有值后估值因子生效 | adapter 输出 PE/PB 与改前一致，零回归 | ✅ |

**验收结论：6/7 通过，V5 受阻于采集端（非本次范围） ✅**

---

## 二、开发关键纠偏（PM 诊断脚本误判确认）

开发正确指出了 PM 诊断脚本 `_diag_pe.py` 的方法错误：

| 查询方法 | PE 填充率 | 结论 |
|---|---|---|
| `MAX(id)`（PM 诊断脚本误用） | 0% | ❌ 误判 |
| `ORDER BY report_date DESC LIMIT 1`（adapter 真实逻辑） | **92%** | ✅ 实际 |

**PM 独立实测确认**：用 adapter 完全相同的查询逻辑，PE=25/27(92%)、PB=25/27(92%)。开发纠偏正确。

无 PE/PB 的 2 只（688795 摩尔线程-U / 688802 沐曦股份-U）为未盈利科创板，腾讯接口本身无数据，属正常。

---

## 三、代码修改评估

**修改文件**：`modules/data_adapter.py` — `_read_fundamental_data`（L211-250）

**质量评价**：
1. 聚合回退逻辑清晰，仅对 `pe_ratio`/`pb_ratio`/`holder_increase` 三个"时点值"回退
2. 注释解释了为何不回退 `gross_margin` 等"期间值"（跨季聚合语义错误）
3. `fund = dict(row)` 正确处理了 sqlite3.Row 只读问题
4. 日志记录回退行为，便于排查
5. 受控测试严谨（插入临时行验证回退，测试后清理还原）

**任务蔓延评估**：无。仅修改 data_adapter.py 一个函数，在任务书允许范围内。

---

## 四、遗留问题（需监理决策后续批次）

| 问题 | 根因 | 修复方向 | 约束 |
|---|---|---|---|
| holder_increase 全库 0 行非空 | `fetch_holder_increase` 雪球接口返回 None | 采集端修复/备选数据源 | 需触碰 data_collector.py（红线），需监理特批 |
| gross_margin 部分股票 NULL | akshare 返回 nan | 数据源限制 | 无法修复（已知问题） |
| 消息面完整度硬顶 50% | holder_increase 无值 | 同上 | 同上 |

---

## 五、验收结论

**B21 验收通过。** 聚合回退防御兜底已实施，PE/PB 读取稳健性提升（92% 且有回退保护），零回归。holder_increase 采集端问题需单独立项（触碰红线，待监理授权）。

建议监理批准关闭本批次。
