# QA 验收报告 019R：总览看板非交易日显示全部自选股最新评分（qa_report_019R_watchlist_latest_scores_20260809）

- QA：QA（019R 批次）
- 验收日期：2026-08-09（约 20:41 ~ 21:10）
- 验收依据：任务书 `docs/tasks/dev_tasks_20260809_019R_watchlist_latest_scores.md` 第五节（6 条验收标准）+ 架构评审意见（风险登记 6 项）+ 开发自测报告（`docs/reports/dev_selftest_019R_watchlist_latest_scores_20260809.md`，36/36 PASS，遗留项 #5 浏览器渲染由本批次实测）
- 验收方式：接口实测 + 生产库**只读**（`sqlite3.connect("file:...?mode=ro", uri=True)`）全量比对 + `:memory:` 内存库兜底断言 + Playwright 无头 Chromium 浏览器渲染实测
- 红线核查：全程对 `stock_analyst.db` 零写入；未修改任何代码文件；**未点击任何个股详情/链接**（未触发 B11 实时分析）；未调用 `/report-latest`
- 验收期间应用未重启（沿用开发交付的运行实例；运行实例已实证为新代码——接口返回新字段 `report_date_min` 与行级 `report_date`）

---

## 一、结论（大白话）

**验收通过。**

一句话理由：看板现在 29 只自选股一只不落地显示各自的评分和报告日期（不再"一刀切"用全库最新那天），每行的分数、评级、日期和数据库里该股最新一份有效报告完全对得上（29/29 全比 0 不一致）；汇总卡片和看板同口径不打架；08-07 那种"全员同一天出报告"的交易日跟以前效果完全一样；没报告/退市/失败报告的边角情况也验证兜底正确；浏览器里亲眼确认了日期列、"较上期"表头、日期区间和排序都正常；全程没有动过数据库一个字（前后两次查库数字一模一样）。

---

## 二、逐项验收结果表

> 通用要求执行：所有断言按"查询时刻现算"（不写死股数/日期分布）；验收前（20:41:47）与验收后（21:07）各查一次库，两次快照见第三节，期间数据零变化，无 B11 干扰。

| # | 验收项（对应任务书/自测） | 结果 | 证据（关键输出摘要） |
|---|---|---|---|
| 1 | 全量可见 + 行级日期正确（验收标准 1） | **PASS** | 接口返回 **29 行** == stocks 表非退市 29 只（不硬编码，差集为空）；**全量 29 只逐行比对**（Python 独立实现"每股最新 status='ok'、daily 优先"期望值）：每行 `report_date`/`total_score`/`rating`/`engine_version` 与库完全一致，**0 不一致**；行值域 `{2026-08-09(7), 2026-08-07(22)}` 与库中每股最新 ok 报告日期一一对应 |
| 2 | 抽查一致性（验收标准 2） | **PASS** | 全量比对已覆盖（强于抽查）。显式点名证据：**宁德时代 300750**：date=2026-08-09, score=80.0, 强烈推荐买入（库期望 08-09）✓；**中国中免 601888**：date=2026-08-09, score=68.4, 推荐买入（库期望 08-09）✓；08-07 股票（快手 HK1024 79.7 / 阿里 HK9988 71.8 / 贵州茅台 600519 68.0 等 22 只全比一致）✓ |
| 3 | 汇总同口径自洽（验收标准 3） | **PASS** | `summary.avg_score=62.1` == 库行集均分 62.1 == 看板行集（API 舍入后）均分 62.1；`rating_distribution={持有观望:17, 推荐买入:9, 强烈推荐买入:1, 建议减仓:2}` 与看板行集完全一致；顶层 `report_date=2026-08-09`、`report_date_min=2026-08-07` 与看板一致；`engine_stats={v5:29}`；持仓汇总字段无回归：总市值 134504.0、浮动盈亏 -706.0、已实现 1182.29、总盈亏 476.29、持仓 6/6（浏览器卡片亦显示"6 / 29"、"62.1"、"v5:29"） |
| 4 | 交易日等价性 + daily 优先（验收标准 4） | **PASS** | ① 股票覆盖集合：新口径 29 只非退市自选股 == 旧单日期口径（08-07 daily ok）29 只，**差集为空**；② 取值等价：该股最新报告日期为 08-07 的 **22 只**逐只与旧口径行（total_score/rating）比对**0 不一致**（另 7 只最新已是 08-09，属本需求修复的正常行为）；③ 纯交易日世界模拟：共享片段加 `report_date<='2026-08-07'`（模拟 08-09 不存在）→ 恰 29 行、每股 1 行、逐只与旧 08-07 口径一致，模拟均分 62.4 / 评级分布与旧口径按 08-07 聚合完全一致；④ **daily 优先**：全库 31 只有报告股票逐股断言"最新日期上存在 daily 必取 daily"，**0 违规**（08-06、07-31 各 29 股 daily+intraday 双行现成数据实测）；⑤ **行数防回归**：接口行数恒 == 非退市股数（29==29），开发曾出现的 899 行 JOIN 漏关联缺陷未再出现 |
| 5 | 无报告兜底（验收标准 5，`:memory:` 内存库，零写入） | **PASS** | 构造 6 股 8 报告行（含 1 无报告股、1 退市股、failed 行、双日期、双类型）执行同一 LEFT JOIN 片段：无报告股 `total_score/report_date/rating` 全 NULL 且**排末尾**；退市股被排除；同日 `failed daily 90.0` 被前置过滤（取到 `intraday ok 85.0`）；双日期取最新日期上 daily（08-07 daily 70.0 胜出 08-06 intraday 99.0）；最新日仅 intraday 时正常取 intraday。8 项断言全 PASS |
| 6 | ETag 与零写入（验收标准 6 + ETag 专项） | **PASS** | 两接口首请 **200** 且带 ETag（如 watchlist ETag=`4dbd6b01...`）；带 `If-None-Match` 复请均返回 **304**（watchlist 与 summary 各验一次）；行级 `report_date` 参与 payload 后 ETag 语义正常（payload 变化→ETag 失效→304 逻辑不变）。**全程连接方式清单**：生产库一律 `sqlite3.connect("file:<db>?mode=ro", uri=True)`（快照脚本、比对脚本、等价性脚本）；兜底断言用 `sqlite3.connect(":memory:")`；API 调用经 HTTP GET（服务端读库）；**无任何 INSERT/UPDATE/DELETE**；验收前后两次库快照逐字段一致（见第三节），零写入留痕 |
| 7 | 浏览器渲染复核（自测报告遗留项 #5，本批次必做） | **PASS** | Playwright 无头 Chromium（chromium-1228）实测 `http://127.0.0.1:5000/#dashboard`（只读浏览，未点击任何详情/链接）：① 行数 **29** 行齐全；② 新增"报告日期"列（表头第 7 列）且 **29 行日期与 API 全量一致 0 不一致**（值域 08-07/08-09；当前库无"暂无"场景，灰色"暂无"兜底由内存库断言+代码 L4882 核验）；③ 头部显示"**报告日期：2026-08-07 ~ 2026-08-09**"；④ 表头为"**较上期 ↕**"（非"较昨日"），另有"报告日期"表头；⑤ 排序点击实测：评分列点击→降序（前5=80/79.7/71.8/68.4/68.0 全降序）→再点→升序（前5=48.5/48.7/50.2/50.9/52.4），"较上期"列点击无异常；页面无 JS 错误、无 4xx/5xx 接口响应；截图留痕（qa_019R_dashboard.png，存工作区） |

**汇总**：有效断言 **51 项全部 PASS，0 FAIL**（接口比对 28 + 等价性修正 4 + 内存库兜底 8 + 浏览器 11；另 1 项初版 FAIL 经甄别为 QA 断言设计错误，见第五节 #1，修正后全绿）。

---

## 三、前后两次查库快照（只读，动态断言依据）

> 快照查询时刻：验收前 **2026-08-09 20:41:47**；验收后 **2026-08-09 21:07 左右**。两次结果**完全一致**，期间报告数零变化——说明验收全程无 B11 实时分析触发、无任何写入。

| 指标 | 验收前 | 验收后 | 说明 |
|---|---|---|---|
| daily_reports 总条数 | 481 | 481 | 一致 |
| status='ok' 条数 | 453 | 453 | 一致 |
| 按日期分布（含类型/状态） | 07-18~07-21 各 12 daily ok；07-22 29；07-23/07-26~07-28/07-30 各 27；07-24 **27 success（旧值）**；07-29 2；07-31 **29 daily ok + 29 intraday ok**；08-03 29；08-04 29 daily ok + **1 intraday failed**；08-05 29；08-06 **29 daily ok + 29 intraday ok**；08-07 29 daily ok；08-09 **7 daily ok** | 同左 | 逐行一致 |
| 非退市自选股（active） | 29 | 29 | 一致 |
| 退市股（delisted） | 0 | 0 | 一致 |
| 全库有报告的股票数 | 31 | 31 | 含 2 只历史遗留（非看板口径） |

---

## 四、架构评审风险登记 6 项核查

| # | 风险 | 核查结果 | 证据 |
|---|---|---|---|
| 1 | 报告动态增长（B11） | **控制良好** | 验收断言全部按查询时刻现算；前后两次快照完全一致（481/453 无变化）——QA 未点详情故未触发 B11；若外界浏览触发，断言仍不失效 |
| 2 | `/report-latest` 单日期口径本期不动 | **确认未改动** | 代码只读核验（app.py L891-975）：仍为"当日 + `_resolve_report_type` 单日期 + 无报告时 B11 兜底 + 全局 MAX 回退"；本接口验收期间未调用；任务书范围红线符合 |
| 3 | "较昨日"→"较上期"（监理裁定） | **已生效** | 浏览器实测表头为"**较上期 ↕**"；代码 L4832 确认；每日报告页（单日期场景）未改，符合裁定 |
| 4 | 未建 (stock_id, report_date) 索引 | 无需验收 | 接口响应即时、无感知变慢；按评审约定本期不建 |
| 5 | daily/intraday 并存排序 | **断言通过** | 窗口排序键 `report_date DESC, daily 优先` 全库 31 只逐股断言 0 违规（08-06、07-31 双行日为现成数据） |
| 6 | status='success' 旧值（07-24 实存 27 条） | **断言通过** | 前置过滤生效：接口入选行中无任何 2026-07-24 行；库中该日 27 条 success 全部被排除 |

---

## 五、异常与遗留如实登记

| # | 类型 | 描述 | 处置/结论 |
|---|---|---|---|
| 1 | **QA 自查断言设计错误（非代码缺陷）** | 主比对脚本初版"08-07 等价性"断言把"标注日期为 08-07 的 22 只"与"旧口径 08-07 全集 29 只"做集合相等，首次执行 FAIL（差集 7 只=有 08-09 新报告者）。甄别：该 7 只差异恰是本需求要修复的正常行为（它们有更新报告），非回归 | 修正为正确断言：① 覆盖集合 29==29 差集空；② 22 只取值逐只一致；③ 纯交易日世界模拟（片段加 `report_date<='2026-08-07'`）29 只全一致 → **全部 PASS**。代码无缺陷，不修改 |
| 2 | **自测报告第四节②表述易误读** | 自测报告称"② 行集等价：新口径与旧单日期口径（08-07 daily ok）股票集合完全一致（29=29，差集为空）"。按**股票 id 覆盖集合**解读成立（29==29）；若按"报告日期标注集合"解读则不成立（22 vs 29）。QA 已用正确口径独立验证（见验收 4） | 仅表述歧义，非事实错误；不构成验收障碍 |
| 3 | **既有小瑕疵（与 019R 无关）** | 首次浏览器访问出现 1 次 404（无 favicon 文件，`/favicon.ico` 404；复跑未复现，浏览器缓存所致；dashboard 功能零影响） | 属既有问题，登记不阻塞；建议后续批次顺手补 favicon（本期不扩范围） |
| 4 | **并列排序顺序未定义（非缺陷）** | 内存库兜底测试中两股并列 85.0 分，SQLite 返回顺序无保证（ORDER BY 并列时未定义） | QA 断言按"多集相同"处理；生产库 29 只无同分并列排序风险 |
| 5 | **浏览器复核方式说明** | 浏览器复核为 Playwright 无头 Chromium 实测（DOM 级验证 + 全页截图），非人工目视；已覆盖任务书要求的全部 5 个子项 | 截图留痕 qa_019R_dashboard.png（工作区，项目外） |
| 6 | 数据动态性说明 | 验收时刻 08-09 报告数为 7（全 daily），非任务书签发时的 3——与评审风险 #1 预期一致；断言全部动态化，不依赖该数字 | 正常，无异常 |

---

## 六、证据脚本清单（已清理）

以下临时脚本位于工作区 `c:\Users\zlb19\Documents\QoderCN\2026-08-09\chat-2\`（项目目录外），**验收归档后全部删除**；项目目录未新增任何临时文件（验收报告除外）。

| 脚本 | 用途 | 状态 |
|---|---|---|
| `qa_019R_snapshot.py` | 前后两次只读库快照（481/453/按日分布） | 已删除 |
| `qa_019R_verify_api.py` | 验收 1/2/3/4/6：接口 vs 库全量比对、点名抽查、汇总自洽、daily 优先、success/failed 过滤、ETag 304 | 已删除 |
| `qa_019R_equiv_fix.py` | 验收 4 修正断言：覆盖集合等价 + 纯交易日世界模拟（片段加 `<=08-07`） | 已删除 |
| `qa_019R_mem_fallback.py` | 验收 5：`:memory:` 内存库兜底断言（8 项） | 已删除 |
| `qa_019R_browser.py` | 验收 7：Playwright 浏览器渲染实测（11 项） | 已删除 |

证据产物（工作区，项目外）：`qa_019R_browser_rows.json`（29 行浏览器渲染明细）、`qa_019R_dashboard.png`（全页截图）。如需查阅可向 QA 索取，确认后可一并清理。

---

*验收结论：**通过**。QA 到此止步，验收报告已归档，交回 PM/监理裁定。*
